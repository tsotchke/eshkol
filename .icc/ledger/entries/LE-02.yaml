id: LE-02
bucket: LOUD-ERROR
subtag: structural
status: fixed
closed_at: '2026-08-28'
remeasured: The captured-parameter Hessian reproducer now verifies and returns the analytic result on
  native JIT and AOT. The dedicated regression exercises one, two, and three captures, let-bound captures,
  lexical shadowing, repeated Hessians, laplacian delegation, vector points, and a captured multi-parameter
  loss.
title: hessian over a lambda capturing its enclosing function's parameter fails LLVM verification
repro: (define (hwrap q) (hessian (lambda (t) (* q t t t)) 2.0)) (display (hwrap 2.0))
observed_before: 'ERROR: LLVM module verification failed: Incorrect number of arguments passed to called
  function! exit 1'
observed_after: '`tests/ad/hessian_capture_arity_test.esk` passes on native JIT and AOT; the scalar Hessian
  path appends captures through resolveGradientCaptures, matching the other AD call sites, and returns
  18 for q=1.5 at t=2.'
evidence: tests/ad/hessian_capture_arity_test.esk; CMake Hessian JIT/AOT smoke tests; lib/backend/autodiff_codegen.cpp
  hessianJetPath
caught_by:
- pr-record-420
- direct-measurement
missed_by:
- icc-correctness-chain
- icc-smoke
- icc-readiness
- KNOWN_ISSUES
notes: The scalar Hessian call now uses the shared capture resolver and its value-typed temp-slot handling.
