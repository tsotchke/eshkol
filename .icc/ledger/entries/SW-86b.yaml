id: SW-86b
bucket: PARITY-RATCHET
status: open
title: 'VM: internal `define`s in a guard BODY do not produce the body''s value'
repro: (display (guard (e (#t -1)) (define x 10) (define y 32) (+ x y)))
observed: wrong value on vm-src and vm-eskb (want 42). Native jit/aot-o0/aot-o2 are correct.
correct: '42'
caught_by:
- guard-coverage-gate
missed_by:
- vm-parity
- differential
repro_file: tests/error_handling/guard_coverage/found/vm_internal_defines_in_guard_body.esk
notes: Measured every run by 16_internal_defines_in_body.esk on the native axes; the two VM axes are declared
  not-required with justification in tests/error_handling/guard_coverage/ENGINES.tsv. Originally numbered
  SW-86 on the guard-coverage branch; renamed on integration to preserve the existing unrelated ledger
  identity.
