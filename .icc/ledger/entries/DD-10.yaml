
id: DD-10
bucket: DOC-DEBT
status: closed
title: "Documented no-ops in shipped surface"
items:
  - "eshkol-run -D NAME[=VALUE] defines a cond-expand feature on native and VM"
  - "eshkol-run -fPIC selects the native AOT PIC object contract; always-PIC or artifactless targets are stated precisely"
  - "provide restricts the exported names; omitted definitions are private on native and VM"
  - "manifold gyro-transport uses the closed-form gyrovector parallel transport"
evidence: "tests/toolchain/dd10_surface_test.sh; tests/modules/visibility_test.esk; tests/modules/visibility_fail_test.esk; tests/vm_parity/corpus/66_gyro_transport.esk; inc/eshkol/backend/riemannian_core.h"
closed_at: "working-tree"
observed_after: >-
  The driver passes every -D name to the native frontend and VM compiler;
  cond-expand selects the feature in native JIT, native AOT, and emitted
  ESKB execution. Native LLVM AOT object emission uses Reloc::PIC_ and the
  focused test emits an object with -fPIC. AOT module loading mangles
  omitted definitions before dependency flattening, and VM source module
  loading applies the same boundary with lexical bindings preserved. The
  hyperbolic module transport computes (lambda_a/lambda_b) gyr[b,-a]v;
  the shared header supplies the VM fallback gyration closed form and the
  parity corpus checks the same result on native and VM.
caught_by: [doc-source-crosscheck]
