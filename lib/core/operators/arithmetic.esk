;;; core/operators/arithmetic.esk
;;; First-class wrappers for arithmetic operators

(provide add sub mul div)

(define (add x y) (+ x y))
(define (sub x y) (- x y))
(define (mul x y) (* x y))
(define (div x y) (/ x y))
