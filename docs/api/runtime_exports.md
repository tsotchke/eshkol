# `eshkol/runtime_exports.h`

47 public symbol(s) — 28 documented, 19 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `eshkol_stdout_stream`

*Variable* — line 28

```c
inline constexpr const char* stdout_stream_symbol = "eshkol_stdout_stream";
```

Linker/JIT symbol names for the extern "C" runtime wrappers declared below. LLVM codegen uses these string constants (rather than hardcoding the name at each call site) when emitting external function declarations or looking up functions already inserted into a Module, so the C++ symbol name and the name generated code links against stay in sync from a single source of truth.

### `eshkol_stdout_stream`

*Function* — line 57

```c
FILE* eshkol_stdout_stream();
```

Returns the process's standard output stream.

**Returns**

The libc `stdout` FILE*. Never NULL; not owned by the caller.

### `eshkol_stdin_stream`

*Function* — line 63

```c
FILE* eshkol_stdin_stream();
```

Returns the process's standard input stream.

**Returns**

The libc `stdin` FILE*. Never NULL; not owned by the caller.

### `eshkol_stderr_stream`

*Function* — line 69

```c
FILE* eshkol_stderr_stream();
```

Returns the process's standard error stream.

**Returns**

The libc `stderr` FILE*. Never NULL; not owned by the caller.

### `eshkol_jmp_buf_size`

*Function* — line 77

```c
std::uint64_t eshkol_jmp_buf_size();
```

Returns the platform's `sizeof(jmp_buf)` so generated code can allocate correctly sized buffers for setjmp/longjmp without embedding a platform-specific constant.

**Returns**

Size of `jmp_buf` in bytes on the current platform.

### `eshkol_drand48`

*Function* — line 90

```c
double eshkol_drand48();
```

Portable drand48()-equivalent pseudo-random number generator. Implements the same 48-bit linear congruential algorithm as POSIX drand48(), providing consistent behavior on platforms (e.g. Windows) that lack a native implementation.

**Returns**

A pseudo-random double uniformly distributed in [0.0, 1.0).

> Note: Thread-safe; the generator state is protected by an internal mutex shared with eshkol_srand48().

### `eshkol_srand48`

*Function* — line 98

```c
void eshkol_srand48(std::int64_t seed);
```

Seeds the generator used by eshkol_drand48().

**Parameters**

- `seed` — Seed value; combined with drand48's fixed low-order bits (0x330E) as in the POSIX algorithm.

> Note: Thread-safe.

### `eshkol_getenv`

*Function* — line 111

```c
char* eshkol_getenv(const char* name);
```

Reads an environment variable, gated by the "env-read" capability.

**Parameters**

- `name` — Name of the environment variable to look up. NULL or empty returns NULL without consulting the environment.

**Returns**

The variable's value on success, or NULL if name is NULL/empty, the "env-read" capability is not granted, or the variable is unset. The returned pointer is owned by the C library (as with plain getenv()); do not free it, and it may be invalidated by a subsequent setenv/putenv.

### `eshkol_setenv`

*Function* — line 124

```c
int eshkol_setenv(const char* name, const char* value, int overwrite);
```

Sets an environment variable, gated by the "env-write" capability.

**Parameters**

- `name` — Variable name; NULL or empty is an error.
- `value` — New value; NULL is an error.
- `overwrite` — If 0 and the variable is already set, leaves it unchanged and returns success (0); if non-zero, always sets it.

**Returns**

0 on success; -1 on error (invalid arguments, capability denied, or the underlying platform call failing), with errno set accordingly (EINVAL for invalid arguments).

### `eshkol_unsetenv`

*Function* — line 133

```c
int eshkol_unsetenv(const char* name);
```

Removes an environment variable, gated by the "env-write" capability.

**Parameters**

- `name` — Variable name; NULL or empty is an error.

**Returns**

0 on success; -1 on error (invalid argument or capability denied), with errno set to EINVAL for invalid arguments.

### `eshkol_usleep`

*Function* — line 140

```c
int eshkol_usleep(std::uint32_t usec);
```

Suspends the calling thread for at least the given duration.

**Parameters**

- `usec` — Sleep duration in microseconds.

**Returns**

Always 0.

### `eshkol_fopen`

*Function* — line 158

```c
FILE* eshkol_fopen(const char* path, const char* mode);
```

Opens a file, gated by the "file-read"/"file-write" capabilities implied by `mode`. On Windows, `/tmp`-rooted and `/<drive-letter>/...` style paths are remapped to their native equivalents before opening.

**Parameters**

- `path` — Path to open; NULL is an error.
- `mode` — fopen()-style mode string (e.g. "r", "w", "a", with a trailing '+' requiring both read and write capability); NULL is an error.

**Returns**

An open FILE* owned by the caller (must be closed with fclose), or NULL if path/mode is NULL, the required capability was denied, or the underlying fopen() failed. errno is set to EINVAL for invalid arguments or otherwise left as set by fopen().

### `eshkol_fputs`

*Function* — line 167

```c
int eshkol_fputs(const char* str, FILE* stream);
```

Writes a NUL-terminated string to a stream.

**Parameters**

- `str` — String to write; NULL is an error.
- `stream` — Destination stream; NULL is an error.

**Returns**

A non-negative value on success, or EOF on error (including NULL arguments, which also set errno to EINVAL).

### `eshkol_access`

*Function* — line 178

```c
int eshkol_access(const char* path, int mode);
```

Checks file accessibility, gated by the "file-read"/"file-write" capabilities implied by `mode`.

**Parameters**

- `path` — Path to check; NULL is an error.
- `mode` — POSIX access() mode bitmask (e.g. R_OK=4, W_OK=2, F_OK=0).

**Returns**

0 if the requested access is permitted; -1 on error (invalid argument, capability denied, or the underlying access()/_access() call failing).

### `eshkol_remove`

*Function* — line 186

```c
int eshkol_remove(const char* path);
```

Deletes a file, gated by the "file-write" capability.

**Parameters**

- `path` — Path to remove; NULL is an error.

**Returns**

0 on success; -1 on error (invalid argument, capability denied, or the underlying remove() call failing).

### `eshkol_rename`

*Function* — line 195

```c
int eshkol_rename(const char* old_path, const char* new_path);
```

Renames/moves a file, gated by the "file-write" capability.

**Parameters**

- `old_path` — Existing path; NULL is an error.
- `new_path` — Destination path; NULL is an error.

**Returns**

0 on success; -1 on error (invalid argument, capability denied, or the underlying rename() call failing).

### `eshkol_mkdir`

*Function* — line 205

```c
int eshkol_mkdir(const char* path, int mode);
```

Creates a directory, gated by the "file-write" capability.

**Parameters**

- `path` — Directory path to create; NULL is an error.
- `mode` — POSIX permission bits for the new directory (ignored on Windows).

**Returns**

0 on success; -1 on error (invalid argument, capability denied, or the underlying mkdir()/_mkdir() call failing).

### `eshkol_rmdir`

*Function* — line 213

```c
int eshkol_rmdir(const char* path);
```

Removes an empty directory, gated by the "file-write" capability.

**Parameters**

- `path` — Directory path to remove; NULL is an error.

**Returns**

0 on success; -1 on error (invalid argument, capability denied, or the underlying rmdir()/_rmdir() call failing).

### `eshkol_chdir`

*Function* — line 222

```c
int eshkol_chdir(const char* path);
```

Changes the process's current working directory, gated by the "file-read" capability.

**Parameters**

- `path` — New working directory; NULL is an error.

**Returns**

0 on success; -1 on error (invalid argument, capability denied, or the underlying chdir()/_chdir() call failing).

### `eshkol_stat`

*Function* — line 233

```c
int eshkol_stat(const char* path, void* buf);
```

Retrieves file status information, gated by the "file-read" capability.

**Parameters**

- `path` — Path to stat; NULL is an error.
- `buf` — Output buffer, treated as a native `struct stat*`; NULL is an error. Caller-owned.

**Returns**

0 on success; -1 on error (invalid argument, capability denied, or the underlying stat() call failing).

### `eshkol_opendir`

*Function* — line 244

```c
void* eshkol_opendir(const char* path);
```

Opens a directory stream for iteration, gated by the "file-read" capability.

**Parameters**

- `path` — Directory path to open; NULL is an error.

**Returns**

An opaque directory handle (a `DIR*` on POSIX platforms, or an internal handle on Windows) owned by the caller and released via the platform's closedir()-equivalent, or NULL on error (invalid argument, capability denied, or the path could not be opened).

### `eshkol_capability_runtime_clear`

*Function* — line 252

```c
void eshkol_capability_runtime_clear();
```

Resets the capability runtime to its default state: policy inactive and the allow-list cleared, so eshkol_capability_runtime_allows() permits everything.

> Note: Thread-safe.

### `eshkol_capability_runtime_begin_install`

*Function* — line 260

```c
void eshkol_capability_runtime_begin_install();
```

Activates capability enforcement and clears any previously installed allow-list, in preparation for a fresh set of eshkol_capability_runtime_allow() calls.

> Note: Thread-safe.

### `eshkol_capability_runtime_allow`

*Function* — line 272

```c
void eshkol_capability_runtime_allow(const char* capability);
```

Grants a capability, adding it to the active allow-list. Has no effect if the capability policy is not active (i.e. this must be called after eshkol_capability_runtime_begin_install()).

**Parameters**

- `capability` — Capability name to grant (e.g. "file-read", "file-write", "env-read", "env-write"). NULL or empty is ignored.

> Note: Thread-safe.

### `eshkol_capability_runtime_is_active`

*Function* — line 281

```c
int eshkol_capability_runtime_is_active();
```

Reports whether capability enforcement is currently active.

**Returns**

1 if a capability policy has been installed via eshkol_capability_runtime_begin_install() and not since cleared; 0 otherwise (in which case all capabilities are implicitly allowed).

### `eshkol_capability_runtime_allows`

*Function* — line 291

```c
int eshkol_capability_runtime_allows(const char* capability);
```

Checks whether a capability is currently permitted.

**Parameters**

- `capability` — Capability name to check. NULL or empty is always denied.

**Returns**

1 if the capability is allowed (either because no policy is active, or because it is present in the active allow-list); 0 otherwise.

### `eshkol_capability_runtime_allows_file_mode`

*Function* — line 305

```c
int eshkol_capability_runtime_allows_file_mode(const char* mode);
```

Checks whether an fopen()-style mode string's implied read/write requirements are currently permitted. A mode requires read for a leading 'r', write for a leading 'w' or 'a', and both when '+' appears anywhere in the mode string.

**Parameters**

- `mode` — fopen()-style mode string (e.g. "r", "w+", "a"). NULL or empty is treated as requiring "file-read".

**Returns**

1 if all capabilities implied by mode are currently allowed; 0 otherwise.

### `eshkol_capability_runtime_deny`

*Function* — line 318

```c
void eshkol_capability_runtime_deny(const char* capability);
```

Records that a capability was denied, for diagnostics. Sets errno to EACCES and prints a one-time "capability denied: <name>" message to stderr the first time a given capability name is denied (subsequent denials of the same name are silent).

**Parameters**

- `capability` — Capability name that was denied; NULL or empty is reported as "unknown".

> Note: Thread-safe.

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `eshkol_stdin_stream` | Variable | 29 |
| `eshkol_stderr_stream` | Variable | 30 |
| `eshkol_jmp_buf_size` | Variable | 31 |
| `eshkol_drand48` | Variable | 32 |
| `eshkol_srand48` | Variable | 33 |
| `eshkol_getenv` | Variable | 34 |
| `eshkol_setenv` | Variable | 35 |
| `eshkol_unsetenv` | Variable | 36 |
| `eshkol_usleep` | Variable | 37 |
| `eshkol_fopen` | Variable | 38 |
| `eshkol_fputs` | Variable | 39 |
| `eshkol_access` | Variable | 40 |
| `eshkol_remove` | Variable | 41 |
| `eshkol_rename` | Variable | 42 |
| `eshkol_mkdir` | Variable | 43 |
| `eshkol_rmdir` | Variable | 44 |
| `eshkol_chdir` | Variable | 45 |
| `eshkol_stat` | Variable | 46 |
| `eshkol_opendir` | Variable | 47 |
