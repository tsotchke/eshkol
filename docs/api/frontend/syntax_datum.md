# `eshkol/frontend/syntax_datum.h`

Syntax as the reader produced it: the object `syntax-rules` matches and instantiates (ADR-0026). R7RS 4.3 defines macro transformers over *syntax* -- the datum the reader produced -- not over the parser's lowered AST. The native expander used to match patterns against lowered ASTs and substitute into a template that had already been parsed as code, so every AST payload shape (a `do` scaffold, a `let-values` formals array, a nested `define-syntax`) needed its own substitution and renaming case, and each missing case was a silent miscompile. Now the parser keeps the datum of every macro use and of every `syntax-rules` transformer in the side tables below, keyed the way ADR-0008 prescribes (a `NodeId` for a use, the definition record for a transformer). The expander rewrites datums and hands the result back to the parser, so a template means exactly what the same text means anywhere else in a program. Every atom keeps the token it was read from (type, spelling, position), so re-parsing an expansion reproduces the original literal exactly -- an exact rational stays exact, a string keeps its escapes decoded once.

27 public symbol(s) — 13 documented, 14 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `SyntaxDatum`

*Struct* — line 46

```c
struct SyntaxDatum
```

One node of reader syntax.

### `SyntaxDatum::properCount`

*Function* — line 67

```c
size_t properCount() const { ... }
```

Number of proper elements (excludes a dotted tail).

### `MacroSyntax`

*Struct* — line 73

```c
struct MacroSyntax
```

A `syntax-rules` transformer as written.

### `syntax_record_use`

*Function* — line 90

```c
void syntax_record_use(eshkol_node_id_t id, SyntaxDatum use);
```

The reader syntax of calls. A call whose head the parser already knew to be a macro keyword is recorded with its datum. Every other symbol-headed call is recorded as a reference into its tokenizer's tape (the tokens in reading order), so its datum can be recovered when the expander finds that the head is a macro keyword after all: a forward reference, a macro exported by another module, or a macro another expansion defined. Nothing is materialized until it is asked for.

### `syntax_use_unparsed`

*Function* — line 95

```c
bool syntax_use_unparsed(eshkol_node_id_t id);
```

True if the parser read call `id` as a macro use: its operands were never parsed as expressions.

### `syntax_use`

*Function* — line 97

```c
std::shared_ptr<const SyntaxDatum> syntax_use(eshkol_node_id_t id);
```

The datum of call `id,` or null if the parser recorded none.

### `syntax_add_use_renames`

*Function* — line 99

```c
void syntax_add_use_renames(eshkol_node_id_t id,
 std::shared_ptr<const std::map<std::string, std::string>> names);
```

Rename identifiers of call `id` when it is materialized (module privacy).

### `syntax_read_tape`

*Function* — line 102

```c
SyntaxDatum syntax_read_tape(const void* tape, uint32_t start);
```

Materialize the datum starting at `start` on a parser tape (parser.cpp).

### `syntax_record_macro`

*Function* — line 105

```c
void syntax_record_macro(const eshkol_macro_def_t* macro, MacroSyntax syntax);
```

Record the transformer of `macro.`

### `syntax_macro`

*Function* — line 107

```c
std::shared_ptr<const MacroSyntax> syntax_macro(const eshkol_macro_def_t* macro);
```

The transformer recorded for `macro,` or null.

### `syntax_replace_macro`

*Function* — line 109

```c
void syntax_replace_macro(const eshkol_macro_def_t* macro, MacroSyntax syntax);
```

Replace the transformer of `macro` (module-private renaming).

### `parse_syntax_datum`

*Function* — line 117

```c
eshkol_ast_t parse_syntax_datum(const SyntaxDatum& datum,
 const std::set<std::string>& macro_names);
```

Parse one datum as an Eshkol expression, exactly as the parser would parse the same text. `macro_names` are the macro keywords visible where the datum is spliced; a list headed by one of them is recorded as a macro use. Implemented by the parser (lib/frontend/parser.cpp).

### `syntax_rename_identifiers`

*Function* — line 125

```c
void syntax_rename_identifiers(SyntaxDatum& datum,
 const std::function<void(std::string&)>& rename);
```

Apply `rename` to every identifier occurrence that is code rather than data: symbols under `quote`, and under `quasiquote` outside its `unquote` escapes, are left alone. Used for module-private renaming.

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `SyntaxDatum::Atom` | Variable | 55 |
| `SyntaxDatum::token_type` | Variable | 56 |
| `SyntaxDatum::text` | Variable | 57 |
| `SyntaxDatum::false` | Variable | 58 |
| `SyntaxDatum::false` | Variable | 59 |
| `SyntaxDatum::line` | Variable | 60 |
| `SyntaxDatum::column` | Variable | 61 |
| `SyntaxDatum::items` | Variable | 62 |
| `SyntaxDatum::isSymbol` | Function | 64 |
| `SyntaxDatum::isList` | Function | 65 |
| `MacroSyntax::ellipsis` | Variable | 74 |
| `MacroSyntax::literals` | Variable | 75 |
| `MacroSyntax::rules` | Variable | 76 |
| `syntax_record_use_tape` | Function | 91 |
