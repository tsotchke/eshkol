# `eshkol/frontend/macro_expander.h`

37 public symbol(s) — 23 documented, 14 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `MacroExpander`

*class* — line 36

```c
class MacroExpander
```

MacroExpander handles syntax-rules macro expansion. Usage: 1. Create a MacroExpander 2. Call expand() on the top-level AST 3. Use the returned AST (with macros expanded) for codegen The expander is recursive - expanded macros may themselves use macros.

### `MacroExpander::~MacroExpander`

*Function* — line 51

```c
~MacroExpander();
```

Destroy the macro expander. Registered eshkol_macro_def_t entries are owned by the AST they came from, not by the expander, so no macro definitions are freed here.

### `MacroExpander::expand`

*Function* — line 60

```c
eshkol_ast_t expand(const eshkol_ast_t& ast);
```

Expand all macros in the given AST. Modifies the AST in-place and returns it.

**Parameters**

- `ast` — The AST to expand

**Returns**

The expanded AST

### `MacroExpander::expandAll`

*Function* — line 68

```c
std::vector<eshkol_ast_t> expandAll(const std::vector<eshkol_ast_t>& asts);
```

Expand macros in a vector of ASTs (e.g., top-level program).

**Parameters**

- `asts` — Vector of ASTs

**Returns**

Vector of expanded ASTs

### `MacroExpander::isMacro`

*Function* — line 76

```c
bool isMacro(const std::string& name) const;
```

Check if a name is a defined macro.

**Parameters**

- `name` — Name to check

**Returns**

true if name is a macro

### `MacroExpander::macroCount`

*Function* — line 81

```c
size_t macroCount() const { ... }
```

Get the number of macros defined.

### `MacroExpander::registerMacro`

*Function* — line 135

```c
void registerMacro(const eshkol_macro_def_t* macro);
```

Register a macro definition.

### `MacroExpander::expandNode`

*Function* — line 140

```c
eshkol_ast_t expandNode(const eshkol_ast_t& ast);
```

Expand a single AST node.

### `MacroExpander::tryExpandMacroCall`

*Function* — line 146

```c
eshkol_ast_t tryExpandMacroCall(const eshkol_ast_t& call);
```

Try to expand a call as a macro. Returns expanded AST if successful, or the original if not a macro call.

### `MacroExpander::matchPattern`

*Function* — line 152

```c
bool matchPattern(const eshkol_macro_pattern_t* pattern,
 const eshkol_ast_t& ast,
 const std::vector<std::string>& literals,
 Bindings& bindings);
```

Match a pattern against an AST. Returns true if match succeeds, populates bindings.

### `MacroExpander::matchPatternSeq`

*Function* — line 166

```c
bool matchPatternSeq(eshkol_macro_pattern_t* const* elements,
 uint64_t pat_start, uint64_t pat_count,
 const eshkol_ast_t* args, uint64_t arg_count,
 const std::vector<std::string>& literals,
 Bindings& bindings);
```

Match a sequence of pattern elements (a macro call's argument list, or the elements of a nested list pattern) against a flat array of ASTs. Shared by top-level macro-call matching and nested MACRO_PAT_LIST matching so both support ellipsis (including nested ellipsis) the same way. At most one element in [pat_start, pat_count) may be followed_by_ellipsis, and (per current grammar) it must be the last pattern element — it greedily consumes all remaining args.

### `MacroExpander::collectPatternVarDepths`

*Function* — line 181

```c
void collectPatternVarDepths(const eshkol_macro_pattern_t* pattern,
 const std::vector<std::string>& literals,
 int depth,
 std::map<std::string, int>& out) const;
```

Structurally collect the pattern variables bound within `pattern` (a single pattern element, possibly a nested list) and the ellipsis depth at which each is bound *relative to* `pattern` itself (i.e. not counting any ellipsis that wraps `pattern` from the outside). Used to know which variables to bind (and at what nested-empty shape) when an ellipsis pattern matches zero repetitions, and to know the variable set produced by one repetition when merging matched repetitions.

### `MacroExpander::instantiateTemplate`

*Function* — line 189

```c
eshkol_ast_t instantiateTemplate(const eshkol_macro_template_t* tmpl,
 const Bindings& bindings);
```

Instantiate a template with bindings.

### `MacroExpander::isLiteral`

*Function* — line 195

```c
bool isLiteral(const std::string& name,
 const std::vector<std::string>& literals) const;
```

Check if an identifier is a literal in the current macro.

### `MacroExpander::copyAst`

*Function* — line 201

```c
eshkol_ast_t copyAst(const eshkol_ast_t& ast);
```

Deep copy an AST node.

### `MacroExpander::isSymbol`

*Function* — line 206

```c
bool isSymbol(const eshkol_ast_t& ast, const std::string& name) const;
```

Check if an AST is a symbol with a given name.

### `MacroExpander::getSymbolName`

*Function* — line 211

```c
std::string getSymbolName(const eshkol_ast_t& ast) const;
```

Get symbol name from an AST (or empty string if not a symbol).

### `MacroExpander::substituteBindings`

*Function* — line 216

```c
eshkol_ast_t substituteBindings(const eshkol_ast_t& ast, const Bindings& bindings);
```

Substitute bindings in an AST (for template instantiation).

### `MacroExpander::isEllipsisSymbol`

*Function* — line 221

```c
bool isEllipsisSymbol(const eshkol_ast_t& ast) const;
```

Check if an AST node is the syntax-rules ellipsis marker.

### `MacroExpander::peelBindingsAtIndex`

*Function* — line 230

```c
Bindings peelBindingsAtIndex(const Bindings& bindings, size_t index) const;
```

"Peel" one ellipsis level off every depth>=1 binding in `bindings`, selecting the sub-tree for repetition `index` (depth-0 bindings are shared unchanged across every repetition). The result is the binding environment to use when instantiating a repeated template element for one specific repetition.

### `MacroExpander::findEllipsisDriver`

*Function* — line 239

```c
bool findEllipsisDriver(const eshkol_ast_t& ast,
 const Bindings& bindings,
 std::string& binding_name) const;
```

Find a pattern variable referenced within `ast` whose bound MatchTree has depth >= 1 (i.e. it was matched under at least one ellipsis) — this is the variable that "drives" the repetition count when `ast` is followed by ellipsis in a template. Depth-0 (non-repeated) bindings referenced alongside it are ignored/shared, not used to drive count.

### `MacroExpander::expandEllipsisElementN`

*Function* — line 250

```c
std::vector<eshkol_ast_t> expandEllipsisElementN(const eshkol_ast_t& ast,
 const Bindings& bindings,
 int ellipsis_count);
```

Expand a template element followed by `ellipsis_count` consecutive ellipses (R7RS 4.3.2 nested ellipsis, e.g. "row ... ..."). For ellipsis_count == 1 this is the ordinary single-ellipsis repetition; for ellipsis_count > 1 each extra ellipsis flattens (splices) one more nesting level into the enclosing list instead of nesting sub-lists.

### `MacroExpander::substituteBindingsInList`

*Function* — line 257

```c
std::vector<eshkol_ast_t> substituteBindingsInList(const eshkol_ast_t* items,
 uint64_t count,
 const Bindings& bindings);
```

Substitute a sequence/call argument list, expanding element ellipses.

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `MacroExpander::MacroExpander` | Function | 37 |
| `MacroExpander::scope_stack_` | Variable | 87 |
| `MacroExpander::pushScope` | Function | 93 |
| `MacroExpander::popScope` | Function | 94 |
| `MacroExpander::lookupMacro` | Function | 97 |
| `MacroExpander::MatchTree` | Struct | 107 |
| `MacroExpander::MatchTree::depth` | Variable | 108 |
| `MacroExpander::MatchTree::elements` | Variable | 110 |
| `MacroExpander::matchTreeHasValue` | Function | 116 |
| `MacroExpander::matchTreeFirstScalar` | Function | 122 |
| `MacroExpander::Binding` | Struct | 126 |
| `MacroExpander::Binding::name` | Variable | 127 |
| `MacroExpander::Binding::tree` | Variable | 128 |
| `MacroExpander::Bindings` | Typedef | 130 |
