# `eshkol/agent_capabilities.h`

C ABI of the agent capability library: compression, SQLite hooks, tree-sitter parsing and queries, and Yoga flexbox layout. Every entry point is a plain C function over integers, doubles, byte buffers and opaque integer handles, so that the same symbols can be bound from compiled Eshkol code (`extern` declarations in lib/agent/*.esk), from the JIT, and from the bytecode VM's native bridge. Conventions shared by the whole header: - Handles are small positive integers indexing process-global tables. 0 is never a valid handle; creation functions return -1 on failure. Passing a stale, out-of-range or never-issued handle is safe: void functions ignore it and value-returning functions report failure. - Buffer-filling functions take a caller-provided buffer and its capacity in bytes and return the number of bytes produced, or -1 on error. Each function documents whether an undersized buffer is an error or a silent truncation. - The prototypes carry no parameter names; the `@param` names below are the names used by the definitions and are listed in declaration order. - The tree-sitter and Yoga tables are each guarded by one process-wide mutex, so those calls may be made from any thread. The compression functions keep no shared state. The SQLite hooks do not lock their handle table.

37 public symbol(s) — 37 documented, 0 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `ESHKOL_AGENT_API`

*Macro* — line 41

```c
# define ESHKOL_AGENT_API __declspec(dllexport)
```

Symbol-visibility decoration applied to every function in this header. This branch (Windows, shared agent library, compiling the library itself) expands to `__declspec(dllexport)`.

### `ESHKOL_AGENT_API`

*Macro* — line 50

```c
# define ESHKOL_AGENT_API __declspec(dllimport)
```

Symbol-visibility decoration applied to every function in this header. This branch (Windows, consuming a shared agent library) expands to `__declspec(dllimport)`. Define `ESHKOL_AGENT_SHARED` only when linking against a DLL build of the agent library.

### `ESHKOL_AGENT_API`

*Macro* — line 71

```c
# define ESHKOL_AGENT_API __attribute__((visibility("default")))
```

Symbol-visibility decoration applied to every function in this header. On non-Windows targets it expands to `__attribute__((visibility("default")))`, which keeps the capability symbols exported from executables and shared objects built with hidden default visibility so that the JIT and the VM can resolve them by name.

### `eshkol_compression_available`

*Function* — line 87

```c
ESHKOL_AGENT_API int32_t eshkol_compression_available(void);
```

Reports whether the zlib-backed compression capability is present. The capability has no unavailable build: a library that defines this symbol always contains the real codec. Hosts that resolve capabilities dynamically treat a missing symbol as "unavailable".

**Returns**

Always 1.

### `eshkol_deflate`

*Function* — line 105

```c
ESHKOL_AGENT_API int32_t eshkol_deflate(const char*, int32_t, char*, int32_t);
```

Compresses bytes into a caller-provided buffer as a zlib stream. The output is the zlib container (RFC 1950: two-byte header, deflate data, Adler-32 trailer) at the default compression level, not a raw RFC 1951 deflate stream. It is the inverse of eshkol_inflate_data().

**Parameters**

- `data` — Input bytes. May be NULL only when `data_len` is 0.
- `data_len` — Number of input bytes; must be >= 0.
- `buf` — Destination buffer; must not be NULL.
- `buf_size` — Capacity of `buf` in bytes; must be > 0.

**Returns**

Number of compressed bytes written to `buf,` or -1 when an argument is invalid or the compressed stream does not fit in

### `eshkol_inflate_data`

*Function* — line 122

```c
ESHKOL_AGENT_API int32_t eshkol_inflate_data(const char*, int32_t, char*, int32_t);
```

Decompresses a zlib stream into a caller-provided buffer. Accepts the RFC 1950 container produced by eshkol_deflate().

**Parameters**

- `data` — Compressed bytes. May be NULL only when `data_len` is 0.
- `data_len` — Number of compressed bytes; must be >= 0.
- `buf` — Destination buffer; must not be NULL.
- `buf_size` — Capacity of `buf` in bytes; must be > 0. It is also the hard limit on the decompressed size.

**Returns**

Number of decompressed bytes written to `buf,` or -1 when an argument is invalid, the input is corrupt or incomplete, or the decompressed data does not fit in `buf_size` bytes. Nothing is NUL-terminated. On -1 the contents of `buf` are unspecified.

### `eshkol_gzip`

*Function* — line 140

```c
ESHKOL_AGENT_API int32_t eshkol_gzip(const char*, int32_t, char*, int32_t);
```

Compresses bytes into a caller-provided buffer in gzip format. The output is a complete RFC 1952 gzip member (header, deflate data, CRC-32 and length trailer) at the default compression level, produced in a single pass.

**Parameters**

- `data` — Input bytes. May be NULL only when `data_len` is 0.
- `data_len` — Number of input bytes; must be >= 0.
- `buf` — Destination buffer; must not be NULL.
- `buf_size` — Capacity of `buf` in bytes; must be > 0.

**Returns**

Number of gzip bytes written to `buf,` or -1 when an argument is invalid, the codec cannot be initialised, or the complete gzip stream does not fit in `buf_size` bytes. On -1 the contents of

### `eshkol_gunzip`

*Function* — line 158

```c
ESHKOL_AGENT_API int32_t eshkol_gunzip(const char*, int32_t, char*, int32_t);
```

Decompresses gzip data into a caller-provided buffer. Accepts an RFC 1952 gzip stream (from eshkol_gzip() or any other gzip producer) and decodes it in a single pass.

**Parameters**

- `data` — Gzip bytes. May be NULL only when `data_len` is 0.
- `data_len` — Number of gzip bytes; must be >= 0.
- `buf` — Destination buffer; must not be NULL.
- `buf_size` — Capacity of `buf` in bytes; must be > 0. It is also the hard limit on the decompressed size.

**Returns**

Number of decompressed bytes written to `buf,` or -1 when an argument is invalid, the input is corrupt or truncated, or the decompressed data does not fit in `buf_size` bytes. Nothing is NUL-terminated. On -1 the contents of `buf` are unspecified.

### `eshkol_deflate_alloc`

*Function* — line 182

```c
ESHKOL_AGENT_API int32_t eshkol_deflate_alloc(const char*, int32_t, int32_t,
 char**, int32_t*);
```

Compresses bytes to a zlib (RFC 1950) stream in a newly allocated buffer. Allocating variant of eshkol_deflate(), used by the Scheme native and VM bridges. The buffer is sized from the codec's worst-case bound for

**Parameters**

- `data` — Input bytes. May be NULL only when `data_len` is 0.
- `data_len` — Number of input bytes; must be >= 0.
- `max_output` — Hard output limit in bytes; must be > 0. The call fails when the worst-case compressed size for `data_len` exceeds this limit, even if the actual output would be smaller.
- `out` — Receives the allocated buffer; must not be NULL. Set to NULL on failure.
- `out_len` — Receives the number of valid bytes in `*out`; must not be NULL. Set to 0 on failure.

**Returns**

0 on success, -1 on invalid arguments, a limit violation, codec failure or allocation failure.

> Note: On success the caller owns `*out` and releases it with eshkol_compression_free(). The buffer is not NUL-terminated.

### `eshkol_inflate_alloc`

*Function* — line 206

```c
ESHKOL_AGENT_API int32_t eshkol_inflate_alloc(const char*, int32_t, int32_t,
 char**, int32_t*);
```

Decompresses a zlib (RFC 1950) stream into a newly allocated buffer. Allocating variant of eshkol_inflate_data(). The output buffer starts at four times the input size (at least 64 KiB, at most `max_output)` and doubles until the stream ends or the limit is reached.

**Parameters**

- `data` — Compressed bytes. May be NULL only when `data_len` is 0.
- `data_len` — Number of compressed bytes; must be >= 0.
- `max_output` — Hard decompression-bomb limit in bytes; must be > 0. A stream that expands beyond it fails; no partial result is returned.
- `out` — Receives the allocated buffer; must not be NULL. Set to NULL on failure.
- `out_len` — Receives the number of valid bytes in `*out`; must not be NULL. Set to 0 on failure.

**Returns**

0 on success, -1 on invalid arguments, corrupt or truncated input, output larger than `max_output,` or allocation failure.

> Note: On success the caller owns `*out` and releases it with eshkol_compression_free(). The buffer is not NUL-terminated.

### `eshkol_gzip_alloc`

*Function* — line 224

```c
ESHKOL_AGENT_API int32_t eshkol_gzip_alloc(const char*, int32_t, int32_t,
 char**, int32_t*);
```

Compresses bytes to gzip (RFC 1952) format in a newly allocated buffer. Allocating variant of eshkol_gzip(); parameters, limit rule, return value and ownership are those of eshkol_deflate_alloc().

**Parameters**

- `data` — Input bytes. May be NULL only when `data_len` is 0.
- `data_len` — Number of input bytes; must be >= 0.
- `max_output` — Hard output limit in bytes; must be > 0 and at least the codec's worst-case compressed size for `data_len.`
- `out` — Receives the allocated buffer (NULL on failure).
- `out_len` — Receives the byte count of `*out` (0 on failure).

**Returns**

0 on success, -1 on failure.

> Note: Release `*out` with eshkol_compression_free().

### `eshkol_gunzip_alloc`

*Function* — line 243

```c
ESHKOL_AGENT_API int32_t eshkol_gunzip_alloc(const char*, int32_t, int32_t,
 char**, int32_t*);
```

Decompresses gzip (RFC 1952) data into a newly allocated buffer, subject to an output limit. Allocating variant of eshkol_gunzip(); growth policy, limit rule, return value and ownership are those of eshkol_inflate_alloc().

**Parameters**

- `data` — Gzip bytes. May be NULL only when `data_len` is 0.
- `data_len` — Number of gzip bytes; must be >= 0.
- `max_output` — Hard decompression-bomb limit in bytes; must be > 0.
- `out` — Receives the allocated buffer (NULL on failure).
- `out_len` — Receives the byte count of `*out` (0 on failure).

**Returns**

0 on success, -1 on invalid arguments, corrupt or truncated input, output larger than `max_output,` or allocation failure.

> Note: Release `*out` with eshkol_compression_free().

### `eshkol_compression_free`

*Function* — line 253

```c
ESHKOL_AGENT_API void eshkol_compression_free(void*);
```

Releases a buffer returned through `out` by one of the `eshkol_*_alloc` compression entry points.

**Parameters**

- `ptr` — Buffer to release; NULL is accepted and ignored. Must not be a pointer obtained from any other allocator.

### `eshkol_sqlite_exec`

*Function* — line 269

```c
ESHKOL_AGENT_API int eshkol_sqlite_exec(int64_t handle, const char* sql);
```

Executes one or more SQL statements that return no rows. One of the SQLite hooks that a hosted VM resolves through this bounded capability ABI. Intended for DDL and DML; result rows, if any, are discarded. The SQLite error message is not retained by this call.

**Parameters**

- `handle` — Database handle issued by the agent SQLite binding (`eshkol_sqlite_open`, bound in lib/agent/sqlite.esk).
- `sql` — NUL-terminated SQL text; may contain several statements separated by semicolons.

**Returns**

0 on success, the positive SQLite result code on an SQL error, or -1 when `handle` is not an open database or `sql` is NULL.

### `eshkol_sqlite_last_insert_rowid`

*Function* — line 278

```c
ESHKOL_AGENT_API int64_t eshkol_sqlite_last_insert_rowid(int64_t handle);
```

Returns the rowid of the most recent successful INSERT on a connection.

**Parameters**

- `handle` — Database handle issued by the agent SQLite binding.

**Returns**

The last inserted rowid (0 when the connection has not inserted a row), or -1 when `handle` is not an open database.

### `eshkol_sqlite_changes`

*Function* — line 288

```c
ESHKOL_AGENT_API int eshkol_sqlite_changes(int64_t handle);
```

Returns the number of rows changed by the most recent INSERT, UPDATE or DELETE on a connection.

**Parameters**

- `handle` — Database handle issued by the agent SQLite binding.

**Returns**

The row-change count, which may legitimately be 0, or -1 when

### `eshkol_ts_available`

*Function* — line 298

```c
ESHKOL_AGENT_API int32_t eshkol_ts_available(void);
```

Reports whether the tree-sitter capability is present. The capability has no fallback build: a library that defines this symbol always contains the real parser and its bundled grammars.

**Returns**

Always 1.

### `eshkol_ts_parser_new`

*Function* — line 312

```c
ESHKOL_AGENT_API int64_t eshkol_ts_parser_new(const char*);
```

Creates a tree-sitter parser for a named language.

**Parameters**

- `language` — NUL-terminated language name or alias, matched exactly: "javascript"/"js", "typescript"/"ts", "tsx", "python"/"py", "rust"/"rs", "go", "c", "cpp"/"c++", "java", "ruby"/"rb", "bash"/"sh".

**Returns**

A parser handle (>= 1), or -1 when `language` is NULL or unknown, the parser cannot be created, or all 31 parser slots are in use.

> Note: Release the handle with eshkol_ts_parser_free(). Trees already produced by the parser stay valid after the parser is freed.

### `eshkol_ts_parser_free`

*Function* — line 320

```c
ESHKOL_AGENT_API void eshkol_ts_parser_free(int64_t);
```

Releases a tree-sitter parser handle.

**Parameters**

- `handle` — Parser handle from eshkol_ts_parser_new(). Out-of-range or already released handles are ignored.

### `eshkol_ts_parse`

*Function* — line 339

```c
ESHKOL_AGENT_API int64_t eshkol_ts_parse(int64_t, const char*, int32_t);
```

Parses source text with a tree-sitter parser handle. The source bytes are copied into storage owned by the returned tree, so the caller may release or modify its input as soon as the call returns. The copy backs eshkol_ts_node_text() and bounds the byte ranges accepted by the node functions. Syntax errors do not fail the parse: they appear as ERROR nodes in the tree.

**Parameters**

- `parser_handle` — Parser handle from eshkol_ts_parser_new().
- `source` — Source bytes; need not be NUL-terminated. May be NULL only when `source_len` is 0.
- `source_len` — Length of `source` in bytes; must be >= 0.

**Returns**

A tree handle (>= 1), or -1 on an invalid handle or argument, allocation or parser failure, or when all 63 tree slots are in use.

> Note: Release the handle with eshkol_ts_tree_free().

### `eshkol_ts_tree_free`

*Function* — line 347

```c
ESHKOL_AGENT_API void eshkol_ts_tree_free(int64_t);
```

Releases a tree-sitter syntax-tree handle and its copy of the source text.

**Parameters**

- `handle` — Tree handle from eshkol_ts_parse(). Out-of-range or already released handles are ignored.

### `eshkol_ts_tree_root`

*Function* — line 369

```c
ESHKOL_AGENT_API int32_t eshkol_ts_tree_root(int64_t, char*, int32_t);
```

Writes the root node record of a parsed tree into a caller-provided buffer. A node record is one line of nine tab-separated fields: `type`, `start_row`, `start_col`, `end_row`, `end_col`, `start_byte`, `end_byte`, `child_count`, `named` (1 for a named node, 0 for an anonymous one). Rows, columns and byte offsets are zero-based; `child_count` counts all children, named or not.

**Parameters**

- `tree_handle` — Tree handle from eshkol_ts_parse().
- `buf` — Destination buffer, or NULL together with
- `buf_size` — Capacity of `buf` in bytes, including room for the terminating NUL; must be >= 0.

**Returns**

Length of the record in bytes, excluding the terminating NUL. With a non-NULL `buf` the record must fit completely (length < `buf_size);` otherwise the call returns -1 and `buf` holds a NUL-terminated truncated prefix that must not be used. Also -1 on an invalid handle or argument.

### `eshkol_ts_node_info`

*Function* — line 390

```c
ESHKOL_AGENT_API int32_t eshkol_ts_node_info(int64_t, uint32_t, uint32_t,
 char*, int32_t);
```

Writes the record of the node covering a byte range into a caller-provided buffer. Selects the smallest named node whose span contains [`start_byte,` `end_byte)` and serialises it in the record format described at eshkol_ts_tree_root().

**Parameters**

- `tree_handle` — Tree handle from eshkol_ts_parse().
- `start_byte` — Inclusive start offset into the parsed source.
- `end_byte` — Exclusive end offset; must be greater than
- `buf` — Destination buffer, or NULL together with
- `buf_size` — Capacity of `buf` in bytes, including the NUL.

**Returns**

Length of the record excluding the NUL, or -1 on an invalid handle, an empty or out-of-range byte range, no matching node, or a record that does not fit completely in `buf.`

### `eshkol_ts_node_children`

*Function* — line 419

```c
ESHKOL_AGENT_API int32_t eshkol_ts_node_children(int64_t, uint32_t, uint32_t,
 char*, int32_t, int32_t*);
```

Writes the named children of a tree-sitter node into a caller-provided buffer. When `start_byte` and `end_byte` are both 0 the target is the root node; otherwise it is the smallest named node containing [`start_byte,` `end_byte).` Each named child is written as one node record (see eshkol_ts_tree_root()); records are separated by a single NUL byte and the last record is followed by a terminating NUL. Anonymous children (punctuation, keywords) are not reported.

**Parameters**

- `tree_handle` — Tree handle from eshkol_ts_parse().
- `start_byte` — Inclusive start offset, or 0 with `end_byte` 0 for the root.
- `end_byte` — Exclusive end offset, no larger than the source length.
- `buf` — Destination buffer, or NULL together with
- `buf_size` — Capacity of `buf` in bytes; must exceed the returned byte count by at least one (room for the final NUL).
- `count_out` — Receives the number of child records; may be NULL. It is set to 0 on every failure after argument validation.

**Returns**

Total bytes occupied by the records and their separators, excluding the final NUL (0 when the node has no named children), or -1 on an invalid handle, an invalid byte range, no matching node, or a buffer too small for all records. There is no truncated result: when the buffer is too small nothing is written.

### `eshkol_ts_node_text`

*Function* — line 444

```c
ESHKOL_AGENT_API int32_t eshkol_ts_node_text(int64_t, uint32_t, uint32_t,
 char*, int32_t);
```

Writes the source text represented by a byte range of a parsed tree into a caller-provided buffer. Copies bytes [`start_byte,` `end_byte)` of the tree's own copy of the source and NUL-terminates the result. Byte ranges normally come from the `start_byte`/`end_byte` fields of a node record.

**Parameters**

- `tree_handle` — Tree handle from eshkol_ts_parse().
- `start_byte` — Inclusive start offset; must be less than the source length.
- `end_byte` — Exclusive end offset; must be greater than
- `buf` — Destination buffer, or NULL together with
- `buf_size` — Capacity of `buf` in bytes including the NUL. When

**Returns**

Number of text bytes written, excluding the NUL. Text longer than

### `eshkol_ts_query_new`

*Function* — line 460

```c
ESHKOL_AGENT_API int64_t eshkol_ts_query_new(const char*, const char*);
```

Creates a tree-sitter query handle from a language and a query string.

**Parameters**

- `language` — NUL-terminated language name or alias, as accepted by eshkol_ts_parser_new(). A query can only be run against trees parsed with the same grammar.
- `pattern` — NUL-terminated tree-sitter query in S-expression form, for example `(function_definition name: (identifier) @name)`.

**Returns**

A query handle (>= 1), or -1 when an argument is NULL, the language is unknown, the pattern does not compile (the error position is not reported), or all 31 query slots are in use.

> Note: Release the handle with eshkol_ts_query_free().

### `eshkol_ts_query_free`

*Function* — line 468

```c
ESHKOL_AGENT_API void eshkol_ts_query_free(int64_t);
```

Releases a tree-sitter query handle.

**Parameters**

- `handle` — Query handle from eshkol_ts_query_new(). Out-of-range or already released handles are ignored.

### `eshkol_ts_query_matches`

*Function* — line 498

```c
ESHKOL_AGENT_API int32_t eshkol_ts_query_matches(int64_t, int64_t, int32_t,
 char*, int32_t, int32_t*);
```

Writes the captures matched by a tree-sitter query against a syntax tree into a caller-provided buffer. Every capture of every match becomes one record of seven tab-separated fields: `capture_name`, `start_byte`, `end_byte`, `start_row`, `start_col`, `end_row`, `end_col`. Records are separated by a single NUL byte and the last record is followed by a terminating NUL.

**Parameters**

- `query_handle` — Query handle from eshkol_ts_query_new().
- `tree_handle` — Tree handle from eshkol_ts_parse(), parsed with the same grammar as the query.
- `max_matches` — Result limit; must be >= 0. 0 selects the built-in ceiling of 100000. The limit is compared with the number of capture records written so far before each new match is taken, so a match with several captures can carry the record count past the limit.
- `buf` — Destination buffer, or NULL together with
- `buf_size` — Capacity of `buf` in bytes; must exceed the returned byte count by at least one.
- `count_out` — Receives the number of capture records; may be NULL.

**Returns**

Total bytes occupied by the records and their separators, excluding the final NUL (0 when nothing matched), or -1 on an invalid handle or argument, a query and tree from different grammars, or a buffer too small for all records. On a too-small buffer there is no partial result: `buf[0]` is set to NUL and `*count_out` to 0.

### `eshkol_ts_tree_sexp`

*Function* — line 518

```c
ESHKOL_AGENT_API int32_t eshkol_ts_tree_sexp(int64_t, char*, int32_t);
```

Writes an S-expression representation of a syntax tree into a caller-provided buffer. The text is tree-sitter's own rendering of the root node, intended for debugging and inspection.

**Parameters**

- `tree_handle` — Tree handle from eshkol_ts_parse().
- `buf` — Destination buffer; must not be NULL (this function has no size-query mode).
- `buf_size` — Capacity of `buf` in bytes including the NUL; must be > 0.

**Returns**

Number of bytes written, excluding the NUL. An S-expression longer than `buf_size` - 1 bytes is silently truncated to that length; a return value equal to `buf_size` - 1 therefore indicates possible truncation. -1 on an invalid handle or argument, or allocation failure.

### `eshkol_yoga_available`

*Function* — line 528

```c
ESHKOL_AGENT_API int32_t eshkol_yoga_available(void);
```

Reports whether the Yoga flexbox layout capability is present. The capability has no no-op build: a library that defines this symbol always contains the real layout engine.

**Returns**

Always 1.

### `eshkol_yoga_node_create`

*Function* — line 539

```c
ESHKOL_AGENT_API int64_t eshkol_yoga_node_create(void);
```

Creates an opaque Yoga layout node handle. The node starts with Yoga's default style and no parent.

**Returns**

A node handle (>= 1), or -1 when the node cannot be allocated or all 511 node slots are in use.

> Note: Release the handle with eshkol_yoga_node_free().

### `eshkol_yoga_node_free`

*Function* — line 551

```c
ESHKOL_AGENT_API void eshkol_yoga_node_free(int64_t);
```

Releases an opaque Yoga layout node handle together with its subtree. The node is detached from its parent, if any, and the node and all of its descendants are destroyed; every handle that referred to a node of that subtree becomes invalid.

**Parameters**

- `handle` — Node handle from eshkol_yoga_node_create(). Out-of-range or already released handles are ignored.

### `eshkol_yoga_node_set_float`

*Function* — line 569

```c
ESHKOL_AGENT_API void eshkol_yoga_node_set_float(int64_t, int32_t, double);
```

Sets a floating-point Yoga node property.

**Parameters**

- `handle` — Node handle.
- `prop` — Property selector: 0 width, 1 height, 2 min-width, 3 min-height, 4 max-width, 5 max-height, 6 flex-grow, 7 flex-shrink, 8 flex-basis, 9 gap (rows and columns), 10-13 padding left/right/top/bottom, 14-17 margin left/right/top/bottom, 18-21 border left/right/top/bottom. Lengths are in points.
- `value` — Property value; narrowed to `float`. It must be finite, and must be >= 0 for every selector except the margins (14-17), which accept negative values.

> Note: The call is ignored, with no error reported, when the handle is invalid, the selector is unknown, or the value is rejected.

### `eshkol_yoga_node_set_int`

*Function* — line 585

```c
ESHKOL_AGENT_API void eshkol_yoga_node_set_int(int64_t, int32_t, int32_t);
```

Sets an integer (enumerated) Yoga node property.

**Parameters**

- `handle` — Node handle.
- `prop` — Property selector: 0 flex-direction, 1 justify-content, 2 align-items, 3 align-self, 4 align-content, 5 position-type, 6 overflow, 7 display.
- `value` — Numeric value of the corresponding Yoga enumeration (`YGFlexDirection`, `YGJustify`, `YGAlign`, `YGPositionType`, `YGOverflow`, `YGDisplay`); values outside the enumeration's range are rejected.

> Note: The call is ignored, with no error reported, when the handle is invalid, the selector is unknown, or the value is rejected.

### `eshkol_yoga_node_add_child`

*Function* — line 601

```c
ESHKOL_AGENT_API void eshkol_yoga_node_add_child(int64_t, int64_t, int32_t);
```

Adds a child node at a requested Yoga index.

**Parameters**

- `parent` — Handle of the node that receives the child.
- `child` — Handle of the node to insert. It must not already have a parent, must differ from `parent,` and must not be an ancestor of `parent.`
- `index` — Zero-based insertion position; must be between 0 and the parent's current child count inclusive.

> Note: The call is ignored, with no error reported, when any of these conditions fails or a handle is invalid. The parent does not take over the child's handle: the child stays addressable, and freeing the parent invalidates it.

### `eshkol_yoga_node_calculate`

*Function* — line 617

```c
ESHKOL_AGENT_API void eshkol_yoga_node_calculate(int64_t, double, double);
```

Calculates a Yoga node layout for the supplied dimensions. Runs the flexbox algorithm over the tree rooted at `root` with left-to-right direction. Afterwards the results for the root and every descendant are available through eshkol_yoga_node_get_computed().

**Parameters**

- `root` — Handle of the root node of the tree to lay out.
- `width` — Available width in points: a finite value >= 0, or NaN for "undefined" (size to content).
- `height` — Available height in points, with the same rule as `width.`

> Note: The call is ignored when the handle is invalid or a dimension is negative or infinite.

### `eshkol_yoga_node_get_computed`

*Function* — line 634

```c
ESHKOL_AGENT_API double eshkol_yoga_node_get_computed(int64_t, int32_t);
```

Reads a computed Yoga node layout value. Values are meaningful after eshkol_yoga_node_calculate() has run on a tree containing the node. Positions are relative to the node's parent.

**Parameters**

- `handle` — Node handle.
- `prop` — Result selector: 0 left, 1 top, 2 width, 3 height, 4-7 padding left/top/right/bottom, 8-11 margin left/top/right/bottom, 12-15 border left/top/right/bottom. Note that the edge order differs from the setter's left/right/top/bottom order.

**Returns**

The computed value, or 0.0 when the handle is invalid or the selector is unknown.
