# `eshkol/backend/collection_codegen.h`

32 public symbol(s) — 20 documented, 12 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `CollectionCodegen`

*class* — line 29

```c
class CollectionCodegen
```

CollectionCodegen handles list and vector operations.

### `CollectionCodegen::cons`

*Function* — line 43

```c
llvm::Value* cons(const eshkol_operations_t* op);
```

Create a cons cell: (cons car cdr)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged cons pointer

### `CollectionCodegen::car`

*Function* — line 50

```c
llvm::Value* car(const eshkol_operations_t* op);
```

Get the car of a pair: (car pair)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged value

### `CollectionCodegen::cdr`

*Function* — line 57

```c
llvm::Value* cdr(const eshkol_operations_t* op);
```

Get the cdr of a pair: (cdr pair)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged value

### `CollectionCodegen::list`

*Function* — line 66

```c
llvm::Value* list(const eshkol_operations_t* op);
```

Create a list: (list elem1 elem2 ...)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged cons pointer to list head

### `CollectionCodegen::listStar`

*Function* — line 73

```c
llvm::Value* listStar(const eshkol_operations_t* op);
```

Create a list with last element as tail: (list* elem1 elem2 ... tail)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged cons pointer to list head

### `CollectionCodegen::isNull`

*Function* — line 80

```c
llvm::Value* isNull(const eshkol_operations_t* op);
```

Check if value is null (empty list): (null? val)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged boolean

### `CollectionCodegen::isPair`

*Function* — line 87

```c
llvm::Value* isPair(const eshkol_operations_t* op);
```

Check if value is a pair: (pair? val)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged boolean

### `CollectionCodegen::makeVector`

*Function* — line 96

```c
llvm::Value* makeVector(const eshkol_operations_t* op);
```

Create a vector with size and fill: (make-vector size [fill])

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged vector pointer

### `CollectionCodegen::vector`

*Function* — line 103

```c
llvm::Value* vector(const eshkol_operations_t* op);
```

Create a vector from elements: (vector elem1 elem2 ...)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged vector pointer

### `CollectionCodegen::vectorLength`

*Function* — line 110

```c
llvm::Value* vectorLength(const eshkol_operations_t* op);
```

Get vector length: (vector-length vec)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged integer

### `CollectionCodegen::vectorRef`

*Function* — line 117

```c
llvm::Value* vectorRef(const eshkol_operations_t* op);
```

Get element at index: (vector-ref vec idx)

**Parameters**

- `op` — The operation AST node

**Returns**

Tagged value

### `CollectionCodegen::vectorSet`

*Function* — line 124

```c
llvm::Value* vectorSet(const eshkol_operations_t* op);
```

Set element at index: (vector-set! vec idx val)

**Parameters**

- `op` — The operation AST node

**Returns**

Unspecified value

### `CollectionCodegen::vectorCopy`

*Function* — line 131

```c
llvm::Value* vectorCopy(const eshkol_operations_t* op);
```

Copy elements between vectors: (vector-copy! to at from [start [end]])

**Parameters**

- `op` — The operation AST node

**Returns**

Unspecified value

### `CollectionCodegen::vectorCopyNew`

*Function* — line 141

```c
llvm::Value* vectorCopyNew(const eshkol_operations_t* op);
```

Copy a vector slice into a fresh vector: (vector-copy v) — whole vector (vector-copy v start) — from start to end (vector-copy v start end) — the [start,end) slice

**Parameters**

- `op` — The operation AST node

**Returns**

New vector as tagged value

### `CollectionCodegen::vectorAppend`

*Function* — line 148

```c
llvm::Value* vectorAppend(const eshkol_operations_t* op);
```

Concatenate vectors: (vector-append vec1 vec2 ...)

**Parameters**

- `op` — The operation AST node

**Returns**

New vector as tagged value

### `CollectionCodegen::vectorFill`

*Function* — line 155

```c
llvm::Value* vectorFill(const eshkol_operations_t* op);
```

Fill vector with value: (vector-fill! vec fill)

**Parameters**

- `op` — The operation AST node

**Returns**

Unspecified value

### `CollectionCodegen::vectorToList`

*Function* — line 162

```c
llvm::Value* vectorToList(const eshkol_operations_t* op);
```

Convert vector to list: (vector->list vec [start [end]])

**Parameters**

- `op` — The operation AST node

**Returns**

List as tagged value

### `CollectionCodegen::listToVector`

*Function* — line 169

```c
llvm::Value* listToVector(const eshkol_operations_t* op);
```

Convert list to vector: (list->vector lst)

**Parameters**

- `op` — The operation AST node

**Returns**

Vector as tagged value

### `CollectionCodegen::allocConsCell`

*Function* — line 179

```c
llvm::Value* allocConsCell(llvm::Value* car_val, llvm::Value* cdr_val);
```

Allocate and initialize a cons cell in arena memory.

**Parameters**

- `car_val` — The car value (tagged)
- `cdr_val` — The cdr value (tagged)

**Returns**

Tagged cons pointer

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `CollectionCodegen::CollectionCodegen` | Function | 30 |
| `CollectionCodegen::ctx_` | Variable | 181 |
| `CollectionCodegen::tagged_` | Variable | 183 |
| `CollectionCodegen::mem_` | Variable | 184 |
| `CollectionCodegen::CodegenASTFunc` | Typedef | 187 |
| `CollectionCodegen::CodegenTypedASTFunc` | Typedef | 188 |
| `CollectionCodegen::TypedToTaggedFunc` | Typedef | 189 |
| `CollectionCodegen::nullptr` | Variable | 191 |
| `CollectionCodegen::nullptr` | Variable | 192 |
| `CollectionCodegen::nullptr` | Variable | 193 |
| `CollectionCodegen::nullptr` | Variable | 194 |
| `CollectionCodegen::setCodegenCallbacks` | Function | 196 |
