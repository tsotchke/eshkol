# `eshkol/backend/arithmetic_codegen.h`

46 public symbol(s) — 39 documented, 7 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `ArithmeticCodegen`

*class* — line 43

```c
class ArithmeticCodegen
```

ArithmeticCodegen handles polymorphic arithmetic on tagged values. All operations take tagged_value inputs and return tagged_value results. The type of the result depends on the input types: - int + int = int - int + double = double - double + double = double - dual + any = dual (forward-mode AD) - ad_node + any = ad_node (reverse-mode AD graph) - vector/tensor + vector/tensor = vector/tensor (element-wise)

### `ArithmeticCodegen::add`

*Function* — line 64

```c
llvm::Value* add(llvm::Value* left, llvm::Value* right);
```

Polymorphic addition: a + b

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::sub`

*Function* — line 72

```c
llvm::Value* sub(llvm::Value* left, llvm::Value* right);
```

Polymorphic subtraction: a - b

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::mul`

*Function* — line 80

```c
llvm::Value* mul(llvm::Value* left, llvm::Value* right);
```

Polymorphic multiplication: a * b

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::div`

*Function* — line 88

```c
llvm::Value* div(llvm::Value* left, llvm::Value* right);
```

Polymorphic division: a / b

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::mod`

*Function* — line 96

```c
llvm::Value* mod(llvm::Value* left, llvm::Value* right);
```

Polymorphic modulo: a % b (integer only)

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::neg`

*Function* — line 105

```c
llvm::Value* neg(llvm::Value* operand);
```

Polymorphic negation: -a

**Parameters**

- `operand` — Operand (tagged_value)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::abs`

*Function* — line 112

```c
llvm::Value* abs(llvm::Value* operand);
```

Polymorphic absolute value: |a|

**Parameters**

- `operand` — Operand (tagged_value)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::compare`

*Function* — line 124

```c
llvm::Value* compare(llvm::Value* left, llvm::Value* right, const std::string& operation);
```

Polymorphic comparison: a <op> b Handles int/double/string comparisons with type promotion.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)
- `operation` — One of: "lt", "gt", "eq", "le", "ge"

**Returns**

Boolean result as tagged_value

### `ArithmeticCodegen::mathFunc`

*Function* — line 134

```c
llvm::Value* mathFunc(llvm::Value* operand, const std::string& func_name);
```

Unary math function with dual number and AD node support.

**Parameters**

- `operand` — Operand (tagged_value)
- `func_name` — Function name (sin, cos, exp, log, etc.)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::pow`

*Function* — line 142

```c
llvm::Value* pow(llvm::Value* base, llvm::Value* exponent);
```

Power function: base^exponent

**Parameters**

- `base` — Base value (tagged_value)
- `exponent` — Exponent value (tagged_value)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::min`

*Function* — line 150

```c
llvm::Value* min(llvm::Value* left, llvm::Value* right);
```

Minimum of two values.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

Minimum as tagged_value

### `ArithmeticCodegen::max`

*Function* — line 158

```c
llvm::Value* max(llvm::Value* left, llvm::Value* right);
```

Maximum of two values.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

Maximum as tagged_value

### `ArithmeticCodegen::remainder`

*Function* — line 166

```c
llvm::Value* remainder(llvm::Value* dividend, llvm::Value* divisor);
```

Integer remainder (Scheme remainder semantics).

**Parameters**

- `dividend` — Dividend (tagged_value)
- `divisor` — Divisor (tagged_value)

**Returns**

Remainder as tagged_value

### `ArithmeticCodegen::quotient`

*Function* — line 174

```c
llvm::Value* quotient(llvm::Value* dividend, llvm::Value* divisor);
```

Integer quotient (truncated division).

**Parameters**

- `dividend` — Dividend (tagged_value)
- `divisor` — Divisor (tagged_value)

**Returns**

Quotient as tagged_value

### `ArithmeticCodegen::intToDouble`

*Function* — line 183

```c
llvm::Value* intToDouble(llvm::Value* int_tagged);
```

Convert integer to double.

**Parameters**

- `int_tagged` — Integer as tagged_value

**Returns**

Double as tagged_value

### `ArithmeticCodegen::doubleToInt`

*Function* — line 190

```c
llvm::Value* doubleToInt(llvm::Value* double_tagged);
```

Convert double to integer (truncation).

**Parameters**

- `double_tagged` — Double as tagged_value

**Returns**

Integer as tagged_value

### `ArithmeticCodegen::extractAsDouble`

*Function* — line 200

```c
llvm::Value* extractAsDouble(llvm::Value* tagged);
```

Extract the numeric value from a tagged value as a double. Works for INT64, DOUBLE, and DUAL_NUMBER types.

**Parameters**

- `tagged` — Tagged value

**Returns**

LLVM double value

### `ArithmeticCodegen::withADBinaryDispatch`

*Function* — line 215

```c
llvm::Value* withADBinaryDispatch(
 llvm::Value* left, llvm::Value* right,
 int ad_op_type,
 std::function<llvm::Value*()> regular_fn);
```

Central binary AD dispatch handler. Checks if either operand is an AD node (CALLABLE + AD_NODE subtype). If so, converts both to AD nodes, records binary op on tape, returns result. Otherwise, calls regular_fn lambda for normal dispatch.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)
- `ad_op_type` — AD operation type code (e.g., AD_NODE_ADD=2, AD_NODE_MUL=4)
- `regular_fn` — Lambda that emits all non-AD code paths, returns tagged_value

**Returns**

Result as tagged_value (either AD-wrapped or regular)

### `ArithmeticCodegen::withADUnaryDispatch`

*Function* — line 230

```c
llvm::Value* withADUnaryDispatch(
 llvm::Value* operand,
 int ad_op_type,
 std::function<llvm::Value*()> regular_fn);
```

Central unary AD dispatch handler. Checks if operand is an AD node (CALLABLE + AD_NODE subtype). If so, records unary op on tape, returns result. Otherwise, calls regular_fn lambda for normal dispatch.

**Parameters**

- `operand` — Operand (tagged_value)
- `ad_op_type` — AD operation type code (e.g., AD_NODE_NEG=11, AD_NODE_ABS=42)
- `regular_fn` — Lambda that emits all non-AD code paths, returns tagged_value

**Returns**

Result as tagged_value (either AD-wrapped or regular)

### `ArithmeticCodegen::emitBignumBinaryCall`

*Function* — line 246

```c
llvm::Value* emitBignumBinaryCall(llvm::Value* left, llvm::Value* right, int op_code);
```

Emit a call into the bignum runtime's binary-op kernel (add/sub/mul/ div/mod/quotient/remainder/negate), used once emitIsBignumCheck has confirmed at least one operand is an arbitrary-precision integer.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)
- `op_code` — Bignum binary operation code (e.g. 0=add, 1=sub, 2=mul, 3=div)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::emitBignumCompareCall`

*Function* — line 257

```c
llvm::Value* emitBignumCompareCall(llvm::Value* left, llvm::Value* right, int op_code);
```

Emit a call into the bignum runtime's comparison kernel, used once emitIsBignumCheck has confirmed at least one operand is an arbitrary-precision integer.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)
- `op_code` — Bignum comparison operation code (e.g. 0=lt, 1=gt)

**Returns**

Boolean result as tagged_value

### `ArithmeticCodegen::emitIsBignumCheck`

*Function* — line 267

```c
llvm::Value* emitIsBignumCheck(llvm::Value* left, llvm::Value* right);
```

Check whether either operand is a bignum (arbitrary-precision integer) tagged value, so arithmetic/comparison ops can be routed to the bignum dispatch helpers instead of the fixed-width int/double fast path.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

Boolean i1 value: true if either operand is a bignum

### `ArithmeticCodegen::emitIsTaylorCheck`

*Function* — line 279

```c
llvm::Value* emitIsTaylorCheck(llvm::Value* left, llvm::Value* right);
```

Check whether either operand is a Taylor tower (HEAP_PTR with HEAP_SUBTYPE_TAYLOR), so it can be intercepted before the generic vector/tensor heap path, which would otherwise misread its storage.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

i1 boolean: true if either operand is a Taylor tower

### `ArithmeticCodegen::emitIsTaylorSingle`

*Function* — line 286

```c
llvm::Value* emitIsTaylorSingle(llvm::Value* v);
```

Check whether a single operand is a Taylor tower.

**Parameters**

- `v` — Operand (tagged_value)

**Returns**

i1 boolean: true if the operand is a Taylor tower

### `ArithmeticCodegen::emitTaylorBinaryCall`

*Function* — line 297

```c
llvm::Value* emitTaylorBinaryCall(llvm::Value* left, llvm::Value* right, int op_code);
```

Emit a call into the Taylor-tower runtime's binary-op kernel (eshkol_taylor_binary_tagged), used once a Taylor tower operand has been detected.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)
- `op_code` — Taylor binary operation code (e.g. 0=add, 1=sub, 2=mul, 3=div)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::emitTaylorUnaryCall`

*Function* — line 306

```c
llvm::Value* emitTaylorUnaryCall(llvm::Value* in, int op_code);
```

Emit a call into the Taylor-tower runtime's unary-op kernel (eshkol_taylor_unary_tagged).

**Parameters**

- `in` — Operand (tagged_value)
- `op_code` — Taylor unary operation code

**Returns**

Result as tagged_value

### `ArithmeticCodegen::emitIsRationalCheck`

*Function* — line 318

```c
llvm::Value* emitIsRationalCheck(llvm::Value* left, llvm::Value* right);
```

Check whether either operand is an exact rational number, so arithmetic/comparison ops can be routed to the rational dispatch helpers instead of the fixed-width int/double fast path.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)

**Returns**

Boolean i1 value: true if either operand is a rational

### `ArithmeticCodegen::emitRationalBinaryCall`

*Function* — line 329

```c
llvm::Value* emitRationalBinaryCall(llvm::Value* left, llvm::Value* right, int op_code);
```

Emit a call into the rational runtime's binary-op kernel (eshkol_rational_binary_tagged_ptr), used once emitIsRationalCheck has confirmed at least one operand is an exact rational.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)
- `op_code` — Rational binary operation code (e.g. 0=add, 1=sub, 2=mul, 3=div)

**Returns**

Result as tagged_value

### `ArithmeticCodegen::emitRationalCompareCall`

*Function* — line 340

```c
llvm::Value* emitRationalCompareCall(llvm::Value* left, llvm::Value* right, int op_code);
```

Emit a call into the rational runtime's comparison kernel (eshkol_rational_compare_tagged_ptr), used once emitIsRationalCheck has confirmed at least one operand is an exact rational.

**Parameters**

- `left` — Left operand (tagged_value)
- `right` — Right operand (tagged_value)
- `op_code` — Rational comparison operation code (e.g. 0=lt, 1=gt)

**Returns**

Boolean result as tagged_value

### `ArithmeticCodegen::emitTypeError`

*Function* — line 347

```c
void emitTypeError(const char* message);
```

Emit code to raise a type error at runtime. Public so other codegen classes (e.g. CallApplyCodegen for (apply min '())) can surface the same architected exception instead of inventing their own mechanism.

### `ArithmeticCodegen::convertToDual`

*Function* — line 365

```c
llvm::Value* convertToDual(llvm::Value* operand, llvm::Value* is_dual, llvm::Value* is_double);
```

Convert operand to dual number (promote constants to dual with zero tangent). Public so other codegen modules can route non-arithmetic ops — modulo, min/max, comparisons — through the forward-mode AD path.

**Parameters**

- `operand` — Tagged value operand
- `is_dual` — Whether operand is already a dual number
- `is_double` — Whether operand is a double

**Returns**

Dual number struct

### `ArithmeticCodegen::convertToADNode`

*Function* — line 383

```c
llvm::Value* convertToADNode(llvm::Value* operand, llvm::Value* is_ad, llvm::Value* base_type);
```

Convert operand to AD node (promote constants to constant AD nodes).

**Parameters**

- `operand` — Tagged value operand
- `is_ad` — Whether operand is already an AD node
- `base_type` — The base type of the operand

**Returns**

AD node pointer

### `ArithmeticCodegen::isADNode`

*Function* — line 393

```c
llvm::Value* isADNode(llvm::Value* operand, llvm::Value* base_type);
```

Check if a tagged value is specifically an AD node. Safely checks both CALLABLE type AND CALLABLE_SUBTYPE_AD_NODE subtype. Uses proper branching to avoid dereferencing non-pointer values.

**Parameters**

- `operand` — Tagged value operand
- `base_type` — The base type of the operand

**Returns**

Boolean i1 value: true if AD node, false otherwise

### `ArithmeticCodegen::emitSetErrorLocation`

*Function* — line 402

```c
void emitSetErrorLocation();
```

Emit a call to eshkol_set_error_location() using the codegen's current source location (file:line:col), so the runtime error formatter can prefix the message with the failing expression's source position. No-op (emits nothing) when no line is known. Only call this on a branch that is about to raise a type error — it must not touch the hot path.

### `ArithmeticCodegen::raiseDivideByZeroException`

*Function* — line 408

```c
void raiseDivideByZeroException();
```

Emit code to raise a divide-by-zero exception. Creates an exception object and calls eshkol_raise().

### `ArithmeticCodegen::emitOverflowError`

*Function* — line 414

```c
void emitOverflowError(const char* message);
```

Emit code to raise an integer overflow exception. Creates an exception object and calls eshkol_raise(), then emits unreachable.

### `ArithmeticCodegen::guardHeapOperandsNumeric`

*Function* — line 422

```c
void guardHeapOperandsNumeric(llvm::Value* left, llvm::Value* right,
 const char* op_name);
```

Runtime guard for the tensor/vector arithmetic path: if either operand is a HEAP_PTR with a subtype that isn't HEAP_SUBTYPE_TENSOR or HEAP_SUBTYPE_VECTOR, raise a TYPE_ERROR exception with `op_name` in the message. Must be called before dispatch into tensorArithmeticInternal.

### `ArithmeticCodegen::convertToComplex`

*Function* — line 433

```c
llvm::Value* convertToComplex(llvm::Value* operand, llvm::Value* is_complex,
 llvm::Value* base_type);
```

Convert a tagged value to a complex number. Promotes int/double to complex with zero imaginary part.

**Parameters**

- `operand` — Tagged value operand
- `is_complex` — Whether operand is already complex
- `base_type` — The base type of the operand

**Returns**

Complex number struct pointer

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `ArithmeticCodegen::ArithmeticCodegen` | Function | 44 |
| `ArithmeticCodegen::emitOperandTypeError` | Function | 353 |
| `ArithmeticCodegen::ctx_` | Variable | 367 |
| `ArithmeticCodegen::tagged_` | Variable | 369 |
| `ArithmeticCodegen::tensor_` | Variable | 370 |
| `ArithmeticCodegen::autodiff_` | Variable | 371 |
| `ArithmeticCodegen::complex_` | Variable | 372 |
