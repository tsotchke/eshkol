# `eshkol/backend/binding_codegen.h`

70 public symbol(s) — 22 documented, 48 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `BindingCodegen`

*class* — line 43

```c
class BindingCodegen
```

BindingCodegen handles variable binding operations. All bindings store values as tagged_value to preserve type information. This ensures that ports, lists, lambdas, etc. can be correctly retrieved.

### `BindingCodegen::define`

*Function* — line 61

```c
llvm::Value* define(const eshkol_operations_t* op);
```

Define a variable: (define name value) For top-level: creates GlobalVariable with tagged_value For nested (inside function): creates AllocaInst with tagged_value

**Parameters**

- `op` — The define operation AST node

**Returns**

The stored value (or nullptr on failure)

### `BindingCodegen::let`

*Function* — line 74

```c
llvm::Value* let(const eshkol_operations_t* op);
```

Let expression: (let ((x 1) (y 2)) body) Sequential evaluation - each binding evaluated in order, but bindings are not visible to each other.

**Parameters**

- `op` — The let operation AST node

**Returns**

Result of body evaluation

### `BindingCodegen::letrec`

*Function* — line 85

```c
llvm::Value* letrec(const eshkol_operations_t* op);
```

Letrec expression: (letrec ((f ...) (g ...)) body) Recursive bindings - all bindings visible to all values. Used for mutually recursive function definitions.

**Parameters**

- `op` — The letrec operation AST node

**Returns**

Result of body evaluation

### `BindingCodegen::letStar`

*Function* — line 95

```c
llvm::Value* letStar(const eshkol_operations_t* op);
```

Let* expression: (let* ((x 1) (y x)) body) Sequential evaluation - each binding visible to subsequent ones.

**Parameters**

- `op` — The let* operation AST node

**Returns**

Result of body evaluation

### `BindingCodegen::letrecStar`

*Function* — line 107

```c
llvm::Value* letrecStar(const eshkol_operations_t* op);
```

Letrec* expression: (letrec* ((f ...) (g ...)) body) R7RS sequential recursive bindings. Like letrec, all bindings are mutually visible, but they are evaluated left-to-right and each binding's value is stored immediately so subsequent bindings can use it.

**Parameters**

- `op` — The letrec* operation AST node

**Returns**

Result of body evaluation

### `BindingCodegen::set`

*Function* — line 119

```c
llvm::Value* set(const eshkol_operations_t* op);
```

Set! expression: (set! name value) Mutates an existing binding.

**Parameters**

- `op` — The set! operation AST node

**Returns**

The new value

### `BindingCodegen::lookupVariable`

*Function* — line 131

```c
llvm::Value* lookupVariable(const std::string& name);
```

Look up a variable by name. Searches local symbol table first, then global.

**Parameters**

- `name` — Variable name

**Returns**

The variable's storage location (alloca or global), or nullptr

### `BindingCodegen::loadVariable`

*Function* — line 139

```c
llvm::Value* loadVariable(const std::string& name);
```

Load a variable's value.

**Parameters**

- `name` — Variable name

**Returns**

The variable's tagged value, or nullptr

### `BindingCodegen::storeBinding`

*Function* — line 202

```c
llvm::Value* storeBinding(
 const std::string& name,
 llvm::Value* value,
 eshkol_value_type_t value_type,
 bool is_global
 );
```

Store a value in a binding (alloca or global). Always converts to tagged_value first.

**Parameters**

- `name` — Variable name
- `value` — The value to store
- `value_type` — The Eshkol type of the value
- `is_global` — Whether to create a global variable

**Returns**

The storage location

### `BindingCodegen::ensureTaggedValue`

*Function* — line 217

```c
llvm::Value* ensureTaggedValue(llvm::Value* value, eshkol_value_type_t value_type);
```

Ensure value is a tagged_value. Converts raw i64, double, etc. to tagged_value struct.

**Parameters**

- `value` — The value to convert
- `value_type` — The Eshkol type

**Returns**

A tagged_value

### `BindingCodegen::registerLambdaBinding`

*Function* — line 226

```c
void registerLambdaBinding(const std::string& var_name, const std::string& lambda_name);
```

Register a lambda function binding. Sets up _func and _sexpr entries in symbol tables.

**Parameters**

- `var_name` — Variable name
- `lambda_name` — Lambda function name

### `BindingCodegen::setMutationAnalysisCallback`

*Function* — line 249

```c
void setMutationAnalysisCallback(IsVarSetFunc callback) { ... }
```

Set the compiler-owned lexical mutation analysis callback.

### `BindingCodegen::setSymbolTables`

*Function* — line 264

```c
void setSymbolTables(
 std::unordered_map<std::string, llvm::Value*>* local,
 std::unordered_map<std::string, llvm::Value*>* global
 ) { ... }
```

Set symbol table references.

### `BindingCodegen::setCurrentFunction`

*Function* — line 275

```c
void setCurrentFunction(llvm::Function** func) { ... }
```

Set current function pointer reference.

### `BindingCodegen::setReplMode`

*Function* — line 282

```c
void setReplMode(bool* repl_mode) { ... }
```

Set REPL mode flag reference.

### `BindingCodegen::setLambdaTracking`

*Function* — line 289

```c
void setLambdaTracking(
 std::string* last_lambda_name,
 std::unordered_map<std::string, llvm::Function*>* func_table
 ) { ... }
```

Set lambda tracking references.

### `BindingCodegen::setLetrecExcludedCaptureNames`

*Function* — line 301

```c
void setLetrecExcludedCaptureNames(std::set<std::string>* names) { ... }
```

Set letrec excluded capture names reference. Used to prevent letrec-bound lambdas from capturing themselves.

### `BindingCodegen::TailCallContext`

*Struct* — line 310

```c
struct TailCallContext
```

TCO context for tracking tail-recursive function generation.

### `BindingCodegen::getTCOContext`

*Function* — line 423

```c
TailCallContext& getTCOContext() { ... }
```

Get the current TCO context.

### `BindingCodegen::isTCOActive`

*Function* — line 428

```c
bool isTCOActive(const std::string& func_name) const { ... }
```

Check if TCO is currently active for a given function name.

### `BindingCodegen::IsTailRecursiveFunc`

*Typedef* — line 436

```c
using IsTailRecursiveFunc = bool (*)(const void* lambda_op, const char* func_name, void* context);
```

Set TCO callback for checking if a lambda is self-tail-recursive. Signature: bool (*)(const eshkol_operations_t* lambda_op, const char* func_name, void* context)

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `TypedValue` | Struct | 35 |
| `BindingCodegen::BindingCodegen` | Function | 44 |
| `BindingCodegen::ctx_` | Variable | 141 |
| `BindingCodegen::tagged_` | Variable | 145 |
| `BindingCodegen::CodegenASTFunc` | Typedef | 148 |
| `BindingCodegen::CodegenTypedASTFunc` | Typedef | 149 |
| `BindingCodegen::TypedToTaggedFunc` | Typedef | 150 |
| `BindingCodegen::GetTypedValueTypeFunc` | Typedef | 152 |
| `BindingCodegen::RegisterFuncBindingFunc` | Typedef | 154 |
| `BindingCodegen::IsVarSetFunc` | Typedef | 156 |
| `BindingCodegen::IsVarObservedFunc` | Typedef | 157 |
| `BindingCodegen::IsContinuationEscapeFunc` | Typedef | 160 |
| `BindingCodegen::nullptr` | Variable | 164 |
| `BindingCodegen::nullptr` | Variable | 165 |
| `BindingCodegen::nullptr` | Variable | 166 |
| `BindingCodegen::nullptr` | Variable | 167 |
| `BindingCodegen::nullptr` | Variable | 168 |
| `BindingCodegen::nullptr` | Variable | 169 |
| `BindingCodegen::nullptr` | Variable | 170 |
| `BindingCodegen::nullptr` | Variable | 171 |
| `BindingCodegen::nullptr` | Variable | 172 |
| `BindingCodegen::nullptr` | Variable | 175 |
| `BindingCodegen::nullptr` | Variable | 176 |
| `BindingCodegen::nullptr` | Variable | 179 |
| `BindingCodegen::nullptr` | Variable | 182 |
| `BindingCodegen::nullptr` | Variable | 185 |
| `BindingCodegen::nullptr` | Variable | 186 |
| `BindingCodegen::nullptr` | Variable | 190 |
| `BindingCodegen::setCodegenCallbacks` | Function | 228 |
| `BindingCodegen::setObservationAnalysisCallback` | Function | 253 |
| `BindingCodegen::setContinuationEscapeAnalysisCallback` | Function | 257 |
| `BindingCodegen::TailCallContext::func_name` | Variable | 311 |
| `BindingCodegen::TailCallContext::nullptr` | Variable | 312 |
| `BindingCodegen::TailCallContext::param_allocas` | Variable | 313 |
| `BindingCodegen::TailCallContext::param_names` | Variable | 314 |
| `BindingCodegen::TailCallContext::false` | Variable | 315 |
| `BindingCodegen::TailCallContext::false` | Variable | 316 |
| `BindingCodegen::TailCallContext::false` | Variable | 334 |
| `BindingCodegen::TailCallContext::nullptr` | Variable | 335 |
| `BindingCodegen::TailCallContext::nullptr` | Variable | 336 |
| `BindingCodegen::TailCallContext::open_guard_handlers` | Variable | 371 |
| `BindingCodegen::TailCallContext::nullptr` | Variable | 372 |
| `BindingCodegen::TailCallContext::false` | Variable | 414 |
| `BindingCodegen::TailCallContext::nullptr` | Variable | 415 |
| `BindingCodegen::TailCallContext::nullptr` | Variable | 416 |
| `BindingCodegen::TailCallContext::open_guard_forbid` | Variable | 417 |
| `BindingCodegen::setTCOCallbacks` | Function | 437 |
| `BindingCodegen::nullptr` | Variable | 445 |
