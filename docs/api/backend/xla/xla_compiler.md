# `eshkol/backend/xla/xla_compiler.h`

28 public symbol(s) — 12 documented, 16 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `CompileOptions`

*Struct* — line 37

```c
struct CompileOptions
```

Compilation options

### `CompileResult`

*Struct* — line 49

```c
struct CompileResult
```

Compilation result

### `XLACompiler`

*class* — line 63

```c
class XLACompiler
```

XLACompiler - Compiles StableHLO to executable code Takes a StableHLO module and compiles it to native code for the specified target backend.

### `XLACompiler::~XLACompiler`

*Function* — line 73

```c
~XLACompiler();
```

Destroy the compiler instance.

### `XLACompiler::compile`

*Function* — line 87

```c
CompileResult compile(void* module, const CompileOptions& options);
```

Compile StableHLO module to executable.

**Parameters**

- `module` — StableHLO module from emitter
- `options` — Compilation options

**Returns**

Compilation result

### `XLACompiler::compileForTarget`

*Function* — line 95

```c
CompileResult compileForTarget(void* module, Target target);
```

Compile with default options for target.

**Parameters**

- `module` — StableHLO module
- `target` — Target backend

**Returns**

Compilation result

### `XLACompiler::isTargetAvailable`

*Function* — line 104

```c
static bool isTargetAvailable(Target target);
```

Check if a target is available on this system.

**Parameters**

- `target` — Target to check

**Returns**

true if available

### `XLACompiler::getDefaultTarget`

*Function* — line 110

```c
static Target getDefaultTarget();
```

Get default target for this system.

**Returns**

Best available target

### `XLACompiler::getAvailableTargets`

*Function* — line 116

```c
static std::vector<Target> getAvailableTargets();
```

Get all available targets.

**Returns**

Vector of available targets

### `XLACompiler::freeExecutable`

*Function* — line 124

```c
void freeExecutable(void* executable);
```

Free a compiled executable.

**Parameters**

- `executable` — Executable to free

### `XLACompiler::serializeExecutable`

*Function* — line 131

```c
std::vector<uint8_t> serializeExecutable(void* executable);
```

Serialize executable to bytes for caching.

**Parameters**

- `executable` — Executable to serialize

**Returns**

Serialized bytes (empty on failure)

### `XLACompiler::deserializeExecutable`

*Function* — line 139

```c
void* deserializeExecutable(const std::vector<uint8_t>& data, Target target);
```

Deserialize executable from bytes.

**Parameters**

- `data` — Serialized bytes
- `target` — Target the executable was compiled for

**Returns**

Deserialized executable (nullptr on failure)

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `Target` | Variable | 22 |
| `CompileOptions::target` | Variable | 38 |
| `CompileOptions::O2` | Variable | 39 |
| `CompileOptions::true` | Variable | 40 |
| `CompileOptions::true` | Variable | 41 |
| `CompileOptions::false` | Variable | 42 |
| `CompileOptions::max_batch_size` | Variable | 43 |
| `CompileResult::success` | Variable | 50 |
| `CompileResult::executable` | Variable | 51 |
| `CompileResult::error_message` | Variable | 52 |
| `CompileResult::compile_time_ms` | Variable | 53 |
| `CompileResult::executable_size_bytes` | Variable | 54 |
| `XLACompiler::XLACompiler` | Function | 64 |
| `XLACompiler::XLACompiler` | Function | 76 |
| `XLACompiler::Impl` | Variable | 141 |
| `XLACompiler::impl_` | Variable | 143 |
