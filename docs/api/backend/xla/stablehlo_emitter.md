# `eshkol/backend/xla/stablehlo_emitter.h`

33 public symbol(s) — 22 documented, 11 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `DotDimensionNumbers`

*Struct* — line 74

```c
struct DotDimensionNumbers
```

Dot dimension specification for DOT_GENERAL

### `StableHLOEmitter`

*class* — line 87

```c
class StableHLOEmitter
```

StableHLOEmitter - Emits StableHLO operations for XLA compilation This class is responsible for translating Eshkol's tensor operations into StableHLO IR that can be compiled by XLA.

### `StableHLOEmitter::~StableHLOEmitter`

*Function* — line 97

```c
~StableHLOEmitter();
```

Destroy the emitter and release any owned MLIR resources.

### `StableHLOEmitter::isAvailable`

*Function* — line 107

```c
bool isAvailable() const;
```

Check if the StableHLO emitter has a real MLIR backend.

**Returns**

true if MLIR+StableHLO dialects are loaded and ready

### `StableHLOEmitter::emitAdd`

*Function* — line 117

```c
void* emitAdd(void* lhs, void* rhs);
```

Emit element-wise addition.

**Parameters**

- `lhs` — Left operand
- `rhs` — Right operand

**Returns**

Result value

### `StableHLOEmitter::emitSubtract`

*Function* — line 122

```c
void* emitSubtract(void* lhs, void* rhs);
```

Emit element-wise subtraction.

### `StableHLOEmitter::emitMultiply`

*Function* — line 127

```c
void* emitMultiply(void* lhs, void* rhs);
```

Emit element-wise multiplication.

### `StableHLOEmitter::emitDivide`

*Function* — line 132

```c
void* emitDivide(void* lhs, void* rhs);
```

Emit element-wise division.

### `StableHLOEmitter::emitMatmul`

*Function* — line 143

```c
void* emitMatmul(void* lhs, void* rhs, const DotDimensionNumbers& dims);
```

Emit matrix multiplication (DOT_GENERAL).

**Parameters**

- `lhs` — Left matrix
- `rhs` — Right matrix
- `dims` — Dimension specification

**Returns**

Result matrix

### `StableHLOEmitter::emitTranspose`

*Function* — line 151

```c
void* emitTranspose(void* input, const std::vector<int64_t>& permutation);
```

Emit matrix transpose.

**Parameters**

- `input` — Input matrix
- `permutation` — Dimension permutation

**Returns**

Transposed matrix

### `StableHLOEmitter::emitExp`

*Function* — line 160

```c
void* emitExp(void* input);
```

Emit element-wise exponential.

**Parameters**

- `input` — Input value

**Returns**

Result value

### `StableHLOEmitter::emitLog`

*Function* — line 167

```c
void* emitLog(void* input);
```

Emit element-wise natural logarithm.

**Parameters**

- `input` — Input value

**Returns**

Result value

### `StableHLOEmitter::emitSin`

*Function* — line 174

```c
void* emitSin(void* input);
```

Emit element-wise sine.

**Parameters**

- `input` — Input value

**Returns**

Result value

### `StableHLOEmitter::emitCos`

*Function* — line 181

```c
void* emitCos(void* input);
```

Emit element-wise cosine.

**Parameters**

- `input` — Input value

**Returns**

Result value

### `StableHLOEmitter::emitTanh`

*Function* — line 188

```c
void* emitTanh(void* input);
```

Emit element-wise hyperbolic tangent.

**Parameters**

- `input` — Input value

**Returns**

Result value

### `StableHLOEmitter::emitReduce`

*Function* — line 199

```c
void* emitReduce(void* input, const std::vector<int64_t>& axes, StableHLOOp op);
```

Emit reduction along axes.

**Parameters**

- `input` — Input tensor
- `axes` — Axes to reduce
- `op` — Reduction operation

**Returns**

Reduced tensor

### `StableHLOEmitter::emitReshape`

*Function* — line 209

```c
void* emitReshape(void* input, const std::vector<int64_t>& new_shape);
```

Emit a reshape to a new shape (same element count).

**Parameters**

- `input` — Input tensor
- `new_shape` — Target shape

**Returns**

Reshaped tensor

### `StableHLOEmitter::emitBroadcast`

*Function* — line 217

```c
void* emitBroadcast(void* input, const std::vector<int64_t>& broadcast_dims);
```

Emit a broadcast along the given dimensions.

**Parameters**

- `input` — Input tensor
- `broadcast_dims` — Mapping of input dimensions to output dimensions

**Returns**

Broadcasted tensor

### `StableHLOEmitter::emitSlice`

*Function* — line 227

```c
void* emitSlice(void* input, const std::vector<int64_t>& start,
 const std::vector<int64_t>& limit,
 const std::vector<int64_t>& strides);
```

Emit a strided slice of a tensor.

**Parameters**

- `input` — Input tensor
- `start` — Start indices per dimension
- `limit` — End indices per dimension
- `strides` — Step sizes per dimension

**Returns**

Sliced tensor

### `StableHLOEmitter::getModule`

*Function* — line 237

```c
void* getModule() const;
```

Get the MLIR module containing all emitted operations.

**Returns**

MLIR module pointer

### `StableHLOEmitter::serializeToString`

*Function* — line 243

```c
std::string serializeToString() const;
```

Serialize module to string for debugging.

**Returns**

StableHLO IR as string

### `StableHLOEmitter::reset`

*Function* — line 248

```c
void reset();
```

Reset emitter for a new computation.

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `MLIRValue` | Struct | 23 |
| `MLIRType` | Struct | 24 |
| `MLIRContext` | Struct | 25 |
| `DotDimensionNumbers::lhs_batching_dims` | Variable | 75 |
| `DotDimensionNumbers::rhs_batching_dims` | Variable | 76 |
| `DotDimensionNumbers::lhs_contracting_dims` | Variable | 77 |
| `DotDimensionNumbers::rhs_contracting_dims` | Variable | 78 |
| `StableHLOEmitter::StableHLOEmitter` | Function | 88 |
| `StableHLOEmitter::StableHLOEmitter` | Function | 100 |
| `StableHLOEmitter::Impl` | Variable | 250 |
| `StableHLOEmitter::impl_` | Variable | 252 |
