# `eshkol/backend/call_apply_codegen.h`

49 public symbol(s) — 22 documented, 27 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `CallApplyCodegen`

*class* — line 46

```c
class CallApplyCodegen
```

CallApplyCodegen handles function invocation operations. Key responsibilities: - Dispatch function calls to appropriate handlers - Handle closure calls with captured variables - Implement Scheme's apply for runtime function application

### `CallApplyCodegen::CallApplyCodegen`

*Function* — line 55

```c
CallApplyCodegen(CodegenContext& ctx, TaggedValueCodegen& tagged, ArithmeticCodegen& arith);
```

Construct CallApplyCodegen with context and helpers.

### `CallApplyCodegen::apply`

*Function* — line 71

```c
llvm::Value* apply(const eshkol_operations_t* op);
```

Apply function to argument list: (apply func args) Handles: - Built-in arithmetic (+, -, *, /) - Built-in operations (cons, list) - User-defined functions - Closures with captures

**Parameters**

- `op` — The apply operation AST node

**Returns**

Result of function application

### `CallApplyCodegen::applyCons`

*Function* — line 79

```c
llvm::Value* applyCons(llvm::Value* list_int);
```

Apply cons to a 2-element list: (apply cons '(a b))

**Parameters**

- `list_int` — List as i64 pointer

**Returns**

Cons cell (a . b) as tagged value

### `CallApplyCodegen::applyReduction`

*Function* — line 95

```c
llvm::Value* applyReduction(const std::string& op, llvm::Value* list_int);
```

Apply a variadic binary reduction to a list of numbers: (apply OP '(x1 x2 ... xn)) folds OP left-to-right over the list. Supported ops: + - * / min max Operations with an identity element (+, -, *, /) return the identity on empty lists. Operations without an identity (min, max) raise a TYPE_ERROR exception on empty lists (matching R7RS).

**Parameters**

- `op` — Operation string
- `list_int` — Argument list as i64 pointer

**Returns**

Result tagged value

### `CallApplyCodegen::applyArithmetic`

*Function* — line 101

```c
llvm::Value* applyArithmetic(const std::string& op, llvm::Value* list_int) { ... }
```

> Deprecated: Old name retained as a thin forwarder for compatibility; prefer applyReduction.

### `CallApplyCodegen::applyUserFunction`

*Function* — line 114

```c
llvm::Value* applyUserFunction(llvm::Function* func, llvm::Value* list_int);
```

Apply user-defined function to list arguments. Handles both fixed-arity and variadic functions.

**Parameters**

- `func` — The LLVM function to call
- `list_int` — Argument list as i64 pointer

**Returns**

Function result as tagged value

### `CallApplyCodegen::applyClosure`

*Function* — line 128

```c
llvm::Value* applyClosure(llvm::Value* func_value, llvm::Value* list_int);
```

Apply closure to list arguments with capture support. This is the most general apply path - handles: - Variable argument counts (0-8) - Variable capture counts (0-8) - Runtime dispatch based on actual counts

**Parameters**

- `func_value` — Closure as tagged value
- `list_int` — Argument list as i64 pointer

**Returns**

Function result as tagged value

### `CallApplyCodegen::closureCall`

*Function* — line 142

```c
llvm::Value* closureCall(llvm::Value* closure, const std::vector<llvm::Value*>& args);
```

Call a closure with pre-evaluated arguments. Used for direct closure invocation, not via apply. Extracts captures from closure environment and appends to args.

**Parameters**

- `closure` — Closure as tagged value
- `args` — Pre-evaluated arguments

**Returns**

Function result as tagged value

### `CallApplyCodegen::setSymbolTables`

*Function* — line 149

```c
void setSymbolTables(
 std::unordered_map<std::string, llvm::Value*>* symbol_table,
 std::unordered_map<std::string, llvm::Value*>* global_symbol_table
 ) { ... }
```

Set symbol table references for function lookup.

### `CallApplyCodegen::setVariadicFunctionInfo`

*Function* — line 160

```c
void setVariadicFunctionInfo(
 std::unordered_map<std::string, std::pair<uint64_t, bool>>* info
 ) { ... }
```

Set variadic function info for proper argument handling.

### `CallApplyCodegen::setFunctionTable`

*Function* — line 174

```c
void setFunctionTable(std::unordered_map<std::string, llvm::Function*>* tbl) { ... }
```

Set the main codegen's function_table for cross-file apply resolution (Noesis Bug P, 2026-04-23). User functions defined in modules brought in via (load …) register here; symbol_table / global_symbol_table do not always carry them, and `module().getFunction(name)` may miss when the name has been mangled or the function lives in a different LLVM module.

### `CallApplyCodegen::CodegenASTCallback`

*Typedef* — line 182

```c
using CodegenASTCallback = llvm::Value* (*)(const eshkol_ast_t*, void*);
```

Set callback for general AST code generation. Used when apply needs to evaluate function argument.

### `CallApplyCodegen::ExtractConsCarCallback`

*Typedef* — line 194

```c
using ExtractConsCarCallback = llvm::Value* (*)(llvm::Value* cons_ptr, void* context);
```

Callback type for extracting car as tagged value.

### `CallApplyCodegen::GetConsAccessorCallback`

*Typedef* — line 202

```c
using GetConsAccessorCallback = llvm::Function* (*)(void* context);
```

Callback type for getting cons cell accessor function.

### `CallApplyCodegen::CreateConsCallback`

*Typedef* — line 210

```c
using CreateConsCallback = llvm::Value* (*)(llvm::Value* car, llvm::Value* cdr, void* context);
```

Callback type for creating tagged cons cell.

### `CallApplyCodegen::GetBuiltinArithmeticCallback`

*Typedef* — line 219

```c
using GetBuiltinArithmeticCallback = llvm::Function* (*)(const std::string& op, void* context);
```

Callback type for getting builtin arithmetic function. Returns the Function* for a given operation (+, -, *, /).

### `CallApplyCodegen::GetBuiltinPredicateCallback`

*Typedef* — line 231

```c
using GetBuiltinPredicateCallback = llvm::Function* (*)(const std::string& name, void* context);
```

Callback type for resolving a comparison / equality / unary predicate builtin (=, <, >, <=, >=, eq?, eqv?, equal?, even?, …) to its first-class wrapper Function* (tagged_value… -> tagged_value). Returns nullptr if the name has no such wrapper. Used by apply so `(apply = …)` / `(apply eq? …)` return proper booleans instead of falling through to "Unknown function".

### `CallApplyCodegen::ApplyBuiltinCallback`

*Typedef* — line 244

```c
using ApplyBuiltinCallback = llvm::Value* (*)(const std::string& func_name, const std::vector<llvm::Value*>& args, llvm::Value* arg_count, void* context);
```

Callback type for applying builtin functions with runtime argument list. Used for tensor/vector functions that need special handling in apply.

**Parameters**

- `func_name` — Name of the builtin function (rand, randn, zeros, ones, etc.)
- `args` — Vector of argument values extracted from the list
- `context` — Callback context pointer

**Returns**

Result of the builtin function, or nullptr if not handled

### `CallApplyCodegen::ApplyForwardRefCallback`

*Typedef* — line 267

```c
using ApplyForwardRefCallback = llvm::Value* (*)(const std::string& func_name, llvm::Value* list_int, void* context);
```

Bug P (2026-04-23): forward-reference apply callback. For cross-file user-defined functions in REPL mode (i.e. (load "module.esk") brings in a `(define (f …) …)`, then a different batch / file does `(apply f args)`), the symbol-table / function_table cascade misses because the function lives in another LLVM module loaded into the JIT. Direct calls handle this via __repl_fwd_<name> indirect calls; apply needs the same. The callback consults the REPL forward-reference registry and emits the appropriate indirect call when the name is registered. Returns nullptr if the name is not a known REPL forward-ref (apply then falls through to its existing "Unknown function" warning).

### `CallApplyCodegen::extractConsCarAsTaggedValue`

*Function* — line 314

```c
llvm::Value* extractConsCarAsTaggedValue(llvm::Value* cons_ptr);
```

Extract car of cons cell as tagged value using callback.

### `CallApplyCodegen::getTaggedConsGetPtrFunc`

*Function* — line 319

```c
llvm::Function* getTaggedConsGetPtrFunc();
```

Get runtime function for cons cell access using callback.

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `TypedValue` | Struct | 36 |
| `CallApplyCodegen::MAX_APPLY_ARGS` | Variable | 47 |
| `CallApplyCodegen::MAX_APPLY_CAPTURES` | Variable | 50 |
| `CallApplyCodegen::setCodegenASTCallback` | Function | 183 |
| `CallApplyCodegen::setExtractConsCarCallback` | Function | 195 |
| `CallApplyCodegen::setGetConsAccessorCallback` | Function | 203 |
| `CallApplyCodegen::setCreateConsCallback` | Function | 211 |
| `CallApplyCodegen::setGetBuiltinArithmeticCallback` | Function | 220 |
| `CallApplyCodegen::setGetBuiltinPredicateCallback` | Function | 232 |
| `CallApplyCodegen::setApplyBuiltinCallback` | Function | 248 |
| `CallApplyCodegen::setApplyForwardRefCallback` | Function | 270 |
| `CallApplyCodegen::ctx_` | Variable | 274 |
| `CallApplyCodegen::tagged_` | Variable | 278 |
| `CallApplyCodegen::arith_` | Variable | 279 |
| `CallApplyCodegen::nullptr` | Variable | 282 |
| `CallApplyCodegen::nullptr` | Variable | 283 |
| `CallApplyCodegen::nullptr` | Variable | 286 |
| `CallApplyCodegen::nullptr` | Variable | 289 |
| `CallApplyCodegen::nullptr` | Variable | 292 |
| `CallApplyCodegen::nullptr` | Variable | 293 |
| `CallApplyCodegen::nullptr` | Variable | 296 |
| `CallApplyCodegen::nullptr` | Variable | 297 |
| `CallApplyCodegen::nullptr` | Variable | 298 |
| `CallApplyCodegen::nullptr` | Variable | 301 |
| `CallApplyCodegen::nullptr` | Variable | 303 |
| `CallApplyCodegen::nullptr` | Variable | 306 |
| `CallApplyCodegen::nullptr` | Variable | 307 |
