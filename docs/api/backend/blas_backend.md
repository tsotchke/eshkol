# `eshkol/backend/blas_backend.h`

23 public symbol(s) — 23 documented, 0 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `g_blas_threshold`

*Variable* — line 42

```c
extern size_t g_blas_threshold;
```

Runtime-configurable threshold for BLAS vs SIMD dispatch. Operations below this element count use SIMD, above use BLAS. Default: 4096 (64x64 matrix) Can be overridden via ESHKOL_BLAS_THRESHOLD environment variable.

### `blas_set_threshold`

*Function* — line 48

```c
void blas_set_threshold(size_t threshold);
```

Set BLAS threshold at runtime.

**Parameters**

- `threshold` — Minimum elements to use BLAS

### `blas_get_threshold`

*Function* — line 54

```c
size_t blas_get_threshold();
```

Get current BLAS threshold.

**Returns**

Current threshold

### `shouldUseBLAS`

*Function* — line 61

```c
inline bool shouldUseBLAS(size_t num_elements) { ... }
```

Check if BLAS should be used for an operation.

**Parameters**

- `num_elements` — Total elements in operation

**Returns**

true if BLAS should be used

### `isAvailable`

*Function* — line 69

```c
bool isAvailable();
```

Check if BLAS backend is available.

**Returns**

true if BLAS is compiled in and functional

### `getBackendName`

*Function* — line 75

```c
const char* getBackendName();
```

Get BLAS backend description.

**Returns**

Human-readable backend name

### `dgemm`

*Function* — line 98

```c
void dgemm(char transA, char transB,
 int M, int N, int K,
 double alpha,
 const double* A, int lda,
 const double* B, int ldb,
 double beta,
 double* C, int ldc);
```

General matrix multiply: C = alpha * A * B + beta * C Computes C = alpha * op(A) * op(B) + beta * C where op(X) is X or X^T.

**Parameters**

- `transA` — 'N' for A, 'T' for A^T
- `transB` — 'N' for B, 'T' for B^T
- `M` — Rows of op(A) and C
- `N` — Columns of op(B) and C
- `K` — Columns of op(A) / Rows of op(B)
- `alpha` — Scalar multiplier for A*B
- `A` — Matrix A (M x K if transA='N', K x M if transA='T')
- `lda` — Leading dimension of A
- `B` — Matrix B (K x N if transB='N', N x K if transB='T')
- `ldb` — Leading dimension of B
- `beta` — Scalar multiplier for C
- `C` — Result matrix (M x N)
- `ldc` — Leading dimension of C

### `matmul`

*Function* — line 118

```c
void matmul(const double* A, const double* B, double* C,
 size_t M, size_t K, size_t N);
```

Simple matrix multiply: C = A * B Convenience wrapper for common case (no transpose, alpha=1, beta=0).

**Parameters**

- `A` — Matrix A (M x K), row-major
- `B` — Matrix B (K x N), row-major
- `C` — Result matrix (M x N), row-major
- `M` — Rows of A and C
- `K` — Columns of A / Rows of B
- `N` — Columns of B and C

### `matmul_backward`

*Function* — line 125

```c
void matmul_backward(const double* grad_c, const double* a, const double* b,
 double* grad_a, double* grad_b,
 size_t M, size_t K, size_t N);
```

Matmul backward: given C = A @ B, accumulate gradients dA += grad_C @ B^T, dB += A^T @ grad_C

### `dgemv`

*Function* — line 132

```c
void dgemv(char trans, int M, int N,
 double alpha, const double* A, int lda,
 const double* x, int incx,
 double beta, double* y, int incy);
```

Matrix-vector multiply: y = alpha * A * x + beta * y

### `batched_dgemm`

*Function* — line 160

```c
void batched_dgemm(char transA, char transB,
 int M, int N, int K,
 double alpha,
 const double* A, int lda,
 const double* B, int ldb,
 double beta,
 double* C, int ldc,
 int batch_count);
```

Batched general matrix multiply: C[b] = alpha * A[b] * B[b] + beta * C[b] Performs batch_count independent matrix multiplications. Matrices are stored contiguously: A[b] starts at A + b * M * K, etc.

**Parameters**

- `transA` — 'N' for A, 'T' for A^T
- `transB` — 'N' for B, 'T' for B^T
- `M` — Rows of each op(A[b]) and C[b]
- `N` — Columns of each op(B[b]) and C[b]
- `K` — Columns of each op(A[b]) / Rows of each op(B[b])
- `alpha` — Scalar multiplier for A*B
- `A` — Batched matrix A (batch_count * M * K)
- `lda` — Leading dimension of each A[b]
- `B` — Batched matrix B (batch_count * K * N)
- `ldb` — Leading dimension of each B[b]
- `beta` — Scalar multiplier for C
- `C` — Batched result matrix (batch_count * M * N)
- `ldc` — Leading dimension of each C[b]
- `batch_count` — Number of matrices in the batch

### `batched_matmul`

*Function* — line 182

```c
void batched_matmul(const double* A, const double* B, double* C,
 size_t M, size_t K, size_t N, size_t batch_count);
```

Simple batched matmul: C[b] = A[b] * B[b] for b=0..batch_count-1 Convenience wrapper. Matrices stored contiguously in row-major order.

**Parameters**

- `A` — Batched matrix A (batch_count * M * K), row-major
- `B` — Batched matrix B (batch_count * K * N), row-major
- `C` — Batched result (batch_count * M * N), row-major
- `M` — Rows of each A[b] and C[b]
- `K` — Columns of each A[b] / Rows of each B[b]
- `N` — Columns of each B[b] and C[b]
- `batch_count` — Number of matrices in the batch

### `ddot`

*Function* — line 196

```c
double ddot(int n, const double* x, int incx, const double* y, int incy);
```

Vector dot product: result = x · y

**Parameters**

- `n` — Vector length
- `x` — First vector
- `incx` — Stride for x
- `y` — Second vector
- `incy` — Stride for y

**Returns**

Dot product

### `dscal`

*Function* — line 205

```c
void dscal(int n, double alpha, double* x, int incx);
```

Vector scale: x = alpha * x

**Parameters**

- `n` — Vector length
- `alpha` — Scale factor
- `x` — Vector to scale (in-place)
- `incx` — Stride for x

### `daxpy`

*Function* — line 216

```c
void daxpy(int n, double alpha, const double* x, int incx, double* y, int incy);
```

Vector add: y = alpha * x + y

**Parameters**

- `n` — Vector length
- `alpha` — Scale factor for x
- `x` — First vector
- `incx` — Stride for x
- `y` — Second vector (in-place result)
- `incy` — Stride for y

### `isAvailable`

*Function* — line 231

```c
inline bool isAvailable() { ... }
```

Stub: BLAS is not compiled in, so it is never available.

**Returns**

Always false

### `getBackendName`

*Function* — line 237

```c
inline const char* getBackendName() { ... }
```

Stub: no BLAS backend is compiled in.

**Returns**

Always "none"

### `shouldUseBLAS`

*Function* — line 243

```c
inline bool shouldUseBLAS(size_t) { ... }
```

Stub: without a BLAS backend, BLAS dispatch is never selected.

**Returns**

Always false

### `eshkol_matmul_f64`

*Function* — line 269

```c
void eshkol_matmul_f64(const double* A, const double* B, double* C,
 uint64_t M, uint64_t K, uint64_t N);
```

Runtime matrix multiplication with automatic BLAS dispatch. Called from generated LLVM IR for tensor-dot operations. Uses BLAS for large matrices, scalar fallback for small ones.

**Parameters**

- `A` — Matrix A data (row-major, M x K)
- `B` — Matrix B data (row-major, K x N)
- `C` — Output matrix data (row-major, M x N)
- `M` — Rows of A and C
- `K` — Columns of A / Rows of B
- `N` — Columns of B and C

### `eshkol_batched_matmul_f64`

*Function* — line 284

```c
void eshkol_batched_matmul_f64(const double* A, const double* B, double* C,
 uint64_t M, uint64_t K, uint64_t N,
 uint64_t batch_count);
```

Runtime batched matrix multiplication with automatic BLAS dispatch. Called from generated LLVM IR for transformer batch operations.

**Parameters**

- `A` — Batched matrix A data (row-major, batch_count * M * K)
- `B` — Batched matrix B data (row-major, batch_count * K * N)
- `C` — Output batched matrix (row-major, batch_count * M * N)
- `M` — Rows of each A[b] and C[b]
- `K` — Columns of each A[b] / Rows of each B[b]
- `N` — Columns of each B[b] and C[b]
- `batch_count` — Number of matrices in the batch

### `eshkol_blas_available`

*Function* — line 292

```c
int eshkol_blas_available(void);
```

Check if BLAS is available at runtime.

**Returns**

1 if BLAS available, 0 otherwise

### `eshkol_blas_get_threshold`

*Function* — line 298

```c
uint64_t eshkol_blas_get_threshold(void);
```

Get BLAS threshold.

**Returns**

Current threshold for BLAS dispatch

### `eshkol_blas_set_threshold`

*Function* — line 304

```c
void eshkol_blas_set_threshold(uint64_t threshold);
```

Set BLAS threshold.

**Parameters**

- `threshold` — New threshold
