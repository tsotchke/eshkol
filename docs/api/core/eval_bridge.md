# `eshkol/core/eval_bridge.h`

9 public symbol(s) — 4 documented, 5 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `eshkol_eval_jit_execute_fn_t`

*Typedef* — line 36

```c
typedef eshkol_tagged_value_t (*eshkol_eval_jit_execute_fn_t)(void* jit,
 eshkol_ast_t* ast);
```

Function pointer type for executing an AST on the acquired JIT context.

**Parameters**

- `jit` — Opaque JIT-context handle returned by an eshkol_eval_jit_acquire_fn_t callback.
- `ast` — AST node to evaluate.

**Returns**

The tagged value produced by evaluating `ast.`

### `eshkol_eval_jit_lookup_fn_t`

*Typedef* — line 45

```c
typedef uint64_t (*eshkol_eval_jit_lookup_fn_t)(void* jit, const char* name);
```

Function pointer type for resolving a bound name in the JIT context.

**Parameters**

- `jit` — Opaque JIT-context handle.
- `name` — Null-terminated identifier to look up.

**Returns**

Raw 64-bit representation of the bound value, or 0 if unbound.

### `eshkol_eval_jit_get_execute`

*Function* — line 60

```c
eshkol_eval_jit_execute_fn_t eshkol_eval_jit_get_execute(void);
```

Get the registered "execute" JIT accessor.

**Returns**

The execute callback registered via eshkol_eval_jit_register(), or NULL if no JIT runtime (eshkol-repl-lib) is linked.

### `eshkol_eval_jit_get_lookup`

*Function* — line 67

```c
eshkol_eval_jit_lookup_fn_t eshkol_eval_jit_get_lookup(void);
```

Get the registered "lookup" JIT accessor.

**Returns**

The lookup callback registered via eshkol_eval_jit_register(), or NULL if no JIT runtime (eshkol-repl-lib) is linked.

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `eshkol_eval_jit_acquire_fn_t` | Typedef | 27 |
| `eshkol_eval_jit_available` | Function | 48 |
| `eshkol_eval_jit_get_acquire` | Function | 53 |
| `eshkol_eval_jit_register` | Function | 71 |
| `eshkol_eval_jit_warn_missing` | Function | 76 |
