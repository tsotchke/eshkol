# `eshkol/core/config.h`

62 public symbol(s) — 19 documented, 43 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `eshkol_log_level_t`

*Enum* — line 35

```c
typedef enum {
 ESHKOL_LOG_LEVEL_DEBUG = 0,
 ESHKOL_LOG_LEVEL_INFO,
 ESHKOL_LOG_LEVEL_WARN,
 ESHKOL_LOG_LEVEL_ERROR,
 ESHKOL_LOG_LEVEL_NONE
} eshkol_log_level_t;
```

Logging verbosity levels, ordered from most to least verbose. Any log message with a severity below the configured eshkol_config_t::log_level is suppressed.

### `eshkol_log_format_t`

*Enum* — line 46

```c
typedef enum {
 ESHKOL_LOG_FORMAT_TEXT = 0, 
 ESHKOL_LOG_FORMAT_JSON 
} eshkol_log_format_t;
```

Output format used for emitted log messages.

### `eshkol_opt_level_t`

*Enum* — line 54

```c
typedef enum {
 ESHKOL_OPT_LEVEL_0 = 0, 
 ESHKOL_OPT_LEVEL_1, 
 ESHKOL_OPT_LEVEL_2, 
 ESHKOL_OPT_LEVEL_3 
} eshkol_opt_level_t;
```

LLVM code generation optimization level, analogous to -O0..-O3.

### `eshkol_config_t`

*Struct* — line 71

```c
typedef struct eshkol_config {
 
 size_t max_heap_bytes; 
 uint64_t timeout_ms; 
 size_t max_stack_depth; 
 size_t max_tensor_elements; 
 size_t max_string_length; 

 
 eshkol_log_level_t log_level; 
 eshkol_log_format_t log_format; 
 const char* log_file; 

 
 eshkol_opt_level_t llvm_opt_level; 
 bool enable_simd; 
 bool enable_xla; 
 bool enable_gpu; 

 
 bool dump_ast; 
 bool dump_ir; 
 bool debug_mode; 

 
 const char** lib_paths; 
 size_t lib_path_count; 

 
 bool strict_mode; 
 bool enable_warnings; 
 bool color_output; 

 
 bool strict_types; 
 bool unsafe_mode; 
} eshkol_config_t;
```

Complete runtime/compiler configuration, assembled from defaults, an optional config file, environment variables, and command-line flags, in that priority order (see eshkol_config_load()). Obtain a populated instance via eshkol_config_defaults() (defaults only) or eshkol_config_get() (fully loaded, process-wide singleton). Any heap-allocated fields (log_file, lib_paths) must be released with eshkol_config_cleanup() before an owned instance is discarded.

### `eshkol_config_source_t`

*Enum* — line 120

```c
typedef enum {
 ESHKOL_CONFIG_SRC_DEFAULT = 0, 
 ESHKOL_CONFIG_SRC_FILE, 
 ESHKOL_CONFIG_SRC_ENV, 
 ESHKOL_CONFIG_SRC_CMDLINE 
} eshkol_config_source_t;
```

Identifies where a configuration value was ultimately sourced from. Ordered by increasing priority. Provided for callers/tools that want to report provenance; the loader itself does not currently track this per-field.

### `eshkol_config_get_max_heap`

*Function* — line 169

```c
size_t eshkol_config_get_max_heap(void);
```

Get the configured maximum heap allocation, in bytes.

**Returns**

eshkol_config_get()->max_heap_bytes.

### `eshkol_config_get_timeout`

*Function* — line 174

```c
uint64_t eshkol_config_get_timeout(void);
```

Get the configured execution timeout, in milliseconds.

**Returns**

eshkol_config_get()->timeout_ms; 0 means unlimited.

### `eshkol_config_get_max_stack`

*Function* — line 179

```c
size_t eshkol_config_get_max_stack(void);
```

Get the configured maximum recursion/stack depth.

**Returns**

eshkol_config_get()->max_stack_depth.

### `eshkol_config_get_log_level`

*Function* — line 184

```c
eshkol_log_level_t eshkol_config_get_log_level(void);
```

Get the configured minimum log level.

**Returns**

eshkol_config_get()->log_level.

### `eshkol_config_get_log_format`

*Function* — line 189

```c
eshkol_log_format_t eshkol_config_get_log_format(void);
```

Get the configured log output format.

**Returns**

eshkol_config_get()->log_format.

### `eshkol_config_get_opt_level`

*Function* — line 194

```c
eshkol_opt_level_t eshkol_config_get_opt_level(void);
```

Get the configured LLVM optimization level.

**Returns**

eshkol_config_get()->llvm_opt_level.

### `eshkol_config_is_simd_enabled`

*Function* — line 199

```c
bool eshkol_config_is_simd_enabled(void);
```

Check whether SIMD vectorization is enabled.

**Returns**

eshkol_config_get()->enable_simd.

### `eshkol_config_is_xla_enabled`

*Function* — line 204

```c
bool eshkol_config_is_xla_enabled(void);
```

Check whether the XLA backend is enabled.

**Returns**

eshkol_config_get()->enable_xla.

### `eshkol_config_is_gpu_enabled`

*Function* — line 209

```c
bool eshkol_config_is_gpu_enabled(void);
```

Check whether GPU acceleration is enabled.

**Returns**

eshkol_config_get()->enable_gpu.

### `eshkol_config_is_debug_mode`

*Function* — line 214

```c
bool eshkol_config_is_debug_mode(void);
```

Check whether debug mode is enabled.

**Returns**

eshkol_config_get()->debug_mode.

### `ESHKOL_ENV_MAX_HEAP`

*Macro* — line 230

```c
#define ESHKOL_ENV_MAX_HEAP "ESHKOL_MAX_HEAP"
```

Names of the environment variables consulted by eshkol_config_load_env(). Environment variables take priority over config-file values but are overridden by explicit command-line flags (see eshkol_config_load()). Boolean-valued variables are parsed permissively ("true"/"1"/"yes"/"on", case-insensitive); size-valued variables accept an optional K/M/G suffix.

### `Config::maxHeap`

*Function* — line 307

```c
size_t maxHeap() const { ... }
```

Typed read-only accessors mirroring the fields of the underlying eshkol_config_t (see the C API above for per-field semantics). All values reflect config_ as last populated by load() or overwritten via raw().

### `Config::libPaths`

*Function* — line 334

```c
std::vector<std::string> libPaths() const;
```

Get the configured library search paths as a vector of strings. Copies eshkol_config_t::lib_paths into an owned std::vector<std::string>.

**Returns**

Library search paths, in configured order.

### `Config::raw`

*Function* — line 342

```c
eshkol_config_t& raw() { ... }
```

Mutable access to the underlying C configuration struct.

**Returns**

Reference to the wrapped eshkol_config_t, allowing in-place edits.

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `eshkol_config_defaults` | Function | 132 |
| `eshkol_config_load` | Function | 136 |
| `eshkol_config_load_file` | Function | 141 |
| `eshkol_config_load_env` | Function | 144 |
| `eshkol_config_apply_args` | Function | 149 |
| `eshkol_config_get` | Function | 156 |
| `eshkol_config_set` | Function | 159 |
| `ESHKOL_ENV_TIMEOUT_MS` | Macro | 231 |
| `ESHKOL_ENV_MAX_STACK` | Macro | 232 |
| `ESHKOL_ENV_MAX_TENSOR` | Macro | 233 |
| `ESHKOL_ENV_MAX_STRING` | Macro | 234 |
| `ESHKOL_ENV_LOG_LEVEL` | Macro | 237 |
| `ESHKOL_ENV_LOG_FORMAT` | Macro | 238 |
| `ESHKOL_ENV_LOG_FILE` | Macro | 239 |
| `ESHKOL_ENV_OPT_LEVEL` | Macro | 242 |
| `ESHKOL_ENV_ENABLE_SIMD` | Macro | 243 |
| `ESHKOL_ENV_ENABLE_XLA` | Macro | 244 |
| `ESHKOL_ENV_ENABLE_GPU` | Macro | 245 |
| `ESHKOL_ENV_DEBUG` | Macro | 248 |
| `ESHKOL_ENV_LIB_PATH` | Macro | 251 |
| `eshkol_config_cleanup` | Function | 266 |
| `Config` | class | 284 |
| `Config::instance` | Function | 285 |
| `Config::load` | Function | 298 |
| `Config::timeout` | Function | 308 |
| `Config::maxStack` | Function | 309 |
| `Config::logLevel` | Function | 311 |
| `Config::logFormat` | Function | 312 |
| `Config::logFile` | Function | 313 |
| `Config::optLevel` | Function | 315 |
| `Config::simdEnabled` | Function | 316 |
| `Config::xlaEnabled` | Function | 317 |
| `Config::gpuEnabled` | Function | 318 |
| `Config::debugMode` | Function | 320 |
| `Config::dumpAst` | Function | 321 |
| `Config::dumpIr` | Function | 322 |
| `Config::strictTypes` | Function | 324 |
| `Config::unsafeMode` | Function | 325 |
| `Config::raw` | Function | 337 |
| `Config::Config` | Function | 344 |
| `Config::~Config` | Function | 346 |
| `Config::config_` | Variable | 348 |
| `Config::false` | Variable | 349 |
