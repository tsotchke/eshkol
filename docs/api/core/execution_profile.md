# `eshkol/core/execution_profile.h`

36 public symbol(s) — 8 documented, 28 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `Info`

*Struct* — line 49

```c
struct Info
```

Static metadata describing one ExecutionProfile. One Info exists per ExecutionProfile value, in the internal profile table defined in execution_profile.cpp; looked up via get()/find().

### `Selection`

*Struct* — line 68

```c
struct Selection
```

Raw CLI/user input describing the desired execution profile and related flags, prior to validation. Built from parsed command-line arguments and passed to resolve(), which validates the combination of profile and flags and produces a Resolution.

### `Resolution`

*Struct* — line 90

```c
struct Resolution
```

Outcome of validating a Selection against its ExecutionProfile's constraints. Produced by resolve(). If `error` is non-empty, the requested combination of profile and flags is invalid and the caller should report `error` and abort compilation; otherwise the remaining fields describe the effective compilation mode to use.

### `find`

*Function* — line 107

```c
const Info* find(std::string_view name);
```

Look up profile metadata by its CLI-facing name.

**Parameters**

- `name` — Profile name, e.g. "hosted-native" (see supported_names() for the full list).

**Returns**

Pointer to the matching Info, or nullptr if no profile has that name.

### `get`

*Function* — line 115

```c
const Info& get(ExecutionProfile profile);
```

Look up profile metadata by ExecutionProfile enum value.

**Parameters**

- `profile` — Profile to look up.

**Returns**

Reference to the matching Info. Falls back to the first table entry (HostedNative) in the unexpected case that no entry matches.

### `name`

*Function* — line 122

```c
const char* name(ExecutionProfile profile);
```

Get the CLI-facing name of a profile.

**Parameters**

- `profile` — Profile to name.

**Returns**

Same as get(profile).name.

### `supported_names`

*Function* — line 130

```c
std::string supported_names();
```

Build a human-readable, comma-separated list of all supported profile names. Intended for help text and error messages (e.g. reporting an unrecognized `--profile` value).

**Returns**

Comma-and-space-separated profile names, in table order.

### `resolve`

*Function* — line 147

```c
Resolution resolve(const Selection& selection);
```

Validate a requested profile/flag Selection and compute its effective compilation settings. When no `--profile` was explicitly requested (Selection::explicit_request is false), only lightweight defaulting is applied (e.g. hosted-wasm implies WebAssembly output) and no validation is performed. When a profile was explicitly requested, each profile enforces its own constraints — e.g. hosted-wasm rejects JIT eval/run and --shared-lib; freestanding profiles require --target; VM-backed profiles require --emit-eskb — and reports a descriptive error on the returned Resolution if violated.

**Parameters**

- `selection` — User-requested profile and flags.

**Returns**

Resolution with effective settings, or with a non-empty Resolution::error describing why the selection is invalid.

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `Info::id` | Variable | 50 |
| `Info::name` | Variable | 51 |
| `Info::backend` | Variable | 52 |
| `Info::hosted` | Variable | 53 |
| `Info::freestanding` | Variable | 54 |
| `Info::experimental` | Variable | 55 |
| `Info::implies_compile_only` | Variable | 56 |
| `Info::implies_no_stdlib` | Variable | 57 |
| `Info::target_triple` | Variable | 58 |
| `Selection::HostedNative` | Variable | 69 |
| `Selection::false` | Variable | 70 |
| `Selection::nullptr` | Variable | 71 |
| `Selection::false` | Variable | 72 |
| `Selection::false` | Variable | 73 |
| `Selection::false` | Variable | 74 |
| `Selection::false` | Variable | 75 |
| `Selection::false` | Variable | 76 |
| `Selection::false` | Variable | 77 |
| `Selection::false` | Variable | 78 |
| `Selection::false` | Variable | 79 |
| `Resolution::nullptr` | Variable | 91 |
| `Resolution::false` | Variable | 92 |
| `Resolution::false` | Variable | 93 |
| `Resolution::false` | Variable | 94 |
| `Resolution::false` | Variable | 95 |
| `Resolution::false` | Variable | 96 |
| `Resolution::nullptr` | Variable | 97 |
| `Resolution::error` | Variable | 98 |
