# `eshkol/core/complex_math.h`

Pure complex transcendental core, shared by the native runtime and the bytecode VM. This header is the SINGLE source of truth for every complex-domain transcendental Eshkol implements. Both engines include it: * the native runtime (`lib/core/complex_math.cpp`) wraps each function in an `extern "C"` symbol (`eshkol_complex_sqrt`, ...) that the LLVM back end emits calls to; * the bytecode VM (`lib/backend/vm_complex.c`) calls the inline functions directly. Because both engines evaluate the same expressions in the same order, the two back ends agree bit-for-bit — engine parity is a property of the code layout, not of a hand-maintained pair of implementations that can drift. This mirrors the arrangement already used for i128 (`<eshkol/core/i128.h>`). Everything is written in the C/C++ common subset over plain `double` pairs: no `_Complex`, no `std::complex`, so a C translation unit and a C++ translation unit both compile it without a language-specific shim. Branch cuts follow C99 Annex G / R7RS 6.2.6 (the principal branch): * `sqrt` — cut along the negative real axis, continuous from above; * `log` — cut along the negative real axis, imaginary part in (-pi, pi]; * `asin`/`acos` — cuts outside [-1, 1] on the real axis; * `atan` — cuts outside [-i, i] on the imaginary axis. Signed zero is respected where it selects the branch, so `sqrt(-1 + 0i) = 0 + 1i` and `sqrt(-1 - 0i) = 0 - 1i`. Copyright (C) Tsotchke Corporation. MIT License.

33 public symbol(s) — 30 documented, 3 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `eshkol_cpx`

*Struct* — line 71

```c
typedef struct eshkol_cpx {
 double re;
 double im;
} eshkol_cpx;
```

A complex value as a bare pair of IEEE-754 doubles. Layout-compatible with `eshkol_complex_number_t` (native runtime) and `VmComplex` (VM): both are `{ double real; double imag; }` with no padding, so a pointer to either can be reinterpreted as this type.

### `eshkol_cpx_make`

*Function* — line 77

```c
static inline eshkol_cpx eshkol_cpx_make(double re, double im) { ... }
```

Build a complex value from its two components.

### `eshkol_cpx_add`

*Function* — line 87

```c
static inline eshkol_cpx eshkol_cpx_add(eshkol_cpx a, eshkol_cpx b) { ... }
```

Sum of two complex values.

### `eshkol_cpx_sub`

*Function* — line 92

```c
static inline eshkol_cpx eshkol_cpx_sub(eshkol_cpx a, eshkol_cpx b) { ... }
```

Difference of two complex values.

### `eshkol_cpx_mul`

*Function* — line 97

```c
static inline eshkol_cpx eshkol_cpx_mul(eshkol_cpx a, eshkol_cpx b) { ... }
```

Product of two complex values: (ac - bd) + (ad + bc)i.

### `eshkol_cpx_div`

*Function* — line 110

```c
static inline eshkol_cpx eshkol_cpx_div(eshkol_cpx a, eshkol_cpx b) { ... }
```

Quotient of two complex values via Smith's formula. Smith's algorithm scales by the larger denominator component first, so the intermediate `c*c + d*d` that would overflow for operands near DBL_MAX is never formed. This matches the division already documented for `/` in COMPLETE_LANGUAGE_SPECIFICATION.md 15.2.2.

### `eshkol_cpx_scale`

*Function* — line 130

```c
static inline eshkol_cpx eshkol_cpx_scale(eshkol_cpx z, double s) { ... }
```

Scale a complex value by a real factor.

### `eshkol_cpx_mul_i`

*Function* — line 135

```c
static inline eshkol_cpx eshkol_cpx_mul_i(eshkol_cpx z) { ... }
```

Multiply by i: i(a+bi) = -b + ai.

### `eshkol_cpx_div_i`

*Function* — line 140

```c
static inline eshkol_cpx eshkol_cpx_div_i(eshkol_cpx z) { ... }
```

Divide by i, i.e. multiply by -i: -i(a+bi) = b - ai.

### `eshkol_cpx_abs`

*Function* — line 145

```c
static inline double eshkol_cpx_abs(eshkol_cpx z) { ... }
```

Overflow-safe magnitude |a+bi|, computed with `hypot`.

### `eshkol_cpx_arg`

*Function* — line 150

```c
static inline double eshkol_cpx_arg(eshkol_cpx z) { ... }
```

Principal argument arg(a+bi) = atan2(b, a), in (-pi, pi].

### `eshkol_cpx_sqrt`

*Function* — line 166

```c
static inline eshkol_cpx eshkol_cpx_sqrt(eshkol_cpx z) { ... }
```

Principal square root. Uses the half-angle identity in its numerically stable algebraic form (C99 Annex G): one `hypot`, one `sqrt` and one division, with the branch selected by `copysign` on the imaginary part. Computing it in polar form instead (`sqrt(r) * (cos(t/2) + i sin(t/2))`) loses the exact zero: for `-1 + 0i` the polar route returns `6.1e-17 + 1i` where the exact principal value — and what ESHKOL_LANGUAGE_GUIDE.md documents — is `0 + 1i`.

### `eshkol_cpx_exp`

*Function* — line 180

```c
static inline eshkol_cpx eshkol_cpx_exp(eshkol_cpx z) { ... }
```

Complex exponential: exp(a+bi) = exp(a)(cos b + i sin b).

### `eshkol_cpx_log`

*Function* — line 186

```c
static inline eshkol_cpx eshkol_cpx_log(eshkol_cpx z) { ... }
```

Principal natural logarithm: log|z| + i arg(z).

### `eshkol_cpx_log2`

*Function* — line 191

```c
static inline eshkol_cpx eshkol_cpx_log2(eshkol_cpx z) { ... }
```

Base-2 logarithm: log(z) / log(2).

### `eshkol_cpx_log10`

*Function* — line 196

```c
static inline eshkol_cpx eshkol_cpx_log10(eshkol_cpx z) { ... }
```

Base-10 logarithm: log(z) / log(10).

### `eshkol_cpx_exp2`

*Function* — line 201

```c
static inline eshkol_cpx eshkol_cpx_exp2(eshkol_cpx z) { ... }
```

Base-2 exponential: exp(z * log 2).

### `eshkol_cpx_pow`

*Function* — line 206

```c
static inline eshkol_cpx eshkol_cpx_pow(eshkol_cpx a, eshkol_cpx b) { ... }
```

Principal power: a^b = exp(b log a), with 0^0 = 1 per R7RS.

### `eshkol_cpx_sin`

*Function* — line 217

```c
static inline eshkol_cpx eshkol_cpx_sin(eshkol_cpx z) { ... }
```

sin(a+bi) = sin a cosh b + i cos a sinh b.

### `eshkol_cpx_cos`

*Function* — line 222

```c
static inline eshkol_cpx eshkol_cpx_cos(eshkol_cpx z) { ... }
```

cos(a+bi) = cos a cosh b - i sin a sinh b.

### `eshkol_cpx_tan`

*Function* — line 233

```c
static inline eshkol_cpx eshkol_cpx_tan(eshkol_cpx z) { ... }
```

tan(a+bi) = (sin 2a + i sinh 2b) / (cos 2a + cosh 2b). The double-angle form is used rather than sin(z)/cos(z): its denominator is a sum of a bounded and a positive term, so it never cancels, and it stays finite for large |b| where sin(z) and cos(z) both overflow.

### `eshkol_cpx_sinh`

*Function* — line 246

```c
static inline eshkol_cpx eshkol_cpx_sinh(eshkol_cpx z) { ... }
```

sinh(a+bi) = sinh a cos b + i cosh a sin b.

### `eshkol_cpx_cosh`

*Function* — line 251

```c
static inline eshkol_cpx eshkol_cpx_cosh(eshkol_cpx z) { ... }
```

cosh(a+bi) = cosh a cos b + i sinh a sin b.

### `eshkol_cpx_tanh`

*Function* — line 260

```c
static inline eshkol_cpx eshkol_cpx_tanh(eshkol_cpx z) { ... }
```

tanh(a+bi) = (sinh 2a + i sin 2b) / (cosh 2a + cos 2b). Double-angle form, stable for the same reason as ::eshkol_cpx_tan.

### `eshkol_cpx_asin`

*Function* — line 272

```c
static inline eshkol_cpx eshkol_cpx_asin(eshkol_cpx z) { ... }
```

asin(z) = -i log(iz + sqrt(1 - z^2)).

### `eshkol_cpx_acos`

*Function* — line 280

```c
static inline eshkol_cpx eshkol_cpx_acos(eshkol_cpx z) { ... }
```

acos(z) = pi/2 - asin(z).

### `eshkol_cpx_atan`

*Function* — line 286

```c
static inline eshkol_cpx eshkol_cpx_atan(eshkol_cpx z) { ... }
```

atan(z) = (i/2)(log(1 - iz) - log(1 + iz)).

### `eshkol_cpx_asinh`

*Function* — line 295

```c
static inline eshkol_cpx eshkol_cpx_asinh(eshkol_cpx z) { ... }
```

asinh(z) = log(z + sqrt(z^2 + 1)).

### `eshkol_cpx_acosh`

*Function* — line 307

```c
static inline eshkol_cpx eshkol_cpx_acosh(eshkol_cpx z) { ... }
```

acosh(z) = log(z + sqrt(z+1) sqrt(z-1)). The product of two separate square roots (rather than `sqrt(z^2 - 1)`) is what places the branch cut on (-inf, 1] as C99 Annex G requires.

### `eshkol_cpx_atanh`

*Function* — line 315

```c
static inline eshkol_cpx eshkol_cpx_atanh(eshkol_cpx z) { ... }
```

atanh(z) = (log(1 + z) - log(1 - z)) / 2.

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `M_LN2` | Macro | 49 |
| `M_LN10` | Macro | 53 |
| `M_PI_2` | Macro | 57 |
