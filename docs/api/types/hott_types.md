# `eshkol/types/hott_types.h`

121 public symbol(s) — 67 documented, 54 undocumented.

Generated by `scripts/gen_api_docs.py`. Do not edit by hand.

## Symbols

### `TypeId`

*Struct* — line 107

```c
struct TypeId
```

Unique identifier for a type. Contains: - id: Unique numeric identifier - level: Universe membership - flags: Type properties (exact, linear, proof)

### `TypeId::isExact`

*Function* — line 121

```c
bool isExact() const { ... }
```

True if TYPE_FLAG_EXACT is set (Scheme exactness).

### `TypeId::isLinear`

*Function* — line 123

```c
bool isLinear() const { ... }
```

True if TYPE_FLAG_LINEAR is set (must be used exactly once).

### `TypeId::isProof`

*Function* — line 125

```c
bool isProof() const { ... }
```

True if TYPE_FLAG_PROOF is set (compile-time only, erased at runtime).

### `TypeId::isAbstract`

*Function* — line 127

```c
bool isAbstract() const { ... }
```

True if TYPE_FLAG_ABSTRACT is set (cannot be instantiated directly).

### `TypeNode`

*Struct* — line 162

```c
struct TypeNode
```

Complete definition of a type in the type graph. Contains: - Identity: id, name - Hierarchy: supertype, subtypes - Parameters: for type constructors like List<A> - Runtime mapping

### `CTValueSimple`

*Struct* — line 289

```c
struct CTValueSimple
```

Compile-time value for dependent type parameters. Simplified version - for full functionality see dependent.h

### `CTValueSimple::makeNat`

*Function* — line 295

```c
static CTValueSimple makeNat(uint64_t n) { ... }
```

Create a Nat-kind value holding `n.`

### `CTValueSimple::makeUnknown`

*Function* — line 303

```c
static CTValueSimple makeUnknown() { ... }
```

Create an Unknown-kind value (no compile-time value available).

### `CTValueSimple::isKnown`

*Function* — line 308

```c
bool isKnown() const { ... }
```

True if this holds a known (Nat) value.

### `CTValueSimple::isNat`

*Function* — line 310

```c
bool isNat() const { ... }
```

True if this holds a Nat value.

### `CTValueSimple::tryGetNat`

*Function* — line 313

```c
std::optional<uint64_t> tryGetNat() const { ... }
```

Get the natural-number value, or nullopt if this is Unknown.

### `ParameterizedType`

*Struct* — line 336

```c
struct ParameterizedType
```

Represents an instantiated parameterized type like List<Int64> or Vector<Float64, 100>. This allows tracking the element type of collections at compile time, as well as dimension information for dependent types.

### `PiType`

*Struct* — line 390

```c
struct PiType
```

Represents a dependent function type (Π-type). A Π-type is a function where the return type can depend on the input value. Examples: - (Π (n : Nat) (Vector Float64 n)) - function returning n-dimensional vector - (Π (T : Type) (T -> T)) - polymorphic identity function In HoTT, Π-types are more general than simple function types (->). When the return type doesn't depend on the input, it degenerates to (A -> B).

### `PiType::Parameter`

*Struct* — line 392

```c
struct Parameter
```

A single formal parameter of a Pi-type (function type).

### `PiType::PiType`

*Function* — line 404

```c
PiType() : return_type(BuiltinTypes::Value), is_dependent(false) { ... }
```

Construct a trivial Pi-type: no parameters, returns Value, non-dependent.

### `PiType::PiType`

*Function* — line 407

```c
PiType(std::vector<Parameter> p, TypeId ret, bool dep = false)
 : params(std::move(p)), return_type(ret), is_dependent(dep) { ... }
```

Construct a Pi-type from explicit parameters, return type, and dependency flag.

### `TypeEnvironment`

*class* — line 442

```c
class TypeEnvironment
```

Type environment maintaining all type information. Provides: - Type registration (builtin and user-defined) - Type lookup by name or id - Subtype checking with caching - Least common supertype computation - Arithmetic type promotion - Parameterized type instantiation (List<Int64>, etc.)

### `TypeEnvironment::registerBuiltinType`

*Function* — line 497

```c
TypeId registerBuiltinType(uint16_t id, const std::string& name,
 Universe level, uint8_t flags,
 RuntimeRep rep,
 std::optional<TypeId> supertype = std::nullopt);
```

Register a builtin type.

### `TypeEnvironment::registerTypeFamily`

*Function* — line 505

```c
TypeId registerTypeFamily(uint16_t id, const std::string& name,
 Universe level,
 const std::vector<std::string>& param_names,
 RuntimeRep rep);
```

Register a type family (parameterized type like List<A>).

### `TypeEnvironment::registerUserType`

*Function* — line 514

```c
TypeId registerUserType(const std::string& name, Universe level,
 uint8_t flags, std::optional<TypeId> supertype);
```

Register a user-defined type. Returns the assigned TypeId.

### `TypeEnvironment::lookupType`

*Function* — line 523

```c
std::optional<TypeId> lookupType(const std::string& name) const;
```

Look up a type by name. Returns nullopt if not found.

### `TypeEnvironment::getTypeNode`

*Function* — line 529

```c
const TypeNode* getTypeNode(TypeId id) const;
```

Get the TypeNode for a TypeId. Returns nullptr if not found.

### `TypeEnvironment::getTypeName`

*Function* — line 535

```c
std::string getTypeName(TypeId id) const;
```

Get type name by id. Delegates to TypeRelation::print(), the one printing function for every type form.

### `TypeEnvironment::isSubtype`

*Function* — line 547

```c
bool isSubtype(TypeId sub, TypeId super) const;
```

Check if 'sub' is a subtype of 'super' (static subtyping, cached). Delegates to TypeRelation::isSubtype(), which owns the rules: the nominal graph, Value as the top and Never as the bottom, sums, covariant pairs, and arrows contravariant in parameters and covariant in results. The gradual judgments (consistency, consistent subtyping, flow evidence) live only on TypeRelation.

### `TypeEnvironment::leastCommonSupertype`

*Function* — line 554

```c
std::optional<TypeId> leastCommonSupertype(TypeId a, TypeId b) const;
```

Compatibility facade for the type join. Delegates to TypeRelation::join(), which handles sums, pairs and signatures before falling back to the registered nominal supertype graph.

### `TypeEnvironment::getSupertypeChain`

*Function* — line 560

```c
std::vector<TypeId> getSupertypeChain(TypeId type) const;
```

Get the chain of supertypes from a type to the root. The first element is the type itself.

### `TypeEnvironment::promoteForArithmetic`

*Function* — line 568

```c
TypeId promoteForArithmetic(TypeId a, TypeId b) const;
```

Determine the result type for arithmetic between two types. Implements the Scheme numeric tower promotion rules.

### `TypeEnvironment::areEquivalent`

*Function* — line 576

```c
bool areEquivalent(TypeId a, TypeId b) const;
```

Check if two types are equivalent. Currently just identity, but will support type aliases later.

### `TypeEnvironment::getRuntimeRep`

*Function* — line 583

```c
RuntimeRep getRuntimeRep(TypeId id) const;
```

Get the runtime representation for a type.

### `TypeEnvironment::fromRuntimeType`

*Function* — line 588

```c
TypeId fromRuntimeType(uint8_t runtime_type) const;
```

Map an eshkol_value_type_t to a HoTT TypeId.

### `TypeEnvironment::toRuntimeType`

*Function* — line 593

```c
uint8_t toRuntimeType(TypeId id) const;
```

Map a HoTT TypeId to an eshkol_value_type_t.

### `TypeEnvironment::instantiateType`

*Function* — line 602

```c
ParameterizedType instantiateType(TypeId base_type,
 const std::vector<TypeId>& type_args) const;
```

Create or retrieve a parameterized type instance. For example, instantiateType(List, {Int64}) creates List<Int64>. Returns a ParameterizedType that can be used for element type tracking.

### `TypeEnvironment::makeListType`

*Function* — line 609

```c
ParameterizedType makeListType(TypeId element_type) const;
```

Create a List type with the given element type. Convenience method for instantiateType(List, {element_type}).

### `TypeEnvironment::makeVectorType`

*Function* — line 614

```c
ParameterizedType makeVectorType(TypeId element_type) const;
```

Create a Vector type with the given element type.

### `TypeEnvironment::makePointerType`

*Function* — line 619

```c
ParameterizedType makePointerType(TypeId element_type) const;
```

Create a Ptr type with the given pointee type.

### `TypeEnvironment::makeHashTableType`

*Function* — line 625

```c
ParameterizedType makeHashTableType(TypeId key_type, TypeId value_type) const;
```

Create a HashTable type with the given key and value types. For example, makeHashTableType(String, Int64) creates HashTable<String, Int64>.

### `TypeEnvironment::isTypeFamily`

*Function* — line 630

```c
bool isTypeFamily(TypeId id) const;
```

Check if a type is a type family (parameterized type constructor).

### `TypeEnvironment::getElementType`

*Function* — line 636

```c
TypeId getElementType(const ParameterizedType& ptype) const;
```

Get the element type from a parameterized collection type. Returns Value (unknown) if the type is not parameterized.

### `TypeEnvironment::inferListElementType`

*Function* — line 642

```c
TypeId inferListElementType(const std::vector<TypeId>& element_types) const;
```

Infer the element type of a homogeneous list from its elements. Returns Value if elements have different types, otherwise the common type.

### `TypeEnvironment::makeVectorTypeWithDim`

*Function* — line 650

```c
ParameterizedType makeVectorTypeWithDim(TypeId element_type, uint64_t dimension) const;
```

Create a Vector type with element type and dimension. For example, makeVectorTypeWithDim(Float64, 100) creates Vector<Float64, 100>.

### `TypeEnvironment::makeTensorTypeWithDims`

*Function* — line 656

```c
ParameterizedType makeTensorTypeWithDims(TypeId element_type,
 const std::vector<uint64_t>& dimensions) const;
```

Create a Tensor type with element type and dimensions. For example, makeTensorTypeWithDims(Float64, {3, 4}) creates Tensor<Float64, 3, 4>.

### `TypeEnvironment::storeDimensionInfo`

*Function* — line 663

```c
void storeDimensionInfo(TypeId type_id, const std::vector<CTValueSimple>& dimensions);
```

Store dimension info for a TypeId (used during type synthesis). This allows extractDimension() to retrieve dimension information later.

### `TypeEnvironment::getDimensionInfo`

*Function* — line 669

```c
std::optional<std::vector<CTValueSimple>> getDimensionInfo(TypeId type_id) const;
```

Retrieve stored dimension info for a TypeId. Returns nullopt if no dimension info was stored.

### `TypeEnvironment::makeFunctionType`

*Function* — line 679

```c
TypeId makeFunctionType(const std::vector<TypeId>& param_types, TypeId return_type) const;
```

Create a function type with the given parameter types and return type. Returns a TypeId that can be used to refer to this function type. Example: makeFunctionType({Int64, Int64}, Int64) creates (Int64, Int64) -> Int64

### `TypeEnvironment::makeSimpleFunctionType`

*Function* — line 688

```c
TypeId makeSimpleFunctionType(TypeId input, TypeId output) const;
```

Create a simple unary function type: input -> output

### `TypeEnvironment::getFunctionType`

*Function* — line 694

```c
const PiType* getFunctionType(TypeId id) const;
```

Get the PiType info for a function TypeId. Returns nullptr if the TypeId is not a function type.

### `TypeEnvironment::isFunctionType`

*Function* — line 699

```c
bool isFunctionType(TypeId id) const;
```

Check if a TypeId represents a function type.

### `TypeEnvironment::getFunctionReturnType`

*Function* — line 705

```c
TypeId getFunctionReturnType(TypeId id) const;
```

Get the return type of a function. Returns Value if not a function type.

### `TypeEnvironment::getFunctionParamTypes`

*Function* — line 711

```c
std::vector<TypeId> getFunctionParamTypes(TypeId id) const;
```

Get the parameter types of a function. Returns empty vector if not a function type.

### `TypeEnvironment::getFunctionTypeName`

*Function* — line 718

```c
std::string getFunctionTypeName(TypeId id) const;
```

Get a human-readable name for a function type, e.g. "(-> Int64 Float64 Boolean)". Returns "Function" if the TypeId is not a function signature. Delegates to TypeRelation::print().

### `TypeEnvironment::makePairType`

*Function* — line 727

```c
TypeId makePairType(TypeId car_type, TypeId cdr_type) const;
```

Create a pair type Pair<car_type, cdr_type>. Returns a unique TypeId that tracks element types. Used by cons to propagate types through car/cdr.

### `TypeEnvironment::getPairElementTypes`

*Function* — line 733

```c
std::optional<std::pair<TypeId, TypeId>> getPairElementTypes(TypeId id) const;
```

Get the element types for a Pair<A,B> TypeId. Returns nullopt if the TypeId is not a tracked pair type.

### `TypeEnvironment::isTrackedPairType`

*Function* — line 738

```c
bool isTrackedPairType(TypeId id) const;
```

Check if a TypeId represents a tracked pair type (with known element types).

### `TypeEnvironment::makeSumType`

*Function* — line 750

```c
TypeId makeSumType(const std::vector<TypeId>& members) const;
```

Create or retrieve a synthetic sum type over the given member arms. A sum type accepts a value if it fits any one arm, and is itself a subtype of some T if *every* arm is a subtype of T. Nested sums are flattened and duplicate arms removed. A degenerate sum with a single distinct arm collapses to that arm; an empty member list yields Value.

### `TypeEnvironment::getSumMembers`

*Function* — line 755

```c
std::optional<std::vector<TypeId>> getSumMembers(TypeId id) const;
```

Get the member arms of a sum TypeId, or nullopt if `id` is not a sum.

### `TypeEnvironment::isSumType`

*Function* — line 760

```c
bool isSumType(TypeId id) const;
```

Check whether a TypeId represents a synthetic sum type.

### `TypeEnvironment::codegenReprOf`

*Function* — line 772

```c
TypeId codegenReprOf(TypeId id) const;
```

Collapse a type to its codegen-facing representation. Never (the checker's type for a call whose recursive result is still being inferred) has no values and never reaches codegen; it collapses to Value. Sum types have no distinct runtime representation (values are tagged), so they collapse to Pair — the same TypeId a `(+ ...)` annotation resolved to before sum tracking existed. Non-sum types are returned unchanged. Used when writing a node's inferred type into the AST so that adding sum-type precision to the checker never perturbs generated code.

### `TypeEnvironment::addSubtype`

*Function* — line 783

```c
void addSubtype(TypeId supertype, TypeId subtype);
```

Add a subtype relationship.

### `universeToString`

*Function* — line 796

```c
const char* universeToString(Universe u);
```

Get a human-readable string for a Universe level.

### `runtimeRepToString`

*Function* — line 801

```c
const char* runtimeRepToString(RuntimeRep rep);
```

Get a human-readable string for a RuntimeRep.

### `isNumericType`

*Function* — line 806

```c
bool isNumericType(const TypeEnvironment& env, TypeId id);
```

Check if a type is numeric (subtype of Number).

### `isCollectionType`

*Function* — line 811

```c
bool isCollectionType(const TypeEnvironment& env, TypeId id);
```

Check if a type is a collection (List, Vector, etc.).

### `BuiltinTypeSpelling`

*Struct* — line 837

```c
struct BuiltinTypeSpelling
```

One canonical spelling of a *bare* (unparameterised) type name. A bare type name is a type written as a single symbol — `number`, `string`, `pair` — as opposed to a parenthesised type form like `(vector integer)`. They appear in parameter annotations (`(x : number)`), in return annotations, and in expression-level checked casts (`(the number e)`). `kind` is the dedicated hott_type_kind_t for spellings that have one (`integer` -> HOTT_TYPE_INTEGER). For every other spelling it is HOTT_TYPE_VAR, which means "carry the name and let TypeChecker::resolveType() resolve it through the type environment's name table" — which is exactly why `id` below must be registered under `name.` `id` is the TypeEnvironment id this spelling denotes, and initializeBuiltinTypes() registers `name` for it. BuiltinTypes::Invalid means "no TypeId denotes this spelling" (the bottom type `nothing` has a HoTT kind but no runtime type node); such spellings are still accepted, and are resolved by their dedicated `kind` alone.

### `builtinTypeSpellings`

*Function* — line 863

```c
const std::vector<BuiltinTypeSpelling>& builtinTypeSpellings();
```

The one canonical registry of bare type-name spellings. This table is the SINGLE SOURCE OF TRUTH for "is this symbol a type name", and it has exactly two consumers: 1. TypeEnvironment::initializeBuiltinTypes(), which registers every entry with a valid `id` into the type environment's name table — so a name that is spellable is always resolvable. 2. The parser: parsePrimitiveType() turns a spelling into a type expression, and the `(the <type> <expr>)` lookahead uses isBuiltinTypeName() to decide whether `the` heads an ascription. Those used to be three independently hand-maintained lists, which is how `(the number 3)` came to be a parse error while `(the string s)` worked and `number` was simultaneously a documented narrowing type: the parser's private allow-list simply omitted it, so the form degraded into a call to an undefined function named `the`. Add a bare type spelling HERE and all three agree by construction.

### `lookupBuiltinTypeSpelling`

*Function* — line 869

```c
const BuiltinTypeSpelling* lookupBuiltinTypeSpelling(const std::string& name);
```

Look up a bare type-name spelling (case-insensitive).

**Returns**

The registry entry, or nullptr if `name` is not a type name.

### `isBuiltinTypeName`

*Function* — line 877

```c
bool isBuiltinTypeName(const std::string& name);
```

True if `name` is a bare type-name spelling (case-insensitive). The predicate the parser uses to tell a type ascription from an ordinary call. See builtinTypeSpellings().

## Undocumented

| Symbol | Kind | Line |
|---|---|---:|
| `TypeId::id` | Variable | 108 |
| `TypeId::level` | Variable | 109 |
| `TypeId::flags` | Variable | 110 |
| `TypeId::isValid` | Function | 130 |
| `TypeId::pack` | Function | 133 |
| `TypeId::unpack` | Function | 140 |
| `TypeNode::id` | Variable | 163 |
| `TypeNode::name` | Variable | 164 |
| `TypeNode::supertype` | Variable | 167 |
| `TypeNode::subtypes` | Variable | 168 |
| `TypeNode::parameters` | Variable | 171 |
| `TypeNode::param_names` | Variable | 172 |
| `TypeNode::runtime_rep` | Variable | 175 |
| `TypeNode::false` | Variable | 178 |
| `TypeNode::Builtin` | Variable | 185 |
| `CTValueSimple::Unknown` | Variable | 291 |
| `CTValueSimple::nat_value` | Variable | 292 |
| `ParameterizedType::base_type` | Variable | 337 |
| `ParameterizedType::type_args` | Variable | 338 |
| `ParameterizedType::value_args` | Variable | 339 |
| `ParameterizedType::isParameterized` | Function | 356 |
| `ParameterizedType::hasDimensions` | Function | 359 |
| `ParameterizedType::elementType` | Function | 362 |
| `ParameterizedType::getDimension` | Function | 367 |
| `PiType::Parameter::name` | Variable | 393 |
| `PiType::Parameter::type` | Variable | 394 |
| `PiType::Parameter::is_value_param` | Variable | 395 |
| `PiType::params` | Variable | 398 |
| `PiType::return_type` | Variable | 399 |
| `PiType::is_dependent` | Variable | 400 |
| `PiType::false` | Variable | 401 |
| `PiType::makeSimple` | Function | 411 |
| `PiType::arity` | Function | 416 |
| `PiType::isSimpleFunction` | Function | 419 |
| `PiType::inputType` | Function | 422 |
| `TypeEnvironment::types_` | Variable | 443 |
| `TypeEnvironment::name_to_id_` | Variable | 445 |
| `TypeEnvironment::next_user_id_` | Variable | 446 |
| `TypeEnvironment::subtype_cache_` | Variable | 449 |
| `TypeEnvironment::parameterized_cache_` | Variable | 452 |
| `TypeEnvironment::dimension_cache_` | Variable | 455 |
| `TypeEnvironment::function_type_cache_` | Variable | 459 |
| `TypeEnvironment::next_function_type_id_` | Variable | 462 |
| `TypeEnvironment::pair_element_cache_` | Variable | 465 |
| `TypeEnvironment::next_pair_type_id_` | Variable | 468 |
| `TypeEnvironment::sum_member_cache_` | Variable | 477 |
| `TypeEnvironment::next_sum_type_id_` | Variable | 484 |
| `TypeEnvironment::TypeEnvironment` | Function | 486 |
| `TypeEnvironment::makeFunctionType` | Function | 682 |
| `TypeEnvironment::initializeBuiltinTypes` | Function | 774 |
| `TypeEnvironment::TypeRelation` | Variable | 786 |
| `BuiltinTypeSpelling::name` | Variable | 838 |
| `BuiltinTypeSpelling::kind` | Variable | 839 |
| `BuiltinTypeSpelling::id` | Variable | 840 |
