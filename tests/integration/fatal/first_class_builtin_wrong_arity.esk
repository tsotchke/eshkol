; First-class builtin closures must reject surplus arguments before reading
; their fixed local slots.
(define string-length-value string-length)
(display (string-length-value "abc" "ignored"))
(display "MUST-NOT-PRINT")
