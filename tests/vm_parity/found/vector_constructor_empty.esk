; FILED: the (vector ...) constructor FORM returns (); vector->list is an
; unhandled fid (140); list->vector returns () — note eshkol_vm.c BUILTINS
; maps BOTH "memq" and "list->vector" to fid 139 (collision).
; native -r : 20 then (1 2) then 7
; vm        : () then () (+ "unhandled native call ID 140") then ()
(display (vector-ref (vector 10 20 30) 1))
(newline)
(display (vector->list (vector 1 2)))
(newline)
(display (vector-ref (list->vector (list 7 8)) 0))
(newline)
