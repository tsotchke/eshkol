; RESOLVED: fids 184/185 preserve the immutable spelling while changing the
; distinct Scheme symbol/string tag.  Both native and VM print abcxyz.
(display (symbol->string (quote abc)))
(display (string->symbol "xyz"))
