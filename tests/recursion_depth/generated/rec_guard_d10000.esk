; recursion-depth probe -- generated by scripts/gen_recursion_depth.py
; deep guard nesting -- clean platform-dependent boundary
; KIND: guard
; DEPTH: 10000
; EXPECT: pass
(define (g n) (if (= n 0) 0 (guard (e (#t -1)) (+ n (g (- n 1))))))
(define __want (quotient (* 10000 (+ 10000 1)) 2))
(define __got (g 10000))
(if (= __got __want)
    (begin (display "PASS: guard d=10000 v=") (display __got) (newline))
    (begin (display "WRONG: guard d=10000 got=") (display __got)
           (display " want=") (display __want) (newline)))
