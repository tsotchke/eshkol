; recursion-depth probe -- generated by scripts/gen_recursion_depth.py
; mutual tail recursion, 3-cycle (a/b/c) -- not TCO'd (ESH-0102)
; KIND: mutual_tail3
; DEPTH: 500000
; EXPECT: xknown ESH-0102
(define (a n acc) (if (= n 0) acc (b (- n 1) (+ acc n))))
(define (b n acc) (if (= n 0) acc (c (- n 1) (+ acc n))))
(define (c n acc) (if (= n 0) acc (a (- n 1) (+ acc n))))
(define __want (quotient (* 500000 (+ 500000 1)) 2))
(define __got (a 500000 0))
(if (= __got __want)
    (begin (display "PASS: mutual_tail3 d=500000 v=") (display __got) (newline))
    (begin (display "WRONG: mutual_tail3 d=500000 got=") (display __got)
           (display " want=") (display __want) (newline)))
