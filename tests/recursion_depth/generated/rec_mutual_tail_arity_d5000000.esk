; recursion-depth probe -- generated by scripts/gen_recursion_depth.py
; mutual tail recursion between DIFFERENT-ARITY procedures -- proper tail call, O(1) stack
; KIND: mutual_tail_arity
; DEPTH: 5000000
; EXPECT: pass
(define (aping n acc) (if (= n 0) acc (apong (- n 1) (+ acc n) 0)))
(define (apong n acc k) (if (= n 0) acc (aping (- n 1) (+ acc n))))
(define __want (quotient (* 5000000 (+ 5000000 1)) 2))
(define __got (aping 5000000 0))
(if (= __got __want)
    (begin (display "PASS: mutual_tail_arity d=5000000 v=") (display __got) (newline))
    (begin (display "WRONG: mutual_tail_arity d=5000000 got=") (display __got)
           (display " want=") (display __want) (newline)))
