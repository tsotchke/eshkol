; recursion-depth probe -- generated by scripts/gen_recursion_depth.py
; recursion routed through map (higher-order) -- clean 100000 guard
; KIND: through_map
; DEPTH: 1000
; EXPECT: pass
(require stdlib)
(define (rec n) (if (= n 0) 0 (+ n (car (map (lambda (_) (rec (- n 1))) (list 0))))))
(define __want (quotient (* 1000 (+ 1000 1)) 2))
(define __got (rec 1000))
(if (= __got __want)
    (begin (display "PASS: through_map d=1000 v=") (display __got) (newline))
    (begin (display "WRONG: through_map d=1000 got=") (display __got)
           (display " want=") (display __want) (newline)))
