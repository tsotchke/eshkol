; recursion-depth probe -- generated by scripts/gen_recursion_depth.py
; non-tail recursion (accumulator on the C stack) -- ESH-0112
; KIND: non_tail
; DEPTH: 100000
; EXPECT: pass
(define (down n) (if (= n 0) 0 (+ n (down (- n 1)))))
(define __want (quotient (* 100000 (+ 100000 1)) 2))
(define __got (down 100000))
(if (= __got __want)
    (begin (display "PASS: non_tail d=100000 v=") (display __got) (newline))
    (begin (display "WRONG: non_tail d=100000 got=") (display __got)
           (display " want=") (display __want) (newline)))
