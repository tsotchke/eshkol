;; Test gradient without let binding

(define test-func (lambda (v) 0))
(define test-vector (vector 1.0 2.0))

(define (main)
  (display "Testing gradient operator...\n")
  (display test-func)
  (display test-vector)
  (display "Gradient test passed\n")
  0)