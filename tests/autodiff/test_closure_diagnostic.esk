;; Simplified test to diagnose closure capture issue

;; Test: Nested lambda with capture
(let ((make-adder (lambda (n) (lambda (x) (+ x n)))))
  (let ((add5 (make-adder 5)))
    (display (add5 10))))

(newline)