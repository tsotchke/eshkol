;;
;; Debug Binary Search 1 - Minimal reproduction attempt
;;


(display "=== DEBUG BINARY SEARCH 1 ===")
(newline)

;; Start with exact same structure as working test but with failing test naming
(display "1. Testing multi-list map with addition:")
(newline)
(define list1 (list 1 2 3))
(define list2 (list 4 5 6))
(display "   List 1: 1 2 3")
(newline)
(display "   List 2: 4 5 6")
(newline)

;; Use exact same variable name as failing test: result1 (not result)
(define result1 (map + list1 list2))
(display "   map + result: ")
(if (null? result1)
    (display "EMPTY")
    (begin
        (display (car result1)) (display " ")
        (display (cadr result1)) (display " ")
        (display (caddr result1))
    ))
(newline)

(display "=== DEBUG BINARY SEARCH 1 COMPLETE ===")
(newline)
