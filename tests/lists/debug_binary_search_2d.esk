;;
;; Debug Binary Search 2d - Test exact begin block pattern
;;


(display "=== DEBUG BINARY SEARCH 2D ===")
(newline)

;; Test 1: Same working operation
(display "1. Testing multi-list map with addition:")
(newline)
(define list1 (list 1 2 3))
(define list2 (list 4 5 6))

(define result1 (map + list1 list2))

;; Test the EXACT begin block pattern from the failing test
(display "   map + result: ")
(if (null? result1)
    (display "EMPTY")
    (begin
        (display (car result1)) (display " ")
        (display (cadr result1)) (display " ")
        (display (caddr result1))
    ))
(newline)

(display "=== DEBUG BINARY SEARCH 2D COMPLETE ===")
(newline)
