;; Test displaying standard library functions
(require stdlib)

(display "=== Testing stdlib function display ===")
(newline)

;; Arithmetic operators (core.operators.arithmetic)
(display "add: ") (display add) (newline)
(display "sub: ") (display sub) (newline)
(display "mul: ") (display mul) (newline)
(display "div: ") (display div) (newline)

;; Comparison operators (core.operators.compare)
(display "eq: ") (display eq) (newline)
(display "lt: ") (display lt) (newline)
(display "gt: ") (display gt) (newline)
(display "le: ") (display le) (newline)
(display "ge: ") (display ge) (newline)

;; Logic predicates (core.logic.predicates)
(display "is-zero?: ") (display is-zero?) (newline)
(display "is-positive?: ") (display is-positive?) (newline)
(display "is-negative?: ") (display is-negative?) (newline)
(display "is-even?: ") (display is-even?) (newline)
(display "is-odd?: ") (display is-odd?) (newline)

;; Logic types (core.logic.types)
(display "is-null?: ") (display is-null?) (newline)
(display "is-pair?: ") (display is-pair?) (newline)

;; Logic boolean (core.logic.boolean)
(display "negate: ") (display negate) (newline)
(display "all?: ") (display all?) (newline)
(display "none?: ") (display none?) (newline)

;; Functional compose (core.functional.compose)
(display "compose: ") (display compose) (newline)
(display "compose3: ") (display compose3) (newline)
(display "identity: ") (display identity) (newline)
(display "constantly: ") (display constantly) (newline)

;; Functional flip (core.functional.flip)
(display "flip: ") (display flip) (newline)

;; Functional curry (core.functional.curry)
(display "curry2: ") (display curry2) (newline)
(display "curry3: ") (display curry3) (newline)
(display "uncurry2: ") (display uncurry2) (newline)
(display "partial1: ") (display partial1) (newline)
(display "partial2: ") (display partial2) (newline)
(display "partial3: ") (display partial3) (newline)

;; List compound accessors (core.list.compound)
(display "caar: ") (display caar) (newline)
(display "cadr: ") (display cadr) (newline)
(display "cdar: ") (display cdar) (newline)
(display "cddr: ") (display cddr) (newline)

;; List generation (core.list.generate)
(display "iota: ") (display iota) (newline)
(display "iota-from: ") (display iota-from) (newline)
(display "iota-step: ") (display iota-step) (newline)
(display "repeat: ") (display repeat) (newline)
(display "make-list: ") (display make-list) (newline)
(display "range: ") (display range) (newline)
(display "zip: ") (display zip) (newline)

;; List transform (core.list.transform)
(display "take: ") (display take) (newline)
(display "drop: ") (display drop) (newline)
(display "append: ") (display append) (newline)
(display "reverse: ") (display reverse) (newline)
(display "filter: ") (display filter) (newline)
(display "unzip: ") (display unzip) (newline)
(display "partition: ") (display partition) (newline)

;; List query (core.list.query)
(display "count-if: ") (display count-if) (newline)
(display "find: ") (display find) (newline)
(display "length: ") (display length) (newline)

;; List higher-order (core.list.higher_order)
(display "map1: ") (display map1) (newline)
(display "map2: ") (display map2) (newline)
(display "map3: ") (display map3) (newline)
(display "fold: ") (display fold) (newline)
(display "fold-right: ") (display fold-right) (newline)
(display "for-each: ") (display for-each) (newline)
(display "any: ") (display any) (newline)
(display "every: ") (display every) (newline)

;; List search (core.list.search)
(display "member: ") (display member) (newline)
(display "memq: ") (display memq) (newline)
(display "memv: ") (display memv) (newline)
(display "assoc: ") (display assoc) (newline)
(display "assq: ") (display assq) (newline)
(display "assv: ") (display assv) (newline)
(display "list-ref: ") (display list-ref) (newline)
(display "list-tail: ") (display list-tail) (newline)

;; List sort (core.list.sort)
(display "sort: ") (display sort) (newline)

(display "=== All stdlib functions displayed ===")
(newline)
