;;
;; Debug Binary Search 2c - Test result access patterns
;;

(require stdlib)

(display "=== DEBUG BINARY SEARCH 2C ===")
(newline)

;; Test 1: Same working operation
(display "1. Testing multi-list map with addition:")
(newline)
(define list1 (list 1 2 3))
(define list2 (list 4 5 6))

(define result1 (map + list1 list2))
(display "   map operation completed")
(newline)

;; Test 2: Add result access one step at a time
(display "2. Testing result access:")
(newline)

;; Test car access
(display "   Accessing car: ")
(display (car result1))
(newline)

;; Test cadr access  
(display "   Accessing cadr: ")
(display (cadr result1))
(newline)

;; Test caddr access (this might be the trigger)
(display "   Accessing caddr: ")
(display (caddr result1))
(newline)

(display "=== DEBUG BINARY SEARCH 2C COMPLETE ===")
(newline)
