;;
;; Debug Binary Search 2a - Add only variable definitions from test 2 
;;

(display "=== DEBUG BINARY SEARCH 2A ===")
(newline)

;; Test 1: Same as working binary search 1
(display "1. Testing multi-list map with addition:")
(newline)
(define list1 (list 1 2 3))
(define list2 (list 4 5 6))
(display "   List 1: 1 2 3")
(newline)
(display "   List 2: 4 5 6")
(newline)

(define result1 (map + list1 list2))
(display "   map + result: ")
(if (null? result1)
    (display "EMPTY")
    (begin
        (display (car result1)) (display " ")
        (display (cadr result1)) (display " ")
        (display (caddr result1))
    ))
(newline)

;; Add ONLY the variable definitions from test 2 (no second map operation)
(display "2. Testing variable definitions only:")
(newline)
(define short-list (list 10 20))
(define long-list (list 1 2 3 4 5))
(display "   Variables defined successfully")
(newline)

(display "=== DEBUG BINARY SEARCH 2A COMPLETE ===")
(newline)
