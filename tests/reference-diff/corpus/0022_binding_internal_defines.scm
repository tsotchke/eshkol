;; binding / internal_defines  (R7RS-small portable; reference-differential corpus)
(define (f x)
  (define y (* x 2))
  (define z (+ y 1))
  (+ y z))
(display (f 10))(newline)
(define (g n)
  (define (helper a b) (if (= a 0) b (helper (- a 1) (+ b a))))
  (helper n 0))
(display (g 100))(newline)
