;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: vectors (producers) composed into tail_position (contexts)
;; CHECKS: 15
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check vectors_vec_ref__in__tail_position_define_tail
(define (tpg1702) (vector-ref (vector 'va 'vb) 1))
(define (edge-check-g1702) (edge-chk "vectors_vec_ref__in__tail_position_define_tail" (tpg1702) (string->symbol "vb")))
(edge-check-g1702)

;; check vectors_vec_ref__in__tail_position_let_tail_in_define
(define (tqg1703) (let ((tu 1)) (vector-ref (vector 'va 'vb) 1)))
(define (edge-check-g1703) (edge-chk "vectors_vec_ref__in__tail_position_let_tail_in_define" (tqg1703) (string->symbol "vb")))
(edge-check-g1703)

;; check vectors_vec_ref__in__tail_position_if_tail
(define (edge-check-g1704) (edge-chk "vectors_vec_ref__in__tail_position_if_tail" (if (> 2 1) (vector-ref (vector 'va 'vb) 1) 'wrong) (string->symbol "vb")))
(edge-check-g1704)

;; check vectors_vec_ref__in__tail_position_begin_tail
(define (edge-check-g1705) (edge-chk "vectors_vec_ref__in__tail_position_begin_tail" (begin 'first (vector-ref (vector 'va 'vb) 1)) (string->symbol "vb")))
(edge-check-g1705)

;; check vectors_vec_ref__in__tail_position_cond_else_tail
(define (edge-check-g1706) (edge-chk "vectors_vec_ref__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (vector-ref (vector 'va 'vb) 1))) (string->symbol "vb")))
(edge-check-g1706)

;; check vectors_make_set_ref__in__tail_position_define_tail
(define (tpg1707) (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)))
(define (edge-check-g1707) (edge-chk "vectors_make_set_ref__in__tail_position_define_tail" (tpg1707) 9))
(edge-check-g1707)

;; check vectors_make_set_ref__in__tail_position_let_tail_in_define
(define (tqg1708) (let ((tu 1)) (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1))))
(define (edge-check-g1708) (edge-chk "vectors_make_set_ref__in__tail_position_let_tail_in_define" (tqg1708) 9))
(edge-check-g1708)

;; check vectors_make_set_ref__in__tail_position_if_tail
(define (edge-check-g1709) (edge-chk "vectors_make_set_ref__in__tail_position_if_tail" (if (> 2 1) (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)) 'wrong) 9))
(edge-check-g1709)

;; check vectors_make_set_ref__in__tail_position_begin_tail
(define (edge-check-g1710) (edge-chk "vectors_make_set_ref__in__tail_position_begin_tail" (begin 'first (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1))) 9))
(edge-check-g1710)

;; check vectors_make_set_ref__in__tail_position_cond_else_tail
(define (edge-check-g1711) (edge-chk "vectors_make_set_ref__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)))) 9))
(edge-check-g1711)

;; check vectors_vec_len__in__tail_position_define_tail
(define (tpg1712) (vector-length (vector 1 2 3)))
(define (edge-check-g1712) (edge-chk "vectors_vec_len__in__tail_position_define_tail" (tpg1712) 3))
(edge-check-g1712)

;; check vectors_vec_len__in__tail_position_let_tail_in_define
(define (tqg1713) (let ((tu 1)) (vector-length (vector 1 2 3))))
(define (edge-check-g1713) (edge-chk "vectors_vec_len__in__tail_position_let_tail_in_define" (tqg1713) 3))
(edge-check-g1713)

;; check vectors_vec_len__in__tail_position_if_tail
(define (edge-check-g1714) (edge-chk "vectors_vec_len__in__tail_position_if_tail" (if (> 2 1) (vector-length (vector 1 2 3)) 'wrong) 3))
(edge-check-g1714)

;; check vectors_vec_len__in__tail_position_begin_tail
(define (edge-check-g1715) (edge-chk "vectors_vec_len__in__tail_position_begin_tail" (begin 'first (vector-length (vector 1 2 3))) 3))
(edge-check-g1715)

;; check vectors_vec_len__in__tail_position_cond_else_tail
(define (edge-check-g1716) (edge-chk "vectors_vec_len__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (vector-length (vector 1 2 3)))) 3))
(edge-check-g1716)

(display "EDGE-MATRIX-DONE") (newline)
