;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: closures (producers) composed into toplevel (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check closures_single_capture__in__toplevel_top_define
(define tlvg671 (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)))
(define (edge-check-g671) (edge-chk "closures_single_capture__in__toplevel_top_define" tlvg671 15))
(edge-check-g671)

;; check closures_single_capture__in__toplevel_top_list_arg
(define tllg672 (list (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5))))
(define (edge-check-g672) (edge-chk "closures_single_capture__in__toplevel_top_list_arg" (car tllg672) 15))
(edge-check-g672)

;; check closures_single_capture__in__toplevel_top_vector_arg
(define tlwg673 (vector (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5))))
(define (edge-check-g673) (edge-chk "closures_single_capture__in__toplevel_top_vector_arg" (vector-ref tlwg673 0) 15))
(edge-check-g673)

;; check closures_sibling_set__in__toplevel_top_define
(define tlvg674 (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))))
(define (edge-check-g674) (edge-chk "closures_sibling_set__in__toplevel_top_define" tlvg674 2))
(edge-check-g674)

;; check closures_sibling_set__in__toplevel_top_list_arg
(define tllg675 (list (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get)))))
(define (edge-check-g675) (edge-chk "closures_sibling_set__in__toplevel_top_list_arg" (car tllg675) 2))
(edge-check-g675)

;; check closures_sibling_set__in__toplevel_top_vector_arg
(define tlwg676 (vector (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get)))))
(define (edge-check-g676) (edge-chk "closures_sibling_set__in__toplevel_top_vector_arg" (vector-ref tlwg676 0) 2))
(edge-check-g676)

;; check closures_multi_instance__in__toplevel_top_define
(define (mkcg677) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag677 (mkcg677))
(define cbg677 (mkcg677))
(define tlvg677 (begin (cag677) (cag677) (+ (cag677) (cbg677))))
(define (edge-check-g677) (edge-chk "closures_multi_instance__in__toplevel_top_define" tlvg677 4))
(edge-check-g677)

;; check closures_multi_instance__in__toplevel_top_list_arg
(define (mkcg678) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag678 (mkcg678))
(define cbg678 (mkcg678))
(define tllg678 (list (begin (cag678) (cag678) (+ (cag678) (cbg678)))))
(define (edge-check-g678) (edge-chk "closures_multi_instance__in__toplevel_top_list_arg" (car tllg678) 4))
(edge-check-g678)

;; check closures_multi_instance__in__toplevel_top_vector_arg
(define (mkcg679) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag679 (mkcg679))
(define cbg679 (mkcg679))
(define tlwg679 (vector (begin (cag679) (cag679) (+ (cag679) (cbg679)))))
(define (edge-check-g679) (edge-chk "closures_multi_instance__in__toplevel_top_vector_arg" (vector-ref tlwg679 0) 4))
(edge-check-g679)

;; check closures_counter_once__in__toplevel_top_define
(define ctrg680 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define tlvg680 (ctrg680))
(define (edge-check-g680) (edge-chk "closures_counter_once__in__toplevel_top_define" tlvg680 1))
(edge-check-g680)

;; check closures_counter_once__in__toplevel_top_list_arg
(define ctrg681 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define tllg681 (list (ctrg681)))
(define (edge-check-g681) (edge-chk "closures_counter_once__in__toplevel_top_list_arg" (car tllg681) 1))
(edge-check-g681)

;; check closures_counter_once__in__toplevel_top_vector_arg
(define ctrg682 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define tlwg682 (vector (ctrg682)))
(define (edge-check-g682) (edge-chk "closures_counter_once__in__toplevel_top_vector_arg" (vector-ref tlwg682 0) 1))
(edge-check-g682)

(display "EDGE-MATRIX-DONE") (newline)
