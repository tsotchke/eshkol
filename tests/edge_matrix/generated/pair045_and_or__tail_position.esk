;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: and_or (producers) composed into tail_position (contexts)
;; CHECKS: 20
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check and_or_and_last__in__tail_position_define_tail
(define (tpg451) (and 1 2 3))
(define (edge-check-g451) (edge-chk "and_or_and_last__in__tail_position_define_tail" (tpg451) 3))
(edge-check-g451)

;; check and_or_and_last__in__tail_position_let_tail_in_define
(define (tqg452) (let ((tu 1)) (and 1 2 3)))
(define (edge-check-g452) (edge-chk "and_or_and_last__in__tail_position_let_tail_in_define" (tqg452) 3))
(edge-check-g452)

;; check and_or_and_last__in__tail_position_if_tail
(define (edge-check-g453) (edge-chk "and_or_and_last__in__tail_position_if_tail" (if (> 2 1) (and 1 2 3) 'wrong) 3))
(edge-check-g453)

;; check and_or_and_last__in__tail_position_begin_tail
(define (edge-check-g454) (edge-chk "and_or_and_last__in__tail_position_begin_tail" (begin 'first (and 1 2 3)) 3))
(edge-check-g454)

;; check and_or_and_last__in__tail_position_cond_else_tail
(define (edge-check-g455) (edge-chk "and_or_and_last__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (and 1 2 3))) 3))
(edge-check-g455)

;; check and_or_or_first__in__tail_position_define_tail
(define (tpg456) (or 42 99))
(define (edge-check-g456) (edge-chk "and_or_or_first__in__tail_position_define_tail" (tpg456) 42))
(edge-check-g456)

;; check and_or_or_first__in__tail_position_let_tail_in_define
(define (tqg457) (let ((tu 1)) (or 42 99)))
(define (edge-check-g457) (edge-chk "and_or_or_first__in__tail_position_let_tail_in_define" (tqg457) 42))
(edge-check-g457)

;; check and_or_or_first__in__tail_position_if_tail
(define (edge-check-g458) (edge-chk "and_or_or_first__in__tail_position_if_tail" (if (> 2 1) (or 42 99) 'wrong) 42))
(edge-check-g458)

;; check and_or_or_first__in__tail_position_begin_tail
(define (edge-check-g459) (edge-chk "and_or_or_first__in__tail_position_begin_tail" (begin 'first (or 42 99)) 42))
(edge-check-g459)

;; check and_or_or_first__in__tail_position_cond_else_tail
(define (edge-check-g460) (edge-chk "and_or_or_first__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (or 42 99))) 42))
(edge-check-g460)

;; check and_or_or_quote_null__in__tail_position_define_tail
(define (tpg461) (or #f '()))
(define (edge-check-g461) (edge-chk "and_or_or_quote_null__in__tail_position_define_tail" (tpg461) (list)))
(edge-check-g461)

;; check and_or_or_quote_null__in__tail_position_let_tail_in_define
(define (tqg462) (let ((tu 1)) (or #f '())))
(define (edge-check-g462) (edge-chk "and_or_or_quote_null__in__tail_position_let_tail_in_define" (tqg462) (list)))
(edge-check-g462)

;; check and_or_or_quote_null__in__tail_position_if_tail
(define (edge-check-g463) (edge-chk "and_or_or_quote_null__in__tail_position_if_tail" (if (> 2 1) (or #f '()) 'wrong) (list)))
(edge-check-g463)

;; check and_or_or_quote_null__in__tail_position_begin_tail
(define (edge-check-g464) (edge-chk "and_or_or_quote_null__in__tail_position_begin_tail" (begin 'first (or #f '())) (list)))
(edge-check-g464)

;; check and_or_or_quote_null__in__tail_position_cond_else_tail
(define (edge-check-g465) (edge-chk "and_or_or_quote_null__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (or #f '()))) (list)))
(edge-check-g465)

;; check and_or_and_empty__in__tail_position_define_tail
(define (tpg466) (and))
(define (edge-check-g466) (edge-chk "and_or_and_empty__in__tail_position_define_tail" (tpg466) #t))
(edge-check-g466)

;; check and_or_and_empty__in__tail_position_let_tail_in_define
(define (tqg467) (let ((tu 1)) (and)))
(define (edge-check-g467) (edge-chk "and_or_and_empty__in__tail_position_let_tail_in_define" (tqg467) #t))
(edge-check-g467)

;; check and_or_and_empty__in__tail_position_if_tail
(define (edge-check-g468) (edge-chk "and_or_and_empty__in__tail_position_if_tail" (if (> 2 1) (and) 'wrong) #t))
(edge-check-g468)

;; check and_or_and_empty__in__tail_position_begin_tail
(define (edge-check-g469) (edge-chk "and_or_and_empty__in__tail_position_begin_tail" (begin 'first (and)) #t))
(edge-check-g469)

;; check and_or_and_empty__in__tail_position_cond_else_tail
(define (edge-check-g470) (edge-chk "and_or_and_empty__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (and))) #t))
(edge-check-g470)

(display "EDGE-MATRIX-DONE") (newline)
