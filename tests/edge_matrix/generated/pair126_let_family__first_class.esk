;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: let_family (producers) composed into first_class (contexts)
;; CHECKS: 16
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check let_family_let_sum__in__first_class_thunk_in_list
(define (edge-check-g1095) (edge-chk "let_family_let_sum__in__first_class_thunk_in_list" ((car (list (lambda () (let ((la 2) (lb 3)) (+ la lb)))))) 5))
(edge-check-g1095)

;; check let_family_let_sum__in__first_class_apply_id
(define (edge-check-g1096) (edge-chk "let_family_let_sum__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((la 2) (lb 3)) (+ la lb)))) 5))
(edge-check-g1096)

;; check let_family_let_sum__in__first_class_map_hole
(define (edge-check-g1097) (edge-chk "let_family_let_sum__in__first_class_map_hole" (car (map (lambda (fi) (let ((la 2) (lb 3)) (+ la lb))) (list 0))) 5))
(edge-check-g1097)

;; check let_family_let_sum__in__first_class_fn_arg_call
(define (edge-check-g1098) (edge-chk "let_family_let_sum__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((la 2) (lb 3)) (+ la lb)))) 5))
(edge-check-g1098)

;; check let_family_letstar_chain__in__first_class_thunk_in_list
(define (edge-check-g1099) (edge-chk "let_family_letstar_chain__in__first_class_thunk_in_list" ((car (list (lambda () (let* ((lc 2) (ld (* lc lc))) ld))))) 4))
(edge-check-g1099)

;; check let_family_letstar_chain__in__first_class_apply_id
(define (edge-check-g1100) (edge-chk "let_family_letstar_chain__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let* ((lc 2) (ld (* lc lc))) ld))) 4))
(edge-check-g1100)

;; check let_family_letstar_chain__in__first_class_map_hole
(define (edge-check-g1101) (edge-chk "let_family_letstar_chain__in__first_class_map_hole" (car (map (lambda (fi) (let* ((lc 2) (ld (* lc lc))) ld)) (list 0))) 4))
(edge-check-g1101)

;; check let_family_letstar_chain__in__first_class_fn_arg_call
(define (edge-check-g1102) (edge-chk "let_family_letstar_chain__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let* ((lc 2) (ld (* lc lc))) ld))) 4))
(edge-check-g1102)

;; check let_family_letrec_mutual__in__first_class_thunk_in_list
(define (edge-check-g1103) (edge-chk "let_family_letrec_mutual__in__first_class_thunk_in_list" ((car (list (lambda () (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10)))))) #t))
(edge-check-g1103)

;; check let_family_letrec_mutual__in__first_class_apply_id
(define (edge-check-g1104) (edge-chk "let_family_letrec_mutual__in__first_class_apply_id" (apply (lambda (fa) fa) (list (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10)))) #t))
(edge-check-g1104)

;; check let_family_letrec_mutual__in__first_class_map_hole
(define (edge-check-g1105) (edge-chk "let_family_letrec_mutual__in__first_class_map_hole" (car (map (lambda (fi) (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10))) (list 0))) #t))
(edge-check-g1105)

;; check let_family_letrec_mutual__in__first_class_fn_arg_call
(define (edge-check-g1106) (edge-chk "let_family_letrec_mutual__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10)))) #t))
(edge-check-g1106)

;; check let_family_named_let_sum__in__first_class_thunk_in_list
(define (edge-check-g1107) (edge-chk "let_family_named_let_sum__in__first_class_thunk_in_list" ((car (list (lambda () (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i)))))))) 10))
(edge-check-g1107)

;; check let_family_named_let_sum__in__first_class_apply_id
(define (edge-check-g1108) (edge-chk "let_family_named_let_sum__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i)))))) 10))
(edge-check-g1108)

;; check let_family_named_let_sum__in__first_class_map_hole
(define (edge-check-g1109) (edge-chk "let_family_named_let_sum__in__first_class_map_hole" (car (map (lambda (fi) (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i))))) (list 0))) 10))
(edge-check-g1109)

;; check let_family_named_let_sum__in__first_class_fn_arg_call
(define (edge-check-g1110) (edge-chk "let_family_named_let_sum__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i)))))) 10))
(edge-check-g1110)

(display "EDGE-MATRIX-DONE") (newline)
