;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: ad_basic (producers) composed into tail_position (contexts)
;; CHECKS: 10
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check ad_basic_deriv_square__in__tail_position_define_tail
(define (tpg393) (derivative (lambda (adx) (* adx adx)) 3.0))
(define (edge-check-g393) (edge-chk "ad_basic_deriv_square__in__tail_position_define_tail" (tpg393) 6.0))
(edge-check-g393)

;; check ad_basic_deriv_square__in__tail_position_let_tail_in_define
(define (tqg394) (let ((tu 1)) (derivative (lambda (adx) (* adx adx)) 3.0)))
(define (edge-check-g394) (edge-chk "ad_basic_deriv_square__in__tail_position_let_tail_in_define" (tqg394) 6.0))
(edge-check-g394)

;; check ad_basic_deriv_square__in__tail_position_if_tail
(define (edge-check-g395) (edge-chk "ad_basic_deriv_square__in__tail_position_if_tail" (if (> 2 1) (derivative (lambda (adx) (* adx adx)) 3.0) 'wrong) 6.0))
(edge-check-g395)

;; check ad_basic_deriv_square__in__tail_position_begin_tail
(define (edge-check-g396) (edge-chk "ad_basic_deriv_square__in__tail_position_begin_tail" (begin 'first (derivative (lambda (adx) (* adx adx)) 3.0)) 6.0))
(edge-check-g396)

;; check ad_basic_deriv_square__in__tail_position_cond_else_tail
(define (edge-check-g397) (edge-chk "ad_basic_deriv_square__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (derivative (lambda (adx) (* adx adx)) 3.0))) 6.0))
(edge-check-g397)

;; check ad_basic_grad_ref__in__tail_position_define_tail
(define (tpg398) (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0))
(define (edge-check-g398) (edge-chk "ad_basic_grad_ref__in__tail_position_define_tail" (tpg398) 3.0))
(edge-check-g398)

;; check ad_basic_grad_ref__in__tail_position_let_tail_in_define
(define (tqg399) (let ((tu 1)) (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0)))
(define (edge-check-g399) (edge-chk "ad_basic_grad_ref__in__tail_position_let_tail_in_define" (tqg399) 3.0))
(edge-check-g399)

;; check ad_basic_grad_ref__in__tail_position_if_tail
(define (edge-check-g400) (edge-chk "ad_basic_grad_ref__in__tail_position_if_tail" (if (> 2 1) (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0) 'wrong) 3.0))
(edge-check-g400)

;; check ad_basic_grad_ref__in__tail_position_begin_tail
(define (edge-check-g401) (edge-chk "ad_basic_grad_ref__in__tail_position_begin_tail" (begin 'first (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0)) 3.0))
(edge-check-g401)

;; check ad_basic_grad_ref__in__tail_position_cond_else_tail
(define (edge-check-g402) (edge-chk "ad_basic_grad_ref__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0))) 3.0))
(edge-check-g402)

(display "EDGE-MATRIX-DONE") (newline)
