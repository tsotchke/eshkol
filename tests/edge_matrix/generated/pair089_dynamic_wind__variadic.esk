;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: dynamic_wind (producers) composed into variadic (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check dynamic_wind_order__in__variadic_rest_car
(define (edge-check-g809) (edge-chk "dynamic_wind_order__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl))) (list 3 2 1)))
(edge-check-g809)

;; check dynamic_wind_order__in__variadic_dotted_first
(define (edge-check-g810) (edge-chk "dynamic_wind_order__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl) 'p1 'p2) (list 3 2 1)))
(edge-check-g810)

;; check dynamic_wind_order__in__variadic_apply_rest
(define (edge-check-g811) (edge-chk "dynamic_wind_order__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl) 0)) (list 3 2 1)))
(edge-check-g811)

;; check dynamic_wind_value__in__variadic_rest_car
(define (edge-check-g812) (edge-chk "dynamic_wind_value__in__variadic_rest_car" (car ((lambda vgs vgs) (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)))) 42))
(edge-check-g812)

;; check dynamic_wind_value__in__variadic_dotted_first
(define (edge-check-g813) (edge-chk "dynamic_wind_value__in__variadic_dotted_first" ((lambda (vh . vt) vh) (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)) 'p1 'p2) 42))
(edge-check-g813)

;; check dynamic_wind_value__in__variadic_apply_rest
(define (edge-check-g814) (edge-chk "dynamic_wind_value__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)) 0)) 42))
(edge-check-g814)

(display "EDGE-MATRIX-DONE") (newline)
