;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into tail_position (contexts)
;; CHECKS: 15
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__tail_position_define_tail
(define (tpg259) (let ((pow 3)) (+ pow 1)))
(define (edge-check-g259) (edge-chk "shadowing_shadow_libc_local__in__tail_position_define_tail" (tpg259) 4))
(edge-check-g259)

;; check shadowing_shadow_libc_local__in__tail_position_let_tail_in_define
(define (tqg260) (let ((tu 1)) (let ((pow 3)) (+ pow 1))))
(define (edge-check-g260) (edge-chk "shadowing_shadow_libc_local__in__tail_position_let_tail_in_define" (tqg260) 4))
(edge-check-g260)

;; check shadowing_shadow_libc_local__in__tail_position_if_tail
(define (edge-check-g261) (edge-chk "shadowing_shadow_libc_local__in__tail_position_if_tail" (if (> 2 1) (let ((pow 3)) (+ pow 1)) 'wrong) 4))
(edge-check-g261)

;; check shadowing_shadow_libc_local__in__tail_position_begin_tail
(define (edge-check-g262) (edge-chk "shadowing_shadow_libc_local__in__tail_position_begin_tail" (begin 'first (let ((pow 3)) (+ pow 1))) 4))
(edge-check-g262)

;; check shadowing_shadow_libc_local__in__tail_position_cond_else_tail
(define (edge-check-g263) (edge-chk "shadowing_shadow_libc_local__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((pow 3)) (+ pow 1)))) 4))
(edge-check-g263)

;; check shadowing_shadow_builtin_rebind__in__tail_position_define_tail
(define (tpg264) (let ((car cdr)) (car (list 1 2 3))))
(define (edge-check-g264) (edge-chk "shadowing_shadow_builtin_rebind__in__tail_position_define_tail" (tpg264) (list 2 3)))
(edge-check-g264)

;; check shadowing_shadow_builtin_rebind__in__tail_position_let_tail_in_define
(define (tqg265) (let ((tu 1)) (let ((car cdr)) (car (list 1 2 3)))))
(define (edge-check-g265) (edge-chk "shadowing_shadow_builtin_rebind__in__tail_position_let_tail_in_define" (tqg265) (list 2 3)))
(edge-check-g265)

;; check shadowing_shadow_builtin_rebind__in__tail_position_if_tail
(define (edge-check-g266) (edge-chk "shadowing_shadow_builtin_rebind__in__tail_position_if_tail" (if (> 2 1) (let ((car cdr)) (car (list 1 2 3))) 'wrong) (list 2 3)))
(edge-check-g266)

;; check shadowing_shadow_builtin_rebind__in__tail_position_begin_tail
(define (edge-check-g267) (edge-chk "shadowing_shadow_builtin_rebind__in__tail_position_begin_tail" (begin 'first (let ((car cdr)) (car (list 1 2 3)))) (list 2 3)))
(edge-check-g267)

;; check shadowing_shadow_builtin_rebind__in__tail_position_cond_else_tail
(define (edge-check-g268) (edge-chk "shadowing_shadow_builtin_rebind__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((car cdr)) (car (list 1 2 3))))) (list 2 3)))
(edge-check-g268)

;; check shadowing_shadow_param__in__tail_position_define_tail
(define (tpg269) ((lambda (ps) (let ((ps 5)) ps)) 1))
(define (edge-check-g269) (edge-chk "shadowing_shadow_param__in__tail_position_define_tail" (tpg269) 5))
(edge-check-g269)

;; check shadowing_shadow_param__in__tail_position_let_tail_in_define
(define (tqg270) (let ((tu 1)) ((lambda (ps) (let ((ps 5)) ps)) 1)))
(define (edge-check-g270) (edge-chk "shadowing_shadow_param__in__tail_position_let_tail_in_define" (tqg270) 5))
(edge-check-g270)

;; check shadowing_shadow_param__in__tail_position_if_tail
(define (edge-check-g271) (edge-chk "shadowing_shadow_param__in__tail_position_if_tail" (if (> 2 1) ((lambda (ps) (let ((ps 5)) ps)) 1) 'wrong) 5))
(edge-check-g271)

;; check shadowing_shadow_param__in__tail_position_begin_tail
(define (edge-check-g272) (edge-chk "shadowing_shadow_param__in__tail_position_begin_tail" (begin 'first ((lambda (ps) (let ((ps 5)) ps)) 1)) 5))
(edge-check-g272)

;; check shadowing_shadow_param__in__tail_position_cond_else_tail
(define (edge-check-g273) (edge-chk "shadowing_shadow_param__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else ((lambda (ps) (let ((ps 5)) ps)) 1))) 5))
(edge-check-g273)

(display "EDGE-MATRIX-DONE") (newline)
