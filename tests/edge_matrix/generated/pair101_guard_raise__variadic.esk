;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: guard_raise (producers) composed into variadic (contexts)
;; CHECKS: 9
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check guard_raise_catch_sym__in__variadic_rest_car
(define (edge-check-g926) (edge-chk "guard_raise_catch_sym__in__variadic_rest_car" (car ((lambda vgs vgs) (guard (ge (#t 'caught)) (raise 'boom)))) (string->symbol "caught")))
(edge-check-g926)

;; check guard_raise_catch_sym__in__variadic_dotted_first
(define (edge-check-g927) (edge-chk "guard_raise_catch_sym__in__variadic_dotted_first" ((lambda (vh . vt) vh) (guard (ge (#t 'caught)) (raise 'boom)) 'p1 'p2) (string->symbol "caught")))
(edge-check-g927)

;; check guard_raise_catch_sym__in__variadic_apply_rest
(define (edge-check-g928) (edge-chk "guard_raise_catch_sym__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (guard (ge (#t 'caught)) (raise 'boom)) 0)) (string->symbol "caught")))
(edge-check-g928)

;; check guard_raise_pass_value__in__variadic_rest_car
(define (edge-check-g929) (edge-chk "guard_raise_pass_value__in__variadic_rest_car" (car ((lambda vgs vgs) (guard (gv (#t gv)) (raise 42)))) 42))
(edge-check-g929)

;; check guard_raise_pass_value__in__variadic_dotted_first
(define (edge-check-g930) (edge-chk "guard_raise_pass_value__in__variadic_dotted_first" ((lambda (vh . vt) vh) (guard (gv (#t gv)) (raise 42)) 'p1 'p2) 42))
(edge-check-g930)

;; check guard_raise_pass_value__in__variadic_apply_rest
(define (edge-check-g931) (edge-chk "guard_raise_pass_value__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (guard (gv (#t gv)) (raise 42)) 0)) 42))
(edge-check-g931)

;; check guard_raise_cond_dispatch__in__variadic_rest_car
(define (edge-check-g932) (edge-chk "guard_raise_cond_dispatch__in__variadic_rest_car" (car ((lambda vgs vgs) (guard (gw ((symbol? gw) 'was-sym) (#t 'other)) (raise 'zap)))) (string->symbol "was-sym")))
(edge-check-g932)

;; check guard_raise_cond_dispatch__in__variadic_dotted_first
(define (edge-check-g933) (edge-chk "guard_raise_cond_dispatch__in__variadic_dotted_first" ((lambda (vh . vt) vh) (guard (gw ((symbol? gw) 'was-sym) (#t 'other)) (raise 'zap)) 'p1 'p2) (string->symbol "was-sym")))
(edge-check-g933)

;; check guard_raise_cond_dispatch__in__variadic_apply_rest
(define (edge-check-g934) (edge-chk "guard_raise_cond_dispatch__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (guard (gw ((symbol? gw) 'was-sym) (#t 'other)) (raise 'zap)) 0)) (string->symbol "was-sym")))
(edge-check-g934)

(display "EDGE-MATRIX-DONE") (newline)
