;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: closures (producers) composed into set_bang (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check closures_single_capture__in__set_bang_store_read
(define (edge-check-g631) (edge-chk "closures_single_capture__in__set_bang_store_read" (let ((svg631 #f)) (set! svg631 (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5))) svg631) 15))
(edge-check-g631)

;; check closures_single_capture__in__set_bang_lambda_store
(define (edge-check-g632) (edge-chk "closures_single_capture__in__set_bang_lambda_store" (let ((swg632 #f)) ((lambda () (set! swg632 (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5))))) swg632) 15))
(edge-check-g632)

;; check closures_single_capture__in__set_bang_set_twice
(define (edge-check-g633) (edge-chk "closures_single_capture__in__set_bang_set_twice" (let ((szg633 0)) (set! szg633 (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5))) (set! szg633 szg633) szg633) 15))
(edge-check-g633)

;; check closures_sibling_set__in__set_bang_store_read
(define (edge-check-g634) (edge-chk "closures_sibling_set__in__set_bang_store_read" (let ((svg634 #f)) (set! svg634 (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get)))) svg634) 2))
(edge-check-g634)

;; check closures_sibling_set__in__set_bang_lambda_store
(define (edge-check-g635) (edge-chk "closures_sibling_set__in__set_bang_lambda_store" (let ((swg635 #f)) ((lambda () (set! swg635 (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get)))))) swg635) 2))
(edge-check-g635)

;; check closures_sibling_set__in__set_bang_set_twice
(define (edge-check-g636) (edge-chk "closures_sibling_set__in__set_bang_set_twice" (let ((szg636 0)) (set! szg636 (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get)))) (set! szg636 szg636) szg636) 2))
(edge-check-g636)

;; check closures_multi_instance__in__set_bang_store_read
(define (mkcg637) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag637 (mkcg637))
(define cbg637 (mkcg637))
(define (edge-check-g637) (edge-chk "closures_multi_instance__in__set_bang_store_read" (let ((svg637 #f)) (set! svg637 (begin (cag637) (cag637) (+ (cag637) (cbg637)))) svg637) 4))
(edge-check-g637)

;; check closures_multi_instance__in__set_bang_lambda_store
(define (mkcg638) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag638 (mkcg638))
(define cbg638 (mkcg638))
(define (edge-check-g638) (edge-chk "closures_multi_instance__in__set_bang_lambda_store" (let ((swg638 #f)) ((lambda () (set! swg638 (begin (cag638) (cag638) (+ (cag638) (cbg638)))))) swg638) 4))
(edge-check-g638)

;; check closures_multi_instance__in__set_bang_set_twice
(define (mkcg639) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag639 (mkcg639))
(define cbg639 (mkcg639))
(define (edge-check-g639) (edge-chk "closures_multi_instance__in__set_bang_set_twice" (let ((szg639 0)) (set! szg639 (begin (cag639) (cag639) (+ (cag639) (cbg639)))) (set! szg639 szg639) szg639) 4))
(edge-check-g639)

;; check closures_counter_once__in__set_bang_store_read
(define ctrg640 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g640) (edge-chk "closures_counter_once__in__set_bang_store_read" (let ((svg640 #f)) (set! svg640 (ctrg640)) svg640) 1))
(edge-check-g640)

;; check closures_counter_once__in__set_bang_lambda_store
(define ctrg641 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g641) (edge-chk "closures_counter_once__in__set_bang_lambda_store" (let ((swg641 #f)) ((lambda () (set! swg641 (ctrg641)))) swg641) 1))
(edge-check-g641)

;; check closures_counter_once__in__set_bang_set_twice
(define ctrg642 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g642) (edge-chk "closures_counter_once__in__set_bang_set_twice" (let ((szg642 0)) (set! szg642 (ctrg642)) (set! szg642 szg642) szg642) 1))
(edge-check-g642)

(display "EDGE-MATRIX-DONE") (newline)
