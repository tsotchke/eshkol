;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: hof (producers) composed into variadic (contexts)
;; CHECKS: 9
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check hof_fold_left__in__variadic_rest_car
(define (edge-check-g1026) (edge-chk "hof_fold_left__in__variadic_rest_car" (car ((lambda vgs vgs) (fold-left + 0 (list 1 2 3)))) 6))
(edge-check-g1026)

;; check hof_fold_left__in__variadic_dotted_first
(define (edge-check-g1027) (edge-chk "hof_fold_left__in__variadic_dotted_first" ((lambda (vh . vt) vh) (fold-left + 0 (list 1 2 3)) 'p1 'p2) 6))
(edge-check-g1027)

;; check hof_fold_left__in__variadic_apply_rest
(define (edge-check-g1028) (edge-chk "hof_fold_left__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (fold-left + 0 (list 1 2 3)) 0)) 6))
(edge-check-g1028)

;; check hof_reduce__in__variadic_rest_car
(define (edge-check-g1029) (edge-chk "hof_reduce__in__variadic_rest_car" (car ((lambda vgs vgs) (reduce + 0 (list 1 2 3)))) 6))
(edge-check-g1029)

;; check hof_reduce__in__variadic_dotted_first
(define (edge-check-g1030) (edge-chk "hof_reduce__in__variadic_dotted_first" ((lambda (vh . vt) vh) (reduce + 0 (list 1 2 3)) 'p1 'p2) 6))
(edge-check-g1030)

;; check hof_reduce__in__variadic_apply_rest
(define (edge-check-g1031) (edge-chk "hof_reduce__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (reduce + 0 (list 1 2 3)) 0)) 6))
(edge-check-g1031)

;; check hof_for_each_acc__in__variadic_rest_car
(define (edge-check-g1032) (edge-chk "hof_for_each_acc__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs))) 6))
(edge-check-g1032)

;; check hof_for_each_acc__in__variadic_dotted_first
(define (edge-check-g1033) (edge-chk "hof_for_each_acc__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs) 'p1 'p2) 6))
(edge-check-g1033)

;; check hof_for_each_acc__in__variadic_apply_rest
(define (edge-check-g1034) (edge-chk "hof_for_each_acc__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs) 0)) 6))
(edge-check-g1034)

(display "EDGE-MATRIX-DONE") (newline)
