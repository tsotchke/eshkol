;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: macros (producers) composed into variadic (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check macros_my_if__in__variadic_rest_car
(define-syntax mig1249 (syntax-rules () ((_ c a b) (if c a b))))
(define (edge-check-g1249) (edge-chk "macros_my_if__in__variadic_rest_car" (car ((lambda vgs vgs) (mig1249 #t 1 2))) 1))
(edge-check-g1249)

;; check macros_my_if__in__variadic_dotted_first
(define-syntax mig1250 (syntax-rules () ((_ c a b) (if c a b))))
(define (edge-check-g1250) (edge-chk "macros_my_if__in__variadic_dotted_first" ((lambda (vh . vt) vh) (mig1250 #t 1 2) 'p1 'p2) 1))
(edge-check-g1250)

;; check macros_my_if__in__variadic_apply_rest
(define-syntax mig1251 (syntax-rules () ((_ c a b) (if c a b))))
(define (edge-check-g1251) (edge-chk "macros_my_if__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (mig1251 #t 1 2) 0)) 1))
(edge-check-g1251)

;; check macros_swap__in__variadic_rest_car
(define-syntax swg1252 (syntax-rules () ((_ a b) (list b a))))
(define (edge-check-g1252) (edge-chk "macros_swap__in__variadic_rest_car" (car ((lambda vgs vgs) (swg1252 1 2))) (list 2 1)))
(edge-check-g1252)

;; check macros_swap__in__variadic_dotted_first
(define-syntax swg1253 (syntax-rules () ((_ a b) (list b a))))
(define (edge-check-g1253) (edge-chk "macros_swap__in__variadic_dotted_first" ((lambda (vh . vt) vh) (swg1253 1 2) 'p1 'p2) (list 2 1)))
(edge-check-g1253)

;; check macros_swap__in__variadic_apply_rest
(define-syntax swg1254 (syntax-rules () ((_ a b) (list b a))))
(define (edge-check-g1254) (edge-chk "macros_swap__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (swg1254 1 2) 0)) (list 2 1)))
(edge-check-g1254)

(display "EDGE-MATRIX-DONE") (newline)
