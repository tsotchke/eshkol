;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: hof (producers) composed into first_class (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check hof_fold_left__in__first_class_thunk_in_list
(define (edge-check-g975) (edge-chk "hof_fold_left__in__first_class_thunk_in_list" ((car (list (lambda () (fold-left + 0 (list 1 2 3)))))) 6))
(edge-check-g975)

;; check hof_fold_left__in__first_class_apply_id
(define (edge-check-g976) (edge-chk "hof_fold_left__in__first_class_apply_id" (apply (lambda (fa) fa) (list (fold-left + 0 (list 1 2 3)))) 6))
(edge-check-g976)

;; check hof_fold_left__in__first_class_map_hole
(define (edge-check-g977) (edge-chk "hof_fold_left__in__first_class_map_hole" (car (map (lambda (fi) (fold-left + 0 (list 1 2 3))) (list 0))) 6))
(edge-check-g977)

;; check hof_fold_left__in__first_class_fn_arg_call
(define (edge-check-g978) (edge-chk "hof_fold_left__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (fold-left + 0 (list 1 2 3)))) 6))
(edge-check-g978)

;; check hof_reduce__in__first_class_thunk_in_list
(define (edge-check-g979) (edge-chk "hof_reduce__in__first_class_thunk_in_list" ((car (list (lambda () (reduce + 0 (list 1 2 3)))))) 6))
(edge-check-g979)

;; check hof_reduce__in__first_class_apply_id
(define (edge-check-g980) (edge-chk "hof_reduce__in__first_class_apply_id" (apply (lambda (fa) fa) (list (reduce + 0 (list 1 2 3)))) 6))
(edge-check-g980)

;; check hof_reduce__in__first_class_map_hole
(define (edge-check-g981) (edge-chk "hof_reduce__in__first_class_map_hole" (car (map (lambda (fi) (reduce + 0 (list 1 2 3))) (list 0))) 6))
(edge-check-g981)

;; check hof_reduce__in__first_class_fn_arg_call
(define (edge-check-g982) (edge-chk "hof_reduce__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (reduce + 0 (list 1 2 3)))) 6))
(edge-check-g982)

;; check hof_for_each_acc__in__first_class_thunk_in_list
(define (edge-check-g983) (edge-chk "hof_for_each_acc__in__first_class_thunk_in_list" ((car (list (lambda () (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs))))) 6))
(edge-check-g983)

;; check hof_for_each_acc__in__first_class_apply_id
(define (edge-check-g984) (edge-chk "hof_for_each_acc__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs))) 6))
(edge-check-g984)

;; check hof_for_each_acc__in__first_class_map_hole
(define (edge-check-g985) (edge-chk "hof_for_each_acc__in__first_class_map_hole" (car (map (lambda (fi) (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs)) (list 0))) 6))
(edge-check-g985)

;; check hof_for_each_acc__in__first_class_fn_arg_call
(define (edge-check-g986) (edge-chk "hof_for_each_acc__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs))) 6))
(edge-check-g986)

(display "EDGE-MATRIX-DONE") (newline)
