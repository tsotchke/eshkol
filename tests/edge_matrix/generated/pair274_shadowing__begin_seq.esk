;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into begin_seq (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__begin_seq_begin_tail
(define (edge-check-g2256) (edge-chk "shadowing_shadow_libc_local__in__begin_seq_begin_tail" (begin 'bfirst (let ((pow 3)) (+ pow 1))) 4))
(edge-check-g2256)

;; check shadowing_shadow_libc_local__in__begin_seq_begin_only
(define (edge-check-g2257) (edge-chk "shadowing_shadow_libc_local__in__begin_seq_begin_only" (begin (let ((pow 3)) (+ pow 1))) 4))
(edge-check-g2257)

;; check shadowing_shadow_builtin_rebind__in__begin_seq_begin_tail
(define (edge-check-g2258) (edge-chk "shadowing_shadow_builtin_rebind__in__begin_seq_begin_tail" (begin 'bfirst (let ((car cdr)) (car (list 1 2 3)))) (list 2 3)))
(edge-check-g2258)

;; check shadowing_shadow_builtin_rebind__in__begin_seq_begin_only
(define (edge-check-g2259) (edge-chk "shadowing_shadow_builtin_rebind__in__begin_seq_begin_only" (begin (let ((car cdr)) (car (list 1 2 3)))) (list 2 3)))
(edge-check-g2259)

;; check shadowing_shadow_param__in__begin_seq_begin_tail
(define (edge-check-g2260) (edge-chk "shadowing_shadow_param__in__begin_seq_begin_tail" (begin 'bfirst ((lambda (ps) (let ((ps 5)) ps)) 1)) 5))
(edge-check-g2260)

;; check shadowing_shadow_param__in__begin_seq_begin_only
(define (edge-check-g2261) (edge-chk "shadowing_shadow_param__in__begin_seq_begin_only" (begin ((lambda (ps) (let ((ps 5)) ps)) 1)) 5))
(edge-check-g2261)

(display "EDGE-MATRIX-DONE") (newline)
