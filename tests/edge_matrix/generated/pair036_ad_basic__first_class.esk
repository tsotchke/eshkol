;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: ad_basic (producers) composed into first_class (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check ad_basic_deriv_square__in__first_class_thunk_in_list
(define (edge-check-g375) (edge-chk "ad_basic_deriv_square__in__first_class_thunk_in_list" ((car (list (lambda () (derivative (lambda (adx) (* adx adx)) 3.0))))) 6.0))
(edge-check-g375)

;; check ad_basic_deriv_square__in__first_class_apply_id
(define (edge-check-g376) (edge-chk "ad_basic_deriv_square__in__first_class_apply_id" (apply (lambda (fa) fa) (list (derivative (lambda (adx) (* adx adx)) 3.0))) 6.0))
(edge-check-g376)

;; check ad_basic_deriv_square__in__first_class_map_hole
(define (edge-check-g377) (edge-chk "ad_basic_deriv_square__in__first_class_map_hole" (car (map (lambda (fi) (derivative (lambda (adx) (* adx adx)) 3.0)) (list 0))) 6.0))
(edge-check-g377)

;; check ad_basic_deriv_square__in__first_class_fn_arg_call
(define (edge-check-g378) (edge-chk "ad_basic_deriv_square__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (derivative (lambda (adx) (* adx adx)) 3.0))) 6.0))
(edge-check-g378)

;; check ad_basic_grad_ref__in__first_class_thunk_in_list
(define (edge-check-g379) (edge-chk "ad_basic_grad_ref__in__first_class_thunk_in_list" ((car (list (lambda () (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0))))) 3.0))
(edge-check-g379)

;; check ad_basic_grad_ref__in__first_class_apply_id
(define (edge-check-g380) (edge-chk "ad_basic_grad_ref__in__first_class_apply_id" (apply (lambda (fa) fa) (list (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0))) 3.0))
(edge-check-g380)

;; check ad_basic_grad_ref__in__first_class_map_hole
(define (edge-check-g381) (edge-chk "ad_basic_grad_ref__in__first_class_map_hole" (car (map (lambda (fi) (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0)) (list 0))) 3.0))
(edge-check-g381)

;; check ad_basic_grad_ref__in__first_class_fn_arg_call
(define (edge-check-g382) (edge-chk "ad_basic_grad_ref__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0))) 3.0))
(edge-check-g382)

(display "EDGE-MATRIX-DONE") (newline)
