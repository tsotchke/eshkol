;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: strings (producers) composed into first_class (contexts)
;; CHECKS: 16
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check strings_append__in__first_class_thunk_in_list
(define (edge-check-g1495) (edge-chk "strings_append__in__first_class_thunk_in_list" ((car (list (lambda () (string-append "ab" "cd"))))) (string #\a #\b #\c #\d)))
(edge-check-g1495)

;; check strings_append__in__first_class_apply_id
(define (edge-check-g1496) (edge-chk "strings_append__in__first_class_apply_id" (apply (lambda (fa) fa) (list (string-append "ab" "cd"))) (string #\a #\b #\c #\d)))
(edge-check-g1496)

;; check strings_append__in__first_class_map_hole
(define (edge-check-g1497) (edge-chk "strings_append__in__first_class_map_hole" (car (map (lambda (fi) (string-append "ab" "cd")) (list 0))) (string #\a #\b #\c #\d)))
(edge-check-g1497)

;; check strings_append__in__first_class_fn_arg_call
(define (edge-check-g1498) (edge-chk "strings_append__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (string-append "ab" "cd"))) (string #\a #\b #\c #\d)))
(edge-check-g1498)

;; check strings_substr__in__first_class_thunk_in_list
(define (edge-check-g1499) (edge-chk "strings_substr__in__first_class_thunk_in_list" ((car (list (lambda () (substring "hello" 1 3))))) "el"))
(edge-check-g1499)

;; check strings_substr__in__first_class_apply_id
(define (edge-check-g1500) (edge-chk "strings_substr__in__first_class_apply_id" (apply (lambda (fa) fa) (list (substring "hello" 1 3))) "el"))
(edge-check-g1500)

;; check strings_substr__in__first_class_map_hole
(define (edge-check-g1501) (edge-chk "strings_substr__in__first_class_map_hole" (car (map (lambda (fi) (substring "hello" 1 3)) (list 0))) "el"))
(edge-check-g1501)

;; check strings_substr__in__first_class_fn_arg_call
(define (edge-check-g1502) (edge-chk "strings_substr__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (substring "hello" 1 3))) "el"))
(edge-check-g1502)

;; check strings_num_to_str__in__first_class_thunk_in_list
(define (edge-check-g1503) (edge-chk "strings_num_to_str__in__first_class_thunk_in_list" ((car (list (lambda () (number->string 42))))) "42"))
(edge-check-g1503)

;; check strings_num_to_str__in__first_class_apply_id
(define (edge-check-g1504) (edge-chk "strings_num_to_str__in__first_class_apply_id" (apply (lambda (fa) fa) (list (number->string 42))) "42"))
(edge-check-g1504)

;; check strings_num_to_str__in__first_class_map_hole
(define (edge-check-g1505) (edge-chk "strings_num_to_str__in__first_class_map_hole" (car (map (lambda (fi) (number->string 42)) (list 0))) "42"))
(edge-check-g1505)

;; check strings_num_to_str__in__first_class_fn_arg_call
(define (edge-check-g1506) (edge-chk "strings_num_to_str__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (number->string 42))) "42"))
(edge-check-g1506)

;; check strings_str_to_num__in__first_class_thunk_in_list
(define (edge-check-g1507) (edge-chk "strings_str_to_num__in__first_class_thunk_in_list" ((car (list (lambda () (string->number "42"))))) 42))
(edge-check-g1507)

;; check strings_str_to_num__in__first_class_apply_id
(define (edge-check-g1508) (edge-chk "strings_str_to_num__in__first_class_apply_id" (apply (lambda (fa) fa) (list (string->number "42"))) 42))
(edge-check-g1508)

;; check strings_str_to_num__in__first_class_map_hole
(define (edge-check-g1509) (edge-chk "strings_str_to_num__in__first_class_map_hole" (car (map (lambda (fi) (string->number "42")) (list 0))) 42))
(edge-check-g1509)

;; check strings_str_to_num__in__first_class_fn_arg_call
(define (edge-check-g1510) (edge-chk "strings_str_to_num__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (string->number "42"))) 42))
(edge-check-g1510)

(display "EDGE-MATRIX-DONE") (newline)
