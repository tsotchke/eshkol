;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into variadic (contexts)
;; CHECKS: 9
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__variadic_rest_car
(define (edge-check-g283) (edge-chk "shadowing_shadow_libc_local__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((pow 3)) (+ pow 1)))) 4))
(edge-check-g283)

;; check shadowing_shadow_libc_local__in__variadic_dotted_first
(define (edge-check-g284) (edge-chk "shadowing_shadow_libc_local__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((pow 3)) (+ pow 1)) 'p1 'p2) 4))
(edge-check-g284)

;; check shadowing_shadow_libc_local__in__variadic_apply_rest
(define (edge-check-g285) (edge-chk "shadowing_shadow_libc_local__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((pow 3)) (+ pow 1)) 0)) 4))
(edge-check-g285)

;; check shadowing_shadow_builtin_rebind__in__variadic_rest_car
(define (edge-check-g286) (edge-chk "shadowing_shadow_builtin_rebind__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((car cdr)) (car (list 1 2 3))))) (list 2 3)))
(edge-check-g286)

;; check shadowing_shadow_builtin_rebind__in__variadic_dotted_first
(define (edge-check-g287) (edge-chk "shadowing_shadow_builtin_rebind__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((car cdr)) (car (list 1 2 3))) 'p1 'p2) (list 2 3)))
(edge-check-g287)

;; check shadowing_shadow_builtin_rebind__in__variadic_apply_rest
(define (edge-check-g288) (edge-chk "shadowing_shadow_builtin_rebind__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((car cdr)) (car (list 1 2 3))) 0)) (list 2 3)))
(edge-check-g288)

;; check shadowing_shadow_param__in__variadic_rest_car
(define (edge-check-g289) (edge-chk "shadowing_shadow_param__in__variadic_rest_car" (car ((lambda vgs vgs) ((lambda (ps) (let ((ps 5)) ps)) 1))) 5))
(edge-check-g289)

;; check shadowing_shadow_param__in__variadic_dotted_first
(define (edge-check-g290) (edge-chk "shadowing_shadow_param__in__variadic_dotted_first" ((lambda (vh . vt) vh) ((lambda (ps) (let ((ps 5)) ps)) 1) 'p1 'p2) 5))
(edge-check-g290)

;; check shadowing_shadow_param__in__variadic_apply_rest
(define (edge-check-g291) (edge-chk "shadowing_shadow_param__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list ((lambda (ps) (let ((ps 5)) ps)) 1) 0)) 5))
(edge-check-g291)

(display "EDGE-MATRIX-DONE") (newline)
