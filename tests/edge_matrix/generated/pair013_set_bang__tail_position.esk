;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into tail_position (contexts)
;; CHECKS: 10
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__tail_position_define_tail
(define (tpg216) (let ((sx 1)) (set! sx 41) (+ sx 1)))
(define (edge-check-g216) (edge-chk "set_bang_local_mut__in__tail_position_define_tail" (tpg216) 42))
(edge-check-g216)

;; check set_bang_local_mut__in__tail_position_let_tail_in_define
(define (tqg217) (let ((tu 1)) (let ((sx 1)) (set! sx 41) (+ sx 1))))
(define (edge-check-g217) (edge-chk "set_bang_local_mut__in__tail_position_let_tail_in_define" (tqg217) 42))
(edge-check-g217)

;; check set_bang_local_mut__in__tail_position_if_tail
(define (edge-check-g218) (edge-chk "set_bang_local_mut__in__tail_position_if_tail" (if (> 2 1) (let ((sx 1)) (set! sx 41) (+ sx 1)) 'wrong) 42))
(edge-check-g218)

;; check set_bang_local_mut__in__tail_position_begin_tail
(define (edge-check-g219) (edge-chk "set_bang_local_mut__in__tail_position_begin_tail" (begin 'first (let ((sx 1)) (set! sx 41) (+ sx 1))) 42))
(edge-check-g219)

;; check set_bang_local_mut__in__tail_position_cond_else_tail
(define (edge-check-g220) (edge-chk "set_bang_local_mut__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((sx 1)) (set! sx 41) (+ sx 1)))) 42))
(edge-check-g220)

;; check set_bang_closure_mut__in__tail_position_define_tail
(define (tpg221) (let ((sy 0)) ((lambda () (set! sy 7))) sy))
(define (edge-check-g221) (edge-chk "set_bang_closure_mut__in__tail_position_define_tail" (tpg221) 7))
(edge-check-g221)

;; check set_bang_closure_mut__in__tail_position_let_tail_in_define
(define (tqg222) (let ((tu 1)) (let ((sy 0)) ((lambda () (set! sy 7))) sy)))
(define (edge-check-g222) (edge-chk "set_bang_closure_mut__in__tail_position_let_tail_in_define" (tqg222) 7))
(edge-check-g222)

;; check set_bang_closure_mut__in__tail_position_if_tail
(define (edge-check-g223) (edge-chk "set_bang_closure_mut__in__tail_position_if_tail" (if (> 2 1) (let ((sy 0)) ((lambda () (set! sy 7))) sy) 'wrong) 7))
(edge-check-g223)

;; check set_bang_closure_mut__in__tail_position_begin_tail
(define (edge-check-g224) (edge-chk "set_bang_closure_mut__in__tail_position_begin_tail" (begin 'first (let ((sy 0)) ((lambda () (set! sy 7))) sy)) 7))
(edge-check-g224)

;; check set_bang_closure_mut__in__tail_position_cond_else_tail
(define (edge-check-g225) (edge-chk "set_bang_closure_mut__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((sy 0)) ((lambda () (set! sy 7))) sy))) 7))
(edge-check-g225)

(display "EDGE-MATRIX-DONE") (newline)
