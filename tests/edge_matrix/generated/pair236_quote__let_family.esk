;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: quote (producers) composed into let_family (contexts)
;; CHECKS: 28
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check quote_sym__in__let_family_let_bind
(define (edge-check-g2048) (edge-chk "quote_sym__in__let_family_let_bind" (let ((lxg2048 'edge-sym)) lxg2048) (string->symbol "edge-sym")))
(edge-check-g2048)

;; check quote_sym__in__let_family_letstar_chain
(define (edge-check-g2049) (edge-chk "quote_sym__in__let_family_letstar_chain" (let* ((lyg2049 'edge-sym) (lzg2049 lyg2049)) lzg2049) (string->symbol "edge-sym")))
(edge-check-g2049)

;; check quote_sym__in__let_family_letrec_thunk
(define (edge-check-g2050) (edge-chk "quote_sym__in__let_family_letrec_thunk" (letrec ((lfg2050 (lambda () 'edge-sym))) (lfg2050)) (string->symbol "edge-sym")))
(edge-check-g2050)

;; check quote_sym__in__let_family_named_let_carry
(define (edge-check-g2051) (edge-chk "quote_sym__in__let_family_named_let_carry" (let nlg2051 ((i 0) (acc #f)) (if (= i 1) acc (nlg2051 (+ i 1) 'edge-sym))) (string->symbol "edge-sym")))
(edge-check-g2051)

;; check quote_list__in__let_family_let_bind
(define (edge-check-g2052) (edge-chk "quote_list__in__let_family_let_bind" (let ((lxg2052 '(1 2 3))) lxg2052) (list 1 2 3)))
(edge-check-g2052)

;; check quote_list__in__let_family_letstar_chain
(define (edge-check-g2053) (edge-chk "quote_list__in__let_family_letstar_chain" (let* ((lyg2053 '(1 2 3)) (lzg2053 lyg2053)) lzg2053) (list 1 2 3)))
(edge-check-g2053)

;; check quote_list__in__let_family_letrec_thunk
(define (edge-check-g2054) (edge-chk "quote_list__in__let_family_letrec_thunk" (letrec ((lfg2054 (lambda () '(1 2 3)))) (lfg2054)) (list 1 2 3)))
(edge-check-g2054)

;; check quote_list__in__let_family_named_let_carry
(define (edge-check-g2055) (edge-chk "quote_list__in__let_family_named_let_carry" (let nlg2055 ((i 0) (acc #f)) (if (= i 1) acc (nlg2055 (+ i 1) '(1 2 3)))) (list 1 2 3)))
(edge-check-g2055)

;; check quote_empty__in__let_family_let_bind
(define (edge-check-g2056) (edge-chk "quote_empty__in__let_family_let_bind" (let ((lxg2056 '())) lxg2056) (list)))
(edge-check-g2056)

;; check quote_empty__in__let_family_letstar_chain
(define (edge-check-g2057) (edge-chk "quote_empty__in__let_family_letstar_chain" (let* ((lyg2057 '()) (lzg2057 lyg2057)) lzg2057) (list)))
(edge-check-g2057)

;; check quote_empty__in__let_family_letrec_thunk
(define (edge-check-g2058) (edge-chk "quote_empty__in__let_family_letrec_thunk" (letrec ((lfg2058 (lambda () '()))) (lfg2058)) (list)))
(edge-check-g2058)

;; check quote_empty__in__let_family_named_let_carry
(define (edge-check-g2059) (edge-chk "quote_empty__in__let_family_named_let_carry" (let nlg2059 ((i 0) (acc #f)) (if (= i 1) acc (nlg2059 (+ i 1) '()))) (list)))
(edge-check-g2059)

;; check quote_nested_quote__in__let_family_let_bind
(define (edge-check-g2060) (edge-chk "quote_nested_quote__in__let_family_let_bind" (let ((lxg2060 (car ''nq))) lxg2060) (string->symbol "quote")))
(edge-check-g2060)

;; check quote_nested_quote__in__let_family_letstar_chain
(define (edge-check-g2061) (edge-chk "quote_nested_quote__in__let_family_letstar_chain" (let* ((lyg2061 (car ''nq)) (lzg2061 lyg2061)) lzg2061) (string->symbol "quote")))
(edge-check-g2061)

;; check quote_nested_quote__in__let_family_letrec_thunk
(define (edge-check-g2062) (edge-chk "quote_nested_quote__in__let_family_letrec_thunk" (letrec ((lfg2062 (lambda () (car ''nq)))) (lfg2062)) (string->symbol "quote")))
(edge-check-g2062)

;; check quote_nested_quote__in__let_family_named_let_carry
(define (edge-check-g2063) (edge-chk "quote_nested_quote__in__let_family_named_let_carry" (let nlg2063 ((i 0) (acc #f)) (if (= i 1) acc (nlg2063 (+ i 1) (car ''nq)))) (string->symbol "quote")))
(edge-check-g2063)

;; check quote_veclit_ref__in__let_family_let_bind
(define (edge-check-g2064) (edge-chk "quote_veclit_ref__in__let_family_let_bind" (let ((lxg2064 (vector-ref #(9 8 7) 2))) lxg2064) 7))
(edge-check-g2064)

;; check quote_veclit_ref__in__let_family_letstar_chain
(define (edge-check-g2065) (edge-chk "quote_veclit_ref__in__let_family_letstar_chain" (let* ((lyg2065 (vector-ref #(9 8 7) 2)) (lzg2065 lyg2065)) lzg2065) 7))
(edge-check-g2065)

;; check quote_veclit_ref__in__let_family_letrec_thunk
(define (edge-check-g2066) (edge-chk "quote_veclit_ref__in__let_family_letrec_thunk" (letrec ((lfg2066 (lambda () (vector-ref #(9 8 7) 2)))) (lfg2066)) 7))
(edge-check-g2066)

;; check quote_veclit_ref__in__let_family_named_let_carry
(define (edge-check-g2067) (edge-chk "quote_veclit_ref__in__let_family_named_let_carry" (let nlg2067 ((i 0) (acc #f)) (if (= i 1) acc (nlg2067 (+ i 1) (vector-ref #(9 8 7) 2)))) 7))
(edge-check-g2067)

;; check quote_qq_sugar__in__let_family_let_bind
(define (edge-check-g2068) (edge-chk "quote_qq_sugar__in__let_family_let_bind" (let ((lxg2068 `(1 ,(+ 1 1) ,@(list 3 4)))) lxg2068) (list 1 2 3 4)))
(edge-check-g2068)

;; check quote_qq_sugar__in__let_family_letstar_chain
(define (edge-check-g2069) (edge-chk "quote_qq_sugar__in__let_family_letstar_chain" (let* ((lyg2069 `(1 ,(+ 1 1) ,@(list 3 4))) (lzg2069 lyg2069)) lzg2069) (list 1 2 3 4)))
(edge-check-g2069)

;; check quote_qq_sugar__in__let_family_letrec_thunk
(define (edge-check-g2070) (edge-chk "quote_qq_sugar__in__let_family_letrec_thunk" (letrec ((lfg2070 (lambda () `(1 ,(+ 1 1) ,@(list 3 4))))) (lfg2070)) (list 1 2 3 4)))
(edge-check-g2070)

;; check quote_qq_sugar__in__let_family_named_let_carry
(define (edge-check-g2071) (edge-chk "quote_qq_sugar__in__let_family_named_let_carry" (let nlg2071 ((i 0) (acc #f)) (if (= i 1) acc (nlg2071 (+ i 1) `(1 ,(+ 1 1) ,@(list 3 4))))) (list 1 2 3 4)))
(edge-check-g2071)

;; check quote_qq_longform__in__let_family_let_bind
(define (edge-check-g2072) (edge-chk "quote_qq_longform__in__let_family_let_bind" (let ((lxg2072 (quasiquote (1 (unquote (+ 1 1)))))) lxg2072) (list 1 2)))
(edge-check-g2072)

;; check quote_qq_longform__in__let_family_letstar_chain
(define (edge-check-g2073) (edge-chk "quote_qq_longform__in__let_family_letstar_chain" (let* ((lyg2073 (quasiquote (1 (unquote (+ 1 1))))) (lzg2073 lyg2073)) lzg2073) (list 1 2)))
(edge-check-g2073)

;; check quote_qq_longform__in__let_family_letrec_thunk
(define (edge-check-g2074) (edge-chk "quote_qq_longform__in__let_family_letrec_thunk" (letrec ((lfg2074 (lambda () (quasiquote (1 (unquote (+ 1 1))))))) (lfg2074)) (list 1 2)))
(edge-check-g2074)

;; check quote_qq_longform__in__let_family_named_let_carry
(define (edge-check-g2075) (edge-chk "quote_qq_longform__in__let_family_named_let_carry" (let nlg2075 ((i 0) (acc #f)) (if (= i 1) acc (nlg2075 (+ i 1) (quasiquote (1 (unquote (+ 1 1))))))) (list 1 2)))
(edge-check-g2075)

(display "EDGE-MATRIX-DONE") (newline)
