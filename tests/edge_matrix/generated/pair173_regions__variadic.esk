;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: regions (producers) composed into variadic (contexts)
;; CHECKS: 3
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check regions_region_value__in__variadic_rest_car
(define (edge-check-g1492) (edge-chk "regions_region_value__in__variadic_rest_car" (car ((lambda vgs vgs) (with-region 'rgp (+ 40 2)))) 42))
(edge-check-g1492)

;; check regions_region_value__in__variadic_dotted_first
(define (edge-check-g1493) (edge-chk "regions_region_value__in__variadic_dotted_first" ((lambda (vh . vt) vh) (with-region 'rgp (+ 40 2)) 'p1 'p2) 42))
(edge-check-g1493)

;; check regions_region_value__in__variadic_apply_rest
(define (edge-check-g1494) (edge-chk "regions_region_value__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (with-region 'rgp (+ 40 2)) 0)) 42))
(edge-check-g1494)

(display "EDGE-MATRIX-DONE") (newline)
