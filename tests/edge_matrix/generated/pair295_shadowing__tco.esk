;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into tco (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__tco_loop_tail_hole
(define (edge-check-g2355) (edge-chk "shadowing_shadow_libc_local__in__tco_loop_tail_hole" (let tlhg2355 ((i 0)) (if (= i 50) (let ((pow 3)) (+ pow 1)) (tlhg2355 (+ i 1)))) 4))
(edge-check-g2355)

;; check shadowing_shadow_libc_local__in__tco_rec_fn_base
(define (rbg2356 n) (if (= n 0) (let ((pow 3)) (+ pow 1)) (rbg2356 (- n 1))))
(define (edge-check-g2356) (edge-chk "shadowing_shadow_libc_local__in__tco_rec_fn_base" (rbg2356 1000) 4))
(edge-check-g2356)

;; check shadowing_shadow_builtin_rebind__in__tco_loop_tail_hole
(define (edge-check-g2357) (edge-chk "shadowing_shadow_builtin_rebind__in__tco_loop_tail_hole" (let tlhg2357 ((i 0)) (if (= i 50) (let ((car cdr)) (car (list 1 2 3))) (tlhg2357 (+ i 1)))) (list 2 3)))
(edge-check-g2357)

;; check shadowing_shadow_builtin_rebind__in__tco_rec_fn_base
(define (rbg2358 n) (if (= n 0) (let ((car cdr)) (car (list 1 2 3))) (rbg2358 (- n 1))))
(define (edge-check-g2358) (edge-chk "shadowing_shadow_builtin_rebind__in__tco_rec_fn_base" (rbg2358 1000) (list 2 3)))
(edge-check-g2358)

;; check shadowing_shadow_param__in__tco_loop_tail_hole
(define (edge-check-g2359) (edge-chk "shadowing_shadow_param__in__tco_loop_tail_hole" (let tlhg2359 ((i 0)) (if (= i 50) ((lambda (ps) (let ((ps 5)) ps)) 1) (tlhg2359 (+ i 1)))) 5))
(edge-check-g2359)

;; check shadowing_shadow_param__in__tco_rec_fn_base
(define (rbg2360 n) (if (= n 0) ((lambda (ps) (let ((ps 5)) ps)) 1) (rbg2360 (- n 1))))
(define (edge-check-g2360) (edge-chk "shadowing_shadow_param__in__tco_rec_fn_base" (rbg2360 1000) 5))
(edge-check-g2360)

(display "EDGE-MATRIX-DONE") (newline)
