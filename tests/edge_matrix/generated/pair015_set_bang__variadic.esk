;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into variadic (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__variadic_rest_car
(define (edge-check-g232) (edge-chk "set_bang_local_mut__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((sx 1)) (set! sx 41) (+ sx 1)))) 42))
(edge-check-g232)

;; check set_bang_local_mut__in__variadic_dotted_first
(define (edge-check-g233) (edge-chk "set_bang_local_mut__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((sx 1)) (set! sx 41) (+ sx 1)) 'p1 'p2) 42))
(edge-check-g233)

;; check set_bang_local_mut__in__variadic_apply_rest
(define (edge-check-g234) (edge-chk "set_bang_local_mut__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((sx 1)) (set! sx 41) (+ sx 1)) 0)) 42))
(edge-check-g234)

;; check set_bang_closure_mut__in__variadic_rest_car
(define (edge-check-g235) (edge-chk "set_bang_closure_mut__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((sy 0)) ((lambda () (set! sy 7))) sy))) 7))
(edge-check-g235)

;; check set_bang_closure_mut__in__variadic_dotted_first
(define (edge-check-g236) (edge-chk "set_bang_closure_mut__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((sy 0)) ((lambda () (set! sy 7))) sy) 'p1 'p2) 7))
(edge-check-g236)

;; check set_bang_closure_mut__in__variadic_apply_rest
(define (edge-check-g237) (edge-chk "set_bang_closure_mut__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((sy 0)) ((lambda () (set! sy 7))) sy) 0)) 7))
(edge-check-g237)

(display "EDGE-MATRIX-DONE") (newline)
