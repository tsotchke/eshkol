;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: first_class (producers) composed into vectors (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check first_class_builtin_var__in__vectors_singleton_ref
(define (edge-check-g1886) (edge-chk "first_class_builtin_var__in__vectors_singleton_ref" (vector-ref (vector (let ((fp null?)) (fp '()))) 0) #t))
(edge-check-g1886)

;; check first_class_builtin_var__in__vectors_set_then_ref
(define (edge-check-g1887) (edge-chk "first_class_builtin_var__in__vectors_set_then_ref" (let ((vvg1887 (make-vector 1 #f))) (vector-set! vvg1887 0 (let ((fp null?)) (fp '()))) (vector-ref vvg1887 0)) #t))
(edge-check-g1887)

;; check first_class_apply_builtin__in__vectors_singleton_ref
(define (edge-check-g1888) (edge-chk "first_class_apply_builtin__in__vectors_singleton_ref" (vector-ref (vector (apply + (list 20 22))) 0) 42))
(edge-check-g1888)

;; check first_class_apply_builtin__in__vectors_set_then_ref
(define (edge-check-g1889) (edge-chk "first_class_apply_builtin__in__vectors_set_then_ref" (let ((vvg1889 (make-vector 1 #f))) (vector-set! vvg1889 0 (apply + (list 20 22))) (vector-ref vvg1889 0)) 42))
(edge-check-g1889)

;; check first_class_builtin_from_list__in__vectors_singleton_ref
(define (edge-check-g1890) (edge-chk "first_class_builtin_from_list__in__vectors_singleton_ref" (vector-ref (vector ((car (list +)) 20 22)) 0) 42))
(edge-check-g1890)

;; check first_class_builtin_from_list__in__vectors_set_then_ref
(define (edge-check-g1891) (edge-chk "first_class_builtin_from_list__in__vectors_set_then_ref" (let ((vvg1891 (make-vector 1 #f))) (vector-set! vvg1891 0 ((car (list +)) 20 22)) (vector-ref vvg1891 0)) 42))
(edge-check-g1891)

;; check first_class_map_builtin__in__vectors_singleton_ref
(define (edge-check-g1892) (edge-chk "first_class_map_builtin__in__vectors_singleton_ref" (vector-ref (vector (map car '((1 2) (3 4)))) 0) (list 1 3)))
(edge-check-g1892)

;; check first_class_map_builtin__in__vectors_set_then_ref
(define (edge-check-g1893) (edge-chk "first_class_map_builtin__in__vectors_set_then_ref" (let ((vvg1893 (make-vector 1 #f))) (vector-set! vvg1893 0 (map car '((1 2) (3 4)))) (vector-ref vvg1893 0)) (list 1 3)))
(edge-check-g1893)

(display "EDGE-MATRIX-DONE") (newline)
