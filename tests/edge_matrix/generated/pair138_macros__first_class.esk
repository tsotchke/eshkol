;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: macros (producers) composed into first_class (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check macros_my_if__in__first_class_thunk_in_list
(define-syntax mig1215 (syntax-rules () ((_ c a b) (if c a b))))
(define (edge-check-g1215) (edge-chk "macros_my_if__in__first_class_thunk_in_list" ((car (list (lambda () (mig1215 #t 1 2))))) 1))
(edge-check-g1215)

;; check macros_my_if__in__first_class_apply_id
(define-syntax mig1216 (syntax-rules () ((_ c a b) (if c a b))))
(define (edge-check-g1216) (edge-chk "macros_my_if__in__first_class_apply_id" (apply (lambda (fa) fa) (list (mig1216 #t 1 2))) 1))
(edge-check-g1216)

;; check macros_my_if__in__first_class_map_hole
(define-syntax mig1217 (syntax-rules () ((_ c a b) (if c a b))))
(define (edge-check-g1217) (edge-chk "macros_my_if__in__first_class_map_hole" (car (map (lambda (fi) (mig1217 #t 1 2)) (list 0))) 1))
(edge-check-g1217)

;; check macros_my_if__in__first_class_fn_arg_call
(define-syntax mig1218 (syntax-rules () ((_ c a b) (if c a b))))
(define (edge-check-g1218) (edge-chk "macros_my_if__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (mig1218 #t 1 2))) 1))
(edge-check-g1218)

;; check macros_swap__in__first_class_thunk_in_list
(define-syntax swg1219 (syntax-rules () ((_ a b) (list b a))))
(define (edge-check-g1219) (edge-chk "macros_swap__in__first_class_thunk_in_list" ((car (list (lambda () (swg1219 1 2))))) (list 2 1)))
(edge-check-g1219)

;; check macros_swap__in__first_class_apply_id
(define-syntax swg1220 (syntax-rules () ((_ a b) (list b a))))
(define (edge-check-g1220) (edge-chk "macros_swap__in__first_class_apply_id" (apply (lambda (fa) fa) (list (swg1220 1 2))) (list 2 1)))
(edge-check-g1220)

;; check macros_swap__in__first_class_map_hole
(define-syntax swg1221 (syntax-rules () ((_ a b) (list b a))))
(define (edge-check-g1221) (edge-chk "macros_swap__in__first_class_map_hole" (car (map (lambda (fi) (swg1221 1 2)) (list 0))) (list 2 1)))
(edge-check-g1221)

;; check macros_swap__in__first_class_fn_arg_call
(define-syntax swg1222 (syntax-rules () ((_ a b) (list b a))))
(define (edge-check-g1222) (edge-chk "macros_swap__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (swg1222 1 2))) (list 2 1)))
(edge-check-g1222)

(display "EDGE-MATRIX-DONE") (newline)
