;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: hof (producers) composed into tail_position (contexts)
;; CHECKS: 15
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check hof_fold_left__in__tail_position_define_tail
(define (tpg1002) (fold-left + 0 (list 1 2 3)))
(define (edge-check-g1002) (edge-chk "hof_fold_left__in__tail_position_define_tail" (tpg1002) 6))
(edge-check-g1002)

;; check hof_fold_left__in__tail_position_let_tail_in_define
(define (tqg1003) (let ((tu 1)) (fold-left + 0 (list 1 2 3))))
(define (edge-check-g1003) (edge-chk "hof_fold_left__in__tail_position_let_tail_in_define" (tqg1003) 6))
(edge-check-g1003)

;; check hof_fold_left__in__tail_position_if_tail
(define (edge-check-g1004) (edge-chk "hof_fold_left__in__tail_position_if_tail" (if (> 2 1) (fold-left + 0 (list 1 2 3)) 'wrong) 6))
(edge-check-g1004)

;; check hof_fold_left__in__tail_position_begin_tail
(define (edge-check-g1005) (edge-chk "hof_fold_left__in__tail_position_begin_tail" (begin 'first (fold-left + 0 (list 1 2 3))) 6))
(edge-check-g1005)

;; check hof_fold_left__in__tail_position_cond_else_tail
(define (edge-check-g1006) (edge-chk "hof_fold_left__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (fold-left + 0 (list 1 2 3)))) 6))
(edge-check-g1006)

;; check hof_reduce__in__tail_position_define_tail
(define (tpg1007) (reduce + 0 (list 1 2 3)))
(define (edge-check-g1007) (edge-chk "hof_reduce__in__tail_position_define_tail" (tpg1007) 6))
(edge-check-g1007)

;; check hof_reduce__in__tail_position_let_tail_in_define
(define (tqg1008) (let ((tu 1)) (reduce + 0 (list 1 2 3))))
(define (edge-check-g1008) (edge-chk "hof_reduce__in__tail_position_let_tail_in_define" (tqg1008) 6))
(edge-check-g1008)

;; check hof_reduce__in__tail_position_if_tail
(define (edge-check-g1009) (edge-chk "hof_reduce__in__tail_position_if_tail" (if (> 2 1) (reduce + 0 (list 1 2 3)) 'wrong) 6))
(edge-check-g1009)

;; check hof_reduce__in__tail_position_begin_tail
(define (edge-check-g1010) (edge-chk "hof_reduce__in__tail_position_begin_tail" (begin 'first (reduce + 0 (list 1 2 3))) 6))
(edge-check-g1010)

;; check hof_reduce__in__tail_position_cond_else_tail
(define (edge-check-g1011) (edge-chk "hof_reduce__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (reduce + 0 (list 1 2 3)))) 6))
(edge-check-g1011)

;; check hof_for_each_acc__in__tail_position_define_tail
(define (tpg1012) (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs))
(define (edge-check-g1012) (edge-chk "hof_for_each_acc__in__tail_position_define_tail" (tpg1012) 6))
(edge-check-g1012)

;; check hof_for_each_acc__in__tail_position_let_tail_in_define
(define (tqg1013) (let ((tu 1)) (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs)))
(define (edge-check-g1013) (edge-chk "hof_for_each_acc__in__tail_position_let_tail_in_define" (tqg1013) 6))
(edge-check-g1013)

;; check hof_for_each_acc__in__tail_position_if_tail
(define (edge-check-g1014) (edge-chk "hof_for_each_acc__in__tail_position_if_tail" (if (> 2 1) (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs) 'wrong) 6))
(edge-check-g1014)

;; check hof_for_each_acc__in__tail_position_begin_tail
(define (edge-check-g1015) (edge-chk "hof_for_each_acc__in__tail_position_begin_tail" (begin 'first (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs)) 6))
(edge-check-g1015)

;; check hof_for_each_acc__in__tail_position_cond_else_tail
(define (edge-check-g1016) (edge-chk "hof_for_each_acc__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs))) 6))
(edge-check-g1016)

(display "EDGE-MATRIX-DONE") (newline)
