;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: begin_seq (producers) composed into variadic (contexts)
;; CHECKS: 3
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check begin_seq_order__in__variadic_rest_car
(define (edge-check-g512) (edge-chk "begin_seq_order__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl))) (list 2 1)))
(edge-check-g512)

;; check begin_seq_order__in__variadic_dotted_first
(define (edge-check-g513) (edge-chk "begin_seq_order__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl) 'p1 'p2) (list 2 1)))
(edge-check-g513)

;; check begin_seq_order__in__variadic_apply_rest
(define (edge-check-g514) (edge-chk "begin_seq_order__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl) 0)) (list 2 1)))
(edge-check-g514)

(display "EDGE-MATRIX-DONE") (newline)
