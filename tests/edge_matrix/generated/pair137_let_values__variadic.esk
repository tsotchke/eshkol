;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: let_values (producers) composed into variadic (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check let_values_two_values__in__variadic_rest_car
(define (edge-check-g1209) (edge-chk "let_values_two_values__in__variadic_rest_car" (car ((lambda vgs vgs) (let-values (((va vb) (values 1 2))) (+ va vb)))) 3))
(edge-check-g1209)

;; check let_values_two_values__in__variadic_dotted_first
(define (edge-check-g1210) (edge-chk "let_values_two_values__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let-values (((va vb) (values 1 2))) (+ va vb)) 'p1 'p2) 3))
(edge-check-g1210)

;; check let_values_two_values__in__variadic_apply_rest
(define (edge-check-g1211) (edge-chk "let_values_two_values__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let-values (((va vb) (values 1 2))) (+ va vb)) 0)) 3))
(edge-check-g1211)

;; check let_values_cwv__in__variadic_rest_car
(define (edge-check-g1212) (edge-chk "let_values_cwv__in__variadic_rest_car" (car ((lambda vgs vgs) (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb))))) 42))
(edge-check-g1212)

;; check let_values_cwv__in__variadic_dotted_first
(define (edge-check-g1213) (edge-chk "let_values_cwv__in__variadic_dotted_first" ((lambda (vh . vt) vh) (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb))) 'p1 'p2) 42))
(edge-check-g1213)

;; check let_values_cwv__in__variadic_apply_rest
(define (edge-check-g1214) (edge-chk "let_values_cwv__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb))) 0)) 42))
(edge-check-g1214)

(display "EDGE-MATRIX-DONE") (newline)
