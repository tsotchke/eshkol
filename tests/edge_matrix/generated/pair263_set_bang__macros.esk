;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into macros (contexts)
;; CHECKS: 4
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__macros_identity_macro
(define-syntax img2221 (syntax-rules () ((_ e) e)))
(define (edge-check-g2221) (edge-chk "set_bang_local_mut__in__macros_identity_macro" (img2221 (let ((sx 1)) (set! sx 41) (+ sx 1))) 42))
(edge-check-g2221)

;; check set_bang_local_mut__in__macros_second_macro
(define-syntax smg2222 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2222) (edge-chk "set_bang_local_mut__in__macros_second_macro" (smg2222 'skip (let ((sx 1)) (set! sx 41) (+ sx 1))) 42))
(edge-check-g2222)

;; check set_bang_closure_mut__in__macros_identity_macro
(define-syntax img2223 (syntax-rules () ((_ e) e)))
(define (edge-check-g2223) (edge-chk "set_bang_closure_mut__in__macros_identity_macro" (img2223 (let ((sy 0)) ((lambda () (set! sy 7))) sy)) 7))
(edge-check-g2223)

;; check set_bang_closure_mut__in__macros_second_macro
(define-syntax smg2224 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2224) (edge-chk "set_bang_closure_mut__in__macros_second_macro" (smg2224 'skip (let ((sy 0)) ((lambda () (set! sy 7))) sy)) 7))
(edge-check-g2224)

(display "EDGE-MATRIX-DONE") (newline)
