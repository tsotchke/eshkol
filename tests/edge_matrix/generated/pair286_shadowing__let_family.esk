;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into let_family (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__let_family_let_bind
(define (edge-check-g2313) (edge-chk "shadowing_shadow_libc_local__in__let_family_let_bind" (let ((lxg2313 (let ((pow 3)) (+ pow 1)))) lxg2313) 4))
(edge-check-g2313)

;; check shadowing_shadow_libc_local__in__let_family_letstar_chain
(define (edge-check-g2314) (edge-chk "shadowing_shadow_libc_local__in__let_family_letstar_chain" (let* ((lyg2314 (let ((pow 3)) (+ pow 1))) (lzg2314 lyg2314)) lzg2314) 4))
(edge-check-g2314)

;; check shadowing_shadow_libc_local__in__let_family_letrec_thunk
(define (edge-check-g2315) (edge-chk "shadowing_shadow_libc_local__in__let_family_letrec_thunk" (letrec ((lfg2315 (lambda () (let ((pow 3)) (+ pow 1))))) (lfg2315)) 4))
(edge-check-g2315)

;; check shadowing_shadow_libc_local__in__let_family_named_let_carry
(define (edge-check-g2316) (edge-chk "shadowing_shadow_libc_local__in__let_family_named_let_carry" (let nlg2316 ((i 0) (acc #f)) (if (= i 1) acc (nlg2316 (+ i 1) (let ((pow 3)) (+ pow 1))))) 4))
(edge-check-g2316)

;; check shadowing_shadow_builtin_rebind__in__let_family_let_bind
(define (edge-check-g2317) (edge-chk "shadowing_shadow_builtin_rebind__in__let_family_let_bind" (let ((lxg2317 (let ((car cdr)) (car (list 1 2 3))))) lxg2317) (list 2 3)))
(edge-check-g2317)

;; check shadowing_shadow_builtin_rebind__in__let_family_letstar_chain
(define (edge-check-g2318) (edge-chk "shadowing_shadow_builtin_rebind__in__let_family_letstar_chain" (let* ((lyg2318 (let ((car cdr)) (car (list 1 2 3)))) (lzg2318 lyg2318)) lzg2318) (list 2 3)))
(edge-check-g2318)

;; check shadowing_shadow_builtin_rebind__in__let_family_letrec_thunk
(define (edge-check-g2319) (edge-chk "shadowing_shadow_builtin_rebind__in__let_family_letrec_thunk" (letrec ((lfg2319 (lambda () (let ((car cdr)) (car (list 1 2 3)))))) (lfg2319)) (list 2 3)))
(edge-check-g2319)

;; check shadowing_shadow_builtin_rebind__in__let_family_named_let_carry
(define (edge-check-g2320) (edge-chk "shadowing_shadow_builtin_rebind__in__let_family_named_let_carry" (let nlg2320 ((i 0) (acc #f)) (if (= i 1) acc (nlg2320 (+ i 1) (let ((car cdr)) (car (list 1 2 3)))))) (list 2 3)))
(edge-check-g2320)

;; check shadowing_shadow_param__in__let_family_let_bind
(define (edge-check-g2321) (edge-chk "shadowing_shadow_param__in__let_family_let_bind" (let ((lxg2321 ((lambda (ps) (let ((ps 5)) ps)) 1))) lxg2321) 5))
(edge-check-g2321)

;; check shadowing_shadow_param__in__let_family_letstar_chain
(define (edge-check-g2322) (edge-chk "shadowing_shadow_param__in__let_family_letstar_chain" (let* ((lyg2322 ((lambda (ps) (let ((ps 5)) ps)) 1)) (lzg2322 lyg2322)) lzg2322) 5))
(edge-check-g2322)

;; check shadowing_shadow_param__in__let_family_letrec_thunk
(define (edge-check-g2323) (edge-chk "shadowing_shadow_param__in__let_family_letrec_thunk" (letrec ((lfg2323 (lambda () ((lambda (ps) (let ((ps 5)) ps)) 1)))) (lfg2323)) 5))
(edge-check-g2323)

;; check shadowing_shadow_param__in__let_family_named_let_carry
(define (edge-check-g2324) (edge-chk "shadowing_shadow_param__in__let_family_named_let_carry" (let nlg2324 ((i 0) (acc #f)) (if (= i 1) acc (nlg2324 (+ i 1) ((lambda (ps) (let ((ps 5)) ps)) 1)))) 5))
(edge-check-g2324)

(display "EDGE-MATRIX-DONE") (newline)
