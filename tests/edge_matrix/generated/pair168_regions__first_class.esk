;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: regions (producers) composed into first_class (contexts)
;; CHECKS: 4
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check regions_region_value__in__first_class_thunk_in_list
(define (edge-check-g1475) (edge-chk "regions_region_value__in__first_class_thunk_in_list" ((car (list (lambda () (with-region 'rgp (+ 40 2)))))) 42))
(edge-check-g1475)

;; check regions_region_value__in__first_class_apply_id
(define (edge-check-g1476) (edge-chk "regions_region_value__in__first_class_apply_id" (apply (lambda (fa) fa) (list (with-region 'rgp (+ 40 2)))) 42))
(edge-check-g1476)

;; check regions_region_value__in__first_class_map_hole
(define (edge-check-g1477) (edge-chk "regions_region_value__in__first_class_map_hole" (car (map (lambda (fi) (with-region 'rgp (+ 40 2))) (list 0))) 42))
(edge-check-g1477)

;; check regions_region_value__in__first_class_fn_arg_call
(define (edge-check-g1478) (edge-chk "regions_region_value__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (with-region 'rgp (+ 40 2)))) 42))
(edge-check-g1478)

(display "EDGE-MATRIX-DONE") (newline)
