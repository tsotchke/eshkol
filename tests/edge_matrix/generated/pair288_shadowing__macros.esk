;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into macros (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__macros_identity_macro
(define-syntax img2331 (syntax-rules () ((_ e) e)))
(define (edge-check-g2331) (edge-chk "shadowing_shadow_libc_local__in__macros_identity_macro" (img2331 (let ((pow 3)) (+ pow 1))) 4))
(edge-check-g2331)

;; check shadowing_shadow_libc_local__in__macros_second_macro
(define-syntax smg2332 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2332) (edge-chk "shadowing_shadow_libc_local__in__macros_second_macro" (smg2332 'skip (let ((pow 3)) (+ pow 1))) 4))
(edge-check-g2332)

;; check shadowing_shadow_builtin_rebind__in__macros_identity_macro
(define-syntax img2333 (syntax-rules () ((_ e) e)))
(define (edge-check-g2333) (edge-chk "shadowing_shadow_builtin_rebind__in__macros_identity_macro" (img2333 (let ((car cdr)) (car (list 1 2 3)))) (list 2 3)))
(edge-check-g2333)

;; check shadowing_shadow_builtin_rebind__in__macros_second_macro
(define-syntax smg2334 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2334) (edge-chk "shadowing_shadow_builtin_rebind__in__macros_second_macro" (smg2334 'skip (let ((car cdr)) (car (list 1 2 3)))) (list 2 3)))
(edge-check-g2334)

;; check shadowing_shadow_param__in__macros_identity_macro
(define-syntax img2335 (syntax-rules () ((_ e) e)))
(define (edge-check-g2335) (edge-chk "shadowing_shadow_param__in__macros_identity_macro" (img2335 ((lambda (ps) (let ((ps 5)) ps)) 1)) 5))
(edge-check-g2335)

;; check shadowing_shadow_param__in__macros_second_macro
(define-syntax smg2336 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2336) (edge-chk "shadowing_shadow_param__in__macros_second_macro" (smg2336 'skip ((lambda (ps) (let ((ps 5)) ps)) 1)) 5))
(edge-check-g2336)

(display "EDGE-MATRIX-DONE") (newline)
