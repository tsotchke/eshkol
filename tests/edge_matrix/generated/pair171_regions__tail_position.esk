;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: regions (producers) composed into tail_position (contexts)
;; CHECKS: 5
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check regions_region_value__in__tail_position_define_tail
(define (tpg1484) (with-region 'rgp (+ 40 2)))
(define (edge-check-g1484) (edge-chk "regions_region_value__in__tail_position_define_tail" (tpg1484) 42))
(edge-check-g1484)

;; check regions_region_value__in__tail_position_let_tail_in_define
(define (tqg1485) (let ((tu 1)) (with-region 'rgp (+ 40 2))))
(define (edge-check-g1485) (edge-chk "regions_region_value__in__tail_position_let_tail_in_define" (tqg1485) 42))
(edge-check-g1485)

;; check regions_region_value__in__tail_position_if_tail
(define (edge-check-g1486) (edge-chk "regions_region_value__in__tail_position_if_tail" (if (> 2 1) (with-region 'rgp (+ 40 2)) 'wrong) 42))
(edge-check-g1486)

;; check regions_region_value__in__tail_position_begin_tail
(define (edge-check-g1487) (edge-chk "regions_region_value__in__tail_position_begin_tail" (begin 'first (with-region 'rgp (+ 40 2))) 42))
(edge-check-g1487)

;; check regions_region_value__in__tail_position_cond_else_tail
(define (edge-check-g1488) (edge-chk "regions_region_value__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (with-region 'rgp (+ 40 2)))) 42))
(edge-check-g1488)

(display "EDGE-MATRIX-DONE") (newline)
