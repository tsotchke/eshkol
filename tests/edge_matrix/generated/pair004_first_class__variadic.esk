;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: first_class (producers) composed into variadic (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check first_class_builtin_var__in__variadic_rest_car
(define (edge-check-g52) (edge-chk "first_class_builtin_var__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((fp null?)) (fp '())))) #t))
(edge-check-g52)

;; check first_class_builtin_var__in__variadic_dotted_first
(define (edge-check-g53) (edge-chk "first_class_builtin_var__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((fp null?)) (fp '())) 'p1 'p2) #t))
(edge-check-g53)

;; check first_class_builtin_var__in__variadic_apply_rest
(define (edge-check-g54) (edge-chk "first_class_builtin_var__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((fp null?)) (fp '())) 0)) #t))
(edge-check-g54)

;; check first_class_apply_builtin__in__variadic_rest_car
(define (edge-check-g55) (edge-chk "first_class_apply_builtin__in__variadic_rest_car" (car ((lambda vgs vgs) (apply + (list 20 22)))) 42))
(edge-check-g55)

;; check first_class_apply_builtin__in__variadic_dotted_first
(define (edge-check-g56) (edge-chk "first_class_apply_builtin__in__variadic_dotted_first" ((lambda (vh . vt) vh) (apply + (list 20 22)) 'p1 'p2) 42))
(edge-check-g56)

;; check first_class_apply_builtin__in__variadic_apply_rest
(define (edge-check-g57) (edge-chk "first_class_apply_builtin__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (apply + (list 20 22)) 0)) 42))
(edge-check-g57)

;; check first_class_builtin_from_list__in__variadic_rest_car
(define (edge-check-g58) (edge-chk "first_class_builtin_from_list__in__variadic_rest_car" (car ((lambda vgs vgs) ((car (list +)) 20 22))) 42))
(edge-check-g58)

;; check first_class_builtin_from_list__in__variadic_dotted_first
(define (edge-check-g59) (edge-chk "first_class_builtin_from_list__in__variadic_dotted_first" ((lambda (vh . vt) vh) ((car (list +)) 20 22) 'p1 'p2) 42))
(edge-check-g59)

;; check first_class_builtin_from_list__in__variadic_apply_rest
(define (edge-check-g60) (edge-chk "first_class_builtin_from_list__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list ((car (list +)) 20 22) 0)) 42))
(edge-check-g60)

;; check first_class_map_builtin__in__variadic_rest_car
(define (edge-check-g61) (edge-chk "first_class_map_builtin__in__variadic_rest_car" (car ((lambda vgs vgs) (map car '((1 2) (3 4))))) (list 1 3)))
(edge-check-g61)

;; check first_class_map_builtin__in__variadic_dotted_first
(define (edge-check-g62) (edge-chk "first_class_map_builtin__in__variadic_dotted_first" ((lambda (vh . vt) vh) (map car '((1 2) (3 4))) 'p1 'p2) (list 1 3)))
(edge-check-g62)

;; check first_class_map_builtin__in__variadic_apply_rest
(define (edge-check-g63) (edge-chk "first_class_map_builtin__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (map car '((1 2) (3 4))) 0)) (list 1 3)))
(edge-check-g63)

(display "EDGE-MATRIX-DONE") (newline)
