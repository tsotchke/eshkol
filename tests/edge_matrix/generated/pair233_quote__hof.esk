;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: quote (producers) composed into hof (contexts)
;; CHECKS: 14
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check quote_sym__in__hof_fold_last
(define (edge-check-g2013) (edge-chk "quote_sym__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list 'edge-sym)) (string->symbol "edge-sym")))
(edge-check-g2013)

;; check quote_sym__in__hof_for_each_capture
(define (edge-check-g2014) (edge-chk "quote_sym__in__hof_for_each_capture" (let ((fcg2014 #f)) (for-each (lambda (fe) (set! fcg2014 fe)) (list 'edge-sym)) fcg2014) (string->symbol "edge-sym")))
(edge-check-g2014)

;; check quote_list__in__hof_fold_last
(define (edge-check-g2015) (edge-chk "quote_list__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list '(1 2 3))) (list 1 2 3)))
(edge-check-g2015)

;; check quote_list__in__hof_for_each_capture
(define (edge-check-g2016) (edge-chk "quote_list__in__hof_for_each_capture" (let ((fcg2016 #f)) (for-each (lambda (fe) (set! fcg2016 fe)) (list '(1 2 3))) fcg2016) (list 1 2 3)))
(edge-check-g2016)

;; check quote_empty__in__hof_fold_last
(define (edge-check-g2017) (edge-chk "quote_empty__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list '())) (list)))
(edge-check-g2017)

;; check quote_empty__in__hof_for_each_capture
(define (edge-check-g2018) (edge-chk "quote_empty__in__hof_for_each_capture" (let ((fcg2018 #f)) (for-each (lambda (fe) (set! fcg2018 fe)) (list '())) fcg2018) (list)))
(edge-check-g2018)

;; check quote_nested_quote__in__hof_fold_last
(define (edge-check-g2019) (edge-chk "quote_nested_quote__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (car ''nq))) (string->symbol "quote")))
(edge-check-g2019)

;; check quote_nested_quote__in__hof_for_each_capture
(define (edge-check-g2020) (edge-chk "quote_nested_quote__in__hof_for_each_capture" (let ((fcg2020 #f)) (for-each (lambda (fe) (set! fcg2020 fe)) (list (car ''nq))) fcg2020) (string->symbol "quote")))
(edge-check-g2020)

;; check quote_veclit_ref__in__hof_fold_last
(define (edge-check-g2021) (edge-chk "quote_veclit_ref__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (vector-ref #(9 8 7) 2))) 7))
(edge-check-g2021)

;; check quote_veclit_ref__in__hof_for_each_capture
(define (edge-check-g2022) (edge-chk "quote_veclit_ref__in__hof_for_each_capture" (let ((fcg2022 #f)) (for-each (lambda (fe) (set! fcg2022 fe)) (list (vector-ref #(9 8 7) 2))) fcg2022) 7))
(edge-check-g2022)

;; check quote_qq_sugar__in__hof_fold_last
(define (edge-check-g2023) (edge-chk "quote_qq_sugar__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list `(1 ,(+ 1 1) ,@(list 3 4)))) (list 1 2 3 4)))
(edge-check-g2023)

;; check quote_qq_sugar__in__hof_for_each_capture
(define (edge-check-g2024) (edge-chk "quote_qq_sugar__in__hof_for_each_capture" (let ((fcg2024 #f)) (for-each (lambda (fe) (set! fcg2024 fe)) (list `(1 ,(+ 1 1) ,@(list 3 4)))) fcg2024) (list 1 2 3 4)))
(edge-check-g2024)

;; check quote_qq_longform__in__hof_fold_last
(define (edge-check-g2025) (edge-chk "quote_qq_longform__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (quasiquote (1 (unquote (+ 1 1)))))) (list 1 2)))
(edge-check-g2025)

;; check quote_qq_longform__in__hof_for_each_capture
(define (edge-check-g2026) (edge-chk "quote_qq_longform__in__hof_for_each_capture" (let ((fcg2026 #f)) (for-each (lambda (fe) (set! fcg2026 fe)) (list (quasiquote (1 (unquote (+ 1 1)))))) fcg2026) (list 1 2)))
(edge-check-g2026)

(display "EDGE-MATRIX-DONE") (newline)
