;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: eqv_pred (producers) composed into first_class (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check eqv_pred_equal_lists__in__first_class_thunk_in_list
(define (edge-check-g815) (edge-chk "eqv_pred_equal_lists__in__first_class_thunk_in_list" ((car (list (lambda () (equal? (list 1 2) '(1 2)))))) #t))
(edge-check-g815)

;; check eqv_pred_equal_lists__in__first_class_apply_id
(define (edge-check-g816) (edge-chk "eqv_pred_equal_lists__in__first_class_apply_id" (apply (lambda (fa) fa) (list (equal? (list 1 2) '(1 2)))) #t))
(edge-check-g816)

;; check eqv_pred_equal_lists__in__first_class_map_hole
(define (edge-check-g817) (edge-chk "eqv_pred_equal_lists__in__first_class_map_hole" (car (map (lambda (fi) (equal? (list 1 2) '(1 2))) (list 0))) #t))
(edge-check-g817)

;; check eqv_pred_equal_lists__in__first_class_fn_arg_call
(define (edge-check-g818) (edge-chk "eqv_pred_equal_lists__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (equal? (list 1 2) '(1 2)))) #t))
(edge-check-g818)

;; check eqv_pred_eqv_sym__in__first_class_thunk_in_list
(define (edge-check-g819) (edge-chk "eqv_pred_eqv_sym__in__first_class_thunk_in_list" ((car (list (lambda () (eqv? 'a 'a))))) #t))
(edge-check-g819)

;; check eqv_pred_eqv_sym__in__first_class_apply_id
(define (edge-check-g820) (edge-chk "eqv_pred_eqv_sym__in__first_class_apply_id" (apply (lambda (fa) fa) (list (eqv? 'a 'a))) #t))
(edge-check-g820)

;; check eqv_pred_eqv_sym__in__first_class_map_hole
(define (edge-check-g821) (edge-chk "eqv_pred_eqv_sym__in__first_class_map_hole" (car (map (lambda (fi) (eqv? 'a 'a)) (list 0))) #t))
(edge-check-g821)

;; check eqv_pred_eqv_sym__in__first_class_fn_arg_call
(define (edge-check-g822) (edge-chk "eqv_pred_eqv_sym__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (eqv? 'a 'a))) #t))
(edge-check-g822)

;; check eqv_pred_eq_null__in__first_class_thunk_in_list
(define (edge-check-g823) (edge-chk "eqv_pred_eq_null__in__first_class_thunk_in_list" ((car (list (lambda () (eq? '() '()))))) #t))
(edge-check-g823)

;; check eqv_pred_eq_null__in__first_class_apply_id
(define (edge-check-g824) (edge-chk "eqv_pred_eq_null__in__first_class_apply_id" (apply (lambda (fa) fa) (list (eq? '() '()))) #t))
(edge-check-g824)

;; check eqv_pred_eq_null__in__first_class_map_hole
(define (edge-check-g825) (edge-chk "eqv_pred_eq_null__in__first_class_map_hole" (car (map (lambda (fi) (eq? '() '())) (list 0))) #t))
(edge-check-g825)

;; check eqv_pred_eq_null__in__first_class_fn_arg_call
(define (edge-check-g826) (edge-chk "eqv_pred_eq_null__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (eq? '() '()))) #t))
(edge-check-g826)

(display "EDGE-MATRIX-DONE") (newline)
