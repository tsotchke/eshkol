;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: parallel_map (producers) composed into first_class (contexts)
;; CHECKS: 4
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check parallel_map_squares__in__first_class_thunk_in_list
(define (edge-check-g1455) (edge-chk "parallel_map_squares__in__first_class_thunk_in_list" ((car (list (lambda () (parallel-map (lambda (pm) (* pm pm)) (list 1 2 3)))))) (list 1 4 9)))
(edge-check-g1455)

;; check parallel_map_squares__in__first_class_apply_id
(define (edge-check-g1456) (edge-chk "parallel_map_squares__in__first_class_apply_id" (apply (lambda (fa) fa) (list (parallel-map (lambda (pm) (* pm pm)) (list 1 2 3)))) (list 1 4 9)))
(edge-check-g1456)

;; check parallel_map_squares__in__first_class_map_hole
(define (edge-check-g1457) (edge-chk "parallel_map_squares__in__first_class_map_hole" (car (map (lambda (fi) (parallel-map (lambda (pm) (* pm pm)) (list 1 2 3))) (list 0))) (list 1 4 9)))
(edge-check-g1457)

;; check parallel_map_squares__in__first_class_fn_arg_call
(define (edge-check-g1458) (edge-chk "parallel_map_squares__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (parallel-map (lambda (pm) (* pm pm)) (list 1 2 3)))) (list 1 4 9)))
(edge-check-g1458)

(display "EDGE-MATRIX-DONE") (newline)
