;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: delay_force (producers) composed into tail_position (contexts)
;; CHECKS: 10
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check delay_force_simple__in__tail_position_define_tail
(define (tpg753) (force (delay (+ 40 2))))
(define (edge-check-g753) (edge-chk "delay_force_simple__in__tail_position_define_tail" (tpg753) 42))
(edge-check-g753)

;; check delay_force_simple__in__tail_position_let_tail_in_define
(define (tqg754) (let ((tu 1)) (force (delay (+ 40 2)))))
(define (edge-check-g754) (edge-chk "delay_force_simple__in__tail_position_let_tail_in_define" (tqg754) 42))
(edge-check-g754)

;; check delay_force_simple__in__tail_position_if_tail
(define (edge-check-g755) (edge-chk "delay_force_simple__in__tail_position_if_tail" (if (> 2 1) (force (delay (+ 40 2))) 'wrong) 42))
(edge-check-g755)

;; check delay_force_simple__in__tail_position_begin_tail
(define (edge-check-g756) (edge-chk "delay_force_simple__in__tail_position_begin_tail" (begin 'first (force (delay (+ 40 2)))) 42))
(edge-check-g756)

;; check delay_force_simple__in__tail_position_cond_else_tail
(define (edge-check-g757) (edge-chk "delay_force_simple__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (force (delay (+ 40 2))))) 42))
(edge-check-g757)

;; check delay_force_memoized__in__tail_position_define_tail
(define cmng758 0)
(define (cmg758) (set! cmng758 (+ cmng758 1)) cmng758)
(define (tpg758) (let ((mpg758 (delay (cmg758)))) (+ (force mpg758) (force mpg758))))
(define (edge-check-g758) (edge-chk "delay_force_memoized__in__tail_position_define_tail" (tpg758) 2))
(edge-check-g758)

;; check delay_force_memoized__in__tail_position_let_tail_in_define
(define cmng759 0)
(define (cmg759) (set! cmng759 (+ cmng759 1)) cmng759)
(define (tqg759) (let ((tu 1)) (let ((mpg759 (delay (cmg759)))) (+ (force mpg759) (force mpg759)))))
(define (edge-check-g759) (edge-chk "delay_force_memoized__in__tail_position_let_tail_in_define" (tqg759) 2))
(edge-check-g759)

;; check delay_force_memoized__in__tail_position_if_tail
(define cmng760 0)
(define (cmg760) (set! cmng760 (+ cmng760 1)) cmng760)
(define (edge-check-g760) (edge-chk "delay_force_memoized__in__tail_position_if_tail" (if (> 2 1) (let ((mpg760 (delay (cmg760)))) (+ (force mpg760) (force mpg760))) 'wrong) 2))
(edge-check-g760)

;; check delay_force_memoized__in__tail_position_begin_tail
(define cmng761 0)
(define (cmg761) (set! cmng761 (+ cmng761 1)) cmng761)
(define (edge-check-g761) (edge-chk "delay_force_memoized__in__tail_position_begin_tail" (begin 'first (let ((mpg761 (delay (cmg761)))) (+ (force mpg761) (force mpg761)))) 2))
(edge-check-g761)

;; check delay_force_memoized__in__tail_position_cond_else_tail
(define cmng762 0)
(define (cmg762) (set! cmng762 (+ cmng762 1)) cmng762)
(define (edge-check-g762) (edge-chk "delay_force_memoized__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((mpg762 (delay (cmg762)))) (+ (force mpg762) (force mpg762))))) 2))
(edge-check-g762)

(display "EDGE-MATRIX-DONE") (newline)
