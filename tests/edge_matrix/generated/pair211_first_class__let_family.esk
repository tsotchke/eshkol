;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: first_class (producers) composed into let_family (contexts)
;; CHECKS: 16
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check first_class_builtin_var__in__let_family_let_bind
(define (edge-check-g1823) (edge-chk "first_class_builtin_var__in__let_family_let_bind" (let ((lxg1823 (let ((fp null?)) (fp '())))) lxg1823) #t))
(edge-check-g1823)

;; check first_class_builtin_var__in__let_family_letstar_chain
(define (edge-check-g1824) (edge-chk "first_class_builtin_var__in__let_family_letstar_chain" (let* ((lyg1824 (let ((fp null?)) (fp '()))) (lzg1824 lyg1824)) lzg1824) #t))
(edge-check-g1824)

;; check first_class_builtin_var__in__let_family_letrec_thunk
(define (edge-check-g1825) (edge-chk "first_class_builtin_var__in__let_family_letrec_thunk" (letrec ((lfg1825 (lambda () (let ((fp null?)) (fp '()))))) (lfg1825)) #t))
(edge-check-g1825)

;; check first_class_builtin_var__in__let_family_named_let_carry
(define (edge-check-g1826) (edge-chk "first_class_builtin_var__in__let_family_named_let_carry" (let nlg1826 ((i 0) (acc #f)) (if (= i 1) acc (nlg1826 (+ i 1) (let ((fp null?)) (fp '()))))) #t))
(edge-check-g1826)

;; check first_class_apply_builtin__in__let_family_let_bind
(define (edge-check-g1827) (edge-chk "first_class_apply_builtin__in__let_family_let_bind" (let ((lxg1827 (apply + (list 20 22)))) lxg1827) 42))
(edge-check-g1827)

;; check first_class_apply_builtin__in__let_family_letstar_chain
(define (edge-check-g1828) (edge-chk "first_class_apply_builtin__in__let_family_letstar_chain" (let* ((lyg1828 (apply + (list 20 22))) (lzg1828 lyg1828)) lzg1828) 42))
(edge-check-g1828)

;; check first_class_apply_builtin__in__let_family_letrec_thunk
(define (edge-check-g1829) (edge-chk "first_class_apply_builtin__in__let_family_letrec_thunk" (letrec ((lfg1829 (lambda () (apply + (list 20 22))))) (lfg1829)) 42))
(edge-check-g1829)

;; check first_class_apply_builtin__in__let_family_named_let_carry
(define (edge-check-g1830) (edge-chk "first_class_apply_builtin__in__let_family_named_let_carry" (let nlg1830 ((i 0) (acc #f)) (if (= i 1) acc (nlg1830 (+ i 1) (apply + (list 20 22))))) 42))
(edge-check-g1830)

;; check first_class_builtin_from_list__in__let_family_let_bind
(define (edge-check-g1831) (edge-chk "first_class_builtin_from_list__in__let_family_let_bind" (let ((lxg1831 ((car (list +)) 20 22))) lxg1831) 42))
(edge-check-g1831)

;; check first_class_builtin_from_list__in__let_family_letstar_chain
(define (edge-check-g1832) (edge-chk "first_class_builtin_from_list__in__let_family_letstar_chain" (let* ((lyg1832 ((car (list +)) 20 22)) (lzg1832 lyg1832)) lzg1832) 42))
(edge-check-g1832)

;; check first_class_builtin_from_list__in__let_family_letrec_thunk
(define (edge-check-g1833) (edge-chk "first_class_builtin_from_list__in__let_family_letrec_thunk" (letrec ((lfg1833 (lambda () ((car (list +)) 20 22)))) (lfg1833)) 42))
(edge-check-g1833)

;; check first_class_builtin_from_list__in__let_family_named_let_carry
(define (edge-check-g1834) (edge-chk "first_class_builtin_from_list__in__let_family_named_let_carry" (let nlg1834 ((i 0) (acc #f)) (if (= i 1) acc (nlg1834 (+ i 1) ((car (list +)) 20 22)))) 42))
(edge-check-g1834)

;; check first_class_map_builtin__in__let_family_let_bind
(define (edge-check-g1835) (edge-chk "first_class_map_builtin__in__let_family_let_bind" (let ((lxg1835 (map car '((1 2) (3 4))))) lxg1835) (list 1 3)))
(edge-check-g1835)

;; check first_class_map_builtin__in__let_family_letstar_chain
(define (edge-check-g1836) (edge-chk "first_class_map_builtin__in__let_family_letstar_chain" (let* ((lyg1836 (map car '((1 2) (3 4)))) (lzg1836 lyg1836)) lzg1836) (list 1 3)))
(edge-check-g1836)

;; check first_class_map_builtin__in__let_family_letrec_thunk
(define (edge-check-g1837) (edge-chk "first_class_map_builtin__in__let_family_letrec_thunk" (letrec ((lfg1837 (lambda () (map car '((1 2) (3 4)))))) (lfg1837)) (list 1 3)))
(edge-check-g1837)

;; check first_class_map_builtin__in__let_family_named_let_carry
(define (edge-check-g1838) (edge-chk "first_class_map_builtin__in__let_family_named_let_carry" (let nlg1838 ((i 0) (acc #f)) (if (= i 1) acc (nlg1838 (+ i 1) (map car '((1 2) (3 4)))))) (list 1 3)))
(edge-check-g1838)

(display "EDGE-MATRIX-DONE") (newline)
