;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into vectors (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__vectors_singleton_ref
(define (edge-check-g2361) (edge-chk "shadowing_shadow_libc_local__in__vectors_singleton_ref" (vector-ref (vector (let ((pow 3)) (+ pow 1))) 0) 4))
(edge-check-g2361)

;; check shadowing_shadow_libc_local__in__vectors_set_then_ref
(define (edge-check-g2362) (edge-chk "shadowing_shadow_libc_local__in__vectors_set_then_ref" (let ((vvg2362 (make-vector 1 #f))) (vector-set! vvg2362 0 (let ((pow 3)) (+ pow 1))) (vector-ref vvg2362 0)) 4))
(edge-check-g2362)

;; check shadowing_shadow_builtin_rebind__in__vectors_singleton_ref
(define (edge-check-g2363) (edge-chk "shadowing_shadow_builtin_rebind__in__vectors_singleton_ref" (vector-ref (vector (let ((car cdr)) (car (list 1 2 3)))) 0) (list 2 3)))
(edge-check-g2363)

;; check shadowing_shadow_builtin_rebind__in__vectors_set_then_ref
(define (edge-check-g2364) (edge-chk "shadowing_shadow_builtin_rebind__in__vectors_set_then_ref" (let ((vvg2364 (make-vector 1 #f))) (vector-set! vvg2364 0 (let ((car cdr)) (car (list 1 2 3)))) (vector-ref vvg2364 0)) (list 2 3)))
(edge-check-g2364)

;; check shadowing_shadow_param__in__vectors_singleton_ref
(define (edge-check-g2365) (edge-chk "shadowing_shadow_param__in__vectors_singleton_ref" (vector-ref (vector ((lambda (ps) (let ((ps 5)) ps)) 1)) 0) 5))
(edge-check-g2365)

;; check shadowing_shadow_param__in__vectors_set_then_ref
(define (edge-check-g2366) (edge-chk "shadowing_shadow_param__in__vectors_set_then_ref" (let ((vvg2366 (make-vector 1 #f))) (vector-set! vvg2366 0 ((lambda (ps) (let ((ps 5)) ps)) 1)) (vector-ref vvg2366 0)) 5))
(edge-check-g2366)

(display "EDGE-MATRIX-DONE") (newline)
