;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into vectors (contexts)
;; CHECKS: 4
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__vectors_singleton_ref
(define (edge-check-g2243) (edge-chk "set_bang_local_mut__in__vectors_singleton_ref" (vector-ref (vector (let ((sx 1)) (set! sx 41) (+ sx 1))) 0) 42))
(edge-check-g2243)

;; check set_bang_local_mut__in__vectors_set_then_ref
(define (edge-check-g2244) (edge-chk "set_bang_local_mut__in__vectors_set_then_ref" (let ((vvg2244 (make-vector 1 #f))) (vector-set! vvg2244 0 (let ((sx 1)) (set! sx 41) (+ sx 1))) (vector-ref vvg2244 0)) 42))
(edge-check-g2244)

;; check set_bang_closure_mut__in__vectors_singleton_ref
(define (edge-check-g2245) (edge-chk "set_bang_closure_mut__in__vectors_singleton_ref" (vector-ref (vector (let ((sy 0)) ((lambda () (set! sy 7))) sy)) 0) 7))
(edge-check-g2245)

;; check set_bang_closure_mut__in__vectors_set_then_ref
(define (edge-check-g2246) (edge-chk "set_bang_closure_mut__in__vectors_set_then_ref" (let ((vvg2246 (make-vector 1 #f))) (vector-set! vvg2246 0 (let ((sy 0)) ((lambda () (set! sy 7))) sy)) (vector-ref vvg2246 0)) 7))
(edge-check-g2246)

(display "EDGE-MATRIX-DONE") (newline)
