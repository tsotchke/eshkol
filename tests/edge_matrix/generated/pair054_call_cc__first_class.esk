;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: call_cc (producers) composed into first_class (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check call_cc_no_escape__in__first_class_thunk_in_list
(define (edge-check-g515) (edge-chk "call_cc_no_escape__in__first_class_thunk_in_list" ((car (list (lambda () (call/cc (lambda (k1) (+ 40 2))))))) 42))
(edge-check-g515)

;; check call_cc_no_escape__in__first_class_apply_id
(define (edge-check-g516) (edge-chk "call_cc_no_escape__in__first_class_apply_id" (apply (lambda (fa) fa) (list (call/cc (lambda (k1) (+ 40 2))))) 42))
(edge-check-g516)

;; check call_cc_no_escape__in__first_class_map_hole
(define (edge-check-g517) (edge-chk "call_cc_no_escape__in__first_class_map_hole" (car (map (lambda (fi) (call/cc (lambda (k1) (+ 40 2)))) (list 0))) 42))
(edge-check-g517)

;; check call_cc_no_escape__in__first_class_fn_arg_call
(define (edge-check-g518) (edge-chk "call_cc_no_escape__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (call/cc (lambda (k1) (+ 40 2))))) 42))
(edge-check-g518)

;; check call_cc_escape_val__in__first_class_thunk_in_list
(define (edge-check-g519) (edge-chk "call_cc_escape_val__in__first_class_thunk_in_list" ((car (list (lambda () (+ 1 (call/cc (lambda (k2) (k2 10) 99))))))) 11))
(edge-check-g519)

;; check call_cc_escape_val__in__first_class_apply_id
(define (edge-check-g520) (edge-chk "call_cc_escape_val__in__first_class_apply_id" (apply (lambda (fa) fa) (list (+ 1 (call/cc (lambda (k2) (k2 10) 99))))) 11))
(edge-check-g520)

;; check call_cc_escape_val__in__first_class_map_hole
(define (edge-check-g521) (edge-chk "call_cc_escape_val__in__first_class_map_hole" (car (map (lambda (fi) (+ 1 (call/cc (lambda (k2) (k2 10) 99)))) (list 0))) 11))
(edge-check-g521)

;; check call_cc_escape_val__in__first_class_fn_arg_call
(define (edge-check-g522) (edge-chk "call_cc_escape_val__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (+ 1 (call/cc (lambda (k2) (k2 10) 99))))) 11))
(edge-check-g522)

(display "EDGE-MATRIX-DONE") (newline)
