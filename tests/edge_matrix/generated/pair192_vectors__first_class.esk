;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: vectors (producers) composed into first_class (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check vectors_vec_ref__in__first_class_thunk_in_list
(define (edge-check-g1675) (edge-chk "vectors_vec_ref__in__first_class_thunk_in_list" ((car (list (lambda () (vector-ref (vector 'va 'vb) 1))))) (string->symbol "vb")))
(edge-check-g1675)

;; check vectors_vec_ref__in__first_class_apply_id
(define (edge-check-g1676) (edge-chk "vectors_vec_ref__in__first_class_apply_id" (apply (lambda (fa) fa) (list (vector-ref (vector 'va 'vb) 1))) (string->symbol "vb")))
(edge-check-g1676)

;; check vectors_vec_ref__in__first_class_map_hole
(define (edge-check-g1677) (edge-chk "vectors_vec_ref__in__first_class_map_hole" (car (map (lambda (fi) (vector-ref (vector 'va 'vb) 1)) (list 0))) (string->symbol "vb")))
(edge-check-g1677)

;; check vectors_vec_ref__in__first_class_fn_arg_call
(define (edge-check-g1678) (edge-chk "vectors_vec_ref__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (vector-ref (vector 'va 'vb) 1))) (string->symbol "vb")))
(edge-check-g1678)

;; check vectors_make_set_ref__in__first_class_thunk_in_list
(define (edge-check-g1679) (edge-chk "vectors_make_set_ref__in__first_class_thunk_in_list" ((car (list (lambda () (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)))))) 9))
(edge-check-g1679)

;; check vectors_make_set_ref__in__first_class_apply_id
(define (edge-check-g1680) (edge-chk "vectors_make_set_ref__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)))) 9))
(edge-check-g1680)

;; check vectors_make_set_ref__in__first_class_map_hole
(define (edge-check-g1681) (edge-chk "vectors_make_set_ref__in__first_class_map_hole" (car (map (lambda (fi) (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1))) (list 0))) 9))
(edge-check-g1681)

;; check vectors_make_set_ref__in__first_class_fn_arg_call
(define (edge-check-g1682) (edge-chk "vectors_make_set_ref__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)))) 9))
(edge-check-g1682)

;; check vectors_vec_len__in__first_class_thunk_in_list
(define (edge-check-g1683) (edge-chk "vectors_vec_len__in__first_class_thunk_in_list" ((car (list (lambda () (vector-length (vector 1 2 3)))))) 3))
(edge-check-g1683)

;; check vectors_vec_len__in__first_class_apply_id
(define (edge-check-g1684) (edge-chk "vectors_vec_len__in__first_class_apply_id" (apply (lambda (fa) fa) (list (vector-length (vector 1 2 3)))) 3))
(edge-check-g1684)

;; check vectors_vec_len__in__first_class_map_hole
(define (edge-check-g1685) (edge-chk "vectors_vec_len__in__first_class_map_hole" (car (map (lambda (fi) (vector-length (vector 1 2 3))) (list 0))) 3))
(edge-check-g1685)

;; check vectors_vec_len__in__first_class_fn_arg_call
(define (edge-check-g1686) (edge-chk "vectors_vec_len__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (vector-length (vector 1 2 3)))) 3))
(edge-check-g1686)

(display "EDGE-MATRIX-DONE") (newline)
