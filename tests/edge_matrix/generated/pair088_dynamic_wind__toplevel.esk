;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: dynamic_wind (producers) composed into toplevel (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check dynamic_wind_order__in__toplevel_top_define
(define tlvg803 (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl))
(define (edge-check-g803) (edge-chk "dynamic_wind_order__in__toplevel_top_define" tlvg803 (list 3 2 1)))
(edge-check-g803)

;; check dynamic_wind_order__in__toplevel_top_list_arg
(define tllg804 (list (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl)))
(define (edge-check-g804) (edge-chk "dynamic_wind_order__in__toplevel_top_list_arg" (car tllg804) (list 3 2 1)))
(edge-check-g804)

;; check dynamic_wind_order__in__toplevel_top_vector_arg
(define tlwg805 (vector (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl)))
(define (edge-check-g805) (edge-chk "dynamic_wind_order__in__toplevel_top_vector_arg" (vector-ref tlwg805 0) (list 3 2 1)))
(edge-check-g805)

;; check dynamic_wind_value__in__toplevel_top_define
(define tlvg806 (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)))
(define (edge-check-g806) (edge-chk "dynamic_wind_value__in__toplevel_top_define" tlvg806 42))
(edge-check-g806)

;; check dynamic_wind_value__in__toplevel_top_list_arg
(define tllg807 (list (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a))))
(define (edge-check-g807) (edge-chk "dynamic_wind_value__in__toplevel_top_list_arg" (car tllg807) 42))
(edge-check-g807)

;; check dynamic_wind_value__in__toplevel_top_vector_arg
(define tlwg808 (vector (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a))))
(define (edge-check-g808) (edge-chk "dynamic_wind_value__in__toplevel_top_vector_arg" (vector-ref tlwg808 0) 42))
(edge-check-g808)

(display "EDGE-MATRIX-DONE") (newline)
