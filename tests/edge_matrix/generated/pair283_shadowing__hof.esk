;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into hof (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__hof_fold_last
(define (edge-check-g2298) (edge-chk "shadowing_shadow_libc_local__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (let ((pow 3)) (+ pow 1)))) 4))
(edge-check-g2298)

;; check shadowing_shadow_libc_local__in__hof_for_each_capture
(define (edge-check-g2299) (edge-chk "shadowing_shadow_libc_local__in__hof_for_each_capture" (let ((fcg2299 #f)) (for-each (lambda (fe) (set! fcg2299 fe)) (list (let ((pow 3)) (+ pow 1)))) fcg2299) 4))
(edge-check-g2299)

;; check shadowing_shadow_builtin_rebind__in__hof_fold_last
(define (edge-check-g2300) (edge-chk "shadowing_shadow_builtin_rebind__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (let ((car cdr)) (car (list 1 2 3))))) (list 2 3)))
(edge-check-g2300)

;; check shadowing_shadow_builtin_rebind__in__hof_for_each_capture
(define (edge-check-g2301) (edge-chk "shadowing_shadow_builtin_rebind__in__hof_for_each_capture" (let ((fcg2301 #f)) (for-each (lambda (fe) (set! fcg2301 fe)) (list (let ((car cdr)) (car (list 1 2 3))))) fcg2301) (list 2 3)))
(edge-check-g2301)

;; check shadowing_shadow_param__in__hof_fold_last
(define (edge-check-g2302) (edge-chk "shadowing_shadow_param__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list ((lambda (ps) (let ((ps 5)) ps)) 1))) 5))
(edge-check-g2302)

;; check shadowing_shadow_param__in__hof_for_each_capture
(define (edge-check-g2303) (edge-chk "shadowing_shadow_param__in__hof_for_each_capture" (let ((fcg2303 #f)) (for-each (lambda (fe) (set! fcg2303 fe)) (list ((lambda (ps) (let ((ps 5)) ps)) 1))) fcg2303) 5))
(edge-check-g2303)

(display "EDGE-MATRIX-DONE") (newline)
