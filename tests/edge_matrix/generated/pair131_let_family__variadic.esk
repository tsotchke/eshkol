;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: let_family (producers) composed into variadic (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check let_family_let_sum__in__variadic_rest_car
(define (edge-check-g1163) (edge-chk "let_family_let_sum__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((la 2) (lb 3)) (+ la lb)))) 5))
(edge-check-g1163)

;; check let_family_let_sum__in__variadic_dotted_first
(define (edge-check-g1164) (edge-chk "let_family_let_sum__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((la 2) (lb 3)) (+ la lb)) 'p1 'p2) 5))
(edge-check-g1164)

;; check let_family_let_sum__in__variadic_apply_rest
(define (edge-check-g1165) (edge-chk "let_family_let_sum__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((la 2) (lb 3)) (+ la lb)) 0)) 5))
(edge-check-g1165)

;; check let_family_letstar_chain__in__variadic_rest_car
(define (edge-check-g1166) (edge-chk "let_family_letstar_chain__in__variadic_rest_car" (car ((lambda vgs vgs) (let* ((lc 2) (ld (* lc lc))) ld))) 4))
(edge-check-g1166)

;; check let_family_letstar_chain__in__variadic_dotted_first
(define (edge-check-g1167) (edge-chk "let_family_letstar_chain__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let* ((lc 2) (ld (* lc lc))) ld) 'p1 'p2) 4))
(edge-check-g1167)

;; check let_family_letstar_chain__in__variadic_apply_rest
(define (edge-check-g1168) (edge-chk "let_family_letstar_chain__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let* ((lc 2) (ld (* lc lc))) ld) 0)) 4))
(edge-check-g1168)

;; check let_family_letrec_mutual__in__variadic_rest_car
(define (edge-check-g1169) (edge-chk "let_family_letrec_mutual__in__variadic_rest_car" (car ((lambda vgs vgs) (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10)))) #t))
(edge-check-g1169)

;; check let_family_letrec_mutual__in__variadic_dotted_first
(define (edge-check-g1170) (edge-chk "let_family_letrec_mutual__in__variadic_dotted_first" ((lambda (vh . vt) vh) (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10)) 'p1 'p2) #t))
(edge-check-g1170)

;; check let_family_letrec_mutual__in__variadic_apply_rest
(define (edge-check-g1171) (edge-chk "let_family_letrec_mutual__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10)) 0)) #t))
(edge-check-g1171)

;; check let_family_named_let_sum__in__variadic_rest_car
(define (edge-check-g1172) (edge-chk "let_family_named_let_sum__in__variadic_rest_car" (car ((lambda vgs vgs) (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i)))))) 10))
(edge-check-g1172)

;; check let_family_named_let_sum__in__variadic_dotted_first
(define (edge-check-g1173) (edge-chk "let_family_named_let_sum__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i)))) 'p1 'p2) 10))
(edge-check-g1173)

;; check let_family_named_let_sum__in__variadic_apply_rest
(define (edge-check-g1174) (edge-chk "let_family_named_let_sum__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i)))) 0)) 10))
(edge-check-g1174)

(display "EDGE-MATRIX-DONE") (newline)
