;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: match (producers) composed into first_class (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check match_list_pat__in__first_class_thunk_in_list
(define (edge-check-g1255) (edge-chk "match_list_pat__in__first_class_thunk_in_list" ((car (list (lambda () (match (list 1 2) ((list ma mb) (+ ma mb)) (_ 'no)))))) 3))
(edge-check-g1255)

;; check match_list_pat__in__first_class_apply_id
(define (edge-check-g1256) (edge-chk "match_list_pat__in__first_class_apply_id" (apply (lambda (fa) fa) (list (match (list 1 2) ((list ma mb) (+ ma mb)) (_ 'no)))) 3))
(edge-check-g1256)

;; check match_list_pat__in__first_class_map_hole
(define (edge-check-g1257) (edge-chk "match_list_pat__in__first_class_map_hole" (car (map (lambda (fi) (match (list 1 2) ((list ma mb) (+ ma mb)) (_ 'no))) (list 0))) 3))
(edge-check-g1257)

;; check match_list_pat__in__first_class_fn_arg_call
(define (edge-check-g1258) (edge-chk "match_list_pat__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (match (list 1 2) ((list ma mb) (+ ma mb)) (_ 'no)))) 3))
(edge-check-g1258)

;; check match_literal_pat__in__first_class_thunk_in_list
(define (edge-check-g1259) (edge-chk "match_literal_pat__in__first_class_thunk_in_list" ((car (list (lambda () (match 5 (5 'five) (_ 'no)))))) (string->symbol "five")))
(edge-check-g1259)

;; check match_literal_pat__in__first_class_apply_id
(define (edge-check-g1260) (edge-chk "match_literal_pat__in__first_class_apply_id" (apply (lambda (fa) fa) (list (match 5 (5 'five) (_ 'no)))) (string->symbol "five")))
(edge-check-g1260)

;; check match_literal_pat__in__first_class_map_hole
(define (edge-check-g1261) (edge-chk "match_literal_pat__in__first_class_map_hole" (car (map (lambda (fi) (match 5 (5 'five) (_ 'no))) (list 0))) (string->symbol "five")))
(edge-check-g1261)

;; check match_literal_pat__in__first_class_fn_arg_call
(define (edge-check-g1262) (edge-chk "match_literal_pat__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (match 5 (5 'five) (_ 'no)))) (string->symbol "five")))
(edge-check-g1262)

(display "EDGE-MATRIX-DONE") (newline)
