;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: first_class (producers) composed into macros (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check first_class_builtin_var__in__macros_identity_macro
(define-syntax img1847 (syntax-rules () ((_ e) e)))
(define (edge-check-g1847) (edge-chk "first_class_builtin_var__in__macros_identity_macro" (img1847 (let ((fp null?)) (fp '()))) #t))
(edge-check-g1847)

;; check first_class_builtin_var__in__macros_second_macro
(define-syntax smg1848 (syntax-rules () ((_ a b) b)))
(define (edge-check-g1848) (edge-chk "first_class_builtin_var__in__macros_second_macro" (smg1848 'skip (let ((fp null?)) (fp '()))) #t))
(edge-check-g1848)

;; check first_class_apply_builtin__in__macros_identity_macro
(define-syntax img1849 (syntax-rules () ((_ e) e)))
(define (edge-check-g1849) (edge-chk "first_class_apply_builtin__in__macros_identity_macro" (img1849 (apply + (list 20 22))) 42))
(edge-check-g1849)

;; check first_class_apply_builtin__in__macros_second_macro
(define-syntax smg1850 (syntax-rules () ((_ a b) b)))
(define (edge-check-g1850) (edge-chk "first_class_apply_builtin__in__macros_second_macro" (smg1850 'skip (apply + (list 20 22))) 42))
(edge-check-g1850)

;; check first_class_builtin_from_list__in__macros_identity_macro
(define-syntax img1851 (syntax-rules () ((_ e) e)))
(define (edge-check-g1851) (edge-chk "first_class_builtin_from_list__in__macros_identity_macro" (img1851 ((car (list +)) 20 22)) 42))
(edge-check-g1851)

;; check first_class_builtin_from_list__in__macros_second_macro
(define-syntax smg1852 (syntax-rules () ((_ a b) b)))
(define (edge-check-g1852) (edge-chk "first_class_builtin_from_list__in__macros_second_macro" (smg1852 'skip ((car (list +)) 20 22)) 42))
(edge-check-g1852)

;; check first_class_map_builtin__in__macros_identity_macro
(define-syntax img1853 (syntax-rules () ((_ e) e)))
(define (edge-check-g1853) (edge-chk "first_class_map_builtin__in__macros_identity_macro" (img1853 (map car '((1 2) (3 4)))) (list 1 3)))
(edge-check-g1853)

;; check first_class_map_builtin__in__macros_second_macro
(define-syntax smg1854 (syntax-rules () ((_ a b) b)))
(define (edge-check-g1854) (edge-chk "first_class_map_builtin__in__macros_second_macro" (smg1854 'skip (map car '((1 2) (3 4)))) (list 1 3)))
(edge-check-g1854)

(display "EDGE-MATRIX-DONE") (newline)
