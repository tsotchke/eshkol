;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into hof (contexts)
;; CHECKS: 4
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__hof_fold_last
(define (edge-check-g2199) (edge-chk "set_bang_local_mut__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (let ((sx 1)) (set! sx 41) (+ sx 1)))) 42))
(edge-check-g2199)

;; check set_bang_local_mut__in__hof_for_each_capture
(define (edge-check-g2200) (edge-chk "set_bang_local_mut__in__hof_for_each_capture" (let ((fcg2200 #f)) (for-each (lambda (fe) (set! fcg2200 fe)) (list (let ((sx 1)) (set! sx 41) (+ sx 1)))) fcg2200) 42))
(edge-check-g2200)

;; check set_bang_closure_mut__in__hof_fold_last
(define (edge-check-g2201) (edge-chk "set_bang_closure_mut__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (let ((sy 0)) ((lambda () (set! sy 7))) sy))) 7))
(edge-check-g2201)

;; check set_bang_closure_mut__in__hof_for_each_capture
(define (edge-check-g2202) (edge-chk "set_bang_closure_mut__in__hof_for_each_capture" (let ((fcg2202 #f)) (for-each (lambda (fe) (set! fcg2202 fe)) (list (let ((sy 0)) ((lambda () (set! sy 7))) sy))) fcg2202) 7))
(edge-check-g2202)

(display "EDGE-MATRIX-DONE") (newline)
