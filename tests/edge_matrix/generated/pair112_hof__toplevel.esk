;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: hof (producers) composed into toplevel (contexts)
;; CHECKS: 9
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check hof_fold_left__in__toplevel_top_define
(define tlvg1017 (fold-left + 0 (list 1 2 3)))
(define (edge-check-g1017) (edge-chk "hof_fold_left__in__toplevel_top_define" tlvg1017 6))
(edge-check-g1017)

;; check hof_fold_left__in__toplevel_top_list_arg
(define tllg1018 (list (fold-left + 0 (list 1 2 3))))
(define (edge-check-g1018) (edge-chk "hof_fold_left__in__toplevel_top_list_arg" (car tllg1018) 6))
(edge-check-g1018)

;; check hof_fold_left__in__toplevel_top_vector_arg
(define tlwg1019 (vector (fold-left + 0 (list 1 2 3))))
(define (edge-check-g1019) (edge-chk "hof_fold_left__in__toplevel_top_vector_arg" (vector-ref tlwg1019 0) 6))
(edge-check-g1019)

;; check hof_reduce__in__toplevel_top_define
(define tlvg1020 (reduce + 0 (list 1 2 3)))
(define (edge-check-g1020) (edge-chk "hof_reduce__in__toplevel_top_define" tlvg1020 6))
(edge-check-g1020)

;; check hof_reduce__in__toplevel_top_list_arg
(define tllg1021 (list (reduce + 0 (list 1 2 3))))
(define (edge-check-g1021) (edge-chk "hof_reduce__in__toplevel_top_list_arg" (car tllg1021) 6))
(edge-check-g1021)

;; check hof_reduce__in__toplevel_top_vector_arg
(define tlwg1022 (vector (reduce + 0 (list 1 2 3))))
(define (edge-check-g1022) (edge-chk "hof_reduce__in__toplevel_top_vector_arg" (vector-ref tlwg1022 0) 6))
(edge-check-g1022)

;; check hof_for_each_acc__in__toplevel_top_define
(define tlvg1023 (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs))
(define (edge-check-g1023) (edge-chk "hof_for_each_acc__in__toplevel_top_define" tlvg1023 6))
(edge-check-g1023)

;; check hof_for_each_acc__in__toplevel_top_list_arg
(define tllg1024 (list (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs)))
(define (edge-check-g1024) (edge-chk "hof_for_each_acc__in__toplevel_top_list_arg" (car tllg1024) 6))
(edge-check-g1024)

;; check hof_for_each_acc__in__toplevel_top_vector_arg
(define tlwg1025 (vector (let ((fs 0)) (for-each (lambda (fx) (set! fs (+ fs fx))) (list 1 2 3)) fs)))
(define (edge-check-g1025) (edge-chk "hof_for_each_acc__in__toplevel_top_vector_arg" (vector-ref tlwg1025 0) 6))
(edge-check-g1025)

(display "EDGE-MATRIX-DONE") (newline)
