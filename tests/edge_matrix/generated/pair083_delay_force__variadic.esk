;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: delay_force (producers) composed into variadic (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check delay_force_simple__in__variadic_rest_car
(define (edge-check-g769) (edge-chk "delay_force_simple__in__variadic_rest_car" (car ((lambda vgs vgs) (force (delay (+ 40 2))))) 42))
(edge-check-g769)

;; check delay_force_simple__in__variadic_dotted_first
(define (edge-check-g770) (edge-chk "delay_force_simple__in__variadic_dotted_first" ((lambda (vh . vt) vh) (force (delay (+ 40 2))) 'p1 'p2) 42))
(edge-check-g770)

;; check delay_force_simple__in__variadic_apply_rest
(define (edge-check-g771) (edge-chk "delay_force_simple__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (force (delay (+ 40 2))) 0)) 42))
(edge-check-g771)

;; check delay_force_memoized__in__variadic_rest_car
(define cmng772 0)
(define (cmg772) (set! cmng772 (+ cmng772 1)) cmng772)
(define (edge-check-g772) (edge-chk "delay_force_memoized__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((mpg772 (delay (cmg772)))) (+ (force mpg772) (force mpg772))))) 2))
(edge-check-g772)

;; check delay_force_memoized__in__variadic_dotted_first
(define cmng773 0)
(define (cmg773) (set! cmng773 (+ cmng773 1)) cmng773)
(define (edge-check-g773) (edge-chk "delay_force_memoized__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((mpg773 (delay (cmg773)))) (+ (force mpg773) (force mpg773))) 'p1 'p2) 2))
(edge-check-g773)

;; check delay_force_memoized__in__variadic_apply_rest
(define cmng774 0)
(define (cmg774) (set! cmng774 (+ cmng774 1)) cmng774)
(define (edge-check-g774) (edge-chk "delay_force_memoized__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((mpg774 (delay (cmg774)))) (+ (force mpg774) (force mpg774))) 0)) 2))
(edge-check-g774)

(display "EDGE-MATRIX-DONE") (newline)
