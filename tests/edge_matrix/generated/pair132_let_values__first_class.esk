;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: let_values (producers) composed into first_class (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check let_values_two_values__in__first_class_thunk_in_list
(define (edge-check-g1175) (edge-chk "let_values_two_values__in__first_class_thunk_in_list" ((car (list (lambda () (let-values (((va vb) (values 1 2))) (+ va vb)))))) 3))
(edge-check-g1175)

;; check let_values_two_values__in__first_class_apply_id
(define (edge-check-g1176) (edge-chk "let_values_two_values__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let-values (((va vb) (values 1 2))) (+ va vb)))) 3))
(edge-check-g1176)

;; check let_values_two_values__in__first_class_map_hole
(define (edge-check-g1177) (edge-chk "let_values_two_values__in__first_class_map_hole" (car (map (lambda (fi) (let-values (((va vb) (values 1 2))) (+ va vb))) (list 0))) 3))
(edge-check-g1177)

;; check let_values_two_values__in__first_class_fn_arg_call
(define (edge-check-g1178) (edge-chk "let_values_two_values__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let-values (((va vb) (values 1 2))) (+ va vb)))) 3))
(edge-check-g1178)

;; check let_values_cwv__in__first_class_thunk_in_list
(define (edge-check-g1179) (edge-chk "let_values_cwv__in__first_class_thunk_in_list" ((car (list (lambda () (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb))))))) 42))
(edge-check-g1179)

;; check let_values_cwv__in__first_class_apply_id
(define (edge-check-g1180) (edge-chk "let_values_cwv__in__first_class_apply_id" (apply (lambda (fa) fa) (list (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb))))) 42))
(edge-check-g1180)

;; check let_values_cwv__in__first_class_map_hole
(define (edge-check-g1181) (edge-chk "let_values_cwv__in__first_class_map_hole" (car (map (lambda (fi) (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb)))) (list 0))) 42))
(edge-check-g1181)

;; check let_values_cwv__in__first_class_fn_arg_call
(define (edge-check-g1182) (edge-chk "let_values_cwv__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb))))) 42))
(edge-check-g1182)

(display "EDGE-MATRIX-DONE") (newline)
