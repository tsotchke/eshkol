;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: delay_force (producers) composed into first_class (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check delay_force_simple__in__first_class_thunk_in_list
(define (edge-check-g735) (edge-chk "delay_force_simple__in__first_class_thunk_in_list" ((car (list (lambda () (force (delay (+ 40 2))))))) 42))
(edge-check-g735)

;; check delay_force_simple__in__first_class_apply_id
(define (edge-check-g736) (edge-chk "delay_force_simple__in__first_class_apply_id" (apply (lambda (fa) fa) (list (force (delay (+ 40 2))))) 42))
(edge-check-g736)

;; check delay_force_simple__in__first_class_map_hole
(define (edge-check-g737) (edge-chk "delay_force_simple__in__first_class_map_hole" (car (map (lambda (fi) (force (delay (+ 40 2)))) (list 0))) 42))
(edge-check-g737)

;; check delay_force_simple__in__first_class_fn_arg_call
(define (edge-check-g738) (edge-chk "delay_force_simple__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (force (delay (+ 40 2))))) 42))
(edge-check-g738)

;; check delay_force_memoized__in__first_class_thunk_in_list
(define cmng739 0)
(define (cmg739) (set! cmng739 (+ cmng739 1)) cmng739)
(define (edge-check-g739) (edge-chk "delay_force_memoized__in__first_class_thunk_in_list" ((car (list (lambda () (let ((mpg739 (delay (cmg739)))) (+ (force mpg739) (force mpg739))))))) 2))
(edge-check-g739)

;; check delay_force_memoized__in__first_class_apply_id
(define cmng740 0)
(define (cmg740) (set! cmng740 (+ cmng740 1)) cmng740)
(define (edge-check-g740) (edge-chk "delay_force_memoized__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((mpg740 (delay (cmg740)))) (+ (force mpg740) (force mpg740))))) 2))
(edge-check-g740)

;; check delay_force_memoized__in__first_class_map_hole
(define cmng741 0)
(define (cmg741) (set! cmng741 (+ cmng741 1)) cmng741)
(define (edge-check-g741) (edge-chk "delay_force_memoized__in__first_class_map_hole" (car (map (lambda (fi) (let ((mpg741 (delay (cmg741)))) (+ (force mpg741) (force mpg741)))) (list 0))) 2))
(edge-check-g741)

;; check delay_force_memoized__in__first_class_fn_arg_call
(define cmng742 0)
(define (cmg742) (set! cmng742 (+ cmng742 1)) cmng742)
(define (edge-check-g742) (edge-chk "delay_force_memoized__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((mpg742 (delay (cmg742)))) (+ (force mpg742) (force mpg742))))) 2))
(edge-check-g742)

(display "EDGE-MATRIX-DONE") (newline)
