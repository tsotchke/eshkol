;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into cond_case (contexts)
;; CHECKS: 9
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__cond_case_cond_hole
(define (edge-check-g2274) (edge-chk "shadowing_shadow_libc_local__in__cond_case_cond_hole" (cond ((= 1 1) (let ((pow 3)) (+ pow 1))) (else 'no)) 4))
(edge-check-g2274)

;; check shadowing_shadow_libc_local__in__cond_case_case_hole
(define (edge-check-g2275) (edge-chk "shadowing_shadow_libc_local__in__cond_case_case_hole" (case 1 ((1) (let ((pow 3)) (+ pow 1))) (else 'no)) 4))
(edge-check-g2275)

;; check shadowing_shadow_libc_local__in__cond_case_when_hole
(define (edge-check-g2276) (edge-chk "shadowing_shadow_libc_local__in__cond_case_when_hole" (when #t (let ((pow 3)) (+ pow 1))) 4))
(edge-check-g2276)

;; check shadowing_shadow_builtin_rebind__in__cond_case_cond_hole
(define (edge-check-g2277) (edge-chk "shadowing_shadow_builtin_rebind__in__cond_case_cond_hole" (cond ((= 1 1) (let ((car cdr)) (car (list 1 2 3)))) (else 'no)) (list 2 3)))
(edge-check-g2277)

;; check shadowing_shadow_builtin_rebind__in__cond_case_case_hole
(define (edge-check-g2278) (edge-chk "shadowing_shadow_builtin_rebind__in__cond_case_case_hole" (case 1 ((1) (let ((car cdr)) (car (list 1 2 3)))) (else 'no)) (list 2 3)))
(edge-check-g2278)

;; check shadowing_shadow_builtin_rebind__in__cond_case_when_hole
(define (edge-check-g2279) (edge-chk "shadowing_shadow_builtin_rebind__in__cond_case_when_hole" (when #t (let ((car cdr)) (car (list 1 2 3)))) (list 2 3)))
(edge-check-g2279)

;; check shadowing_shadow_param__in__cond_case_cond_hole
(define (edge-check-g2280) (edge-chk "shadowing_shadow_param__in__cond_case_cond_hole" (cond ((= 1 1) ((lambda (ps) (let ((ps 5)) ps)) 1)) (else 'no)) 5))
(edge-check-g2280)

;; check shadowing_shadow_param__in__cond_case_case_hole
(define (edge-check-g2281) (edge-chk "shadowing_shadow_param__in__cond_case_case_hole" (case 1 ((1) ((lambda (ps) (let ((ps 5)) ps)) 1)) (else 'no)) 5))
(edge-check-g2281)

;; check shadowing_shadow_param__in__cond_case_when_hole
(define (edge-check-g2282) (edge-chk "shadowing_shadow_param__in__cond_case_when_hole" (when #t ((lambda (ps) (let ((ps 5)) ps)) 1)) 5))
(edge-check-g2282)

(display "EDGE-MATRIX-DONE") (newline)
