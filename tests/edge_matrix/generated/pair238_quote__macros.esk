;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: quote (producers) composed into macros (contexts)
;; CHECKS: 14
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check quote_sym__in__macros_identity_macro
(define-syntax img2090 (syntax-rules () ((_ e) e)))
(define (edge-check-g2090) (edge-chk "quote_sym__in__macros_identity_macro" (img2090 'edge-sym) (string->symbol "edge-sym")))
(edge-check-g2090)

;; check quote_sym__in__macros_second_macro
(define-syntax smg2091 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2091) (edge-chk "quote_sym__in__macros_second_macro" (smg2091 'skip 'edge-sym) (string->symbol "edge-sym")))
(edge-check-g2091)

;; check quote_list__in__macros_identity_macro
(define-syntax img2092 (syntax-rules () ((_ e) e)))
(define (edge-check-g2092) (edge-chk "quote_list__in__macros_identity_macro" (img2092 '(1 2 3)) (list 1 2 3)))
(edge-check-g2092)

;; check quote_list__in__macros_second_macro
(define-syntax smg2093 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2093) (edge-chk "quote_list__in__macros_second_macro" (smg2093 'skip '(1 2 3)) (list 1 2 3)))
(edge-check-g2093)

;; check quote_empty__in__macros_identity_macro
(define-syntax img2094 (syntax-rules () ((_ e) e)))
(define (edge-check-g2094) (edge-chk "quote_empty__in__macros_identity_macro" (img2094 '()) (list)))
(edge-check-g2094)

;; check quote_empty__in__macros_second_macro
(define-syntax smg2095 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2095) (edge-chk "quote_empty__in__macros_second_macro" (smg2095 'skip '()) (list)))
(edge-check-g2095)

;; check quote_nested_quote__in__macros_identity_macro
(define-syntax img2096 (syntax-rules () ((_ e) e)))
(define (edge-check-g2096) (edge-chk "quote_nested_quote__in__macros_identity_macro" (img2096 (car ''nq)) (string->symbol "quote")))
(edge-check-g2096)

;; check quote_nested_quote__in__macros_second_macro
(define-syntax smg2097 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2097) (edge-chk "quote_nested_quote__in__macros_second_macro" (smg2097 'skip (car ''nq)) (string->symbol "quote")))
(edge-check-g2097)

;; check quote_veclit_ref__in__macros_identity_macro
(define-syntax img2098 (syntax-rules () ((_ e) e)))
(define (edge-check-g2098) (edge-chk "quote_veclit_ref__in__macros_identity_macro" (img2098 (vector-ref #(9 8 7) 2)) 7))
(edge-check-g2098)

;; check quote_veclit_ref__in__macros_second_macro
(define-syntax smg2099 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2099) (edge-chk "quote_veclit_ref__in__macros_second_macro" (smg2099 'skip (vector-ref #(9 8 7) 2)) 7))
(edge-check-g2099)

;; check quote_qq_sugar__in__macros_identity_macro
(define-syntax img2100 (syntax-rules () ((_ e) e)))
(define (edge-check-g2100) (edge-chk "quote_qq_sugar__in__macros_identity_macro" (img2100 `(1 ,(+ 1 1) ,@(list 3 4))) (list 1 2 3 4)))
(edge-check-g2100)

;; check quote_qq_sugar__in__macros_second_macro
(define-syntax smg2101 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2101) (edge-chk "quote_qq_sugar__in__macros_second_macro" (smg2101 'skip `(1 ,(+ 1 1) ,@(list 3 4))) (list 1 2 3 4)))
(edge-check-g2101)

;; check quote_qq_longform__in__macros_identity_macro
(define-syntax img2102 (syntax-rules () ((_ e) e)))
(define (edge-check-g2102) (edge-chk "quote_qq_longform__in__macros_identity_macro" (img2102 (quasiquote (1 (unquote (+ 1 1))))) (list 1 2)))
(edge-check-g2102)

;; check quote_qq_longform__in__macros_second_macro
(define-syntax smg2103 (syntax-rules () ((_ a b) b)))
(define (edge-check-g2103) (edge-chk "quote_qq_longform__in__macros_second_macro" (smg2103 'skip (quasiquote (1 (unquote (+ 1 1))))) (list 1 2)))
(edge-check-g2103)

(display "EDGE-MATRIX-DONE") (newline)
