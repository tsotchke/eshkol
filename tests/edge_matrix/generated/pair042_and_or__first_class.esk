;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: and_or (producers) composed into first_class (contexts)
;; CHECKS: 16
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check and_or_and_last__in__first_class_thunk_in_list
(define (edge-check-g415) (edge-chk "and_or_and_last__in__first_class_thunk_in_list" ((car (list (lambda () (and 1 2 3))))) 3))
(edge-check-g415)

;; check and_or_and_last__in__first_class_apply_id
(define (edge-check-g416) (edge-chk "and_or_and_last__in__first_class_apply_id" (apply (lambda (fa) fa) (list (and 1 2 3))) 3))
(edge-check-g416)

;; check and_or_and_last__in__first_class_map_hole
(define (edge-check-g417) (edge-chk "and_or_and_last__in__first_class_map_hole" (car (map (lambda (fi) (and 1 2 3)) (list 0))) 3))
(edge-check-g417)

;; check and_or_and_last__in__first_class_fn_arg_call
(define (edge-check-g418) (edge-chk "and_or_and_last__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (and 1 2 3))) 3))
(edge-check-g418)

;; check and_or_or_first__in__first_class_thunk_in_list
(define (edge-check-g419) (edge-chk "and_or_or_first__in__first_class_thunk_in_list" ((car (list (lambda () (or 42 99))))) 42))
(edge-check-g419)

;; check and_or_or_first__in__first_class_apply_id
(define (edge-check-g420) (edge-chk "and_or_or_first__in__first_class_apply_id" (apply (lambda (fa) fa) (list (or 42 99))) 42))
(edge-check-g420)

;; check and_or_or_first__in__first_class_map_hole
(define (edge-check-g421) (edge-chk "and_or_or_first__in__first_class_map_hole" (car (map (lambda (fi) (or 42 99)) (list 0))) 42))
(edge-check-g421)

;; check and_or_or_first__in__first_class_fn_arg_call
(define (edge-check-g422) (edge-chk "and_or_or_first__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (or 42 99))) 42))
(edge-check-g422)

;; check and_or_or_quote_null__in__first_class_thunk_in_list
(define (edge-check-g423) (edge-chk "and_or_or_quote_null__in__first_class_thunk_in_list" ((car (list (lambda () (or #f '()))))) (list)))
(edge-check-g423)

;; check and_or_or_quote_null__in__first_class_apply_id
(define (edge-check-g424) (edge-chk "and_or_or_quote_null__in__first_class_apply_id" (apply (lambda (fa) fa) (list (or #f '()))) (list)))
(edge-check-g424)

;; check and_or_or_quote_null__in__first_class_map_hole
(define (edge-check-g425) (edge-chk "and_or_or_quote_null__in__first_class_map_hole" (car (map (lambda (fi) (or #f '())) (list 0))) (list)))
(edge-check-g425)

;; check and_or_or_quote_null__in__first_class_fn_arg_call
(define (edge-check-g426) (edge-chk "and_or_or_quote_null__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (or #f '()))) (list)))
(edge-check-g426)

;; check and_or_and_empty__in__first_class_thunk_in_list
(define (edge-check-g427) (edge-chk "and_or_and_empty__in__first_class_thunk_in_list" ((car (list (lambda () (and))))) #t))
(edge-check-g427)

;; check and_or_and_empty__in__first_class_apply_id
(define (edge-check-g428) (edge-chk "and_or_and_empty__in__first_class_apply_id" (apply (lambda (fa) fa) (list (and))) #t))
(edge-check-g428)

;; check and_or_and_empty__in__first_class_map_hole
(define (edge-check-g429) (edge-chk "and_or_and_empty__in__first_class_map_hole" (car (map (lambda (fi) (and)) (list 0))) #t))
(edge-check-g429)

;; check and_or_and_empty__in__first_class_fn_arg_call
(define (edge-check-g430) (edge-chk "and_or_and_empty__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (and))) #t))
(edge-check-g430)

(display "EDGE-MATRIX-DONE") (newline)
