;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into closures (contexts)
;; CHECKS: 4
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__closures_escaping_set
(define (edge-check-g2179) (edge-chk "set_bang_local_mut__in__closures_escaping_set" (let ((ethg2179 #f)) (set! ethg2179 (lambda () (let ((sx 1)) (set! sx 41) (+ sx 1)))) (ethg2179)) 42))
(edge-check-g2179)

;; check set_bang_local_mut__in__closures_immediate_lambda
(define (edge-check-g2180) (edge-chk "set_bang_local_mut__in__closures_immediate_lambda" ((lambda () (let ((sx 1)) (set! sx 41) (+ sx 1)))) 42))
(edge-check-g2180)

;; check set_bang_closure_mut__in__closures_escaping_set
(define (edge-check-g2181) (edge-chk "set_bang_closure_mut__in__closures_escaping_set" (let ((ethg2181 #f)) (set! ethg2181 (lambda () (let ((sy 0)) ((lambda () (set! sy 7))) sy))) (ethg2181)) 7))
(edge-check-g2181)

;; check set_bang_closure_mut__in__closures_immediate_lambda
(define (edge-check-g2182) (edge-chk "set_bang_closure_mut__in__closures_immediate_lambda" ((lambda () (let ((sy 0)) ((lambda () (set! sy 7))) sy))) 7))
(edge-check-g2182)

(display "EDGE-MATRIX-DONE") (newline)
