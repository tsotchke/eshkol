;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: let_family (producers) composed into set_bang (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check let_family_let_sum__in__set_bang_store_read
(define (edge-check-g1111) (edge-chk "let_family_let_sum__in__set_bang_store_read" (let ((svg1111 #f)) (set! svg1111 (let ((la 2) (lb 3)) (+ la lb))) svg1111) 5))
(edge-check-g1111)

;; check let_family_let_sum__in__set_bang_lambda_store
(define (edge-check-g1112) (edge-chk "let_family_let_sum__in__set_bang_lambda_store" (let ((swg1112 #f)) ((lambda () (set! swg1112 (let ((la 2) (lb 3)) (+ la lb))))) swg1112) 5))
(edge-check-g1112)

;; check let_family_let_sum__in__set_bang_set_twice
(define (edge-check-g1113) (edge-chk "let_family_let_sum__in__set_bang_set_twice" (let ((szg1113 0)) (set! szg1113 (let ((la 2) (lb 3)) (+ la lb))) (set! szg1113 szg1113) szg1113) 5))
(edge-check-g1113)

;; check let_family_letstar_chain__in__set_bang_store_read
(define (edge-check-g1114) (edge-chk "let_family_letstar_chain__in__set_bang_store_read" (let ((svg1114 #f)) (set! svg1114 (let* ((lc 2) (ld (* lc lc))) ld)) svg1114) 4))
(edge-check-g1114)

;; check let_family_letstar_chain__in__set_bang_lambda_store
(define (edge-check-g1115) (edge-chk "let_family_letstar_chain__in__set_bang_lambda_store" (let ((swg1115 #f)) ((lambda () (set! swg1115 (let* ((lc 2) (ld (* lc lc))) ld)))) swg1115) 4))
(edge-check-g1115)

;; check let_family_letstar_chain__in__set_bang_set_twice
(define (edge-check-g1116) (edge-chk "let_family_letstar_chain__in__set_bang_set_twice" (let ((szg1116 0)) (set! szg1116 (let* ((lc 2) (ld (* lc lc))) ld)) (set! szg1116 szg1116) szg1116) 4))
(edge-check-g1116)

;; check let_family_letrec_mutual__in__set_bang_store_read
(define (edge-check-g1117) (edge-chk "let_family_letrec_mutual__in__set_bang_store_read" (let ((svg1117 #f)) (set! svg1117 (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10))) svg1117) #t))
(edge-check-g1117)

;; check let_family_letrec_mutual__in__set_bang_lambda_store
(define (edge-check-g1118) (edge-chk "let_family_letrec_mutual__in__set_bang_lambda_store" (let ((swg1118 #f)) ((lambda () (set! swg1118 (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10))))) swg1118) #t))
(edge-check-g1118)

;; check let_family_letrec_mutual__in__set_bang_set_twice
(define (edge-check-g1119) (edge-chk "let_family_letrec_mutual__in__set_bang_set_twice" (let ((szg1119 0)) (set! szg1119 (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10))) (set! szg1119 szg1119) szg1119) #t))
(edge-check-g1119)

;; check let_family_named_let_sum__in__set_bang_store_read
(define (edge-check-g1120) (edge-chk "let_family_named_let_sum__in__set_bang_store_read" (let ((svg1120 #f)) (set! svg1120 (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i))))) svg1120) 10))
(edge-check-g1120)

;; check let_family_named_let_sum__in__set_bang_lambda_store
(define (edge-check-g1121) (edge-chk "let_family_named_let_sum__in__set_bang_lambda_store" (let ((swg1121 #f)) ((lambda () (set! swg1121 (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i))))))) swg1121) 10))
(edge-check-g1121)

;; check let_family_named_let_sum__in__set_bang_set_twice
(define (edge-check-g1122) (edge-chk "let_family_named_let_sum__in__set_bang_set_twice" (let ((szg1122 0)) (set! szg1122 (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i))))) (set! szg1122 szg1122) szg1122) 10))
(edge-check-g1122)

(display "EDGE-MATRIX-DONE") (newline)
