;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: begin_seq (producers) composed into tail_position (contexts)
;; CHECKS: 5
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check begin_seq_order__in__tail_position_define_tail
(define (tpg504) (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl))
(define (edge-check-g504) (edge-chk "begin_seq_order__in__tail_position_define_tail" (tpg504) (list 2 1)))
(edge-check-g504)

;; check begin_seq_order__in__tail_position_let_tail_in_define
(define (tqg505) (let ((tu 1)) (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl)))
(define (edge-check-g505) (edge-chk "begin_seq_order__in__tail_position_let_tail_in_define" (tqg505) (list 2 1)))
(edge-check-g505)

;; check begin_seq_order__in__tail_position_if_tail
(define (edge-check-g506) (edge-chk "begin_seq_order__in__tail_position_if_tail" (if (> 2 1) (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl) 'wrong) (list 2 1)))
(edge-check-g506)

;; check begin_seq_order__in__tail_position_begin_tail
(define (edge-check-g507) (edge-chk "begin_seq_order__in__tail_position_begin_tail" (begin 'first (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl)) (list 2 1)))
(edge-check-g507)

;; check begin_seq_order__in__tail_position_cond_else_tail
(define (edge-check-g508) (edge-chk "begin_seq_order__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl))) (list 2 1)))
(edge-check-g508)

(display "EDGE-MATRIX-DONE") (newline)
