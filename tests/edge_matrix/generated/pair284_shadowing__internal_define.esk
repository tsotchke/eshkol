;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into internal_define (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__internal_define_def_then_use
(define (idcg2304) (define it (let ((pow 3)) (+ pow 1))) it)
(define (edge-check-g2304) (edge-chk "shadowing_shadow_libc_local__in__internal_define_def_then_use" (idcg2304) 4))
(edge-check-g2304)

;; check shadowing_shadow_libc_local__in__internal_define_def_chain
(define (iddg2305) (define iu (let ((pow 3)) (+ pow 1))) (define iv iu) iv)
(define (edge-check-g2305) (edge-chk "shadowing_shadow_libc_local__in__internal_define_def_chain" (iddg2305) 4))
(edge-check-g2305)

;; check shadowing_shadow_builtin_rebind__in__internal_define_def_then_use
(define (idcg2306) (define it (let ((car cdr)) (car (list 1 2 3)))) it)
(define (edge-check-g2306) (edge-chk "shadowing_shadow_builtin_rebind__in__internal_define_def_then_use" (idcg2306) (list 2 3)))
(edge-check-g2306)

;; check shadowing_shadow_builtin_rebind__in__internal_define_def_chain
(define (iddg2307) (define iu (let ((car cdr)) (car (list 1 2 3)))) (define iv iu) iv)
(define (edge-check-g2307) (edge-chk "shadowing_shadow_builtin_rebind__in__internal_define_def_chain" (iddg2307) (list 2 3)))
(edge-check-g2307)

;; check shadowing_shadow_param__in__internal_define_def_then_use
(define (idcg2308) (define it ((lambda (ps) (let ((ps 5)) ps)) 1)) it)
(define (edge-check-g2308) (edge-chk "shadowing_shadow_param__in__internal_define_def_then_use" (idcg2308) 5))
(edge-check-g2308)

;; check shadowing_shadow_param__in__internal_define_def_chain
(define (iddg2309) (define iu ((lambda (ps) (let ((ps 5)) ps)) 1)) (define iv iu) iv)
(define (edge-check-g2309) (edge-chk "shadowing_shadow_param__in__internal_define_def_chain" (iddg2309) 5))
(edge-check-g2309)

(display "EDGE-MATRIX-DONE") (newline)
