;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into let_family (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__let_family_let_bind
(define (edge-check-g2209) (edge-chk "set_bang_local_mut__in__let_family_let_bind" (let ((lxg2209 (let ((sx 1)) (set! sx 41) (+ sx 1)))) lxg2209) 42))
(edge-check-g2209)

;; check set_bang_local_mut__in__let_family_letstar_chain
(define (edge-check-g2210) (edge-chk "set_bang_local_mut__in__let_family_letstar_chain" (let* ((lyg2210 (let ((sx 1)) (set! sx 41) (+ sx 1))) (lzg2210 lyg2210)) lzg2210) 42))
(edge-check-g2210)

;; check set_bang_local_mut__in__let_family_letrec_thunk
(define (edge-check-g2211) (edge-chk "set_bang_local_mut__in__let_family_letrec_thunk" (letrec ((lfg2211 (lambda () (let ((sx 1)) (set! sx 41) (+ sx 1))))) (lfg2211)) 42))
(edge-check-g2211)

;; check set_bang_local_mut__in__let_family_named_let_carry
(define (edge-check-g2212) (edge-chk "set_bang_local_mut__in__let_family_named_let_carry" (let nlg2212 ((i 0) (acc #f)) (if (= i 1) acc (nlg2212 (+ i 1) (let ((sx 1)) (set! sx 41) (+ sx 1))))) 42))
(edge-check-g2212)

;; check set_bang_closure_mut__in__let_family_let_bind
(define (edge-check-g2213) (edge-chk "set_bang_closure_mut__in__let_family_let_bind" (let ((lxg2213 (let ((sy 0)) ((lambda () (set! sy 7))) sy))) lxg2213) 7))
(edge-check-g2213)

;; check set_bang_closure_mut__in__let_family_letstar_chain
(define (edge-check-g2214) (edge-chk "set_bang_closure_mut__in__let_family_letstar_chain" (let* ((lyg2214 (let ((sy 0)) ((lambda () (set! sy 7))) sy)) (lzg2214 lyg2214)) lzg2214) 7))
(edge-check-g2214)

;; check set_bang_closure_mut__in__let_family_letrec_thunk
(define (edge-check-g2215) (edge-chk "set_bang_closure_mut__in__let_family_letrec_thunk" (letrec ((lfg2215 (lambda () (let ((sy 0)) ((lambda () (set! sy 7))) sy)))) (lfg2215)) 7))
(edge-check-g2215)

;; check set_bang_closure_mut__in__let_family_named_let_carry
(define (edge-check-g2216) (edge-chk "set_bang_closure_mut__in__let_family_named_let_carry" (let nlg2216 ((i 0) (acc #f)) (if (= i 1) acc (nlg2216 (+ i 1) (let ((sy 0)) ((lambda () (set! sy 7))) sy)))) 7))
(edge-check-g2216)

(display "EDGE-MATRIX-DONE") (newline)
