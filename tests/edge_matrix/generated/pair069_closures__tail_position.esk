;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: closures (producers) composed into tail_position (contexts)
;; CHECKS: 20
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check closures_single_capture__in__tail_position_define_tail
(define (tpg651) (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)))
(define (edge-check-g651) (edge-chk "closures_single_capture__in__tail_position_define_tail" (tpg651) 15))
(edge-check-g651)

;; check closures_single_capture__in__tail_position_let_tail_in_define
(define (tqg652) (let ((tu 1)) (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5))))
(define (edge-check-g652) (edge-chk "closures_single_capture__in__tail_position_let_tail_in_define" (tqg652) 15))
(edge-check-g652)

;; check closures_single_capture__in__tail_position_if_tail
(define (edge-check-g653) (edge-chk "closures_single_capture__in__tail_position_if_tail" (if (> 2 1) (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)) 'wrong) 15))
(edge-check-g653)

;; check closures_single_capture__in__tail_position_begin_tail
(define (edge-check-g654) (edge-chk "closures_single_capture__in__tail_position_begin_tail" (begin 'first (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5))) 15))
(edge-check-g654)

;; check closures_single_capture__in__tail_position_cond_else_tail
(define (edge-check-g655) (edge-chk "closures_single_capture__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)))) 15))
(edge-check-g655)

;; check closures_sibling_set__in__tail_position_define_tail
(define (tpg656) (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))))
(define (edge-check-g656) (edge-chk "closures_sibling_set__in__tail_position_define_tail" (tpg656) 2))
(edge-check-g656)

;; check closures_sibling_set__in__tail_position_let_tail_in_define
(define (tqg657) (let ((tu 1)) (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get)))))
(define (edge-check-g657) (edge-chk "closures_sibling_set__in__tail_position_let_tail_in_define" (tqg657) 2))
(edge-check-g657)

;; check closures_sibling_set__in__tail_position_if_tail
(define (edge-check-g658) (edge-chk "closures_sibling_set__in__tail_position_if_tail" (if (> 2 1) (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))) 'wrong) 2))
(edge-check-g658)

;; check closures_sibling_set__in__tail_position_begin_tail
(define (edge-check-g659) (edge-chk "closures_sibling_set__in__tail_position_begin_tail" (begin 'first (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get)))) 2))
(edge-check-g659)

;; check closures_sibling_set__in__tail_position_cond_else_tail
(define (edge-check-g660) (edge-chk "closures_sibling_set__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))))) 2))
(edge-check-g660)

;; check closures_multi_instance__in__tail_position_define_tail
(define (mkcg661) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag661 (mkcg661))
(define cbg661 (mkcg661))
(define (tpg661) (begin (cag661) (cag661) (+ (cag661) (cbg661))))
(define (edge-check-g661) (edge-chk "closures_multi_instance__in__tail_position_define_tail" (tpg661) 4))
(edge-check-g661)

;; check closures_multi_instance__in__tail_position_let_tail_in_define
(define (mkcg662) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag662 (mkcg662))
(define cbg662 (mkcg662))
(define (tqg662) (let ((tu 1)) (begin (cag662) (cag662) (+ (cag662) (cbg662)))))
(define (edge-check-g662) (edge-chk "closures_multi_instance__in__tail_position_let_tail_in_define" (tqg662) 4))
(edge-check-g662)

;; check closures_multi_instance__in__tail_position_if_tail
(define (mkcg663) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag663 (mkcg663))
(define cbg663 (mkcg663))
(define (edge-check-g663) (edge-chk "closures_multi_instance__in__tail_position_if_tail" (if (> 2 1) (begin (cag663) (cag663) (+ (cag663) (cbg663))) 'wrong) 4))
(edge-check-g663)

;; check closures_multi_instance__in__tail_position_begin_tail
(define (mkcg664) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag664 (mkcg664))
(define cbg664 (mkcg664))
(define (edge-check-g664) (edge-chk "closures_multi_instance__in__tail_position_begin_tail" (begin 'first (begin (cag664) (cag664) (+ (cag664) (cbg664)))) 4))
(edge-check-g664)

;; check closures_multi_instance__in__tail_position_cond_else_tail
(define (mkcg665) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag665 (mkcg665))
(define cbg665 (mkcg665))
(define (edge-check-g665) (edge-chk "closures_multi_instance__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (begin (cag665) (cag665) (+ (cag665) (cbg665))))) 4))
(edge-check-g665)

;; check closures_counter_once__in__tail_position_define_tail
(define ctrg666 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (tpg666) (ctrg666))
(define (edge-check-g666) (edge-chk "closures_counter_once__in__tail_position_define_tail" (tpg666) 1))
(edge-check-g666)

;; check closures_counter_once__in__tail_position_let_tail_in_define
(define ctrg667 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (tqg667) (let ((tu 1)) (ctrg667)))
(define (edge-check-g667) (edge-chk "closures_counter_once__in__tail_position_let_tail_in_define" (tqg667) 1))
(edge-check-g667)

;; check closures_counter_once__in__tail_position_if_tail
(define ctrg668 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g668) (edge-chk "closures_counter_once__in__tail_position_if_tail" (if (> 2 1) (ctrg668) 'wrong) 1))
(edge-check-g668)

;; check closures_counter_once__in__tail_position_begin_tail
(define ctrg669 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g669) (edge-chk "closures_counter_once__in__tail_position_begin_tail" (begin 'first (ctrg669)) 1))
(edge-check-g669)

;; check closures_counter_once__in__tail_position_cond_else_tail
(define ctrg670 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g670) (edge-chk "closures_counter_once__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (ctrg670))) 1))
(edge-check-g670)

(display "EDGE-MATRIX-DONE") (newline)
