;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: cond_case (producers) composed into variadic (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check cond_case_cond_else__in__variadic_rest_car
(define (edge-check-g729) (edge-chk "cond_case_cond_else__in__variadic_rest_car" (car ((lambda vgs vgs) (cond ((= 1 2) 'a) (else 'b)))) (string->symbol "b")))
(edge-check-g729)

;; check cond_case_cond_else__in__variadic_dotted_first
(define (edge-check-g730) (edge-chk "cond_case_cond_else__in__variadic_dotted_first" ((lambda (vh . vt) vh) (cond ((= 1 2) 'a) (else 'b)) 'p1 'p2) (string->symbol "b")))
(edge-check-g730)

;; check cond_case_cond_else__in__variadic_apply_rest
(define (edge-check-g731) (edge-chk "cond_case_cond_else__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (cond ((= 1 2) 'a) (else 'b)) 0)) (string->symbol "b")))
(edge-check-g731)

;; check cond_case_case_hit__in__variadic_rest_car
(define (edge-check-g732) (edge-chk "cond_case_case_hit__in__variadic_rest_car" (car ((lambda vgs vgs) (case 2 ((1) 'one) ((2) 'two) (else 'other)))) (string->symbol "two")))
(edge-check-g732)

;; check cond_case_case_hit__in__variadic_dotted_first
(define (edge-check-g733) (edge-chk "cond_case_case_hit__in__variadic_dotted_first" ((lambda (vh . vt) vh) (case 2 ((1) 'one) ((2) 'two) (else 'other)) 'p1 'p2) (string->symbol "two")))
(edge-check-g733)

;; check cond_case_case_hit__in__variadic_apply_rest
(define (edge-check-g734) (edge-chk "cond_case_case_hit__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (case 2 ((1) 'one) ((2) 'two) (else 'other)) 0)) (string->symbol "two")))
(edge-check-g734)

(display "EDGE-MATRIX-DONE") (newline)
