;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: vectors (producers) composed into toplevel (contexts)
;; CHECKS: 9
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check vectors_vec_ref__in__toplevel_top_define
(define tlvg1717 (vector-ref (vector 'va 'vb) 1))
(define (edge-check-g1717) (edge-chk "vectors_vec_ref__in__toplevel_top_define" tlvg1717 (string->symbol "vb")))
(edge-check-g1717)

;; check vectors_vec_ref__in__toplevel_top_list_arg
(define tllg1718 (list (vector-ref (vector 'va 'vb) 1)))
(define (edge-check-g1718) (edge-chk "vectors_vec_ref__in__toplevel_top_list_arg" (car tllg1718) (string->symbol "vb")))
(edge-check-g1718)

;; check vectors_vec_ref__in__toplevel_top_vector_arg
(define tlwg1719 (vector (vector-ref (vector 'va 'vb) 1)))
(define (edge-check-g1719) (edge-chk "vectors_vec_ref__in__toplevel_top_vector_arg" (vector-ref tlwg1719 0) (string->symbol "vb")))
(edge-check-g1719)

;; check vectors_make_set_ref__in__toplevel_top_define
(define tlvg1720 (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)))
(define (edge-check-g1720) (edge-chk "vectors_make_set_ref__in__toplevel_top_define" tlvg1720 9))
(edge-check-g1720)

;; check vectors_make_set_ref__in__toplevel_top_list_arg
(define tllg1721 (list (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1))))
(define (edge-check-g1721) (edge-chk "vectors_make_set_ref__in__toplevel_top_list_arg" (car tllg1721) 9))
(edge-check-g1721)

;; check vectors_make_set_ref__in__toplevel_top_vector_arg
(define tlwg1722 (vector (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1))))
(define (edge-check-g1722) (edge-chk "vectors_make_set_ref__in__toplevel_top_vector_arg" (vector-ref tlwg1722 0) 9))
(edge-check-g1722)

;; check vectors_vec_len__in__toplevel_top_define
(define tlvg1723 (vector-length (vector 1 2 3)))
(define (edge-check-g1723) (edge-chk "vectors_vec_len__in__toplevel_top_define" tlvg1723 3))
(edge-check-g1723)

;; check vectors_vec_len__in__toplevel_top_list_arg
(define tllg1724 (list (vector-length (vector 1 2 3))))
(define (edge-check-g1724) (edge-chk "vectors_vec_len__in__toplevel_top_list_arg" (car tllg1724) 3))
(edge-check-g1724)

;; check vectors_vec_len__in__toplevel_top_vector_arg
(define tlwg1725 (vector (vector-length (vector 1 2 3))))
(define (edge-check-g1725) (edge-chk "vectors_vec_len__in__toplevel_top_vector_arg" (vector-ref tlwg1725 0) 3))
(edge-check-g1725)

(display "EDGE-MATRIX-DONE") (newline)
