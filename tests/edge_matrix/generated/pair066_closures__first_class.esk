;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: closures (producers) composed into first_class (contexts)
;; CHECKS: 16
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check closures_single_capture__in__first_class_thunk_in_list
(define (edge-check-g615) (edge-chk "closures_single_capture__in__first_class_thunk_in_list" ((car (list (lambda () (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)))))) 15))
(edge-check-g615)

;; check closures_single_capture__in__first_class_apply_id
(define (edge-check-g616) (edge-chk "closures_single_capture__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)))) 15))
(edge-check-g616)

;; check closures_single_capture__in__first_class_map_hole
(define (edge-check-g617) (edge-chk "closures_single_capture__in__first_class_map_hole" (car (map (lambda (fi) (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5))) (list 0))) 15))
(edge-check-g617)

;; check closures_single_capture__in__first_class_fn_arg_call
(define (edge-check-g618) (edge-chk "closures_single_capture__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)))) 15))
(edge-check-g618)

;; check closures_sibling_set__in__first_class_thunk_in_list
(define (edge-check-g619) (edge-chk "closures_sibling_set__in__first_class_thunk_in_list" ((car (list (lambda () (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))))))) 2))
(edge-check-g619)

;; check closures_sibling_set__in__first_class_apply_id
(define (edge-check-g620) (edge-chk "closures_sibling_set__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))))) 2))
(edge-check-g620)

;; check closures_sibling_set__in__first_class_map_hole
(define (edge-check-g621) (edge-chk "closures_sibling_set__in__first_class_map_hole" (car (map (lambda (fi) (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get)))) (list 0))) 2))
(edge-check-g621)

;; check closures_sibling_set__in__first_class_fn_arg_call
(define (edge-check-g622) (edge-chk "closures_sibling_set__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))))) 2))
(edge-check-g622)

;; check closures_multi_instance__in__first_class_thunk_in_list
(define (mkcg623) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag623 (mkcg623))
(define cbg623 (mkcg623))
(define (edge-check-g623) (edge-chk "closures_multi_instance__in__first_class_thunk_in_list" ((car (list (lambda () (begin (cag623) (cag623) (+ (cag623) (cbg623))))))) 4))
(edge-check-g623)

;; check closures_multi_instance__in__first_class_apply_id
(define (mkcg624) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag624 (mkcg624))
(define cbg624 (mkcg624))
(define (edge-check-g624) (edge-chk "closures_multi_instance__in__first_class_apply_id" (apply (lambda (fa) fa) (list (begin (cag624) (cag624) (+ (cag624) (cbg624))))) 4))
(edge-check-g624)

;; check closures_multi_instance__in__first_class_map_hole
(define (mkcg625) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag625 (mkcg625))
(define cbg625 (mkcg625))
(define (edge-check-g625) (edge-chk "closures_multi_instance__in__first_class_map_hole" (car (map (lambda (fi) (begin (cag625) (cag625) (+ (cag625) (cbg625)))) (list 0))) 4))
(edge-check-g625)

;; check closures_multi_instance__in__first_class_fn_arg_call
(define (mkcg626) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag626 (mkcg626))
(define cbg626 (mkcg626))
(define (edge-check-g626) (edge-chk "closures_multi_instance__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (begin (cag626) (cag626) (+ (cag626) (cbg626))))) 4))
(edge-check-g626)

;; check closures_counter_once__in__first_class_thunk_in_list
(define ctrg627 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g627) (edge-chk "closures_counter_once__in__first_class_thunk_in_list" ((car (list (lambda () (ctrg627))))) 1))
(edge-check-g627)

;; check closures_counter_once__in__first_class_apply_id
(define ctrg628 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g628) (edge-chk "closures_counter_once__in__first_class_apply_id" (apply (lambda (fa) fa) (list (ctrg628))) 1))
(edge-check-g628)

;; check closures_counter_once__in__first_class_map_hole
(define ctrg629 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g629) (edge-chk "closures_counter_once__in__first_class_map_hole" (car (map (lambda (fi) (ctrg629)) (list 0))) 1))
(edge-check-g629)

;; check closures_counter_once__in__first_class_fn_arg_call
(define ctrg630 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g630) (edge-chk "closures_counter_once__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (ctrg630))) 1))
(edge-check-g630)

(display "EDGE-MATRIX-DONE") (newline)
