;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: quote (producers) composed into begin_seq (contexts)
;; CHECKS: 14
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check quote_sym__in__begin_seq_begin_tail
(define (edge-check-g1915) (edge-chk "quote_sym__in__begin_seq_begin_tail" (begin 'bfirst 'edge-sym) (string->symbol "edge-sym")))
(edge-check-g1915)

;; check quote_sym__in__begin_seq_begin_only
(define (edge-check-g1916) (edge-chk "quote_sym__in__begin_seq_begin_only" (begin 'edge-sym) (string->symbol "edge-sym")))
(edge-check-g1916)

;; check quote_list__in__begin_seq_begin_tail
(define (edge-check-g1917) (edge-chk "quote_list__in__begin_seq_begin_tail" (begin 'bfirst '(1 2 3)) (list 1 2 3)))
(edge-check-g1917)

;; check quote_list__in__begin_seq_begin_only
(define (edge-check-g1918) (edge-chk "quote_list__in__begin_seq_begin_only" (begin '(1 2 3)) (list 1 2 3)))
(edge-check-g1918)

;; check quote_empty__in__begin_seq_begin_tail
(define (edge-check-g1919) (edge-chk "quote_empty__in__begin_seq_begin_tail" (begin 'bfirst '()) (list)))
(edge-check-g1919)

;; check quote_empty__in__begin_seq_begin_only
(define (edge-check-g1920) (edge-chk "quote_empty__in__begin_seq_begin_only" (begin '()) (list)))
(edge-check-g1920)

;; check quote_nested_quote__in__begin_seq_begin_tail
(define (edge-check-g1921) (edge-chk "quote_nested_quote__in__begin_seq_begin_tail" (begin 'bfirst (car ''nq)) (string->symbol "quote")))
(edge-check-g1921)

;; check quote_nested_quote__in__begin_seq_begin_only
(define (edge-check-g1922) (edge-chk "quote_nested_quote__in__begin_seq_begin_only" (begin (car ''nq)) (string->symbol "quote")))
(edge-check-g1922)

;; check quote_veclit_ref__in__begin_seq_begin_tail
(define (edge-check-g1923) (edge-chk "quote_veclit_ref__in__begin_seq_begin_tail" (begin 'bfirst (vector-ref #(9 8 7) 2)) 7))
(edge-check-g1923)

;; check quote_veclit_ref__in__begin_seq_begin_only
(define (edge-check-g1924) (edge-chk "quote_veclit_ref__in__begin_seq_begin_only" (begin (vector-ref #(9 8 7) 2)) 7))
(edge-check-g1924)

;; check quote_qq_sugar__in__begin_seq_begin_tail
(define (edge-check-g1925) (edge-chk "quote_qq_sugar__in__begin_seq_begin_tail" (begin 'bfirst `(1 ,(+ 1 1) ,@(list 3 4))) (list 1 2 3 4)))
(edge-check-g1925)

;; check quote_qq_sugar__in__begin_seq_begin_only
(define (edge-check-g1926) (edge-chk "quote_qq_sugar__in__begin_seq_begin_only" (begin `(1 ,(+ 1 1) ,@(list 3 4))) (list 1 2 3 4)))
(edge-check-g1926)

;; check quote_qq_longform__in__begin_seq_begin_tail
(define (edge-check-g1927) (edge-chk "quote_qq_longform__in__begin_seq_begin_tail" (begin 'bfirst (quasiquote (1 (unquote (+ 1 1))))) (list 1 2)))
(edge-check-g1927)

;; check quote_qq_longform__in__begin_seq_begin_only
(define (edge-check-g1928) (edge-chk "quote_qq_longform__in__begin_seq_begin_only" (begin (quasiquote (1 (unquote (+ 1 1))))) (list 1 2)))
(edge-check-g1928)

(display "EDGE-MATRIX-DONE") (newline)
