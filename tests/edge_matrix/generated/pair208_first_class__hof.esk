;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: first_class (producers) composed into hof (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check first_class_builtin_var__in__hof_fold_last
(define (edge-check-g1803) (edge-chk "first_class_builtin_var__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (let ((fp null?)) (fp '())))) #t))
(edge-check-g1803)

;; check first_class_builtin_var__in__hof_for_each_capture
(define (edge-check-g1804) (edge-chk "first_class_builtin_var__in__hof_for_each_capture" (let ((fcg1804 #f)) (for-each (lambda (fe) (set! fcg1804 fe)) (list (let ((fp null?)) (fp '())))) fcg1804) #t))
(edge-check-g1804)

;; check first_class_apply_builtin__in__hof_fold_last
(define (edge-check-g1805) (edge-chk "first_class_apply_builtin__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (apply + (list 20 22)))) 42))
(edge-check-g1805)

;; check first_class_apply_builtin__in__hof_for_each_capture
(define (edge-check-g1806) (edge-chk "first_class_apply_builtin__in__hof_for_each_capture" (let ((fcg1806 #f)) (for-each (lambda (fe) (set! fcg1806 fe)) (list (apply + (list 20 22)))) fcg1806) 42))
(edge-check-g1806)

;; check first_class_builtin_from_list__in__hof_fold_last
(define (edge-check-g1807) (edge-chk "first_class_builtin_from_list__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list ((car (list +)) 20 22))) 42))
(edge-check-g1807)

;; check first_class_builtin_from_list__in__hof_for_each_capture
(define (edge-check-g1808) (edge-chk "first_class_builtin_from_list__in__hof_for_each_capture" (let ((fcg1808 #f)) (for-each (lambda (fe) (set! fcg1808 fe)) (list ((car (list +)) 20 22))) fcg1808) 42))
(edge-check-g1808)

;; check first_class_map_builtin__in__hof_fold_last
(define (edge-check-g1809) (edge-chk "first_class_map_builtin__in__hof_fold_last" (fold-left (lambda (facc fx) fx) #f (list (map car '((1 2) (3 4))))) (list 1 3)))
(edge-check-g1809)

;; check first_class_map_builtin__in__hof_for_each_capture
(define (edge-check-g1810) (edge-chk "first_class_map_builtin__in__hof_for_each_capture" (let ((fcg1810 #f)) (for-each (lambda (fe) (set! fcg1810 fe)) (list (map car '((1 2) (3 4))))) fcg1810) (list 1 3)))
(edge-check-g1810)

(display "EDGE-MATRIX-DONE") (newline)
