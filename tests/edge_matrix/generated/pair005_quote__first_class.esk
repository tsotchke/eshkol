;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: quote (producers) composed into first_class (contexts)
;; CHECKS: 28
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check quote_sym__in__first_class_thunk_in_list
(define (edge-check-g64) (edge-chk "quote_sym__in__first_class_thunk_in_list" ((car (list (lambda () 'edge-sym)))) (string->symbol "edge-sym")))
(edge-check-g64)

;; check quote_sym__in__first_class_apply_id
(define (edge-check-g65) (edge-chk "quote_sym__in__first_class_apply_id" (apply (lambda (fa) fa) (list 'edge-sym)) (string->symbol "edge-sym")))
(edge-check-g65)

;; check quote_sym__in__first_class_map_hole
(define (edge-check-g66) (edge-chk "quote_sym__in__first_class_map_hole" (car (map (lambda (fi) 'edge-sym) (list 0))) (string->symbol "edge-sym")))
(edge-check-g66)

;; check quote_sym__in__first_class_fn_arg_call
(define (edge-check-g67) (edge-chk "quote_sym__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () 'edge-sym)) (string->symbol "edge-sym")))
(edge-check-g67)

;; check quote_list__in__first_class_thunk_in_list
(define (edge-check-g68) (edge-chk "quote_list__in__first_class_thunk_in_list" ((car (list (lambda () '(1 2 3))))) (list 1 2 3)))
(edge-check-g68)

;; check quote_list__in__first_class_apply_id
(define (edge-check-g69) (edge-chk "quote_list__in__first_class_apply_id" (apply (lambda (fa) fa) (list '(1 2 3))) (list 1 2 3)))
(edge-check-g69)

;; check quote_list__in__first_class_map_hole
(define (edge-check-g70) (edge-chk "quote_list__in__first_class_map_hole" (car (map (lambda (fi) '(1 2 3)) (list 0))) (list 1 2 3)))
(edge-check-g70)

;; check quote_list__in__first_class_fn_arg_call
(define (edge-check-g71) (edge-chk "quote_list__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () '(1 2 3))) (list 1 2 3)))
(edge-check-g71)

;; check quote_empty__in__first_class_thunk_in_list
(define (edge-check-g72) (edge-chk "quote_empty__in__first_class_thunk_in_list" ((car (list (lambda () '())))) (list)))
(edge-check-g72)

;; check quote_empty__in__first_class_apply_id
(define (edge-check-g73) (edge-chk "quote_empty__in__first_class_apply_id" (apply (lambda (fa) fa) (list '())) (list)))
(edge-check-g73)

;; check quote_empty__in__first_class_map_hole
(define (edge-check-g74) (edge-chk "quote_empty__in__first_class_map_hole" (car (map (lambda (fi) '()) (list 0))) (list)))
(edge-check-g74)

;; check quote_empty__in__first_class_fn_arg_call
(define (edge-check-g75) (edge-chk "quote_empty__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () '())) (list)))
(edge-check-g75)

;; check quote_nested_quote__in__first_class_thunk_in_list
(define (edge-check-g76) (edge-chk "quote_nested_quote__in__first_class_thunk_in_list" ((car (list (lambda () (car ''nq))))) (string->symbol "quote")))
(edge-check-g76)

;; check quote_nested_quote__in__first_class_apply_id
(define (edge-check-g77) (edge-chk "quote_nested_quote__in__first_class_apply_id" (apply (lambda (fa) fa) (list (car ''nq))) (string->symbol "quote")))
(edge-check-g77)

;; check quote_nested_quote__in__first_class_map_hole
(define (edge-check-g78) (edge-chk "quote_nested_quote__in__first_class_map_hole" (car (map (lambda (fi) (car ''nq)) (list 0))) (string->symbol "quote")))
(edge-check-g78)

;; check quote_nested_quote__in__first_class_fn_arg_call
(define (edge-check-g79) (edge-chk "quote_nested_quote__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (car ''nq))) (string->symbol "quote")))
(edge-check-g79)

;; check quote_veclit_ref__in__first_class_thunk_in_list
(define (edge-check-g80) (edge-chk "quote_veclit_ref__in__first_class_thunk_in_list" ((car (list (lambda () (vector-ref #(9 8 7) 2))))) 7))
(edge-check-g80)

;; check quote_veclit_ref__in__first_class_apply_id
(define (edge-check-g81) (edge-chk "quote_veclit_ref__in__first_class_apply_id" (apply (lambda (fa) fa) (list (vector-ref #(9 8 7) 2))) 7))
(edge-check-g81)

;; check quote_veclit_ref__in__first_class_map_hole
(define (edge-check-g82) (edge-chk "quote_veclit_ref__in__first_class_map_hole" (car (map (lambda (fi) (vector-ref #(9 8 7) 2)) (list 0))) 7))
(edge-check-g82)

;; check quote_veclit_ref__in__first_class_fn_arg_call
(define (edge-check-g83) (edge-chk "quote_veclit_ref__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (vector-ref #(9 8 7) 2))) 7))
(edge-check-g83)

;; check quote_qq_sugar__in__first_class_thunk_in_list
(define (edge-check-g84) (edge-chk "quote_qq_sugar__in__first_class_thunk_in_list" ((car (list (lambda () `(1 ,(+ 1 1) ,@(list 3 4)))))) (list 1 2 3 4)))
(edge-check-g84)

;; check quote_qq_sugar__in__first_class_apply_id
(define (edge-check-g85) (edge-chk "quote_qq_sugar__in__first_class_apply_id" (apply (lambda (fa) fa) (list `(1 ,(+ 1 1) ,@(list 3 4)))) (list 1 2 3 4)))
(edge-check-g85)

;; check quote_qq_sugar__in__first_class_map_hole
(define (edge-check-g86) (edge-chk "quote_qq_sugar__in__first_class_map_hole" (car (map (lambda (fi) `(1 ,(+ 1 1) ,@(list 3 4))) (list 0))) (list 1 2 3 4)))
(edge-check-g86)

;; check quote_qq_sugar__in__first_class_fn_arg_call
(define (edge-check-g87) (edge-chk "quote_qq_sugar__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () `(1 ,(+ 1 1) ,@(list 3 4)))) (list 1 2 3 4)))
(edge-check-g87)

;; check quote_qq_longform__in__first_class_thunk_in_list
(define (edge-check-g88) (edge-chk "quote_qq_longform__in__first_class_thunk_in_list" ((car (list (lambda () (quasiquote (1 (unquote (+ 1 1)))))))) (list 1 2)))
(edge-check-g88)

;; check quote_qq_longform__in__first_class_apply_id
(define (edge-check-g89) (edge-chk "quote_qq_longform__in__first_class_apply_id" (apply (lambda (fa) fa) (list (quasiquote (1 (unquote (+ 1 1)))))) (list 1 2)))
(edge-check-g89)

;; check quote_qq_longform__in__first_class_map_hole
(define (edge-check-g90) (edge-chk "quote_qq_longform__in__first_class_map_hole" (car (map (lambda (fi) (quasiquote (1 (unquote (+ 1 1))))) (list 0))) (list 1 2)))
(edge-check-g90)

;; check quote_qq_longform__in__first_class_fn_arg_call
(define (edge-check-g91) (edge-chk "quote_qq_longform__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (quasiquote (1 (unquote (+ 1 1)))))) (list 1 2)))
(edge-check-g91)

(display "EDGE-MATRIX-DONE") (newline)
