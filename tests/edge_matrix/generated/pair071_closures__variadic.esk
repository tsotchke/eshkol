;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: closures (producers) composed into variadic (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check closures_single_capture__in__variadic_rest_car
(define (edge-check-g683) (edge-chk "closures_single_capture__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)))) 15))
(edge-check-g683)

;; check closures_single_capture__in__variadic_dotted_first
(define (edge-check-g684) (edge-chk "closures_single_capture__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)) 'p1 'p2) 15))
(edge-check-g684)

;; check closures_single_capture__in__variadic_apply_rest
(define (edge-check-g685) (edge-chk "closures_single_capture__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)) 0)) 15))
(edge-check-g685)

;; check closures_sibling_set__in__variadic_rest_car
(define (edge-check-g686) (edge-chk "closures_sibling_set__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))))) 2))
(edge-check-g686)

;; check closures_sibling_set__in__variadic_dotted_first
(define (edge-check-g687) (edge-chk "closures_sibling_set__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))) 'p1 'p2) 2))
(edge-check-g687)

;; check closures_sibling_set__in__variadic_apply_rest
(define (edge-check-g688) (edge-chk "closures_sibling_set__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))) 0)) 2))
(edge-check-g688)

;; check closures_multi_instance__in__variadic_rest_car
(define (mkcg689) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag689 (mkcg689))
(define cbg689 (mkcg689))
(define (edge-check-g689) (edge-chk "closures_multi_instance__in__variadic_rest_car" (car ((lambda vgs vgs) (begin (cag689) (cag689) (+ (cag689) (cbg689))))) 4))
(edge-check-g689)

;; check closures_multi_instance__in__variadic_dotted_first
(define (mkcg690) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag690 (mkcg690))
(define cbg690 (mkcg690))
(define (edge-check-g690) (edge-chk "closures_multi_instance__in__variadic_dotted_first" ((lambda (vh . vt) vh) (begin (cag690) (cag690) (+ (cag690) (cbg690))) 'p1 'p2) 4))
(edge-check-g690)

;; check closures_multi_instance__in__variadic_apply_rest
(define (mkcg691) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag691 (mkcg691))
(define cbg691 (mkcg691))
(define (edge-check-g691) (edge-chk "closures_multi_instance__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (begin (cag691) (cag691) (+ (cag691) (cbg691))) 0)) 4))
(edge-check-g691)

;; check closures_counter_once__in__variadic_rest_car
(define ctrg692 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g692) (edge-chk "closures_counter_once__in__variadic_rest_car" (car ((lambda vgs vgs) (ctrg692))) 1))
(edge-check-g692)

;; check closures_counter_once__in__variadic_dotted_first
(define ctrg693 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g693) (edge-chk "closures_counter_once__in__variadic_dotted_first" ((lambda (vh . vt) vh) (ctrg693) 'p1 'p2) 1))
(edge-check-g693)

;; check closures_counter_once__in__variadic_apply_rest
(define ctrg694 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g694) (edge-chk "closures_counter_once__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (ctrg694) 0)) 1))
(edge-check-g694)

(display "EDGE-MATRIX-DONE") (newline)
