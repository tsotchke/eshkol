;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into begin_seq (contexts)
;; CHECKS: 4
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__begin_seq_begin_tail
(define (edge-check-g2171) (edge-chk "set_bang_local_mut__in__begin_seq_begin_tail" (begin 'bfirst (let ((sx 1)) (set! sx 41) (+ sx 1))) 42))
(edge-check-g2171)

;; check set_bang_local_mut__in__begin_seq_begin_only
(define (edge-check-g2172) (edge-chk "set_bang_local_mut__in__begin_seq_begin_only" (begin (let ((sx 1)) (set! sx 41) (+ sx 1))) 42))
(edge-check-g2172)

;; check set_bang_closure_mut__in__begin_seq_begin_tail
(define (edge-check-g2173) (edge-chk "set_bang_closure_mut__in__begin_seq_begin_tail" (begin 'bfirst (let ((sy 0)) ((lambda () (set! sy 7))) sy)) 7))
(edge-check-g2173)

;; check set_bang_closure_mut__in__begin_seq_begin_only
(define (edge-check-g2174) (edge-chk "set_bang_closure_mut__in__begin_seq_begin_only" (begin (let ((sy 0)) ((lambda () (set! sy 7))) sy)) 7))
(edge-check-g2174)

(display "EDGE-MATRIX-DONE") (newline)
