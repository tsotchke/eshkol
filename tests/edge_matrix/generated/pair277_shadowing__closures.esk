;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into closures (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__closures_escaping_set
(define (edge-check-g2268) (edge-chk "shadowing_shadow_libc_local__in__closures_escaping_set" (let ((ethg2268 #f)) (set! ethg2268 (lambda () (let ((pow 3)) (+ pow 1)))) (ethg2268)) 4))
(edge-check-g2268)

;; check shadowing_shadow_libc_local__in__closures_immediate_lambda
(define (edge-check-g2269) (edge-chk "shadowing_shadow_libc_local__in__closures_immediate_lambda" ((lambda () (let ((pow 3)) (+ pow 1)))) 4))
(edge-check-g2269)

;; check shadowing_shadow_builtin_rebind__in__closures_escaping_set
(define (edge-check-g2270) (edge-chk "shadowing_shadow_builtin_rebind__in__closures_escaping_set" (let ((ethg2270 #f)) (set! ethg2270 (lambda () (let ((car cdr)) (car (list 1 2 3))))) (ethg2270)) (list 2 3)))
(edge-check-g2270)

;; check shadowing_shadow_builtin_rebind__in__closures_immediate_lambda
(define (edge-check-g2271) (edge-chk "shadowing_shadow_builtin_rebind__in__closures_immediate_lambda" ((lambda () (let ((car cdr)) (car (list 1 2 3))))) (list 2 3)))
(edge-check-g2271)

;; check shadowing_shadow_param__in__closures_escaping_set
(define (edge-check-g2272) (edge-chk "shadowing_shadow_param__in__closures_escaping_set" (let ((ethg2272 #f)) (set! ethg2272 (lambda () ((lambda (ps) (let ((ps 5)) ps)) 1))) (ethg2272)) 5))
(edge-check-g2272)

;; check shadowing_shadow_param__in__closures_immediate_lambda
(define (edge-check-g2273) (edge-chk "shadowing_shadow_param__in__closures_immediate_lambda" ((lambda () ((lambda (ps) (let ((ps 5)) ps)) 1))) 5))
(edge-check-g2273)

(display "EDGE-MATRIX-DONE") (newline)
