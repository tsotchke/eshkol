;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: dynamic_wind (producers) composed into first_class (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check dynamic_wind_order__in__first_class_thunk_in_list
(define (edge-check-g775) (edge-chk "dynamic_wind_order__in__first_class_thunk_in_list" ((car (list (lambda () (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl))))) (list 3 2 1)))
(edge-check-g775)

;; check dynamic_wind_order__in__first_class_apply_id
(define (edge-check-g776) (edge-chk "dynamic_wind_order__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl))) (list 3 2 1)))
(edge-check-g776)

;; check dynamic_wind_order__in__first_class_map_hole
(define (edge-check-g777) (edge-chk "dynamic_wind_order__in__first_class_map_hole" (car (map (lambda (fi) (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl)) (list 0))) (list 3 2 1)))
(edge-check-g777)

;; check dynamic_wind_order__in__first_class_fn_arg_call
(define (edge-check-g778) (edge-chk "dynamic_wind_order__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl))) (list 3 2 1)))
(edge-check-g778)

;; check dynamic_wind_value__in__first_class_thunk_in_list
(define (edge-check-g779) (edge-chk "dynamic_wind_value__in__first_class_thunk_in_list" ((car (list (lambda () (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)))))) 42))
(edge-check-g779)

;; check dynamic_wind_value__in__first_class_apply_id
(define (edge-check-g780) (edge-chk "dynamic_wind_value__in__first_class_apply_id" (apply (lambda (fa) fa) (list (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)))) 42))
(edge-check-g780)

;; check dynamic_wind_value__in__first_class_map_hole
(define (edge-check-g781) (edge-chk "dynamic_wind_value__in__first_class_map_hole" (car (map (lambda (fi) (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a))) (list 0))) 42))
(edge-check-g781)

;; check dynamic_wind_value__in__first_class_fn_arg_call
(define (edge-check-g782) (edge-chk "dynamic_wind_value__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)))) 42))
(edge-check-g782)

(display "EDGE-MATRIX-DONE") (newline)
