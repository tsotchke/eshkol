;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into toplevel (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__toplevel_top_define
(define tlvg226 (let ((sx 1)) (set! sx 41) (+ sx 1)))
(define (edge-check-g226) (edge-chk "set_bang_local_mut__in__toplevel_top_define" tlvg226 42))
(edge-check-g226)

;; check set_bang_local_mut__in__toplevel_top_list_arg
(define tllg227 (list (let ((sx 1)) (set! sx 41) (+ sx 1))))
(define (edge-check-g227) (edge-chk "set_bang_local_mut__in__toplevel_top_list_arg" (car tllg227) 42))
(edge-check-g227)

;; check set_bang_local_mut__in__toplevel_top_vector_arg
(define tlwg228 (vector (let ((sx 1)) (set! sx 41) (+ sx 1))))
(define (edge-check-g228) (edge-chk "set_bang_local_mut__in__toplevel_top_vector_arg" (vector-ref tlwg228 0) 42))
(edge-check-g228)

;; check set_bang_closure_mut__in__toplevel_top_define
(define tlvg229 (let ((sy 0)) ((lambda () (set! sy 7))) sy))
(define (edge-check-g229) (edge-chk "set_bang_closure_mut__in__toplevel_top_define" tlvg229 7))
(edge-check-g229)

;; check set_bang_closure_mut__in__toplevel_top_list_arg
(define tllg230 (list (let ((sy 0)) ((lambda () (set! sy 7))) sy)))
(define (edge-check-g230) (edge-chk "set_bang_closure_mut__in__toplevel_top_list_arg" (car tllg230) 7))
(edge-check-g230)

;; check set_bang_closure_mut__in__toplevel_top_vector_arg
(define tlwg231 (vector (let ((sy 0)) ((lambda () (set! sy 7))) sy)))
(define (edge-check-g231) (edge-chk "set_bang_closure_mut__in__toplevel_top_vector_arg" (vector-ref tlwg231 0) 7))
(edge-check-g231)

(display "EDGE-MATRIX-DONE") (newline)
