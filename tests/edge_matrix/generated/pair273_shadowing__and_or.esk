;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into and_or (contexts)
;; CHECKS: 9
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__and_or_and_pass
(define (edge-check-g2247) (edge-chk "shadowing_shadow_libc_local__in__and_or_and_pass" (and #t (let ((pow 3)) (+ pow 1))) 4))
(edge-check-g2247)

;; check shadowing_shadow_libc_local__in__and_or_or_pass
(define (edge-check-g2248) (edge-chk "shadowing_shadow_libc_local__in__and_or_or_pass" (or #f (let ((pow 3)) (+ pow 1))) 4))
(edge-check-g2248)

;; check shadowing_shadow_libc_local__in__and_or_if_or_else
(define (edge-check-g2249) (edge-chk "shadowing_shadow_libc_local__in__and_or_if_or_else" (if (or #f #f) 'no (let ((pow 3)) (+ pow 1))) 4))
(edge-check-g2249)

;; check shadowing_shadow_builtin_rebind__in__and_or_and_pass
(define (edge-check-g2250) (edge-chk "shadowing_shadow_builtin_rebind__in__and_or_and_pass" (and #t (let ((car cdr)) (car (list 1 2 3)))) (list 2 3)))
(edge-check-g2250)

;; check shadowing_shadow_builtin_rebind__in__and_or_or_pass
(define (edge-check-g2251) (edge-chk "shadowing_shadow_builtin_rebind__in__and_or_or_pass" (or #f (let ((car cdr)) (car (list 1 2 3)))) (list 2 3)))
(edge-check-g2251)

;; check shadowing_shadow_builtin_rebind__in__and_or_if_or_else
(define (edge-check-g2252) (edge-chk "shadowing_shadow_builtin_rebind__in__and_or_if_or_else" (if (or #f #f) 'no (let ((car cdr)) (car (list 1 2 3)))) (list 2 3)))
(edge-check-g2252)

;; check shadowing_shadow_param__in__and_or_and_pass
(define (edge-check-g2253) (edge-chk "shadowing_shadow_param__in__and_or_and_pass" (and #t ((lambda (ps) (let ((ps 5)) ps)) 1)) 5))
(edge-check-g2253)

;; check shadowing_shadow_param__in__and_or_or_pass
(define (edge-check-g2254) (edge-chk "shadowing_shadow_param__in__and_or_or_pass" (or #f ((lambda (ps) (let ((ps 5)) ps)) 1)) 5))
(edge-check-g2254)

;; check shadowing_shadow_param__in__and_or_if_or_else
(define (edge-check-g2255) (edge-chk "shadowing_shadow_param__in__and_or_if_or_else" (if (or #f #f) 'no ((lambda (ps) (let ((ps 5)) ps)) 1)) 5))
(edge-check-g2255)

(display "EDGE-MATRIX-DONE") (newline)
