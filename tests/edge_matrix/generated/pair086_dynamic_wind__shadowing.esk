;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: dynamic_wind (producers) composed into shadowing (contexts)
;; CHECKS: 4
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check dynamic_wind_order__in__shadowing_shadow_unused_builtin
(define (edge-check-g789) (edge-chk "dynamic_wind_order__in__shadowing_shadow_unused_builtin" (let ((length 5)) (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl)) (list 3 2 1)))
(edge-check-g789)

;; check dynamic_wind_order__in__shadowing_shadow_param_rebind
(define (edge-check-g790) (edge-chk "dynamic_wind_order__in__shadowing_shadow_param_rebind" ((lambda (spg790) (let ((spg790 (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl))) spg790)) 'outer) (list 3 2 1)))
(edge-check-g790)

;; check dynamic_wind_value__in__shadowing_shadow_unused_builtin
(define (edge-check-g791) (edge-chk "dynamic_wind_value__in__shadowing_shadow_unused_builtin" (let ((length 5)) (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a))) 42))
(edge-check-g791)

;; check dynamic_wind_value__in__shadowing_shadow_param_rebind
(define (edge-check-g792) (edge-chk "dynamic_wind_value__in__shadowing_shadow_param_rebind" ((lambda (spg792) (let ((spg792 (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)))) spg792)) 'outer) 42))
(edge-check-g792)

(display "EDGE-MATRIX-DONE") (newline)
