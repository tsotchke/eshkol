;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: begin_seq (producers) composed into toplevel (contexts)
;; CHECKS: 3
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check begin_seq_order__in__toplevel_top_define
(define tlvg509 (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl))
(define (edge-check-g509) (edge-chk "begin_seq_order__in__toplevel_top_define" tlvg509 (list 2 1)))
(edge-check-g509)

;; check begin_seq_order__in__toplevel_top_list_arg
(define tllg510 (list (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl)))
(define (edge-check-g510) (edge-chk "begin_seq_order__in__toplevel_top_list_arg" (car tllg510) (list 2 1)))
(edge-check-g510)

;; check begin_seq_order__in__toplevel_top_vector_arg
(define tlwg511 (vector (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl)))
(define (edge-check-g511) (edge-chk "begin_seq_order__in__toplevel_top_vector_arg" (vector-ref tlwg511 0) (list 2 1)))
(edge-check-g511)

(display "EDGE-MATRIX-DONE") (newline)
