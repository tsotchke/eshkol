;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: strings (producers) composed into variadic (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check strings_append__in__variadic_rest_car
(define (edge-check-g1563) (edge-chk "strings_append__in__variadic_rest_car" (car ((lambda vgs vgs) (string-append "ab" "cd"))) (string #\a #\b #\c #\d)))
(edge-check-g1563)

;; check strings_append__in__variadic_dotted_first
(define (edge-check-g1564) (edge-chk "strings_append__in__variadic_dotted_first" ((lambda (vh . vt) vh) (string-append "ab" "cd") 'p1 'p2) (string #\a #\b #\c #\d)))
(edge-check-g1564)

;; check strings_append__in__variadic_apply_rest
(define (edge-check-g1565) (edge-chk "strings_append__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (string-append "ab" "cd") 0)) (string #\a #\b #\c #\d)))
(edge-check-g1565)

;; check strings_substr__in__variadic_rest_car
(define (edge-check-g1566) (edge-chk "strings_substr__in__variadic_rest_car" (car ((lambda vgs vgs) (substring "hello" 1 3))) "el"))
(edge-check-g1566)

;; check strings_substr__in__variadic_dotted_first
(define (edge-check-g1567) (edge-chk "strings_substr__in__variadic_dotted_first" ((lambda (vh . vt) vh) (substring "hello" 1 3) 'p1 'p2) "el"))
(edge-check-g1567)

;; check strings_substr__in__variadic_apply_rest
(define (edge-check-g1568) (edge-chk "strings_substr__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (substring "hello" 1 3) 0)) "el"))
(edge-check-g1568)

;; check strings_num_to_str__in__variadic_rest_car
(define (edge-check-g1569) (edge-chk "strings_num_to_str__in__variadic_rest_car" (car ((lambda vgs vgs) (number->string 42))) "42"))
(edge-check-g1569)

;; check strings_num_to_str__in__variadic_dotted_first
(define (edge-check-g1570) (edge-chk "strings_num_to_str__in__variadic_dotted_first" ((lambda (vh . vt) vh) (number->string 42) 'p1 'p2) "42"))
(edge-check-g1570)

;; check strings_num_to_str__in__variadic_apply_rest
(define (edge-check-g1571) (edge-chk "strings_num_to_str__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (number->string 42) 0)) "42"))
(edge-check-g1571)

;; check strings_str_to_num__in__variadic_rest_car
(define (edge-check-g1572) (edge-chk "strings_str_to_num__in__variadic_rest_car" (car ((lambda vgs vgs) (string->number "42"))) 42))
(edge-check-g1572)

;; check strings_str_to_num__in__variadic_dotted_first
(define (edge-check-g1573) (edge-chk "strings_str_to_num__in__variadic_dotted_first" ((lambda (vh . vt) vh) (string->number "42") 'p1 'p2) 42))
(edge-check-g1573)

;; check strings_str_to_num__in__variadic_apply_rest
(define (edge-check-g1574) (edge-chk "strings_str_to_num__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (string->number "42") 0)) 42))
(edge-check-g1574)

(display "EDGE-MATRIX-DONE") (newline)
