;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into and_or (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__and_or_and_pass
(define (edge-check-g2165) (edge-chk "set_bang_local_mut__in__and_or_and_pass" (and #t (let ((sx 1)) (set! sx 41) (+ sx 1))) 42))
(edge-check-g2165)

;; check set_bang_local_mut__in__and_or_or_pass
(define (edge-check-g2166) (edge-chk "set_bang_local_mut__in__and_or_or_pass" (or #f (let ((sx 1)) (set! sx 41) (+ sx 1))) 42))
(edge-check-g2166)

;; check set_bang_local_mut__in__and_or_if_or_else
(define (edge-check-g2167) (edge-chk "set_bang_local_mut__in__and_or_if_or_else" (if (or #f #f) 'no (let ((sx 1)) (set! sx 41) (+ sx 1))) 42))
(edge-check-g2167)

;; check set_bang_closure_mut__in__and_or_and_pass
(define (edge-check-g2168) (edge-chk "set_bang_closure_mut__in__and_or_and_pass" (and #t (let ((sy 0)) ((lambda () (set! sy 7))) sy)) 7))
(edge-check-g2168)

;; check set_bang_closure_mut__in__and_or_or_pass
(define (edge-check-g2169) (edge-chk "set_bang_closure_mut__in__and_or_or_pass" (or #f (let ((sy 0)) ((lambda () (set! sy 7))) sy)) 7))
(edge-check-g2169)

;; check set_bang_closure_mut__in__and_or_if_or_else
(define (edge-check-g2170) (edge-chk "set_bang_closure_mut__in__and_or_if_or_else" (if (or #f #f) 'no (let ((sy 0)) ((lambda () (set! sy 7))) sy)) 7))
(edge-check-g2170)

(display "EDGE-MATRIX-DONE") (newline)
