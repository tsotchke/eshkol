;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: delay_force (producers) composed into toplevel (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check delay_force_simple__in__toplevel_top_define
(define tlvg763 (force (delay (+ 40 2))))
(define (edge-check-g763) (edge-chk "delay_force_simple__in__toplevel_top_define" tlvg763 42))
(edge-check-g763)

;; check delay_force_simple__in__toplevel_top_list_arg
(define tllg764 (list (force (delay (+ 40 2)))))
(define (edge-check-g764) (edge-chk "delay_force_simple__in__toplevel_top_list_arg" (car tllg764) 42))
(edge-check-g764)

;; check delay_force_simple__in__toplevel_top_vector_arg
(define tlwg765 (vector (force (delay (+ 40 2)))))
(define (edge-check-g765) (edge-chk "delay_force_simple__in__toplevel_top_vector_arg" (vector-ref tlwg765 0) 42))
(edge-check-g765)

;; check delay_force_memoized__in__toplevel_top_define
(define cmng766 0)
(define (cmg766) (set! cmng766 (+ cmng766 1)) cmng766)
(define tlvg766 (let ((mpg766 (delay (cmg766)))) (+ (force mpg766) (force mpg766))))
(define (edge-check-g766) (edge-chk "delay_force_memoized__in__toplevel_top_define" tlvg766 2))
(edge-check-g766)

;; check delay_force_memoized__in__toplevel_top_list_arg
(define cmng767 0)
(define (cmg767) (set! cmng767 (+ cmng767 1)) cmng767)
(define tllg767 (list (let ((mpg767 (delay (cmg767)))) (+ (force mpg767) (force mpg767)))))
(define (edge-check-g767) (edge-chk "delay_force_memoized__in__toplevel_top_list_arg" (car tllg767) 2))
(edge-check-g767)

;; check delay_force_memoized__in__toplevel_top_vector_arg
(define cmng768 0)
(define (cmg768) (set! cmng768 (+ cmng768 1)) cmng768)
(define tlwg768 (vector (let ((mpg768 (delay (cmg768)))) (+ (force mpg768) (force mpg768)))))
(define (edge-check-g768) (edge-chk "delay_force_memoized__in__toplevel_top_vector_arg" (vector-ref tlwg768 0) 2))
(edge-check-g768)

(display "EDGE-MATRIX-DONE") (newline)
