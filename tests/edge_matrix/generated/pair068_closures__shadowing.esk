;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: closures (producers) composed into shadowing (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check closures_single_capture__in__shadowing_shadow_unused_builtin
(define (edge-check-g643) (edge-chk "closures_single_capture__in__shadowing_shadow_unused_builtin" (let ((length 5)) (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5))) 15))
(edge-check-g643)

;; check closures_single_capture__in__shadowing_shadow_param_rebind
(define (edge-check-g644) (edge-chk "closures_single_capture__in__shadowing_shadow_param_rebind" ((lambda (spg644) (let ((spg644 (let ((cn 10)) ((lambda (cx) (+ cx cn)) 5)))) spg644)) 'outer) 15))
(edge-check-g644)

;; check closures_sibling_set__in__shadowing_shadow_unused_builtin
(define (edge-check-g645) (edge-chk "closures_sibling_set__in__shadowing_shadow_unused_builtin" (let ((length 5)) (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get)))) 2))
(edge-check-g645)

;; check closures_sibling_set__in__shadowing_shadow_param_rebind
(define (edge-check-g646) (edge-chk "closures_sibling_set__in__shadowing_shadow_param_rebind" ((lambda (spg646) (let ((spg646 (let ((sn 0)) (let ((inc (lambda () (set! sn (+ sn 1)))) (get (lambda () sn))) (inc) (inc) (get))))) spg646)) 'outer) 2))
(edge-check-g646)

;; check closures_multi_instance__in__shadowing_shadow_unused_builtin
(define (mkcg647) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag647 (mkcg647))
(define cbg647 (mkcg647))
(define (edge-check-g647) (edge-chk "closures_multi_instance__in__shadowing_shadow_unused_builtin" (let ((length 5)) (begin (cag647) (cag647) (+ (cag647) (cbg647)))) 4))
(edge-check-g647)

;; check closures_multi_instance__in__shadowing_shadow_param_rebind
(define (mkcg648) (let ((mn 0)) (lambda () (set! mn (+ mn 1)) mn)))
(define cag648 (mkcg648))
(define cbg648 (mkcg648))
(define (edge-check-g648) (edge-chk "closures_multi_instance__in__shadowing_shadow_param_rebind" ((lambda (spg648) (let ((spg648 (begin (cag648) (cag648) (+ (cag648) (cbg648))))) spg648)) 'outer) 4))
(edge-check-g648)

;; check closures_counter_once__in__shadowing_shadow_unused_builtin
(define ctrg649 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g649) (edge-chk "closures_counter_once__in__shadowing_shadow_unused_builtin" (let ((length 5)) (ctrg649)) 1))
(edge-check-g649)

;; check closures_counter_once__in__shadowing_shadow_param_rebind
(define ctrg650 (let ((qn 0)) (lambda () (set! qn (+ qn 1)) qn)))
(define (edge-check-g650) (edge-chk "closures_counter_once__in__shadowing_shadow_param_rebind" ((lambda (spg650) (let ((spg650 (ctrg650))) spg650)) 'outer) 1))
(edge-check-g650)

(display "EDGE-MATRIX-DONE") (newline)
