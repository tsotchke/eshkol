;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: shadowing (producers) composed into first_class (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check shadowing_shadow_libc_local__in__first_class_thunk_in_list
(define (edge-check-g238) (edge-chk "shadowing_shadow_libc_local__in__first_class_thunk_in_list" ((car (list (lambda () (let ((pow 3)) (+ pow 1)))))) 4))
(edge-check-g238)

;; check shadowing_shadow_libc_local__in__first_class_apply_id
(define (edge-check-g239) (edge-chk "shadowing_shadow_libc_local__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((pow 3)) (+ pow 1)))) 4))
(edge-check-g239)

;; check shadowing_shadow_libc_local__in__first_class_map_hole
(define (edge-check-g240) (edge-chk "shadowing_shadow_libc_local__in__first_class_map_hole" (car (map (lambda (fi) (let ((pow 3)) (+ pow 1))) (list 0))) 4))
(edge-check-g240)

;; check shadowing_shadow_libc_local__in__first_class_fn_arg_call
(define (edge-check-g241) (edge-chk "shadowing_shadow_libc_local__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((pow 3)) (+ pow 1)))) 4))
(edge-check-g241)

;; check shadowing_shadow_builtin_rebind__in__first_class_thunk_in_list
(define (edge-check-g242) (edge-chk "shadowing_shadow_builtin_rebind__in__first_class_thunk_in_list" ((car (list (lambda () (let ((car cdr)) (car (list 1 2 3))))))) (list 2 3)))
(edge-check-g242)

;; check shadowing_shadow_builtin_rebind__in__first_class_apply_id
(define (edge-check-g243) (edge-chk "shadowing_shadow_builtin_rebind__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((car cdr)) (car (list 1 2 3))))) (list 2 3)))
(edge-check-g243)

;; check shadowing_shadow_builtin_rebind__in__first_class_map_hole
(define (edge-check-g244) (edge-chk "shadowing_shadow_builtin_rebind__in__first_class_map_hole" (car (map (lambda (fi) (let ((car cdr)) (car (list 1 2 3)))) (list 0))) (list 2 3)))
(edge-check-g244)

;; check shadowing_shadow_builtin_rebind__in__first_class_fn_arg_call
(define (edge-check-g245) (edge-chk "shadowing_shadow_builtin_rebind__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((car cdr)) (car (list 1 2 3))))) (list 2 3)))
(edge-check-g245)

;; check shadowing_shadow_param__in__first_class_thunk_in_list
(define (edge-check-g246) (edge-chk "shadowing_shadow_param__in__first_class_thunk_in_list" ((car (list (lambda () ((lambda (ps) (let ((ps 5)) ps)) 1))))) 5))
(edge-check-g246)

;; check shadowing_shadow_param__in__first_class_apply_id
(define (edge-check-g247) (edge-chk "shadowing_shadow_param__in__first_class_apply_id" (apply (lambda (fa) fa) (list ((lambda (ps) (let ((ps 5)) ps)) 1))) 5))
(edge-check-g247)

;; check shadowing_shadow_param__in__first_class_map_hole
(define (edge-check-g248) (edge-chk "shadowing_shadow_param__in__first_class_map_hole" (car (map (lambda (fi) ((lambda (ps) (let ((ps 5)) ps)) 1)) (list 0))) 5))
(edge-check-g248)

;; check shadowing_shadow_param__in__first_class_fn_arg_call
(define (edge-check-g249) (edge-chk "shadowing_shadow_param__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () ((lambda (ps) (let ((ps 5)) ps)) 1))) 5))
(edge-check-g249)

(display "EDGE-MATRIX-DONE") (newline)
