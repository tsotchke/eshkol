;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: guard_raise (producers) composed into first_class (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check guard_raise_catch_sym__in__first_class_thunk_in_list
(define (edge-check-g875) (edge-chk "guard_raise_catch_sym__in__first_class_thunk_in_list" ((car (list (lambda () (guard (ge (#t 'caught)) (raise 'boom)))))) (string->symbol "caught")))
(edge-check-g875)

;; check guard_raise_catch_sym__in__first_class_apply_id
(define (edge-check-g876) (edge-chk "guard_raise_catch_sym__in__first_class_apply_id" (apply (lambda (fa) fa) (list (guard (ge (#t 'caught)) (raise 'boom)))) (string->symbol "caught")))
(edge-check-g876)

;; check guard_raise_catch_sym__in__first_class_map_hole
(define (edge-check-g877) (edge-chk "guard_raise_catch_sym__in__first_class_map_hole" (car (map (lambda (fi) (guard (ge (#t 'caught)) (raise 'boom))) (list 0))) (string->symbol "caught")))
(edge-check-g877)

;; check guard_raise_catch_sym__in__first_class_fn_arg_call
(define (edge-check-g878) (edge-chk "guard_raise_catch_sym__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (guard (ge (#t 'caught)) (raise 'boom)))) (string->symbol "caught")))
(edge-check-g878)

;; check guard_raise_pass_value__in__first_class_thunk_in_list
(define (edge-check-g879) (edge-chk "guard_raise_pass_value__in__first_class_thunk_in_list" ((car (list (lambda () (guard (gv (#t gv)) (raise 42)))))) 42))
(edge-check-g879)

;; check guard_raise_pass_value__in__first_class_apply_id
(define (edge-check-g880) (edge-chk "guard_raise_pass_value__in__first_class_apply_id" (apply (lambda (fa) fa) (list (guard (gv (#t gv)) (raise 42)))) 42))
(edge-check-g880)

;; check guard_raise_pass_value__in__first_class_map_hole
(define (edge-check-g881) (edge-chk "guard_raise_pass_value__in__first_class_map_hole" (car (map (lambda (fi) (guard (gv (#t gv)) (raise 42))) (list 0))) 42))
(edge-check-g881)

;; check guard_raise_pass_value__in__first_class_fn_arg_call
(define (edge-check-g882) (edge-chk "guard_raise_pass_value__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (guard (gv (#t gv)) (raise 42)))) 42))
(edge-check-g882)

;; check guard_raise_cond_dispatch__in__first_class_thunk_in_list
(define (edge-check-g883) (edge-chk "guard_raise_cond_dispatch__in__first_class_thunk_in_list" ((car (list (lambda () (guard (gw ((symbol? gw) 'was-sym) (#t 'other)) (raise 'zap)))))) (string->symbol "was-sym")))
(edge-check-g883)

;; check guard_raise_cond_dispatch__in__first_class_apply_id
(define (edge-check-g884) (edge-chk "guard_raise_cond_dispatch__in__first_class_apply_id" (apply (lambda (fa) fa) (list (guard (gw ((symbol? gw) 'was-sym) (#t 'other)) (raise 'zap)))) (string->symbol "was-sym")))
(edge-check-g884)

;; check guard_raise_cond_dispatch__in__first_class_map_hole
(define (edge-check-g885) (edge-chk "guard_raise_cond_dispatch__in__first_class_map_hole" (car (map (lambda (fi) (guard (gw ((symbol? gw) 'was-sym) (#t 'other)) (raise 'zap))) (list 0))) (string->symbol "was-sym")))
(edge-check-g885)

;; check guard_raise_cond_dispatch__in__first_class_fn_arg_call
(define (edge-check-g886) (edge-chk "guard_raise_cond_dispatch__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (guard (gw ((symbol? gw) 'was-sym) (#t 'other)) (raise 'zap)))) (string->symbol "was-sym")))
(edge-check-g886)

(display "EDGE-MATRIX-DONE") (newline)
