;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: let_family (producers) composed into toplevel (contexts)
;; CHECKS: 12
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check let_family_let_sum__in__toplevel_top_define
(define tlvg1151 (let ((la 2) (lb 3)) (+ la lb)))
(define (edge-check-g1151) (edge-chk "let_family_let_sum__in__toplevel_top_define" tlvg1151 5))
(edge-check-g1151)

;; check let_family_let_sum__in__toplevel_top_list_arg
(define tllg1152 (list (let ((la 2) (lb 3)) (+ la lb))))
(define (edge-check-g1152) (edge-chk "let_family_let_sum__in__toplevel_top_list_arg" (car tllg1152) 5))
(edge-check-g1152)

;; check let_family_let_sum__in__toplevel_top_vector_arg
(define tlwg1153 (vector (let ((la 2) (lb 3)) (+ la lb))))
(define (edge-check-g1153) (edge-chk "let_family_let_sum__in__toplevel_top_vector_arg" (vector-ref tlwg1153 0) 5))
(edge-check-g1153)

;; check let_family_letstar_chain__in__toplevel_top_define
(define tlvg1154 (let* ((lc 2) (ld (* lc lc))) ld))
(define (edge-check-g1154) (edge-chk "let_family_letstar_chain__in__toplevel_top_define" tlvg1154 4))
(edge-check-g1154)

;; check let_family_letstar_chain__in__toplevel_top_list_arg
(define tllg1155 (list (let* ((lc 2) (ld (* lc lc))) ld)))
(define (edge-check-g1155) (edge-chk "let_family_letstar_chain__in__toplevel_top_list_arg" (car tllg1155) 4))
(edge-check-g1155)

;; check let_family_letstar_chain__in__toplevel_top_vector_arg
(define tlwg1156 (vector (let* ((lc 2) (ld (* lc lc))) ld)))
(define (edge-check-g1156) (edge-chk "let_family_letstar_chain__in__toplevel_top_vector_arg" (vector-ref tlwg1156 0) 4))
(edge-check-g1156)

;; check let_family_letrec_mutual__in__toplevel_top_define
(define tlvg1157 (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10)))
(define (edge-check-g1157) (edge-chk "let_family_letrec_mutual__in__toplevel_top_define" tlvg1157 #t))
(edge-check-g1157)

;; check let_family_letrec_mutual__in__toplevel_top_list_arg
(define tllg1158 (list (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10))))
(define (edge-check-g1158) (edge-chk "let_family_letrec_mutual__in__toplevel_top_list_arg" (car tllg1158) #t))
(edge-check-g1158)

;; check let_family_letrec_mutual__in__toplevel_top_vector_arg
(define tlwg1159 (vector (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10))))
(define (edge-check-g1159) (edge-chk "let_family_letrec_mutual__in__toplevel_top_vector_arg" (vector-ref tlwg1159 0) #t))
(edge-check-g1159)

;; check let_family_named_let_sum__in__toplevel_top_define
(define tlvg1160 (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i)))))
(define (edge-check-g1160) (edge-chk "let_family_named_let_sum__in__toplevel_top_define" tlvg1160 10))
(edge-check-g1160)

;; check let_family_named_let_sum__in__toplevel_top_list_arg
(define tllg1161 (list (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i))))))
(define (edge-check-g1161) (edge-chk "let_family_named_let_sum__in__toplevel_top_list_arg" (car tllg1161) 10))
(edge-check-g1161)

;; check let_family_named_let_sum__in__toplevel_top_vector_arg
(define tlwg1162 (vector (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i))))))
(define (edge-check-g1162) (edge-chk "let_family_named_let_sum__in__toplevel_top_vector_arg" (vector-ref tlwg1162 0) 10))
(edge-check-g1162)

(display "EDGE-MATRIX-DONE") (newline)
