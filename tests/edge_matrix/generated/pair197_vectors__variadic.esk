;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: vectors (producers) composed into variadic (contexts)
;; CHECKS: 9
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check vectors_vec_ref__in__variadic_rest_car
(define (edge-check-g1726) (edge-chk "vectors_vec_ref__in__variadic_rest_car" (car ((lambda vgs vgs) (vector-ref (vector 'va 'vb) 1))) (string->symbol "vb")))
(edge-check-g1726)

;; check vectors_vec_ref__in__variadic_dotted_first
(define (edge-check-g1727) (edge-chk "vectors_vec_ref__in__variadic_dotted_first" ((lambda (vh . vt) vh) (vector-ref (vector 'va 'vb) 1) 'p1 'p2) (string->symbol "vb")))
(edge-check-g1727)

;; check vectors_vec_ref__in__variadic_apply_rest
(define (edge-check-g1728) (edge-chk "vectors_vec_ref__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (vector-ref (vector 'va 'vb) 1) 0)) (string->symbol "vb")))
(edge-check-g1728)

;; check vectors_make_set_ref__in__variadic_rest_car
(define (edge-check-g1729) (edge-chk "vectors_make_set_ref__in__variadic_rest_car" (car ((lambda vgs vgs) (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)))) 9))
(edge-check-g1729)

;; check vectors_make_set_ref__in__variadic_dotted_first
(define (edge-check-g1730) (edge-chk "vectors_make_set_ref__in__variadic_dotted_first" ((lambda (vh . vt) vh) (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)) 'p1 'p2) 9))
(edge-check-g1730)

;; check vectors_make_set_ref__in__variadic_apply_rest
(define (edge-check-g1731) (edge-chk "vectors_make_set_ref__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)) 0)) 9))
(edge-check-g1731)

;; check vectors_vec_len__in__variadic_rest_car
(define (edge-check-g1732) (edge-chk "vectors_vec_len__in__variadic_rest_car" (car ((lambda vgs vgs) (vector-length (vector 1 2 3)))) 3))
(edge-check-g1732)

;; check vectors_vec_len__in__variadic_dotted_first
(define (edge-check-g1733) (edge-chk "vectors_vec_len__in__variadic_dotted_first" ((lambda (vh . vt) vh) (vector-length (vector 1 2 3)) 'p1 'p2) 3))
(edge-check-g1733)

;; check vectors_vec_len__in__variadic_apply_rest
(define (edge-check-g1734) (edge-chk "vectors_vec_len__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (vector-length (vector 1 2 3)) 0)) 3))
(edge-check-g1734)

(display "EDGE-MATRIX-DONE") (newline)
