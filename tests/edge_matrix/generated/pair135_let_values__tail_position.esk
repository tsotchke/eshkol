;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: let_values (producers) composed into tail_position (contexts)
;; CHECKS: 10
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check let_values_two_values__in__tail_position_define_tail
(define (tpg1193) (let-values (((va vb) (values 1 2))) (+ va vb)))
(define (edge-check-g1193) (edge-chk "let_values_two_values__in__tail_position_define_tail" (tpg1193) 3))
(edge-check-g1193)

;; check let_values_two_values__in__tail_position_let_tail_in_define
(define (tqg1194) (let ((tu 1)) (let-values (((va vb) (values 1 2))) (+ va vb))))
(define (edge-check-g1194) (edge-chk "let_values_two_values__in__tail_position_let_tail_in_define" (tqg1194) 3))
(edge-check-g1194)

;; check let_values_two_values__in__tail_position_if_tail
(define (edge-check-g1195) (edge-chk "let_values_two_values__in__tail_position_if_tail" (if (> 2 1) (let-values (((va vb) (values 1 2))) (+ va vb)) 'wrong) 3))
(edge-check-g1195)

;; check let_values_two_values__in__tail_position_begin_tail
(define (edge-check-g1196) (edge-chk "let_values_two_values__in__tail_position_begin_tail" (begin 'first (let-values (((va vb) (values 1 2))) (+ va vb))) 3))
(edge-check-g1196)

;; check let_values_two_values__in__tail_position_cond_else_tail
(define (edge-check-g1197) (edge-chk "let_values_two_values__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let-values (((va vb) (values 1 2))) (+ va vb)))) 3))
(edge-check-g1197)

;; check let_values_cwv__in__tail_position_define_tail
(define (tpg1198) (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb))))
(define (edge-check-g1198) (edge-chk "let_values_cwv__in__tail_position_define_tail" (tpg1198) 42))
(edge-check-g1198)

;; check let_values_cwv__in__tail_position_let_tail_in_define
(define (tqg1199) (let ((tu 1)) (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb)))))
(define (edge-check-g1199) (edge-chk "let_values_cwv__in__tail_position_let_tail_in_define" (tqg1199) 42))
(edge-check-g1199)

;; check let_values_cwv__in__tail_position_if_tail
(define (edge-check-g1200) (edge-chk "let_values_cwv__in__tail_position_if_tail" (if (> 2 1) (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb))) 'wrong) 42))
(edge-check-g1200)

;; check let_values_cwv__in__tail_position_begin_tail
(define (edge-check-g1201) (edge-chk "let_values_cwv__in__tail_position_begin_tail" (begin 'first (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb)))) 42))
(edge-check-g1201)

;; check let_values_cwv__in__tail_position_cond_else_tail
(define (edge-check-g1202) (edge-chk "let_values_cwv__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (call-with-values (lambda () (values 20 22)) (lambda (cva cvb) (+ cva cvb))))) 42))
(edge-check-g1202)

(display "EDGE-MATRIX-DONE") (newline)
