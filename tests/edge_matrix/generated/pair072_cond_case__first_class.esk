;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: cond_case (producers) composed into first_class (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check cond_case_cond_else__in__first_class_thunk_in_list
(define (edge-check-g695) (edge-chk "cond_case_cond_else__in__first_class_thunk_in_list" ((car (list (lambda () (cond ((= 1 2) 'a) (else 'b)))))) (string->symbol "b")))
(edge-check-g695)

;; check cond_case_cond_else__in__first_class_apply_id
(define (edge-check-g696) (edge-chk "cond_case_cond_else__in__first_class_apply_id" (apply (lambda (fa) fa) (list (cond ((= 1 2) 'a) (else 'b)))) (string->symbol "b")))
(edge-check-g696)

;; check cond_case_cond_else__in__first_class_map_hole
(define (edge-check-g697) (edge-chk "cond_case_cond_else__in__first_class_map_hole" (car (map (lambda (fi) (cond ((= 1 2) 'a) (else 'b))) (list 0))) (string->symbol "b")))
(edge-check-g697)

;; check cond_case_cond_else__in__first_class_fn_arg_call
(define (edge-check-g698) (edge-chk "cond_case_cond_else__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (cond ((= 1 2) 'a) (else 'b)))) (string->symbol "b")))
(edge-check-g698)

;; check cond_case_case_hit__in__first_class_thunk_in_list
(define (edge-check-g699) (edge-chk "cond_case_case_hit__in__first_class_thunk_in_list" ((car (list (lambda () (case 2 ((1) 'one) ((2) 'two) (else 'other)))))) (string->symbol "two")))
(edge-check-g699)

;; check cond_case_case_hit__in__first_class_apply_id
(define (edge-check-g700) (edge-chk "cond_case_case_hit__in__first_class_apply_id" (apply (lambda (fa) fa) (list (case 2 ((1) 'one) ((2) 'two) (else 'other)))) (string->symbol "two")))
(edge-check-g700)

;; check cond_case_case_hit__in__first_class_map_hole
(define (edge-check-g701) (edge-chk "cond_case_case_hit__in__first_class_map_hole" (car (map (lambda (fi) (case 2 ((1) 'one) ((2) 'two) (else 'other))) (list 0))) (string->symbol "two")))
(edge-check-g701)

;; check cond_case_case_hit__in__first_class_fn_arg_call
(define (edge-check-g702) (edge-chk "cond_case_case_hit__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (case 2 ((1) 'one) ((2) 'two) (else 'other)))) (string->symbol "two")))
(edge-check-g702)

(display "EDGE-MATRIX-DONE") (newline)
