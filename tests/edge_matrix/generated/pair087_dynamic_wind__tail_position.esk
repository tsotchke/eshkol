;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: dynamic_wind (producers) composed into tail_position (contexts)
;; CHECKS: 10
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check dynamic_wind_order__in__tail_position_define_tail
(define (tpg793) (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl))
(define (edge-check-g793) (edge-chk "dynamic_wind_order__in__tail_position_define_tail" (tpg793) (list 3 2 1)))
(edge-check-g793)

;; check dynamic_wind_order__in__tail_position_let_tail_in_define
(define (tqg794) (let ((tu 1)) (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl)))
(define (edge-check-g794) (edge-chk "dynamic_wind_order__in__tail_position_let_tail_in_define" (tqg794) (list 3 2 1)))
(edge-check-g794)

;; check dynamic_wind_order__in__tail_position_if_tail
(define (edge-check-g795) (edge-chk "dynamic_wind_order__in__tail_position_if_tail" (if (> 2 1) (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl) 'wrong) (list 3 2 1)))
(edge-check-g795)

;; check dynamic_wind_order__in__tail_position_begin_tail
(define (edge-check-g796) (edge-chk "dynamic_wind_order__in__tail_position_begin_tail" (begin 'first (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl)) (list 3 2 1)))
(edge-check-g796)

;; check dynamic_wind_order__in__tail_position_cond_else_tail
(define (edge-check-g797) (edge-chk "dynamic_wind_order__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (let ((dwl '())) (dynamic-wind (lambda () (set! dwl (cons 1 dwl))) (lambda () (set! dwl (cons 2 dwl))) (lambda () (set! dwl (cons 3 dwl)))) dwl))) (list 3 2 1)))
(edge-check-g797)

;; check dynamic_wind_value__in__tail_position_define_tail
(define (tpg798) (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)))
(define (edge-check-g798) (edge-chk "dynamic_wind_value__in__tail_position_define_tail" (tpg798) 42))
(edge-check-g798)

;; check dynamic_wind_value__in__tail_position_let_tail_in_define
(define (tqg799) (let ((tu 1)) (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a))))
(define (edge-check-g799) (edge-chk "dynamic_wind_value__in__tail_position_let_tail_in_define" (tqg799) 42))
(edge-check-g799)

;; check dynamic_wind_value__in__tail_position_if_tail
(define (edge-check-g800) (edge-chk "dynamic_wind_value__in__tail_position_if_tail" (if (> 2 1) (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)) 'wrong) 42))
(edge-check-g800)

;; check dynamic_wind_value__in__tail_position_begin_tail
(define (edge-check-g801) (edge-chk "dynamic_wind_value__in__tail_position_begin_tail" (begin 'first (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a))) 42))
(edge-check-g801)

;; check dynamic_wind_value__in__tail_position_cond_else_tail
(define (edge-check-g802) (edge-chk "dynamic_wind_value__in__tail_position_cond_else_tail" (cond ((= 1 2) 'wrong) (else (dynamic-wind (lambda () 'b) (lambda () 42) (lambda () 'a)))) 42))
(edge-check-g802)

(display "EDGE-MATRIX-DONE") (newline)
