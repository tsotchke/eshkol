;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: ad_basic (producers) composed into variadic (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check ad_basic_deriv_square__in__variadic_rest_car
(define (edge-check-g409) (edge-chk "ad_basic_deriv_square__in__variadic_rest_car" (car ((lambda vgs vgs) (derivative (lambda (adx) (* adx adx)) 3.0))) 6.0))
(edge-check-g409)

;; check ad_basic_deriv_square__in__variadic_dotted_first
(define (edge-check-g410) (edge-chk "ad_basic_deriv_square__in__variadic_dotted_first" ((lambda (vh . vt) vh) (derivative (lambda (adx) (* adx adx)) 3.0) 'p1 'p2) 6.0))
(edge-check-g410)

;; check ad_basic_deriv_square__in__variadic_apply_rest
(define (edge-check-g411) (edge-chk "ad_basic_deriv_square__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (derivative (lambda (adx) (* adx adx)) 3.0) 0)) 6.0))
(edge-check-g411)

;; check ad_basic_grad_ref__in__variadic_rest_car
(define (edge-check-g412) (edge-chk "ad_basic_grad_ref__in__variadic_rest_car" (car ((lambda vgs vgs) (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0))) 3.0))
(edge-check-g412)

;; check ad_basic_grad_ref__in__variadic_dotted_first
(define (edge-check-g413) (edge-chk "ad_basic_grad_ref__in__variadic_dotted_first" ((lambda (vh . vt) vh) (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0) 'p1 'p2) 3.0))
(edge-check-g413)

;; check ad_basic_grad_ref__in__variadic_apply_rest
(define (edge-check-g414) (edge-chk "ad_basic_grad_ref__in__variadic_apply_rest" (apply (lambda (vk . vm) vk) (list (vector-ref (gradient (lambda (agv) (* (vector-ref agv 0) (vector-ref agv 1))) (vector 2.0 3.0)) 0) 0)) 3.0))
(edge-check-g414)

(display "EDGE-MATRIX-DONE") (newline)
