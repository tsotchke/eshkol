;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: let_family (producers) composed into shadowing (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check let_family_let_sum__in__shadowing_shadow_unused_builtin
(define (edge-check-g1123) (edge-chk "let_family_let_sum__in__shadowing_shadow_unused_builtin" (let ((length 5)) (let ((la 2) (lb 3)) (+ la lb))) 5))
(edge-check-g1123)

;; check let_family_let_sum__in__shadowing_shadow_param_rebind
(define (edge-check-g1124) (edge-chk "let_family_let_sum__in__shadowing_shadow_param_rebind" ((lambda (spg1124) (let ((spg1124 (let ((la 2) (lb 3)) (+ la lb)))) spg1124)) 'outer) 5))
(edge-check-g1124)

;; check let_family_letstar_chain__in__shadowing_shadow_unused_builtin
(define (edge-check-g1125) (edge-chk "let_family_letstar_chain__in__shadowing_shadow_unused_builtin" (let ((length 5)) (let* ((lc 2) (ld (* lc lc))) ld)) 4))
(edge-check-g1125)

;; check let_family_letstar_chain__in__shadowing_shadow_param_rebind
(define (edge-check-g1126) (edge-chk "let_family_letstar_chain__in__shadowing_shadow_param_rebind" ((lambda (spg1126) (let ((spg1126 (let* ((lc 2) (ld (* lc lc))) ld))) spg1126)) 'outer) 4))
(edge-check-g1126)

;; check let_family_letrec_mutual__in__shadowing_shadow_unused_builtin
(define (edge-check-g1127) (edge-chk "let_family_letrec_mutual__in__shadowing_shadow_unused_builtin" (let ((length 5)) (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10))) #t))
(edge-check-g1127)

;; check let_family_letrec_mutual__in__shadowing_shadow_param_rebind
(define (edge-check-g1128) (edge-chk "let_family_letrec_mutual__in__shadowing_shadow_param_rebind" ((lambda (spg1128) (let ((spg1128 (letrec ((ev? (lambda (n) (if (= n 0) #t (od? (- n 1))))) (od? (lambda (n) (if (= n 0) #f (ev? (- n 1)))))) (ev? 10)))) spg1128)) 'outer) #t))
(edge-check-g1128)

;; check let_family_named_let_sum__in__shadowing_shadow_unused_builtin
(define (edge-check-g1129) (edge-chk "let_family_named_let_sum__in__shadowing_shadow_unused_builtin" (let ((length 5)) (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i))))) 10))
(edge-check-g1129)

;; check let_family_named_let_sum__in__shadowing_shadow_param_rebind
(define (edge-check-g1130) (edge-chk "let_family_named_let_sum__in__shadowing_shadow_param_rebind" ((lambda (spg1130) (let ((spg1130 (let nsum ((i 0) (s 0)) (if (= i 5) s (nsum (+ i 1) (+ s i)))))) spg1130)) 'outer) 10))
(edge-check-g1130)

(display "EDGE-MATRIX-DONE") (newline)
