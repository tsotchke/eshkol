;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: vectors (producers) composed into shadowing (contexts)
;; CHECKS: 6
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check vectors_vec_ref__in__shadowing_shadow_unused_builtin
(define (edge-check-g1696) (edge-chk "vectors_vec_ref__in__shadowing_shadow_unused_builtin" (let ((length 5)) (vector-ref (vector 'va 'vb) 1)) (string->symbol "vb")))
(edge-check-g1696)

;; check vectors_vec_ref__in__shadowing_shadow_param_rebind
(define (edge-check-g1697) (edge-chk "vectors_vec_ref__in__shadowing_shadow_param_rebind" ((lambda (spg1697) (let ((spg1697 (vector-ref (vector 'va 'vb) 1))) spg1697)) 'outer) (string->symbol "vb")))
(edge-check-g1697)

;; check vectors_make_set_ref__in__shadowing_shadow_unused_builtin
(define (edge-check-g1698) (edge-chk "vectors_make_set_ref__in__shadowing_shadow_unused_builtin" (let ((length 5)) (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1))) 9))
(edge-check-g1698)

;; check vectors_make_set_ref__in__shadowing_shadow_param_rebind
(define (edge-check-g1699) (edge-chk "vectors_make_set_ref__in__shadowing_shadow_param_rebind" ((lambda (spg1699) (let ((spg1699 (let ((mv (make-vector 3 0))) (vector-set! mv 1 9) (vector-ref mv 1)))) spg1699)) 'outer) 9))
(edge-check-g1699)

;; check vectors_vec_len__in__shadowing_shadow_unused_builtin
(define (edge-check-g1700) (edge-chk "vectors_vec_len__in__shadowing_shadow_unused_builtin" (let ((length 5)) (vector-length (vector 1 2 3))) 3))
(edge-check-g1700)

;; check vectors_vec_len__in__shadowing_shadow_param_rebind
(define (edge-check-g1701) (edge-chk "vectors_vec_len__in__shadowing_shadow_param_rebind" ((lambda (spg1701) (let ((spg1701 (vector-length (vector 1 2 3)))) spg1701)) 'outer) 3))
(edge-check-g1701)

(display "EDGE-MATRIX-DONE") (newline)
