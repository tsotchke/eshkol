;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: begin_seq (producers) composed into first_class (contexts)
;; CHECKS: 4
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check begin_seq_order__in__first_class_thunk_in_list
(define (edge-check-g495) (edge-chk "begin_seq_order__in__first_class_thunk_in_list" ((car (list (lambda () (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl))))) (list 2 1)))
(edge-check-g495)

;; check begin_seq_order__in__first_class_apply_id
(define (edge-check-g496) (edge-chk "begin_seq_order__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl))) (list 2 1)))
(edge-check-g496)

;; check begin_seq_order__in__first_class_map_hole
(define (edge-check-g497) (edge-chk "begin_seq_order__in__first_class_map_hole" (car (map (lambda (fi) (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl)) (list 0))) (list 2 1)))
(edge-check-g497)

;; check begin_seq_order__in__first_class_fn_arg_call
(define (edge-check-g498) (edge-chk "begin_seq_order__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((bl '())) (begin (set! bl (cons 1 bl)) (set! bl (cons 2 bl))) bl))) (list 2 1)))
(edge-check-g498)

(display "EDGE-MATRIX-DONE") (newline)
