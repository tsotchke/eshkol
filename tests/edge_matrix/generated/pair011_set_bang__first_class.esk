;; Generated by tests/edge_matrix/gen_matrix.py -- DO NOT EDIT
;; pair: set_bang (producers) composed into first_class (contexts)
;; CHECKS: 8
(define (edge-chk nm actual expected)
  (if (equal? actual expected)
      (begin (display "PASS: ") (display nm) (newline))
      (begin (display "FAIL: ") (display nm)
             (display " expected=") (write expected)
             (display " actual=") (write actual)
             (newline))))

;; check set_bang_local_mut__in__first_class_thunk_in_list
(define (edge-check-g204) (edge-chk "set_bang_local_mut__in__first_class_thunk_in_list" ((car (list (lambda () (let ((sx 1)) (set! sx 41) (+ sx 1)))))) 42))
(edge-check-g204)

;; check set_bang_local_mut__in__first_class_apply_id
(define (edge-check-g205) (edge-chk "set_bang_local_mut__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((sx 1)) (set! sx 41) (+ sx 1)))) 42))
(edge-check-g205)

;; check set_bang_local_mut__in__first_class_map_hole
(define (edge-check-g206) (edge-chk "set_bang_local_mut__in__first_class_map_hole" (car (map (lambda (fi) (let ((sx 1)) (set! sx 41) (+ sx 1))) (list 0))) 42))
(edge-check-g206)

;; check set_bang_local_mut__in__first_class_fn_arg_call
(define (edge-check-g207) (edge-chk "set_bang_local_mut__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((sx 1)) (set! sx 41) (+ sx 1)))) 42))
(edge-check-g207)

;; check set_bang_closure_mut__in__first_class_thunk_in_list
(define (edge-check-g208) (edge-chk "set_bang_closure_mut__in__first_class_thunk_in_list" ((car (list (lambda () (let ((sy 0)) ((lambda () (set! sy 7))) sy))))) 7))
(edge-check-g208)

;; check set_bang_closure_mut__in__first_class_apply_id
(define (edge-check-g209) (edge-chk "set_bang_closure_mut__in__first_class_apply_id" (apply (lambda (fa) fa) (list (let ((sy 0)) ((lambda () (set! sy 7))) sy))) 7))
(edge-check-g209)

;; check set_bang_closure_mut__in__first_class_map_hole
(define (edge-check-g210) (edge-chk "set_bang_closure_mut__in__first_class_map_hole" (car (map (lambda (fi) (let ((sy 0)) ((lambda () (set! sy 7))) sy)) (list 0))) 7))
(edge-check-g210)

;; check set_bang_closure_mut__in__first_class_fn_arg_call
(define (edge-check-g211) (edge-chk "set_bang_closure_mut__in__first_class_fn_arg_call" ((lambda (ff) (ff)) (lambda () (let ((sy 0)) ((lambda () (set! sy 7))) sy))) 7))
(edge-check-g211)

(display "EDGE-MATRIX-DONE") (newline)
