;; GENERATED by tests/ad_oracle/gen_ad_oracle.py — DO NOT EDIT BY HAND.
;; AD composition oracle (adversarial pillar P3). Every AD value below is
;; checked against an in-language central finite difference.
(define op-pass 0)
(define op-fail 0)
(define op-xknown 0)
(define cap-g 1.7)
(define (close? ad fd atol rtol)
  (<= (abs (- ad fd)) (+ atol (* rtol (abs fd)))))
(define (chk id ad fd atol rtol)
  (if (close? ad fd atol rtol)
      (begin (set! op-pass (+ op-pass 1))
             (display "PASS: ") (display id) (newline))
      (begin (set! op-fail (+ op-fail 1))
             (display "FAIL: ") (display id)
             (display " ad=") (display ad)
             (display " fd=") (display fd) (newline))))
(define (chk-x id task ad fd atol rtol)
  (if (close? ad fd atol rtol)
      (begin (set! op-pass (+ op-pass 1))
             (display "PASS: ") (display id)
             (display " (fixed: ") (display task) (display ")") (newline))
      (begin (set! op-xknown (+ op-xknown 1))
             (display "XKNOWN: ") (display id)
             (display " (") (display task)
             (display ") ad=") (display ad)
             (display " fd=") (display fd) (newline))))

;; probe carrier.expsin.s.named.capnone
(define (fdf241 x) (* (exp (* 0.3 x)) (sin x)))
(define twr241 (derivative-n (lambda (y) (derivative-n fdf241 y 1)) 1.3 1))
(define jot241 (derivative (lambda (y) (derivative-n fdf241 y 1)) 1.3))
(define toj241 (derivative-n (lambda (y) (derivative fdf241 y)) 1.3 1))
(define clo241 (derivative fdf241))
(define tcl241 (derivative-n clo241 1.3 1))
(define ref2241 (derivative-n fdf241 1.3 2))
(define ref3241 (derivative-n fdf241 1.3 3))
(define fd2241 (/ (+ (- (fdf241 (+ 1.3 1e-4)) (* 2.0 (fdf241 1.3))) (fdf241 (- 1.3 1e-4))) (* 1e-4 1e-4)))
(define ct241 (derivative-n (lambda (y) (derivative-n fdf241 y 2)) 1.3 1))
(define rd241 (derivative-n (lambda (y) (derivative-n fdf241 y 1)) 1.3 2))
(define tay241 (taylor (lambda (y) (derivative fdf241 y)) 1.3 1))
(chk "carrier.towerovertower.expsin.s.named.capnone" twr241 fd2241 1e-5 1e-4)
(chk "carrier.towerovertower.expsin.s.named.capnone[agree]" twr241 ref2241 1e-6 1e-4)
(chk "carrier.jetovertower.expsin.s.named.capnone" jot241 fd2241 1e-5 1e-4)
(chk "carrier.jetovertower.expsin.s.named.capnone[agree]" jot241 ref2241 1e-6 1e-4)
(chk "carrier.toweroverjet.expsin.s.named.capnone" toj241 fd2241 1e-5 1e-4)
(chk "carrier.toweroverjet.expsin.s.named.capnone[agree]" toj241 ref2241 1e-6 1e-4)
(chk "carrier.overclosure.expsin.s.named.capnone" tcl241 fd2241 1e-5 1e-4)
(chk "carrier.overclosure.expsin.s.named.capnone[agree]" tcl241 ref2241 1e-6 1e-4)
(chk "carrier.carrytower.expsin.s.named.capnone[agree]" ct241 ref3241 1e-6 1e-4)
(chk "carrier.ridetower.expsin.s.named.capnone[agree]" rd241 ref3241 1e-6 1e-4)
(chk "carrier.taylorovers.expsin.s.named.capnone[c1]" (car (cdr tay241)) ref2241 1e-6 1e-4)

(display "Passed: ") (display op-pass) (newline)
(display "Failed: ") (display op-fail) (newline)
(display "Xknown: ") (display op-xknown) (newline)
