;; GENERATED by tests/ad_oracle/gen_ad_oracle.py — DO NOT EDIT BY HAND.
;; AD composition oracle (adversarial pillar P3). Every AD value below is
;; checked against an in-language central finite difference.
(define op-pass 0)
(define op-fail 0)
(define op-xknown 0)
(define cap-g 1.7)
(define (close? ad fd atol rtol)
  (<= (abs (- ad fd)) (+ atol (* rtol (abs fd)))))
(define (chk id ad fd atol rtol)
  (if (close? ad fd atol rtol)
      (begin (set! op-pass (+ op-pass 1))
             (display "PASS: ") (display id) (newline))
      (begin (set! op-fail (+ op-fail 1))
             (display "FAIL: ") (display id)
             (display " ad=") (display ad)
             (display " fd=") (display fd) (newline))))
(define (chk-x id task ad fd atol rtol)
  (if (close? ad fd atol rtol)
      (begin (set! op-pass (+ op-pass 1))
             (display "PASS: ") (display id)
             (display " (fixed: ") (display task) (display ")") (newline))
      (begin (set! op-xknown (+ op-xknown 1))
             (display "XKNOWN: ") (display id)
             (display " (") (display task)
             (display ") ad=") (display ad)
             (display " fd=") (display fd) (newline))))

;; probe carrier.expsin.s.inline.capnone
(define (fdf240 x) (* (exp (* 0.3 x)) (sin x)))
(define twr240 (derivative-n (lambda (y) (derivative-n (lambda (x) (* (exp (* 0.3 x)) (sin x))) y 1)) 1.3 1))
(define jot240 (derivative (lambda (y) (derivative-n (lambda (x) (* (exp (* 0.3 x)) (sin x))) y 1)) 1.3))
(define toj240 (derivative-n (lambda (y) (derivative (lambda (x) (* (exp (* 0.3 x)) (sin x))) y)) 1.3 1))
(define clo240 (derivative (lambda (x) (* (exp (* 0.3 x)) (sin x)))))
(define tcl240 (derivative-n clo240 1.3 1))
(define ref2240 (derivative-n (lambda (x) (* (exp (* 0.3 x)) (sin x))) 1.3 2))
(define ref3240 (derivative-n (lambda (x) (* (exp (* 0.3 x)) (sin x))) 1.3 3))
(define fd2240 (/ (+ (- (fdf240 (+ 1.3 1e-4)) (* 2.0 (fdf240 1.3))) (fdf240 (- 1.3 1e-4))) (* 1e-4 1e-4)))
(define ct240 (derivative-n (lambda (y) (derivative-n (lambda (x) (* (exp (* 0.3 x)) (sin x))) y 2)) 1.3 1))
(define rd240 (derivative-n (lambda (y) (derivative-n (lambda (x) (* (exp (* 0.3 x)) (sin x))) y 1)) 1.3 2))
(define tay240 (taylor (lambda (y) (derivative (lambda (x) (* (exp (* 0.3 x)) (sin x))) y)) 1.3 1))
(chk "carrier.towerovertower.expsin.s.inline.capnone" twr240 fd2240 1e-5 1e-4)
(chk "carrier.towerovertower.expsin.s.inline.capnone[agree]" twr240 ref2240 1e-6 1e-4)
(chk "carrier.jetovertower.expsin.s.inline.capnone" jot240 fd2240 1e-5 1e-4)
(chk "carrier.jetovertower.expsin.s.inline.capnone[agree]" jot240 ref2240 1e-6 1e-4)
(chk "carrier.toweroverjet.expsin.s.inline.capnone" toj240 fd2240 1e-5 1e-4)
(chk "carrier.toweroverjet.expsin.s.inline.capnone[agree]" toj240 ref2240 1e-6 1e-4)
(chk "carrier.overclosure.expsin.s.inline.capnone" tcl240 fd2240 1e-5 1e-4)
(chk "carrier.overclosure.expsin.s.inline.capnone[agree]" tcl240 ref2240 1e-6 1e-4)
(chk "carrier.carrytower.expsin.s.inline.capnone[agree]" ct240 ref3240 1e-6 1e-4)
(chk "carrier.ridetower.expsin.s.inline.capnone[agree]" rd240 ref3240 1e-6 1e-4)
(chk "carrier.taylorovers.expsin.s.inline.capnone[c1]" (car (cdr tay240)) ref2240 1e-6 1e-4)

(display "Passed: ") (display op-pass) (newline)
(display "Failed: ") (display op-fail) (newline)
(display "Xknown: ") (display op-xknown) (newline)
