;; GENERATED by tests/ad_oracle/gen_ad_oracle.py — DO NOT EDIT BY HAND.
;; AD composition oracle (adversarial pillar P3). Every AD value below is
;; checked against an in-language central finite difference.
(define op-pass 0)
(define op-fail 0)
(define op-xknown 0)
(define cap-g 1.7)
(define (close? ad fd atol rtol)
  (<= (abs (- ad fd)) (+ atol (* rtol (abs fd)))))
(define (chk id ad fd atol rtol)
  (if (close? ad fd atol rtol)
      (begin (set! op-pass (+ op-pass 1))
             (display "PASS: ") (display id) (newline))
      (begin (set! op-fail (+ op-fail 1))
             (display "FAIL: ") (display id)
             (display " ad=") (display ad)
             (display " fd=") (display fd) (newline))))
(define (chk-x id task ad fd atol rtol)
  (if (close? ad fd atol rtol)
      (begin (set! op-pass (+ op-pass 1))
             (display "PASS: ") (display id)
             (display " (fixed: ") (display task) (display ")") (newline))
      (begin (set! op-xknown (+ op-xknown 1))
             (display "XKNOWN: ") (display id)
             (display " (") (display task)
             (display ") ad=") (display ad)
             (display " fd=") (display fd) (newline))))

;; probe carrier.namedlet.s.inline.capnone
(define (fdf244 x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i))))))))
(define twr244 (derivative-n (lambda (y) (derivative-n (lambda (x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i)))))))) y 1)) 1.3 1))
(define jot244 (derivative (lambda (y) (derivative-n (lambda (x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i)))))))) y 1)) 1.3))
(define toj244 (derivative-n (lambda (y) (derivative (lambda (x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i)))))))) y)) 1.3 1))
(define clo244 (derivative (lambda (x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i))))))))))
(define tcl244 (derivative-n clo244 1.3 1))
(define ref2244 (derivative-n (lambda (x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i)))))))) 1.3 2))
(define ref3244 (derivative-n (lambda (x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i)))))))) 1.3 3))
(define fd2244 (/ (+ (- (fdf244 (+ 1.3 1e-4)) (* 2.0 (fdf244 1.3))) (fdf244 (- 1.3 1e-4))) (* 1e-4 1e-4)))
(define ct244 (derivative-n (lambda (y) (derivative-n (lambda (x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i)))))))) y 2)) 1.3 1))
(define rd244 (derivative-n (lambda (y) (derivative-n (lambda (x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i)))))))) y 1)) 1.3 2))
(define tay244 (taylor (lambda (y) (derivative (lambda (x) (let loop ((i 0) (acc 0.0)) (if (>= i 3) acc (loop (+ i 1) (+ acc (* x x (+ 1.0 (* 0.5 i)))))))) y)) 1.3 1))
(chk "carrier.towerovertower.namedlet.s.inline.capnone" twr244 fd2244 1e-5 1e-4)
(chk "carrier.towerovertower.namedlet.s.inline.capnone[agree]" twr244 ref2244 1e-6 1e-4)
(chk "carrier.jetovertower.namedlet.s.inline.capnone" jot244 fd2244 1e-5 1e-4)
(chk "carrier.jetovertower.namedlet.s.inline.capnone[agree]" jot244 ref2244 1e-6 1e-4)
(chk "carrier.toweroverjet.namedlet.s.inline.capnone" toj244 fd2244 1e-5 1e-4)
(chk "carrier.toweroverjet.namedlet.s.inline.capnone[agree]" toj244 ref2244 1e-6 1e-4)
(chk "carrier.overclosure.namedlet.s.inline.capnone" tcl244 fd2244 1e-5 1e-4)
(chk "carrier.overclosure.namedlet.s.inline.capnone[agree]" tcl244 ref2244 1e-6 1e-4)
(chk "carrier.carrytower.namedlet.s.inline.capnone[agree]" ct244 ref3244 1e-6 1e-4)
(chk "carrier.ridetower.namedlet.s.inline.capnone[agree]" rd244 ref3244 1e-6 1e-4)
(chk "carrier.taylorovers.namedlet.s.inline.capnone[c1]" (car (cdr tay244)) ref2244 1e-6 1e-4)

(display "Passed: ") (display op-pass) (newline)
(display "Failed: ") (display op-fail) (newline)
(display "Xknown: ") (display op-xknown) (newline)
