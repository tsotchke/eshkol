;; GENERATED by tests/ad_oracle/gen_ad_oracle.py — DO NOT EDIT BY HAND.
;; AD composition oracle (adversarial pillar P3). Every AD value below is
;; checked against an in-language central finite difference.
(define op-pass 0)
(define op-fail 0)
(define op-xknown 0)
(define cap-g 1.7)
(define (close? ad fd atol rtol)
  (<= (abs (- ad fd)) (+ atol (* rtol (abs fd)))))
(define (chk id ad fd atol rtol)
  (if (close? ad fd atol rtol)
      (begin (set! op-pass (+ op-pass 1))
             (display "PASS: ") (display id) (newline))
      (begin (set! op-fail (+ op-fail 1))
             (display "FAIL: ") (display id)
             (display " ad=") (display ad)
             (display " fd=") (display fd) (newline))))
(define (chk-x id task ad fd atol rtol)
  (if (close? ad fd atol rtol)
      (begin (set! op-pass (+ op-pass 1))
             (display "PASS: ") (display id)
             (display " (fixed: ") (display task) (display ")") (newline))
      (begin (set! op-xknown (+ op-xknown 1))
             (display "XKNOWN: ") (display id)
             (display " (") (display task)
             (display ") ad=") (display ad)
             (display " fd=") (display fd) (newline))))

;; probe carrier.letreuse.s.inline.capnone
(define (fdf242 x) (let ((t (* x x))) (+ (* t t) t)))
(define twr242 (derivative-n (lambda (y) (derivative-n (lambda (x) (let ((t (* x x))) (+ (* t t) t))) y 1)) 1.3 1))
(define jot242 (derivative (lambda (y) (derivative-n (lambda (x) (let ((t (* x x))) (+ (* t t) t))) y 1)) 1.3))
(define toj242 (derivative-n (lambda (y) (derivative (lambda (x) (let ((t (* x x))) (+ (* t t) t))) y)) 1.3 1))
(define clo242 (derivative (lambda (x) (let ((t (* x x))) (+ (* t t) t)))))
(define tcl242 (derivative-n clo242 1.3 1))
(define ref2242 (derivative-n (lambda (x) (let ((t (* x x))) (+ (* t t) t))) 1.3 2))
(define ref3242 (derivative-n (lambda (x) (let ((t (* x x))) (+ (* t t) t))) 1.3 3))
(define fd2242 (/ (+ (- (fdf242 (+ 1.3 1e-4)) (* 2.0 (fdf242 1.3))) (fdf242 (- 1.3 1e-4))) (* 1e-4 1e-4)))
(define ct242 (derivative-n (lambda (y) (derivative-n (lambda (x) (let ((t (* x x))) (+ (* t t) t))) y 2)) 1.3 1))
(define rd242 (derivative-n (lambda (y) (derivative-n (lambda (x) (let ((t (* x x))) (+ (* t t) t))) y 1)) 1.3 2))
(define tay242 (taylor (lambda (y) (derivative (lambda (x) (let ((t (* x x))) (+ (* t t) t))) y)) 1.3 1))
(chk "carrier.towerovertower.letreuse.s.inline.capnone" twr242 fd2242 1e-5 1e-4)
(chk "carrier.towerovertower.letreuse.s.inline.capnone[agree]" twr242 ref2242 1e-6 1e-4)
(chk "carrier.jetovertower.letreuse.s.inline.capnone" jot242 fd2242 1e-5 1e-4)
(chk "carrier.jetovertower.letreuse.s.inline.capnone[agree]" jot242 ref2242 1e-6 1e-4)
(chk "carrier.toweroverjet.letreuse.s.inline.capnone" toj242 fd2242 1e-5 1e-4)
(chk "carrier.toweroverjet.letreuse.s.inline.capnone[agree]" toj242 ref2242 1e-6 1e-4)
(chk "carrier.overclosure.letreuse.s.inline.capnone" tcl242 fd2242 1e-5 1e-4)
(chk "carrier.overclosure.letreuse.s.inline.capnone[agree]" tcl242 ref2242 1e-6 1e-4)
(chk "carrier.carrytower.letreuse.s.inline.capnone[agree]" ct242 ref3242 1e-6 1e-4)
(chk "carrier.ridetower.letreuse.s.inline.capnone[agree]" rd242 ref3242 1e-6 1e-4)
(chk "carrier.taylorovers.letreuse.s.inline.capnone[c1]" (car (cdr tay242)) ref2242 1e-6 1e-4)

(display "Passed: ") (display op-pass) (newline)
(display "Failed: ") (display op-fail) (newline)
(display "Xknown: ") (display op-xknown) (newline)
