{
  "id": "ESH-0230",
  "title": "tensor_embedding_backward is a row-0-only stub \u2014 gradients past row 0 are dropped",
  "status": "done",
  "priority": "high",
  "workstream": "autodiff/tensor-backward",
  "owner": null,
  "preferred_backend": "claude",
  "preferred_node": "atlas",
  "max_cost_usd": 4.0,
  "goal": "Embedding lookup forward is y[i,:] = W[idx[i],:]; the correct backward scatters dL/dy rows into dL/dW at idx[i]. tensor_embedding_backward (lib/bridge/tensor_backward.cpp) doesn't have the lookup-index tensor wired through the ad_node_t structure yet, so it falls back to accumulating every row's gradient into row 0 of the weight tensor. Training any model with embeddings indexed at rows other than 0 will not converge correctly. A one-shot stderr warning already fires on first invocation (so this cannot regress silently), and docs/breakdown/AUTODIFF.md documents the limitation. Previously the code/docs claimed this would ship 'in v1.3' without a tracked ticket backing that claim; both have now been corrected to reference this ticket instead of a version promise.",
  "file_globs": [
    "lib/bridge/tensor_backward.cpp",
    "lib/backend/tensor_extras_codegen.cpp",
    "inc/eshkol/backend/ad_node.h",
    "docs/breakdown/AUTODIFF.md",
    "tests/autodiff/embedding_backward_test.esk"
  ],
  "next_actions": [
    "DONE: index tensor threaded through node->input2 (contract documented in lib/bridge/tensor_backward.cpp and docs/breakdown/AUTODIFF.md).",
    "DONE: real indexed scatter dW[idx[i]] += dy[i] replaces both the row-0 accumulation and the later eshkol_fatal refusal.",
    "DONE: tests/backend/tensor_embedding_backward_gradcheck_test.cpp \u2014 closed form, central finite differences, duplicate-index accumulation, exact-zero unselected rows, refusals, and gradient-buffer lifetime across the dispatcher arena scope.",
    "DONE: docs/breakdown/AUTODIFF.md Embedding backward section rewritten; the stderr stub warning was already gone from master before this work."
  ],
  "acceptance": [
    "Training a model with embedding lookups against non-zero rows converges (gradient reaches the correct rows, not just row 0).",
    "tests/autodiff/embedding_backward_test.esk passes with per-row gradient assertions.",
    "The stderr stub warning is removed once fixed (its continued presence is itself evidence this ticket is still open)."
  ],
  "blocked_by": [],
  "verification": [
    "cd build && cmake --build . --target eshkol-run",
    "./build/eshkol-run tests/autodiff/embedding_backward_test.esk",
    "scripts/run_ad_oracle.sh"
  ],
  "ux_payoff": "Unblocks correct training of any embedding-based model (the standard first layer of virtually every NLP/recommendation architecture) \u2014 currently a silent correctness trap for anyone using embeddings beyond row 0.",
  "resolution": "Exact indexed scatter-add in lib/bridge/tensor_backward.cpp. The blocker named in the ticket (index tensor absent from the AD node) is closed by making node->input2 the index operand. Verified by ctest tensor_embedding_backward_gradcheck (9 checks). NOTE: no in-tree forward records an AD_NODE_TENSOR_EMBEDDING node yet, so the rule is exercised through the C dispatcher rather than through (gradient (lambda (W) ... (embedding idx W))); wiring the (embedding ...) tensor codegen to record the node is the remaining step and is tracked separately."
}
