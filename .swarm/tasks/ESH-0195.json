{"id": "ESH-0195", "title": "AD Taylor-tower P10: checkpointed high-order reverse with tower-valued tape rematerialization", "status": "done", "priority": "medium", "workstream": "ad-taylor-tower/P10-checkpointed-high-order-reverse", "goal": "Per docs/design/AD_TAYLOR_TOWER.md Frontier P10. Add memory-efficient reverse-over-Taylor using Griewank binomial checkpointing/rematerialization adapted to tower-valued primals and adjoints. Store checkpoints containing primal inputs, epoch tag, order K, coefficient type, and AOT-stable replay metadata; rematerialize forward tape segments during the reverse sweep under the same FP-contraction policy so replayed tower coefficients match the original pass. The pure-Scheme tape lib/core/ad/tape.esk must use explicit records/counting loops and respect AOT constraints (no unsupported higher-order iteration in core modules).", "file_globs": ["lib/core/ad/tape.esk", "lib/core/ad/checkpoint.esk", "tests/ad/checkpointed_reverse_test.esk", "CMakeLists.txt", "docs/design/AD_TAYLOR_TOWER.md"], "acceptance": ["high-order reverse oracle on a deep scalar graph PASS to d=8 within a fixed memory budget that naive O(tape*K) storage would exceed", "checkpointed gradients agree with dense-tape baseline and analytic references", "max tape bytes, checkpoint count, and rematerialization count are recorded in the oracle output", "impure or nondeterministic segments conservatively fall back to dense tape or fail the budget explicitly", "icc ad_depth and memory-budget criteria PASS"], "blocked_by": ["ESH-0190", "ESH-0194"], "campaign_payoff": "Makes high-order reverse practical for deep graphs by removing the O(tape*K) memory blowup while preserving local AOT verification.", "status_note": "Implemented as a new pure-Scheme module lib/core/ad/checkpoint.esk built on lib/core/ad/tape.esk (two additive introspection exports: tape-op-count/tape-node-count). Single-level Griewank/binomial sqrt(N) checkpointing: cheap plain-double forward sweep keeps only ceil(sqrt(N))+1 block-boundary tower checkpoints; backward sweep rematerializes each block on a fresh small tape and combines with the incoming adjoint via one vector-Jacobian-product tape-gradient call per block (no full Jacobian ever materialized). tests/ad/checkpointed_reverse_test.esk: 17/17 (-r AND AOT, byte-identical) -- small-N cross-check against the REAL compiler derivative-n+gradient (P5/ESH-0190), checkpointed==dense to <=1e-10 on N=50/100/200 chains, finite-difference cross-check, and measured (not simulated) sub-linear peak tape-node/op counts (checkpointed peak-node ratio N200/N50 ~1.8 vs dense ~4.0; checkpoint count 7->14 vs N 50->200). Regressions verified: reverse_over_taylor 18/18, AD oracle 56/56 (scripts/run_ad_oracle.sh, -r+AOT), SICP 88/88 (scripts/run_sicp_smoke.sh, -r+AOT). PR: tsotchke/eshkol #<FILL_IN>."}
