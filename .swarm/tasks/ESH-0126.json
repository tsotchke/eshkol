{"id": "ESH-0126", "title": "syntax-rules: quote/quasiquote SHORTHAND as a whole rule template is rejected ('expected closing paren after macro rule template')", "status": "done", "priority": "medium", "workstream": "depth-parametric/P6e-metaprog", "goal": "Found by the P6e metaprogramming depth harness (2026-07-03). A syntax-rules rule whose ENTIRE template is a reader-macro shorthand form -- quote 'x or quasiquote `x -- fails to parse with 'expected closing paren after macro rule template'. The long form (quote ...) works, and the shorthand works when NESTED inside a larger template (e.g. (cons x '())), so the bug is specifically the template reader not accepting a top-level ' or ` shorthand. This blocks the idiomatic recursive-macro base case ((_) '()). Workaround in the harness: use (quote ()) long form.", "file_globs": ["lib/frontend/parser.cpp"], "acceptance": ["(define-syntax nil (syntax-rules () ((_) '()))) (nil) => ()", "(define-syntax q (syntax-rules () ((_ x) `(a ,x)))) parses (see ESH-0127 for the substitution bug)", "harness recmac_list base case can use '() instead of (quote ())"], "blocked_by": [], "campaign_payoff": "Recursive macros with a quoted base case are textbook R7RS; rejecting the shorthand breaks common macro code.", "repro": "printf \"(define-syntax nil (syntax-rules () ((_) '())))\\n(display (nil))(newline)\\n\" > /tmp/t.esk; build/eshkol-run -r /tmp/t.esk  # error: expected closing paren after macro rule template", "resolution": "RESOLVED 2026-07-04 (branch fix/qq-macro-depth): parser now accepts a whole-template reader shorthand ('x / `x / ,x / ,@x / #(...)) at both syntax-rules template parse sites via pushBack + parse_expression (same idiom ESH-0094 applied to the match subject), instead of parse_atom which dropped the datum. (define-syntax nil (syntax-rules () ((_) '()))) (nil) => () now works; (qq-a 7) with ((_ x) `(a ,x)) => (a 7)."}
