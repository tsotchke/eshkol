;; Test file for :real modifier in extern operations

;; Declare printf with its real C function name
(extern void my_print :real printf char* ...)

;; Declare a hypothetical function with a different real name
(extern int add_numbers :real actual_add_impl int int)

;; Regular extern without :real modifier for comparison
(extern void regular_print char* ...)

(define (main)
    ;; This should call the real printf function
    (my_print "Hello from real modifier test: %d\n" 42)
    
    ;; This should call actual_add_impl but be known as add_numbers in Eshkol
    ;; (add_numbers 10 20)  ; commented out since we don't have the actual impl
    
    0)