;; Final test for :real modifier functionality
;; We'll use malloc as the real function name but call it memory_alloc

(extern void* memory_alloc :real malloc int)

(define (main)
    (printf "Starting test with malloc alias...\n")
    ;; Test would be: (memory_alloc 100)
    ;; But since we don't have proper memory management, we'll just declare it
    (printf "Extern declaration with :real modifier parsed successfully!\n") 
    0)