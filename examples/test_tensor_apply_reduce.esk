;; Test tensor-apply and tensor-reduce functions

(define (main)
  ;; Create a simple vector
  (define v1 (vector 10 20 30))
  
  ;; Test tensor-apply with doubling function
  (define v2 (tensor-apply v1 double))
  
  ;; Test tensor-reduce with addition
  (define sum (tensor-reduce v1 + 0))
  
  ;; Test tensor-reduce with max
  (define max_val (tensor-reduce v1 max 0))
  
  ;; Return success
  0)